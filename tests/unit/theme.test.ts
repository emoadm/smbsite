import { describe, it, expect } from 'vitest';

describe('BRAND-01 — Tailwind v4 brand theme tokens', () => {
  it.todo('CSS custom properties --color-brand-primary and --color-brand-accent are defined');
  it.todo('brand colors match sinyabulgaria.bg palette');
  it('SCAFFOLD MISSING — wired in plan 1.04', () => {
    expect.fail('Stub: implement in src/styles/globals.css (plan 1.04)');
  });
});
