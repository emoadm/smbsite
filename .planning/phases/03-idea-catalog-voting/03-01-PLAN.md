---
phase: 03-idea-catalog-voting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/voting-schema.test.ts
  - tests/unit/voting-no-pgenum.test.ts
  - tests/unit/append-only-vote-log.test.ts
  - tests/unit/server-action-no-revalidate.test.ts
  - tests/unit/voting-i18n.test.ts
  - tests/unit/cooling-config.test.ts
  - tests/unit/slug.test.ts
  - tests/unit/ideas-lexical-config.test.ts
  - messages/bg.json
autonomous: true
requirements: [IDEA-01, IDEA-02, IDEA-03, IDEA-04, IDEA-05, IDEA-06, IDEA-07, IDEA-08, MEMB-01, MEMB-02, MEMB-03, EDIT-02, OPS-04]
threat_ids: [T-03-01-01, T-03-01-02, T-03-01-03]

must_haves:
  truths:
    - "All Wave 0 unit-level test scaffolds exist (red, missing-source-on-purpose) so downstream plans can implement against them"
    - "messages/bg.json contains every D-25 locked Bulgarian copy string under stable next-intl key paths"
    - "messages/bg.json includes every UI-SPEC §Copywriting Contract Claude's-discretion key (sort labels, page titles, etc.)"
    - "messages/bg.json includes every email.voteAnomaly.* key consumed by plan 03-08's React Email template (loaded via i18n-direct.ts loadT in BullMQ worker)"
    - "i18n tone tests forbid forbidden vocative tokens (`Уважаем`) inside the new Phase 3 namespaces (idea.*, member.activity, member.cta, profile, admin.*, email.voteAnomaly)"
  artifacts:
    - path: "messages/bg.json"
      provides: "All Phase 3 next-intl strings (idea.*, vote.*, member.activity.*, member.profile.*, member.cta.*, member.nav.profile, profile.*, admin.voteAnomalies.*, admin.idea.*, admin.votes.*, email.voteAnomaly.*)"
      contains: "idea.vote.approve"
    - path: "tests/unit/voting-schema.test.ts"
      provides: "GDPR-09 schema-grep regression: no inet/raw_ip/ip_address/user_agent in src/db/schema/voting.ts; UNIQUE on (user_id,idea_id); ON DELETE CASCADE; user_id NULLABLE on vote_events_log"
    - path: "tests/unit/voting-no-pgenum.test.ts"
      provides: "Project convention: no pgEnum() in voting/moderation schemas"
    - path: "tests/unit/append-only-vote-log.test.ts"
      provides: "App-level append-only invariant — no db.update / db.delete on vote_events_log or moderation_log"
    - path: "tests/unit/server-action-no-revalidate.test.ts"
      provides: "Pitfall 7 lock — castVote/retractVote/undoRetract MUST NOT call revalidatePath/revalidateTag (D-03 cache-no-bust)"
    - path: "tests/unit/voting-i18n.test.ts"
      provides: "Locked Bulgarian copy verification — every D-25 string AND every email.voteAnomaly.* string present at the expected key; no forbidden vocative tokens"
    - path: "tests/unit/cooling-config.test.ts"
      provides: "D-10 env-regex validation — `^\\d+ (hours|days)$` only; invalid value throws at startup"
    - path: "tests/unit/slug.test.ts"
      provides: "D-18 slugifyBg fixture covering all 30 Bulgarian Cyrillic letters per Streamlined System + word-final -ия exception"
    - path: "tests/unit/ideas-lexical-config.test.ts"
      provides: "D-17 Lexical allowed-blocks parity with Newsletters; banned: code blocks, blockquotes, raw HTML"
  key_links:
    - from: "tests/unit/voting-schema.test.ts"
      to: "src/db/schema/voting.ts"
      via: "readFileSync grep"
      pattern: "readFileSync\\('src/db/schema/voting\\.ts'"
    - from: "tests/unit/voting-i18n.test.ts"
      to: "messages/bg.json"
      via: "JSON import key check"
      pattern: "messages/bg\\.json"
    - from: "tests/unit/server-action-no-revalidate.test.ts"
      to: "src/app/actions/cast-vote.ts | retract-vote.ts | undo-retract.ts"
      via: "readFileSync grep for revalidatePath|revalidateTag"
      pattern: "revalidatePath|revalidateTag"
  art9_lawyer_opinion_gate:
    - "GDPR Art.9 lawyer opinion on file at .planning/legal/art9-opinion.md before merge to main"

user_setup: []
---

<objective>
Wave 0 test scaffolding + Bulgarian string lock-in for Phase 3.

Purpose: Per RESEARCH §"Wave 0 Gaps" and Nyquist Dimension 8 (every implementation task in this phase must have an automated `<verify>` command), the Wave 0 plan creates the test files BEFORE downstream plans implement the source files those tests guard. Each test starts as RED (the file under test does not exist yet) and turns GREEN as Wave 1+ plans implement. This plan also drops every D-25 locked Bulgarian copy + every UI-SPEC §Copywriting Contract key + every email.voteAnomaly.* key (consumed by plan 03-08's React Email template) into `messages/bg.json` so that downstream plans never inline Cyrillic in JSX (Phase 1 D-27) or in React Email templates.

Output: 8 test files + `messages/bg.json` extended with idea.*, vote.*, member.activity.*, member.profile.*, member.cta.*, member.nav.profile, profile.*, admin.voteAnomalies.*, admin.idea.*, admin.votes.*, email.voteAnomaly.* namespaces. No source-code production files; all tests fail BY DESIGN until Wave 1+ ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-idea-catalog-voting/03-CONTEXT.md
@.planning/phases/03-idea-catalog-voting/03-RESEARCH.md
@.planning/phases/03-idea-catalog-voting/03-PATTERNS.md
@.planning/phases/03-idea-catalog-voting/03-UI-SPEC.md
@CLAUDE.md
@tests/unit/attribution-schema.test.ts
@tests/unit/consents-append-only.test.ts
@messages/bg.json

<interfaces>
<!-- Existing patterns Wave 0 mirrors. -->

From `tests/unit/attribution-schema.test.ts` (Phase 2.1 schema regression — direct mirror for voting-schema.test.ts):
```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';

describe('Phase 02.1 D-19 / GDPR-09 — attribution_events schema invariants', () => {
  it('attribution schema contains no inet/raw_ip column', () => {
    const src = readFileSync('src/db/schema/attribution.ts', 'utf8');
    const codeOnly = src.split('\n').filter((l) => !l.trim().startsWith('//')).join('\n');
    expect(codeOnly).not.toMatch(/\binet\b/);
    expect(codeOnly).not.toMatch(/\braw_ip\b/);
  });
});
```

From `tests/unit/consents-append-only.test.ts` (Phase 1 D-13 — direct mirror for append-only-vote-log.test.ts):
```typescript
it('no db.update / db.delete on consents table', () => {
  const grep = execSync(
    `grep -rE "db\\.update\\(consents\\)|db\\.delete\\(consents\\)" src/`,
    { encoding: 'utf8' }
  ).trim();
  expect(grep).toBe('');
});
```

From `messages/bg.json` (existing namespaces, append-only — do NOT restructure):
- Top-level keys already in use: `attribution.*`, `auth.*`, `email.*`, `errorsZod.*`, `member.preferences.*`, `member.welcome.*`, `nav.*`, `community.*`. Phase 3 ADDS new top-level `idea.*` and extends `member.*`, `admin.*`, `vote.*`, `profile.*`, `email.*` (adds `email.voteAnomaly.*` subkey to existing email namespace).

From `src/lib/email/i18n-direct.ts` (Phase 5 — direct loader for BullMQ workers):
```typescript
export function loadT(namespace: string): EmailT;  // returns t(key, vars) factory
// Plan 03-08 uses: const messages = loadT('email.voteAnomaly');
//                  messages('subject', { ideaTitle: 'Foo' })
// ICU placeholders use {var} syntax (not %{var}).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all Phase 3 next-intl keys to messages/bg.json (incl. email.voteAnomaly for the BullMQ worker)</name>
  <files>messages/bg.json</files>
  <read_first>
    - messages/bg.json (entire file — verify it remains valid JSON after edit; current size ~582 lines per PATTERNS.md §"messages/bg.json")
    - .planning/phases/03-idea-catalog-voting/03-UI-SPEC.md § "Copywriting Contract" lines 661–740 (every key + Bulgarian value)
    - .planning/phases/03-idea-catalog-voting/03-CONTEXT.md D-25 (locked copy) and D-07 (topic enum)
    - .planning/phases/03-idea-catalog-voting/03-PATTERNS.md § "messages/bg.json (modify — add Phase 3 keys)" lines 1175–1230
    - src/lib/email/i18n-direct.ts (Phase 5 loadT factory — confirms ICU `{var}` placeholder syntax for the email.voteAnomaly.* keys below)
  </read_first>
  <action>
Add the following EXACT next-intl keys to `messages/bg.json`, merging at top level (do NOT replace existing keys; the file remains a single valid JSON document). All Cyrillic values copied verbatim from UI-SPEC.md §"Copywriting Contract" — DO NOT edit, paraphrase, or shorten.

Top-level `idea` namespace (NEW):
```json
"idea": {
  "catalog": {
    "title": "Идеи на коалицията",
    "subtitle": "Гласувай по политическите ни идеи и помогни да определим приоритетите."
  },
  "topic": {
    "taxes": "Данъци",
    "labor": "Трудово законодателство",
    "regulation": "Регулаторни режими",
    "financing": "Финансиране и кредит",
    "digitalization": "Цифровизация",
    "other": "Други"
  },
  "sort": {
    "newest": "Най-нови",
    "mostApproved": "Най-одобрени",
    "editorPicks": "Отбор на редактора"
  },
  "badge": {
    "editorPick": "Отбор на редактора"
  },
  "vote": {
    "approve": "Одобрявам",
    "reject": "Не одобрявам",
    "selectedApprove": "Одобрено от теб",
    "selectedReject": "Не одобрено от теб",
    "hint": "Гласът ти е таен. Един глас на акаунт.",
    "ariaApproveSelected": "Одобрявам — натисни за оттегляне",
    "ariaRejectSelected": "Не одобрявам — натисни за оттегляне",
    "ariaApproveChange": "Одобрявам — натисни за промяна",
    "ariaRejectChange": "Не одобрявам — натисни за промяна",
    "srAnnounceCast": "Гласът ти е записан.",
    "srAnnounceChangeApprove": "Гласът ти е променен на 'Одобрявам'.",
    "srAnnounceChangeReject": "Гласът ти е променен на 'Не одобрявам'.",
    "srAnnounceRetracted": "Гласът ти е оттеглен."
  },
  "count": {
    "approve": "{n} одобряват",
    "percent": "{p}% одобрение",
    "reject": "{n} не одобряват",
    "membersOnly": "за членове",
    "membersOnlySr": "Видимо само за регистрирани членове."
  },
  "threshold": {
    "detail": "Гласуването е в ход — резултатите ще се покажат след първите {N} гласа.",
    "cardShort": "Очаква още {N} гласа за резултат."
  },
  "cooling": {
    "indicator": "Гласът ти ще се отчете след {HH}ч {MM}м"
  },
  "toast": {
    "changed": "Гласът ти е променен.",
    "retracted": "Гласът ти е оттеглен.",
    "undo": "Отмени",
    "undoSuccess": "Възстановен."
  },
  "captcha": {
    "prompt": "Моля потвърди, че не си бот."
  },
  "rateLimit": {
    "hardError": "Превишаваш допустимата честота на гласуване. Опитай отново след няколко минути."
  },
  "empty": {
    "platform": "Скоро ще започнем да публикуваме идеи...",
    "platformBody": "Междувременно се присъедини към нашите общностни канали в WhatsApp и Telegram.",
    "platformCta": "Към общността",
    "filter": "Нямаме идеи в избраните теми.",
    "filterBody": "Опитай с други теми или премахни филтъра.",
    "filterReset": "Нулирай филтъра"
  },
  "gate": {
    "unverified": "За да гласуваш, потвърди имейла си.",
    "unverifiedCta": "Потвърди"
  },
  "error": {
    "network": "Възникна грешка. Моля опитай отново.",
    "loadCatalog": "Не успяхме да заредим идеите. Опитай по-късно.",
    "notFound": "Идеята не съществува или е архивирана.",
    "backToCatalog": "Към каталога"
  }
}
```

Extend existing `member` namespace (ADD, do NOT remove existing keys):
```json
"member": {
  "...": "existing keys preserved",
  "activity": {
    "title": "Моите гласове",
    "subtitle": "Идеите, по които си гласувал/а.",
    "empty": "Все още не си гласувал/а.",
    "emptyCta": "Разгледай идеи",
    "selectedApprove": "Одобрено от теб",
    "selectedReject": "Не одобрено от теб",
    "noResultYet": "Резултатът все още не се показва.",
    "change": "Промени",
    "retract": "Отдръпни"
  },
  "cta": {
    "exploreIdeas": "Към идеите",
    "exploreIdeasShort": "Разгледай идеи",
    "exploreCommunity": "Присъедини се към общността",
    "exploreCommunityShort": "Към общността"
  },
  "nav": {
    "profile": "Профил"
  }
}
```

(Note: keys `selectedApprove`, `selectedReject`, `noResultYet`, `change`, `retract` and the `*Short` CTA variants are added so plan 03-06 can replace its hardcoded Cyrillic example code with `t('member.activity.selectedApprove')` etc. — checker W-3.)

New top-level `profile` namespace (NEW):
```json
"profile": {
  "title": "Моят профил",
  "fields": {
    "fullName": "Име",
    "email": "Имейл",
    "registeredAt": "Регистрация",
    "sector": "Сектор",
    "role": "Роля",
    "preferredChannel": "Канал за известия"
  },
  "links": {
    "preferencesAside": "Промени в Настройки"
  },
  "gdpr": {
    "title": "GDPR — Моите данни",
    "body": "Под GDPR имаш право да изтеглиш всички данни, които пазим за теб.",
    "cta": "Изнеси моите данни",
    "coming": "Тази функция ще бъде достъпна в следваща версия."
  }
}
```

Extend existing `admin` namespace (ADD; if `admin` does not exist at top level, create it):
```json
"admin": {
  "voteAnomalies": {
    "title": "Аномалии при гласуване",
    "warning": "Замразяването е ръчно решение — масов глас НЕ е масова злоупотреба.",
    "helpExpand": "Какво означава това?",
    "helpBody": "Многобройни гласове за кратко време често са знак за виралност (споделяне на идеята). Замразявайте САМО ако виждате клъстер от freshly-registered акаунти от един и същи /24 subnet.",
    "trigger": {
      "perIdeaVelocity": "velocity",
      "subnetCluster": "subnet/24",
      "freshAccountShare": "fresh-acct"
    },
    "status": {
      "unresolved": "Нерешен",
      "dismissed": "Отхвърлен",
      "acted": "Реагиран"
    },
    "emptyPeriod": "Няма открити аномалии в избрания период.",
    "denied": "Достъпът отказан.",
    "loginRequired": "Трябва да влезеш."
  },
  "idea": {
    "viewOnSite": "Виж как изглежда на сайта",
    "viewOnSiteTooltip": "Отваря в нов прозорец.",
    "freeze": "Замрази отображаването",
    "unfreeze": "Размрази",
    "frozenBadge": "Замразено от {name} на {date}",
    "featuredOrderHelp": "Подредба за „Отбор на редактора“ (по-малки числа са първи).",
    "sidebar": {
      "stats": "Статистика",
      "totalVotes": "Общо гласове",
      "approveLine": "Одобряват",
      "rejectLine": "Не одобряват",
      "anomaliesLine": "Аномалии",
      "freezeSection": "Замрази публичното отображаване",
      "freezeBlurb": "Замразяването е ръчно решение — масов глас НЕ е масова злоупотреба.",
      "events": "Последни 5 събития",
      "allAnomalies": "Виж всички аномалии",
      "hidden": "Скрито"
    }
  },
  "votes": {
    "exclude": "Изключи избраните гласове",
    "selectionToolbar": "Избрани {n} гласа",
    "selectionCancel": "Отмяна на избора",
    "confirmTitle": "Изключване на гласове",
    "confirmBody": "Това ще премахне {n} гласа от резултата на идеята „{title}“.",
    "confirmWarning": "Тази операция е необратима. Гласовете ще бъдат премахнати от резултата, но запазени в одиторския журнал.",
    "confirmNoteLabel": "Бележка за журнала (по избор)",
    "confirmCancel": "Отказ",
    "confirmConfirm": "Изключи {n}-те гласа"
  }
}
```

Extend existing `email` top-level namespace (ADD `voteAnomaly` SUBKEY only — do NOT remove or rewrite existing email.* keys; the existing `email` object already contains Phase 1/5 keys):
```json
"email": {
  "...": "existing email.* keys preserved (Phase 1 / Phase 5)",
  "voteAnomaly": {
    "subject": "Аномалия в гласуване — Идея {ideaTitle}",
    "heading": "Аномалия в гласуване",
    "ideaLabel": "Идея",
    "triggerLabel": "Тригер",
    "countLabel": "Брой",
    "firstSeenLabel": "Първо засечен",
    "lastSeenLabel": "Последно засечен",
    "manualNote": "Замразяването е ръчно решение. Прегледай аномалията преди да предприемеш действие.",
    "cta": "Прегледай аномалията",
    "anomalyIdLabel": "Аномалия ID",
    "trigger": {
      "perIdeaVelocity": "Висока скорост на гласовете на тази идея",
      "subnetCluster": "Гласове от една и съща /24 подмрежа",
      "freshAccountShare": "Голям дял от млади акаунти"
    }
  }
}
```

(`email.voteAnomaly.*` is consumed by plan 03-08's React Email template via `loadT('email.voteAnomaly')` from `src/lib/email/i18n-direct.ts`. Placeholders use ICU `{ideaTitle}` syntax — i18n-direct's regex replaces `{var}` directly. No `%{var}` syntax.)

Validation: after the edit run `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"` to confirm JSON parses. Confirm zero key-name collisions with existing namespaces (especially the existing `email.*` subkeys — your edit MUST be additive within `email`).

**STRICT NO-VOCATIVE rule (Phase 1 D-27):** zero occurrences of `Уважаем` in any of the new keys (none of the strings above contain it; verify when copying).
  </action>
  <verify>
    <automated>node -e "const j = JSON.parse(require('fs').readFileSync('messages/bg.json','utf8')); const must = ['idea.vote.approve','idea.vote.reject','idea.threshold.detail','idea.cooling.indicator','idea.toast.retracted','idea.toast.undo','idea.captcha.prompt','idea.rateLimit.hardError','idea.empty.platform','idea.empty.filter','idea.topic.taxes','idea.topic.labor','idea.topic.regulation','idea.topic.financing','idea.topic.digitalization','idea.topic.other','idea.sort.newest','idea.sort.mostApproved','idea.sort.editorPicks','idea.catalog.title','member.activity.title','member.activity.selectedApprove','member.activity.selectedReject','member.activity.noResultYet','member.activity.change','member.activity.retract','member.cta.exploreIdeas','member.cta.exploreIdeasShort','member.cta.exploreCommunity','member.cta.exploreCommunityShort','member.nav.profile','profile.title','profile.gdpr.cta','profile.gdpr.coming','admin.voteAnomalies.title','admin.voteAnomalies.warning','admin.idea.freeze','admin.idea.unfreeze','admin.idea.viewOnSite','admin.votes.exclude','admin.votes.confirmConfirm','email.voteAnomaly.subject','email.voteAnomaly.heading','email.voteAnomaly.ideaLabel','email.voteAnomaly.triggerLabel','email.voteAnomaly.countLabel','email.voteAnomaly.firstSeenLabel','email.voteAnomaly.lastSeenLabel','email.voteAnomaly.manualNote','email.voteAnomaly.cta','email.voteAnomaly.anomalyIdLabel','email.voteAnomaly.trigger.perIdeaVelocity','email.voteAnomaly.trigger.subnetCluster','email.voteAnomaly.trigger.freshAccountShare']; for (const k of must) { const v = k.split('.').reduce((a,p)=>a&&a[p],j); if (typeof v !== 'string') { console.error('MISSING',k); process.exit(1);} } console.log('OK',must.length,'keys present');"</automated>
  </verify>
  <acceptance_criteria>
    - `messages/bg.json` parses as valid JSON
    - Every D-25 locked string is present at the documented key path with verbatim Cyrillic
    - Every UI-SPEC §Copywriting Contract Claude's-discretion key is present
    - All 13 `email.voteAnomaly.*` keys present with verbatim Cyrillic (consumed by plan 03-08)
    - All 5 new `member.activity.*` keys (selectedApprove/selectedReject/noResultYet/change/retract) present (consumed by plan 03-06)
    - All 4 `member.cta.*` keys (incl. `*Short` variants) present
    - Existing keys (auth.*, email.*, member.preferences.*, attribution.*, etc.) are unchanged
    - File grep `grep -c "Уважаем" messages/bg.json` returns `0`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wave 0 schema-grep regression test scaffolds</name>
  <files>tests/unit/voting-schema.test.ts, tests/unit/voting-no-pgenum.test.ts, tests/unit/append-only-vote-log.test.ts, tests/unit/server-action-no-revalidate.test.ts</files>
  <read_first>
    - tests/unit/attribution-schema.test.ts (existing analog — Phase 2.1 D-19 schema regression; lines 1–62)
    - tests/unit/consents-append-only.test.ts (Phase 1 D-13 append-only mirror — for append-only-vote-log.test.ts shape)
    - .planning/phases/03-idea-catalog-voting/03-RESEARCH.md § "Schema Regression Tests" lines 1300–1311
    - .planning/phases/03-idea-catalog-voting/03-PATTERNS.md § "tests/unit/voting-schema.test.ts" lines 1234–1284
  </read_first>
  <action>
Create FOUR Vitest unit-test files. Each file targets `src/db/schema/voting.ts`, `src/db/schema/moderation.ts`, or the Server Action files that DO NOT YET EXIST — every test in this task is RED on Wave 0. They turn GREEN as plans 03-02 and 03-04 ship.

**File 1: `tests/unit/voting-schema.test.ts`** — exactly the four tests from RESEARCH.md §"Schema Regression Tests" + PATTERNS.md §"tests/unit/voting-schema.test.ts". Use `readFileSync('src/db/schema/voting.ts', 'utf8')`. Strip comment lines via `src.split('\n').filter((line) => !line.trim().startsWith('//')).join('\n')` BEFORE grep (project convention — see PATTERNS line 1247 + tests/unit/attribution-schema.test.ts:8). Bare `grep -c` on raw source is a self-invalidating gate.

Tests in this file:
1. `it('vote_events_log schema contains no inet/raw_ip/ip_address/user_agent column (D-15)')` — codeOnly grep MUST NOT match `\binet\b|\braw_ip\b|\bip_address\b|\buser_agent\b`. The `ua_hash` column is allowed.
2. `it('votes table declares UNIQUE on (user_id, idea_id) — IDEA-04')` — match `unique\(.*user_id.*idea_id|user_id.*idea_id.*unique` (case-insensitive). Drizzle's `unique('votes_user_idea_unique').on(t.user_id, t.idea_id)` shape passes.
3. `it('vote_events_log declares onDelete cascade FK to users + ideas')` — match `references\(\(\)\s*=>\s*users\.id,\s*\{\s*onDelete:\s*'cascade'\s*\}\)` AND same for `ideas.id`.
4. `it('vote_events_log.user_id is NULLABLE (D-16 lawyer-flip path)')` — extract the `user_id:` column block via `src.match(/user_id:\s*uuid\([^)]+\)\s*\.references[^,]*\)/)`; assert it does NOT contain `.notNull()`.
5. `it('moderation_log target_ids is uuid array (D-21)')` — match `target_ids:.*uuid.*\.array\(\)` or `target_ids:.*uuid\(.*\)\.array\(\)`. Read `src/db/schema/moderation.ts`.
6. `it('moderation_log actor_user_id uses onDelete: \'restrict\' (audit preservation)')` — Read moderation.ts; match `actor_user_id[\s\S]*?references[^)]*onDelete:\s*'restrict'`.
7. `it('vote_anomalies has (status, last_detected_at DESC) admin-list index')` — read `src/db/schema/voting.ts` (or wherever vote_anomalies lives); assert presence of index named pattern `vote_anomalies.*(status.*last_detected|status_last)`.

Each test reads source via `readFileSync` and uses `expect(...).toMatch(...)` / `not.toMatch(...)`. RED state on Wave 0 because the schema files do not exist yet — that's the point.

**File 2: `tests/unit/voting-no-pgenum.test.ts`** — single describe; assert `src/db/schema/voting.ts` and `src/db/schema/moderation.ts` use `text(...)` not `pgEnum(...)` for `choice`, `action`, `topic`, `status`, `trigger_type`, `target_kind`. Implementation: read both files, comment-strip, grep `\bpgEnum\(` — must be empty. Mirrors `tests/unit/attribution-schema.test.ts` text-cols rule.

**File 3: `tests/unit/append-only-vote-log.test.ts`** — mirrors `tests/unit/consents-append-only.test.ts`. Use `execSync` to grep across `src/lib/voting/` AND `src/app/actions/`:
```typescript
const grep = execSync(
  `grep -rE "db\\.update\\(vote_events_log\\)|db\\.delete\\(vote_events_log\\)|db\\.update\\(moderation_log\\)|db\\.delete\\(moderation_log\\)" src/lib/voting/ src/app/actions/ 2>/dev/null || true`,
  { encoding: 'utf8' }
).trim();
expect(grep).toBe('');
```
Note: directories may not exist yet on Wave 0; the `|| true` keeps the assertion meaningful (an empty grep result is the success state). When directories appear in Wave 1+, the grep starts running for real.

**File 4: `tests/unit/server-action-no-revalidate.test.ts`** — Pitfall 7 + D-03 cache-no-bust lock. Read each of `src/app/actions/cast-vote.ts`, `src/app/actions/retract-vote.ts`, `src/app/actions/undo-retract.ts` if they exist; assert NONE matches `revalidatePath|revalidateTag`. Use a try/catch ENOENT skip during Wave 0 (file does not exist yet) and turn it into a passing assertion when the file exists. Pattern:
```typescript
import { existsSync, readFileSync } from 'node:fs';
const sources = [
  'src/app/actions/cast-vote.ts',
  'src/app/actions/retract-vote.ts',
  'src/app/actions/undo-retract.ts',
];
for (const f of sources) {
  if (!existsSync(f)) continue; // Wave 0 — file not yet created
  const src = readFileSync(f, 'utf8');
  expect(src).not.toMatch(/revalidatePath|revalidateTag/);
}
```

All four test files start with `import { describe, it, expect } from 'vitest';` and the appropriate `node:fs` / `node:child_process` imports. NO `'use server'` directive. NO Drizzle / Vitest mocks.

**Strict invariant:** every grep gate that uses `grep -c` must filter comments first (`grep -v '^\\s*//' | grep -c token`). Bare `== 0` gates on unfiltered files are forbidden per planner spec.
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/voting-schema.test.ts tests/unit/voting-no-pgenum.test.ts tests/unit/append-only-vote-log.test.ts tests/unit/server-action-no-revalidate.test.ts --run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist
    - `pnpm test:unit -- tests/unit/voting-schema.test.ts --run` exits non-zero (RED — voting.ts not yet created); test FAILURE messages reference "Cannot find file" or similar (this is the expected Wave 0 state)
    - `pnpm test:unit -- tests/unit/append-only-vote-log.test.ts --run` exits 0 (GREEN — empty grep on absent dirs is success)
    - `pnpm test:unit -- tests/unit/server-action-no-revalidate.test.ts --run` exits 0 (GREEN — files don't exist, loop skips, vacuously true)
    - `pnpm test:unit -- tests/unit/voting-no-pgenum.test.ts --run` exits non-zero (RED — voting.ts not yet created)
    - Grep ASSERT line `grep -c "grep -c" tests/unit/voting-schema.test.ts | grep -v "filter\\|comment"` confirms no bare `grep -c` gate exists in the test bodies
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wave 0 i18n + cooling-config + slug + Lexical-config tests</name>
  <files>tests/unit/voting-i18n.test.ts, tests/unit/cooling-config.test.ts, tests/unit/slug.test.ts, tests/unit/ideas-lexical-config.test.ts</files>
  <read_first>
    - tests/unit/i18n-tone.test.ts (Phase 1 — vocative-form ban pattern)
    - .planning/phases/03-idea-catalog-voting/03-CONTEXT.md D-25 + D-07 + D-10 + D-17 + D-18
    - .planning/phases/03-idea-catalog-voting/03-RESEARCH.md § "Bulgarian Streamlined Slug Helper" lines 798–867 + § "Lexical Allowed-Blocks Whitelist" lines 1151–1156
    - src/collections/Newsletters.ts (lines 95–119 — Lexical features list to mirror)
    - messages/bg.json (after Task 1 completes — keys to assert against)
  </read_first>
  <action>
**File 1: `tests/unit/voting-i18n.test.ts`** — every D-25 locked string AND every email.voteAnomaly.* string present at expected key path; no forbidden tokens.

```typescript
import { describe, it, expect } from 'vitest';
import bg from '../../messages/bg.json';

describe('Phase 3 D-25 — locked Bulgarian copy strings', () => {
  it('every D-25 locked vote string is present at the documented key path', () => {
    const j = bg as Record<string, any>;
    expect(j.idea.vote.approve).toBe('Одобрявам');
    expect(j.idea.vote.reject).toBe('Не одобрявам');
    expect(j.idea.vote.selectedApprove).toBe('Одобрено от теб');
    expect(j.idea.vote.selectedReject).toBe('Не одобрено от теб');
    expect(j.idea.count.approve).toBe('{n} одобряват');
    expect(j.idea.count.percent).toBe('{p}% одобрение');
    expect(j.idea.count.reject).toBe('{n} не одобряват');
    expect(j.idea.threshold.detail).toBe('Гласуването е в ход — резултатите ще се покажат след първите {N} гласа.');
    expect(j.idea.cooling.indicator).toBe('Гласът ти ще се отчете след {HH}ч {MM}м');
    expect(j.idea.toast.changed).toBe('Гласът ти е променен.');
    expect(j.idea.toast.retracted).toBe('Гласът ти е оттеглен.');
    expect(j.idea.toast.undo).toBe('Отмени');
    expect(j.idea.captcha.prompt).toBe('Моля потвърди, че не си бот.');
    expect(j.idea.rateLimit.hardError).toBe('Превишаваш допустимата честота на гласуване. Опитай отново след няколко минути.');
    expect(j.idea.empty.platform).toBe('Скоро ще започнем да публикуваме идеи...');
    expect(j.idea.empty.filter).toBe('Нямаме идеи в избраните теми.');
    expect(j.idea.empty.filterReset).toBe('Нулирай филтъра');
  });

  it('topic enum (D-07) has all six labels', () => {
    expect(bg.idea.topic.taxes).toBe('Данъци');
    expect(bg.idea.topic.labor).toBe('Трудово законодателство');
    expect(bg.idea.topic.regulation).toBe('Регулаторни режими');
    expect(bg.idea.topic.financing).toBe('Финансиране и кредит');
    expect(bg.idea.topic.digitalization).toBe('Цифровизация');
    expect(bg.idea.topic.other).toBe('Други');
  });

  it('member.activity copy (consumed by plan 03-06)', () => {
    expect(bg.member.activity.title).toBe('Моите гласове');
    expect(bg.member.activity.selectedApprove).toBe('Одобрено от теб');
    expect(bg.member.activity.selectedReject).toBe('Не одобрено от теб');
    expect(bg.member.activity.noResultYet).toBe('Резултатът все още не се показва.');
    expect(bg.member.activity.change).toBe('Промени');
    expect(bg.member.activity.retract).toBe('Отдръпни');
  });

  it('member.cta copy (consumed by plan 03-06)', () => {
    expect(bg.member.cta.exploreIdeas).toBe('Към идеите');
    expect(bg.member.cta.exploreIdeasShort).toBe('Разгледай идеи');
    expect(bg.member.cta.exploreCommunity).toBe('Присъедини се към общността');
    expect(bg.member.cta.exploreCommunityShort).toBe('Към общността');
  });

  it('admin moderation copy is present', () => {
    expect(bg.admin.votes.exclude).toBe('Изключи избраните гласове');
    expect(bg.admin.votes.confirmWarning).toBe('Тази операция е необратима. Гласовете ще бъдат премахнати от резултата, но запазени в одиторския журнал.');
    expect(bg.admin.idea.freeze).toBe('Замрази отображаването');
    expect(bg.admin.idea.unfreeze).toBe('Размрази');
  });

  it('email.voteAnomaly copy (consumed by plan 03-08 React Email template via i18n-direct loadT)', () => {
    expect(bg.email.voteAnomaly.subject).toBe('Аномалия в гласуване — Идея {ideaTitle}');
    expect(bg.email.voteAnomaly.heading).toBe('Аномалия в гласуване');
    expect(bg.email.voteAnomaly.ideaLabel).toBe('Идея');
    expect(bg.email.voteAnomaly.triggerLabel).toBe('Тригер');
    expect(bg.email.voteAnomaly.countLabel).toBe('Брой');
    expect(bg.email.voteAnomaly.firstSeenLabel).toBe('Първо засечен');
    expect(bg.email.voteAnomaly.lastSeenLabel).toBe('Последно засечен');
    expect(bg.email.voteAnomaly.manualNote).toBe('Замразяването е ръчно решение. Прегледай аномалията преди да предприемеш действие.');
    expect(bg.email.voteAnomaly.cta).toBe('Прегледай аномалията');
    expect(bg.email.voteAnomaly.anomalyIdLabel).toBe('Аномалия ID');
    expect(bg.email.voteAnomaly.trigger.perIdeaVelocity).toBe('Висока скорост на гласовете на тази идея');
    expect(bg.email.voteAnomaly.trigger.subnetCluster).toBe('Гласове от една и съща /24 подмрежа');
    expect(bg.email.voteAnomaly.trigger.freshAccountShare).toBe('Голям дял от млади акаунти');
  });

  it('NO vocative tokens in any Phase 3 namespace', () => {
    const namespaces = [
      bg.idea,
      bg.member?.activity,
      bg.member?.cta,
      bg.profile,
      bg.admin?.voteAnomalies,
      bg.admin?.idea,
      bg.admin?.votes,
      bg.email?.voteAnomaly,
    ];
    const blob = JSON.stringify(namespaces);
    expect(blob).not.toMatch(/Уважаем/);
    expect(blob).not.toMatch(/Уважаеми/);
  });
});
```

**File 2: `tests/unit/cooling-config.test.ts`** — D-10 regex validation. Read `src/lib/voting/cooling.ts` (when it exists Wave 1+); assert `parseInterval` throws on invalid + accepts valid:

```typescript
import { describe, it, expect } from 'vitest';
import { existsSync } from 'node:fs';

describe('Phase 3 D-10 — VOTE_COUNTABLE_INTERVAL parsing', () => {
  if (!existsSync('src/lib/voting/cooling.ts')) {
    it.skip('cooling.ts not yet created — Wave 1', () => {});
    return;
  }
  it('accepts valid intervals', async () => {
    const { parseInterval } = await import('../../src/lib/voting/cooling');
    expect(() => parseInterval('48 hours')).not.toThrow();
    expect(() => parseInterval('7 days')).not.toThrow();
    expect(() => parseInterval('1 hours')).not.toThrow();
  });
  it('rejects invalid format with throw', async () => {
    const { parseInterval } = await import('../../src/lib/voting/cooling');
    expect(() => parseInterval('48h')).toThrow(/Invalid VOTE_COUNTABLE_INTERVAL/);
    expect(() => parseInterval('7day')).toThrow();
    expect(() => parseInterval("48'; DROP TABLE users; --")).toThrow();
    expect(() => parseInterval('infinity')).toThrow();
    expect(() => parseInterval('')).toThrow();
  });
});
```

**File 3: `tests/unit/slug.test.ts`** — D-18 Bulgarian Streamlined transliteration. Wave 0 skips if `src/lib/voting/slug.ts` absent; Wave 1+ runs:

```typescript
import { describe, it, expect } from 'vitest';
import { existsSync } from 'node:fs';

describe('Phase 3 D-18 — slugifyBg Bulgarian Streamlined System', () => {
  if (!existsSync('src/lib/voting/slug.ts')) {
    it.skip('slug.ts not yet created — Wave 1', () => {});
    return;
  }
  it('full alphabet maps to Streamlined System', async () => {
    const { slugifyBg } = await import('../../src/lib/voting/slug');
    // Pairs: input → expected slug per Wikipedia Streamlined System table
    const cases: Array<[string, string]> = [
      ['Намаляване на ДДС', 'namalyavane-na-dds'],
      ['Жълтица', 'zhaltitsa'],     // Ж→zh, Ъ→a
      ['Щъркел', 'shtarkel'],         // Щ→sht
      ['Чужбина', 'chuzhbina'],       // Ч→ch, Ж→zh
      ['Юбилей', 'yubilei'],          // Ю→yu, Й→i
      ['Ябълка', 'yabalka'],          // Я→ya, Ъ→a
      ['Цар', 'tsar'],                // Ц→ts
      ['Шапка', 'shapka'],            // Ш→sh
      ['България', 'balgaria'],       // word-final -ия→-ia override (Bulgaria)
      ['София', 'sofia'],             // -ия→-ia
      ['Здравей свят', 'zdravei-svyat'],
    ];
    for (const [input, expected] of cases) {
      expect(slugifyBg(input)).toBe(expected);
    }
  });
  it('strips punctuation, collapses whitespace, lowercases', async () => {
    const { slugifyBg } = await import('../../src/lib/voting/slug');
    expect(slugifyBg('  Здравей,  свят!  ')).toBe('zdravei-svyat');
    expect(slugifyBg('A--B')).toBe('a-b');
    expect(slugifyBg('!!!')).toBe('');
  });
});
```

**File 4: `tests/unit/ideas-lexical-config.test.ts`** — D-17 allowed-blocks parity with Newsletters.

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync, existsSync } from 'node:fs';

describe('Phase 3 D-17 — Ideas Lexical allowed-blocks whitelist', () => {
  if (!existsSync('src/collections/Ideas.ts')) {
    it.skip('Ideas.ts not yet created — Wave 1', () => {});
    return;
  }
  const ideasSrc = readFileSync('src/collections/Ideas.ts', 'utf8');
  const newslettersSrc = readFileSync('src/collections/Newsletters.ts', 'utf8');

  const REQUIRED = [
    'ParagraphFeature',
    'HeadingFeature',
    'LinkFeature',
    'UnorderedListFeature',
    'OrderedListFeature',
    'BoldFeature',
    'ItalicFeature',
    'FixedToolbarFeature',
    'InlineToolbarFeature',
  ];
  const BANNED = ['CodeFeature', 'BlockquoteFeature', 'BlocksFeature', 'HTMLConverterFeature'];

  for (const feat of REQUIRED) {
    it(`Ideas Lexical config includes ${feat}`, () => {
      expect(ideasSrc).toMatch(new RegExp(`\\b${feat}\\b`));
    });
  }
  for (const feat of BANNED) {
    it(`Ideas Lexical config DOES NOT include ${feat} (D-17 banned)`, () => {
      expect(ideasSrc).not.toMatch(new RegExp(`\\b${feat}\\b`));
    });
  }
  it('Heading enabled sizes are exactly h2 + h3 (no h1, no h4–h6)', () => {
    expect(ideasSrc).toMatch(/enabledHeadingSizes:\s*\[\s*'h2',\s*'h3'\s*\]/);
  });
});
```

All four files use `import { describe, it, expect } from 'vitest';`. Existence checks via `existsSync` allow Wave 0 to pass `it.skip` until Wave 1+ creates the source.

**Coverage gate:** the slug test fixture has ≥10 cases including all 7 special letters (Ж, Ц, Ч, Ш, Щ, Ю, Я, Ъ). The cooling test covers SQL-injection input and empty string. The Lexical test asserts BOTH inclusion and exclusion. The voting-i18n test now also pins `email.voteAnomaly.*` (13 keys) so plan 03-08's worker email cannot drift.
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/voting-i18n.test.ts tests/unit/cooling-config.test.ts tests/unit/slug.test.ts tests/unit/ideas-lexical-config.test.ts --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist; Vitest can load them without syntax errors
    - `tests/unit/voting-i18n.test.ts` is GREEN (after Task 1 completes — `messages/bg.json` has all keys including email.voteAnomaly.*)
    - `tests/unit/cooling-config.test.ts`, `tests/unit/slug.test.ts`, `tests/unit/ideas-lexical-config.test.ts` are SKIP (existsSync false) on Wave 0 — turn GREEN when Wave 1+ creates source files
    - `pnpm test:unit -- tests/unit/voting-i18n.test.ts --run` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → CI | Test files are read by Vitest in CI; failing tests block PR merges |
| messages/bg.json → public bundle | Bulgarian copy is shipped to client; tone violations leak to user |
| messages/bg.json → BullMQ worker | email.voteAnomaly.* loaded via i18n-direct loadT; tone violations leak to editor inbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | bg.json — vocative-form regression | mitigate | `tests/unit/voting-i18n.test.ts` blocks `Уважаем` regex across all Phase 3 namespaces including email.voteAnomaly (Phase 1 D-27 / Pitfall 10 enforcement) |
| T-03-01-02 | Tampering | Wave 0 grep gates (self-invalidating headers) | mitigate | Every grep that counts uses `grep -v '^\s*//' | grep -c TOKEN`; bare `grep -c` on raw source is forbidden by planner spec; tests follow attribution-schema.test.ts:8 comment-strip pattern |
| T-03-01-03 | Repudiation | locked-string drift between CONTEXT D-25 / 03-08 email copy and bg.json | mitigate | The voting-i18n test pins each D-25 string AND each email.voteAnomaly.* string by exact equality; coalition can revise but the test fails loudly on drift, forcing a deliberate update |

All threats LOW severity (compile/test-time only; no runtime exposure). No HIGH+ threats in this plan; no blocking dependencies.
</threat_model>

<verification>
- All Wave 0 tests created (8 files); 4 turn GREEN immediately, 4 are RED-by-design until Wave 1 ships source
- `messages/bg.json` parses as valid JSON with all D-25 keys + all email.voteAnomaly.* keys present
- `grep -c "Уважаем" messages/bg.json` returns 0 (Phase 1 D-27 vocative ban)
- No regression on existing bg.json keys (`pnpm test:unit -- tests/unit/i18n-tone.test.ts --run` still passes)
- No source file under `src/` is created or modified — this plan is tests + i18n only
</verification>

<success_criteria>
- 8 new test files exist under `tests/unit/`
- `messages/bg.json` extended with idea.*, vote.*, member.activity.* (incl. selectedApprove/selectedReject/noResultYet/change/retract), member.cta.* (incl. *Short variants), member.nav.profile, profile.*, admin.voteAnomalies.*, admin.idea.*, admin.votes.*, email.voteAnomaly.* namespaces; existing keys unchanged
- `pnpm test:unit -- tests/unit/voting-i18n.test.ts tests/unit/append-only-vote-log.test.ts tests/unit/server-action-no-revalidate.test.ts --run` all GREEN
- `pnpm test:unit -- tests/unit/voting-schema.test.ts tests/unit/voting-no-pgenum.test.ts --run` all RED (source not yet created — by design)
- `tests/unit/cooling-config.test.ts`, `tests/unit/slug.test.ts`, `tests/unit/ideas-lexical-config.test.ts` SKIP (existsSync false)
- Existing test suite (`pnpm test:unit -- tests/unit/i18n-tone.test.ts --run`) still passes — no regression
</success_criteria>

<output>
After completion, create `.planning/phases/03-idea-catalog-voting/03-01-SUMMARY.md` documenting:
- All 8 test files created with their RED/GREEN/SKIP status table
- All bg.json keys added (count + namespace inventory, incl. email.voteAnomaly.* added per checker W-4)
- Verification commands and outputs
- Confirmation that no existing key was renamed or deleted
- Confirmation that ICU `{var}` placeholders (not `%{var}`) are used in email.voteAnomaly.* keys (compatible with i18n-direct loadT)
</output>
</content>
</invoke>