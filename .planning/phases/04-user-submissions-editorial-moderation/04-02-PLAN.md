---
phase: 04-user-submissions-editorial-moderation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/bg.json
  - tests/unit/i18n-phase-04-strings.test.ts
autonomous: true
requirements:
  - PROP-01
  - PROP-02
  - PROP-03
  - PROP-04
  - PROB-01
  - PROB-02
  - PROB-03
  - PROB-04
  - PROB-05
  - EDIT-04
  - EDIT-05
  - EDIT-06
tags: [phase-4, wave-1, i18n, d-25-string-lock, foundation]
must_haves:
  truths:
    - "Every Bulgarian string referenced by Phase 4 source files (forms, status pages, public pages, admin queue, DSA report, status-change emails, suspended-account gate) exists in messages/bg.json under one of the new namespaces (submission.*, problem.*, admin.queue.*, admin.moderation.*, admin.suspended.*, dsa.report.*, email.submissionStatus.*) BEFORE any source file references those keys (D-25 contract)"
    - "The two D-C1 canonical attribution strings 'Член на коалицията' (proposals) and 'Анонимен сигнал' (problems) appear EXACTLY ONCE each across all of bg.json — no variants like 'Анонимен член', 'Гражданин', 'Потребител'"
    - "The 7-topic taxonomy is defined ONCE under submission.topics.* and is referenced by both the proposal form (Plan 04-03) and the problem report form (Plan 04-03) — single source of truth"
    - "Status badge labels (pending|approved|rejected) under submission.status.* match the locked UI-SPEC §S5 copy verbatim ('Изчаква преглед', 'Одобрено', 'Отхвърлено')"
  artifacts:
    - path: messages/bg.json
      provides: "Phase 4 string registry under submission.*, problem.*, admin.queue.*, admin.moderation.*, admin.suspended.*, dsa.report.*, email.submissionStatus.* namespaces"
      contains: "submission.proposals.pageTitle"
    - path: tests/unit/i18n-phase-04-strings.test.ts
      provides: "Wave 0 string-presence test: assert every Phase 4 namespace key from UI-SPEC §Copywriting Contract resolves; assert canonical attribution strings appear exactly once"
      contains: "Член на коалицията"
  key_links:
    - from: "messages/bg.json"
      to: "src/components/forms/ProposalForm.tsx (Plan 04-03)"
      via: "useTranslations('submission.proposal') resolves submitTitle/submitCta/successToast/fields.* without falling back"
      pattern: "submission\\.proposal\\."
    - from: "messages/bg.json"
      to: "src/app/(payload)/admin/views/moderation-queue/* (Plan 04-06)"
      via: "direct JSON import (no next-intl Provider in Payload admin) — keys under admin.queue.* and admin.moderation.*"
      pattern: "admin\\.(queue|moderation|suspended)\\."
    - from: "messages/bg.json"
      to: "src/lib/email/templates/* (Plan 04-07)"
      via: "loadT pattern reads email.submissionStatus.{approved,rejected}.{subject,body} for status-change notifications"
      pattern: "email\\.submissionStatus\\."
---

<objective>
Lock every Phase 4 user-facing Bulgarian string into `messages/bg.json` BEFORE any JSX, Server Action, or email template references them — the D-25 string-lock contract that worked for Phases 02.x, 3, 5.

Purpose: All six Wave 2/3 plans (member forms, member status views, public pages, moderation queue, editorial actions, DSA reporting) consume these keys. Locking the registry once in Wave 1 prevents the "string drift" failure mode where developers paste Bulgarian text into JSX directly (lint:i18n catches some; D-C1 string-uniqueness violations slip through).

Output: One `messages/bg.json` edit + one Wave 0 unit test (`tests/unit/i18n-phase-04-strings.test.ts`) that guards key presence and the D-C1 anti-variant invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-user-submissions-editorial-moderation/04-CONTEXT.md
@.planning/phases/04-user-submissions-editorial-moderation/04-RESEARCH.md
@.planning/phases/04-user-submissions-editorial-moderation/04-PATTERNS.md
@.planning/phases/04-user-submissions-editorial-moderation/04-UI-SPEC.md
@.planning/phases/04-user-submissions-editorial-moderation/04-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- Existing JSON structure to extend. Read first; do NOT re-format the existing keys. -->

From messages/bg.json (existing file is 586 lines, top-level keys include `site`, `auth`, `cookie`, `landing`, `agenda`, `faq`, `member`, `community`, `attribution`, `email`, `oblast`, plus more). Phase 4 ADDS three top-level namespaces (`submission`, `problem`, `dsa`) and EXTENDS two existing namespaces (`admin`, `email`).

Existing top-level skeleton (illustrative; do not modify):
```json
{
  "site": { ... },
  "auth": { ... },
  "cookie": { ... },
  "landing": { ... },
  "agenda": { ... },
  "faq": { ... },
  "member": { ... },
  "community": { ... },
  "attribution": { ... },
  "email": {
    "subjects": { ... }
  },
  "oblast": { "BG-01": "Благоевград", ... }
}
```

From .planning/phases/04-user-submissions-editorial-moderation/04-UI-SPEC.md §Copywriting Contract (lines 624-684) — the AUTHORITATIVE list of strings to insert. Use those exact Bulgarian labels and the exact key paths in the table.

From .planning/phases/04-user-submissions-editorial-moderation/04-PATTERNS.md (lines 217-287) — JSON shape sketch the executor adapts to the final UI-SPEC keys.

From .planning/phases/04-user-submissions-editorial-moderation/04-RESEARCH.md §bg.json String Registry (lines 750-786) — provisional 7-topic taxonomy:
- `taxes` → "Данъчна тежест"
- `admin_barriers` → "Административни пречки"
- `financing` → "Достъп до финансиране"
- `labor` → "Трудов пазар"
- `digitalization` → "Цифровизация"
- `energy` → "Енергийни разходи"
- `other` → "Друго"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend messages/bg.json with Phase 4 string namespaces</name>
  <files>messages/bg.json</files>
  <read_first>
    - messages/bg.json (the WHOLE file — read it once and identify the right insertion points; do NOT touch unrelated keys)
    - .planning/phases/04-user-submissions-editorial-moderation/04-UI-SPEC.md §Copywriting Contract (the exact strings + key paths)
    - .planning/phases/04-user-submissions-editorial-moderation/04-PATTERNS.md (lines 217-287 JSON sketch)
  </read_first>
  <behavior>
    - bg.json adds top-level `submission` namespace with sub-namespaces: `proposals`, `proposal`, `myProposals`, `problem`, `myProblems`, `status`, `error`, `gate`, `topics`
    - bg.json adds top-level `problem` namespace with sub-namespaces: `heatmap`, `anonymousByline`, `level`
    - bg.json adds top-level `dsa` namespace with sub-namespace: `report`
    - bg.json EXTENDS existing top-level `admin` namespace (or creates if absent) with `queue`, `moderation`, `suspended`
    - bg.json EXTENDS existing top-level `email` namespace with `submissionStatus.{approved,rejected}.{subject,body}` and `email.suspended.{subject,body}` (used by Plan 04-07 status-change worker job)
    - Every UI-SPEC §Copywriting Contract row has a corresponding key in bg.json
    - "Член на коалицията" appears EXACTLY ONCE in bg.json (under `submission.proposals.anonymousByline`); "Анонимен сигнал" appears EXACTLY ONCE (under `problem.anonymousByline`)
  </behavior>
  <action>
    1. Read `messages/bg.json` to confirm whether `admin` and `email` are top-level keys already. The Phase 5 `email.subjects.*` namespace exists; Phase 4 extends `email.submissionStatus.*` and `email.suspended.*` alongside it.

    2. Add the following key blocks to `messages/bg.json`. PLACE the new top-level keys (`submission`, `problem`, `dsa`) ALPHABETICALLY between existing keys to keep the file scannable; the key order within a JSON object is not load-bearing for next-intl resolution but operator readability matters. Strings below are EXACT — do not translate, paraphrase, or substitute.

       **Add top-level `submission`:**
       ```json
       "submission": {
         "proposals": {
           "pageTitle": "Предложения от общността",
           "pageDescription": "Одобрените предложения на членовете на коалицията.",
           "votingSoon": "Гласуването по предложенията предстои. Следете за обновления.",
           "anonymousByline": "Член на коалицията",
           "emptyHeading": "Предложенията скоро ще се появят тук.",
           "emptyBody": "Бъди първият — изпрати предложение за политическо решение.",
           "emptyCta": "Предложи идея"
         },
         "proposal": {
           "formTitle": "Предложи политическо решение",
           "formDescription": "Предложенията минават модерация и след одобрение стават видими за всички членове.",
           "submitCta": "Изпрати предложение",
           "successToast": "Предложението е изпратено за преглед.",
           "fields": {
             "title": "Заглавие",
             "titlePlaceholder": "Опиши предложението накратко...",
             "titleHelp": "Макс. 120 знака",
             "body": "Описание",
             "bodyPlaceholder": "Опиши проблема, предложеното решение и очаквания ефект за МСП сектора...",
             "bodyHelp": "Мин. 50 знака, макс. 2 000 знака",
             "topic": "Тема",
             "topicPlaceholder": "Избери тема..."
           }
         },
         "myProposals": {
           "pageTitle": "Мои предложения",
           "newCta": "Ново предложение",
           "emptyHeading": "Все още нямаш подадени предложения.",
           "emptyBody": "Подай своето първо предложение за политическо решение.",
           "emptyCta": "Предложи идея"
         },
         "problem": {
           "formTitle": "Сигнализирай за проблем",
           "formDescription": "Сигналите минават модерация. Публично се показват само агрегирано по области — без лично идентифициране.",
           "submitCta": "Изпрати сигнал",
           "successToast": "Сигналът е изпратен за преглед.",
           "fields": {
             "body": "Описание",
             "bodyPlaceholder": "Опиши конкретния проблем и неговото въздействие върху твоя бизнес...",
             "bodyHelp": "Мин. 30 знака, макс. 1 000 знака",
             "topic": "Тема",
             "level": "Ниво",
             "oblast": "Област"
           }
         },
         "myProblems": {
           "pageTitle": "Мои сигнали",
           "newCta": "Нов сигнал",
           "emptyHeading": "Все още нямаш подадени сигнали.",
           "emptyBody": "Сигнализирай за проблем, засягащ твоя бизнес.",
           "emptyCta": "Изпрати сигнал"
         },
         "status": {
           "pending": "Изчаква преглед",
           "approved": "Одобрено",
           "rejected": "Отхвърлено",
           "rejectionNotePrefix": "Бележка от редактора:"
         },
         "error": {
           "rateLimit": "Превишаваш допустимата честота на подаване. Опитай отново след {n} минути.",
           "captchaFailed": "Проверката за бот не премина. Презареди страницата и опитай отново.",
           "validation": "Моля, попълни всички задължителни полета."
         },
         "gate": {
           "unverified": "Потвърди имейла си, за да подаваш предложения.",
           "suspended": "Акаунтът ти е временно спрян. За въпроси се свържи с екипа на коалицията."
         },
         "topics": {
           "taxes": "Данъчна тежест",
           "admin_barriers": "Административни пречки",
           "financing": "Достъп до финансиране",
           "labor": "Трудов пазар",
           "digitalization": "Цифровизация",
           "energy": "Енергийни разходи",
           "other": "Друго"
         }
       },
       ```

       **Add top-level `problem`:**
       ```json
       "problem": {
         "heatmap": {
           "pageTitle": "Сигнали за проблеми",
           "pageDescription": "Агрегирани сигнали на членовете на коалицията по области.",
           "suppressed": "данните са недостатъчни",
           "tooltipFormat": "{oblastName}: {count} сигнала",
           "emptyBody": "Все още нямаме достатъчно сигнали за показване. Изпрати сигнал и помогни да картографираме проблемите на МСП сектора.",
           "emptyCta": "Изпрати сигнал",
           "table": {
             "columnOblast": "Област",
             "columnCount": "Сигнали",
             "columnTopTopic": "Топ тема",
             "nationalLabel": "Централно ниво"
           },
           "ariaMapLabel": "Карта на сигналите по области"
         },
         "anonymousByline": "Анонимен сигнал",
         "level": {
           "local": "Местно ниво",
           "national": "Централно ниво"
         }
       },
       ```

       **Add top-level `dsa`:**
       ```json
       "dsa": {
         "report": {
           "buttonLabel": "Сигнализирай за съдържание",
           "heading": "Сигнализирай за неподходящо съдържание",
           "body": "По силата на чл. 16 от Регламента за цифровите услуги (DSA) можеш да подадеш сигнал за съдържание, което смяташ за нарушаващо правилата.",
           "categoryLabel": "Категория на нарушението",
           "categories": {
             "illegal": "Незаконно съдържание",
             "harassment": "Тормоз или заплахи",
             "misinformation": "Дезинформация",
             "spam": "Спам или измама",
             "other": "Друго"
           },
           "reasonLabel": "Опиши причината за сигнализиране",
           "reasonPlaceholder": "Какво в това съдържание смяташ за неподходящо?",
           "goodFaithLabel": "Потвърждавам, че докладвам добросъвестно и предоставям точна информация.",
           "submitCta": "Изпрати сигнал",
           "successHeading": "Сигналът ти беше получен.",
           "successBody": "Ще те уведомим за решението на редакционния екип на твоя имейл."
         }
       },
       ```

       **Extend top-level `admin` (create the key if absent — Phase 5 may have introduced `admin` already; if so, MERGE):**
       ```json
       "admin": {
         "queue": {
           "pageTitle": "Опашка за модерация",
           "denied": "Нямаш права за достъп до тази страница.",
           "loginRequired": "Необходим е вход в системата.",
           "pendingSummary": "Изчакващи преглед: {proposals} предложения, {problems} сигнала, {dsa} DSA сигнала",
           "tabProposals": "Предложения",
           "tabProblems": "Сигнали",
           "tabDsa": "DSA сигнали",
           "filterPending": "Изчакват ({n})",
           "filterApproved": "Одобрени",
           "filterRejected": "Отхвърлени",
           "filterAll": "Всички",
           "columnSubmittedAt": "Подадено",
           "columnKind": "Тип",
           "columnTitleExcerpt": "Заглавие / Откъс",
           "columnSubmitter": "Подател",
           "columnAction": "Действие",
           "kindProposal": "Предложение",
           "kindProblem": "Сигнал",
           "kindDsa": "DSA сигнал",
           "reviewAction": "Прегледай заявката",
           "empty": "Няма чакащи за преглед."
         },
         "moderation": {
           "submitterDetailsToggle": "Виж подробности за подателя",
           "submitterRegisteredOn": "Регистриран:",
           "submitterChannel": "Канал:",
           "submitterRoleSector": "Роля / Сектор:",
           "submitterSource": "Източник:",
           "moderatorNoteLabel": "Бележка (задължителна при отхвърляне)",
           "moderatorNotePlaceholder": "Причина за решението...",
           "approveHeading": "Одобри предложението?",
           "approveBody": "Предложението ще стане видимо публично на страница /предложения.",
           "approveDismiss": "Не одобрявай",
           "approveAction": "Одобри",
           "rejectHeading": "Отхвърли предложението?",
           "rejectBody": "Членът ще види статус 'Отхвърлено' и бележката ти.",
           "rejectDismiss": "Не отхвърляй",
           "rejectAction": "Отхвърли",
           "suspendHeading": "Спри акаунта на {name}?",
           "suspendBody": "Посочи причина — тя ще бъде записана в журнала на модерацията.",
           "suspendDismiss": "Не спирай акаунта",
           "suspendAction": "Спри акаунта",
           "overrideHeading": "Промени решението?",
           "overrideBody": "Сигурен ли си? Ще трябва да добавиш бележка за причината.",
           "overrideDismiss": "Не променяй",
           "overrideAction": "Промени решението"
         },
         "suspended": {
           "pageTitle": "Акаунтът е спрян",
           "body": "Твоят акаунт е временно спрян от редакционния екип. За въпроси се свържи с нас."
         }
       },
       ```

       **Extend top-level `email` (do not overwrite existing keys — add the two sub-namespaces):**
       ```json
       "email": {
         /* ... existing email.* keys preserved verbatim ... */,
         "submissionStatus": {
           "approved": {
             "subject": "Вашето предложение беше одобрено",
             "body": "Здравей, {fullName}!\n\nПредложението ти „{title}\" беше прегледано и одобрено от редакционния екип. Вече е видимо публично на страница /предложения.\n\nБлагодарим ти за приноса към общността на коалицията."
           },
           "rejected": {
             "subject": "Вашето предложение не беше одобрено",
             "body": "Здравей, {fullName}!\n\nПредложението ти „{title}\" не беше одобрено.\n\nБележка от редактора: {note}\n\nМожеш да подадеш ново предложение по всяко време."
           }
         },
         "suspended": {
           "subject": "Акаунтът ти е временно спрян",
           "body": "Здравей, {fullName}!\n\nАкаунтът ти беше временно спрян от редакционния екип на коалицията. Причина: {reason}\n\nЗа въпроси се свържи с нас."
         }
       },
       ```

    3. Validate the file is well-formed JSON: `pnpm exec node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"`. If parse fails, fix the trailing-comma or escaped-quote issue.

    4. Run the existing i18n linter (it shipped in Phase 02.x): `pnpm lint:i18n`. The linter must NOT regress.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))" &amp;&amp; pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"` exits 0
    - `grep -c '"submission"' messages/bg.json` returns at least 1 (top-level key present)
    - `grep -c '"problem"' messages/bg.json` returns at least 1
    - `grep -c '"dsa"' messages/bg.json` returns at least 1
    - `grep -c '"submissionStatus"' messages/bg.json` returns 1
    - `grep -c '"Член на коалицията"' messages/bg.json` returns EXACTLY 1
    - `grep -c '"Анонимен сигнал"' messages/bg.json` returns EXACTLY 1
    - `grep -c '"votingSoon"' messages/bg.json` returns 1 with the exact value `"Гласуването по предложенията предстои. Следете за обновления."`
    - `pnpm lint:i18n` exits 0
  </acceptance_criteria>
  <done>messages/bg.json contains every Phase 4 key from UI-SPEC §Copywriting Contract; D-C1 anonymity strings appear exactly once each; file is valid JSON; i18n linter passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 string-presence test (lock the registry)</name>
  <files>tests/unit/i18n-phase-04-strings.test.ts</files>
  <read_first>
    - messages/bg.json (after Task 1 — to know which keys must resolve)
    - .planning/phases/04-user-submissions-editorial-moderation/04-VALIDATION.md (row "bg.json keys for Phase 4 are present and resolved")
    - .planning/phases/04-user-submissions-editorial-moderation/04-UI-SPEC.md §Copywriting Contract (the canonical list)
  </read_first>
  <behavior>
    - Test file imports `bg.json` and asserts every required key path resolves to a non-empty string
    - Test asserts the D-C1 invariant: "Член на коалицията" appears EXACTLY ONCE; "Анонимен сигнал" appears EXACTLY ONCE; no banned variants ("Анонимен член", "Гражданин", "Потребител") appear anywhere
    - Test asserts the topic taxonomy is internally consistent (all 7 topic keys exist under submission.topics)
    - Test asserts `submission.error.rateLimit` contains the `{n}` placeholder (next-intl ICU)
    - Test asserts `email.submissionStatus.approved.body` contains both `{fullName}` and `{title}` placeholders; `email.submissionStatus.rejected.body` contains `{fullName}`, `{title}`, and `{note}`
  </behavior>
  <action>
    Create `tests/unit/i18n-phase-04-strings.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import bg from '../../messages/bg.json';
    import { readFileSync } from 'fs';
    import { join } from 'path';

    type Bag = Record<string, unknown>;
    function getPath(obj: Bag, path: string): unknown {
      return path.split('.').reduce<unknown>((acc, k) => {
        if (acc && typeof acc === 'object') return (acc as Bag)[k];
        return undefined;
      }, obj as unknown);
    }

    const REQUIRED_KEYS = [
      // Public proposals page
      'submission.proposals.pageTitle',
      'submission.proposals.pageDescription',
      'submission.proposals.votingSoon',
      'submission.proposals.anonymousByline',
      'submission.proposals.emptyHeading',
      'submission.proposals.emptyBody',
      'submission.proposals.emptyCta',
      // Proposal form
      'submission.proposal.formTitle',
      'submission.proposal.formDescription',
      'submission.proposal.submitCta',
      'submission.proposal.successToast',
      'submission.proposal.fields.title',
      'submission.proposal.fields.body',
      'submission.proposal.fields.topic',
      // My proposals page
      'submission.myProposals.pageTitle',
      'submission.myProposals.emptyHeading',
      'submission.myProposals.emptyBody',
      'submission.myProposals.emptyCta',
      // Problem form
      'submission.problem.formTitle',
      'submission.problem.formDescription',
      'submission.problem.submitCta',
      'submission.problem.successToast',
      'submission.problem.fields.body',
      'submission.problem.fields.topic',
      'submission.problem.fields.level',
      'submission.problem.fields.oblast',
      // My problems page
      'submission.myProblems.pageTitle',
      'submission.myProblems.emptyHeading',
      'submission.myProblems.emptyBody',
      'submission.myProblems.emptyCta',
      // Status badges
      'submission.status.pending',
      'submission.status.approved',
      'submission.status.rejected',
      'submission.status.rejectionNotePrefix',
      // Errors and gates
      'submission.error.rateLimit',
      'submission.error.captchaFailed',
      'submission.error.validation',
      'submission.gate.unverified',
      'submission.gate.suspended',
      // Topics (7)
      'submission.topics.taxes',
      'submission.topics.admin_barriers',
      'submission.topics.financing',
      'submission.topics.labor',
      'submission.topics.digitalization',
      'submission.topics.energy',
      'submission.topics.other',
      // Public heat-map
      'problem.heatmap.pageTitle',
      'problem.heatmap.pageDescription',
      'problem.heatmap.suppressed',
      'problem.heatmap.tooltipFormat',
      'problem.heatmap.emptyBody',
      'problem.heatmap.emptyCta',
      'problem.heatmap.table.columnOblast',
      'problem.heatmap.table.columnCount',
      'problem.heatmap.table.columnTopTopic',
      'problem.heatmap.table.nationalLabel',
      'problem.heatmap.ariaMapLabel',
      'problem.anonymousByline',
      'problem.level.local',
      'problem.level.national',
      // DSA report
      'dsa.report.buttonLabel',
      'dsa.report.heading',
      'dsa.report.body',
      'dsa.report.categoryLabel',
      'dsa.report.categories.illegal',
      'dsa.report.categories.other',
      'dsa.report.reasonLabel',
      'dsa.report.reasonPlaceholder',
      'dsa.report.goodFaithLabel',
      'dsa.report.submitCta',
      'dsa.report.successHeading',
      'dsa.report.successBody',
      // Admin queue
      'admin.queue.pageTitle',
      'admin.queue.denied',
      'admin.queue.loginRequired',
      'admin.queue.pendingSummary',
      'admin.queue.tabProposals',
      'admin.queue.tabProblems',
      'admin.queue.tabDsa',
      'admin.queue.reviewAction',
      'admin.queue.empty',
      // Admin moderation dialogs
      'admin.moderation.approveHeading',
      'admin.moderation.approveBody',
      'admin.moderation.approveDismiss',
      'admin.moderation.approveAction',
      'admin.moderation.rejectHeading',
      'admin.moderation.rejectBody',
      'admin.moderation.rejectDismiss',
      'admin.moderation.rejectAction',
      'admin.moderation.suspendHeading',
      'admin.moderation.suspendBody',
      'admin.moderation.suspendDismiss',
      'admin.moderation.suspendAction',
      'admin.moderation.overrideHeading',
      'admin.moderation.overrideBody',
      'admin.moderation.overrideAction',
      // Admin suspended page
      'admin.suspended.pageTitle',
      'admin.suspended.body',
      // Email status-change templates
      'email.submissionStatus.approved.subject',
      'email.submissionStatus.approved.body',
      'email.submissionStatus.rejected.subject',
      'email.submissionStatus.rejected.body',
      'email.suspended.subject',
      'email.suspended.body',
    ];

    describe('Phase 4 i18n string registry', () => {
      it('every required key resolves to a non-empty string', () => {
        for (const key of REQUIRED_KEYS) {
          const value = getPath(bg as Bag, key);
          expect(value, `Missing key: ${key}`).toBeTypeOf('string');
          expect((value as string).length, `Empty string for key: ${key}`).toBeGreaterThan(0);
        }
      });

      it('D-C1: anonymous attribution strings appear exactly once across all of bg.json', () => {
        const raw = readFileSync(join(process.cwd(), 'messages/bg.json'), 'utf8');
        expect((raw.match(/"Член на коалицията"/g) ?? []).length).toBe(1);
        expect((raw.match(/"Анонимен сигнал"/g) ?? []).length).toBe(1);
      });

      it('D-C1: banned anonymous-byline variants do not appear anywhere', () => {
        const raw = readFileSync(join(process.cwd(), 'messages/bg.json'), 'utf8');
        expect(raw).not.toMatch(/"Анонимен член"/);
        expect(raw).not.toMatch(/"Гражданин"/);
        expect(raw).not.toMatch(/"Потребител"/);
      });

      it('rate-limit error carries the {n} ICU placeholder', () => {
        const v = getPath(bg as Bag, 'submission.error.rateLimit') as string;
        expect(v).toMatch(/\{n\}/);
      });

      it('email.submissionStatus templates carry required ICU placeholders', () => {
        const approvedBody = getPath(bg as Bag, 'email.submissionStatus.approved.body') as string;
        expect(approvedBody).toMatch(/\{fullName\}/);
        expect(approvedBody).toMatch(/\{title\}/);
        const rejectedBody = getPath(bg as Bag, 'email.submissionStatus.rejected.body') as string;
        expect(rejectedBody).toMatch(/\{fullName\}/);
        expect(rejectedBody).toMatch(/\{title\}/);
        expect(rejectedBody).toMatch(/\{note\}/);
      });

      it('topic taxonomy: 7 keys exist under submission.topics', () => {
        const topics = getPath(bg as Bag, 'submission.topics') as Bag;
        expect(Object.keys(topics).sort()).toEqual(
          ['admin_barriers', 'digitalization', 'energy', 'financing', 'labor', 'other', 'taxes'].sort(),
        );
      });

      it('status badge labels match UI-SPEC §S5 verbatim', () => {
        expect(getPath(bg as Bag, 'submission.status.pending')).toBe('Изчаква преглед');
        expect(getPath(bg as Bag, 'submission.status.approved')).toBe('Одобрено');
        expect(getPath(bg as Bag, 'submission.status.rejected')).toBe('Отхвърлено');
      });
    });
    ```
  </action>
  <verify>
    <automated>pnpm test:unit -- --run tests/unit/i18n-phase-04-strings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit -- --run tests/unit/i18n-phase-04-strings.test.ts` exits 0 with all 7 it-blocks passing
    - Test fails loudly if any required key is missing or empty
    - Test fails loudly if either anonymity string appears 0 or >1 times
    - Test fails loudly if any banned variant appears
  </acceptance_criteria>
  <done>String registry locked and guarded by an automated test that runs on every Phase 4 commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-edited bg.json → user-visible Bulgarian copy | A typo or accidental delete becomes user-visible immediately on next deploy |
| Phase 4 keys → Phase 5 status-change emails | email.submissionStatus.* is read by the BullMQ worker (Plan 04-07); ICU placeholder mismatch produces broken Bulgarian |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | D-C1 anonymity violation via stray Bulgarian variant in JSX bypassing bg.json | mitigate | tests/unit/i18n-phase-04-strings.test.ts asserts canonical strings appear EXACTLY once; pnpm lint:i18n (Phase 02.x) catches hardcoded Bulgarian in JSX. Two layers. |
| T-04-02-02 | Tampering | Wrong ICU placeholder in email body produces literal "{title}" in the recipient's inbox | mitigate | The Wave 0 test asserts `email.submissionStatus.*.body` contain {fullName}/{title}/{note} placeholders; Plan 04-07 worker handler must reference these exact keys. |
| T-04-02-03 | Spoofing | Translator drift: a contributor "improves" "Изчаква преглед" → "Чака преглед" breaking UI-SPEC contract | mitigate | Status-badge it-block in the test pins the exact strings ('Изчаква преглед', 'Одобрено', 'Отхвърлено') — drift fails CI. |
| T-04-02-04 | Denial of Service | Malformed JSON breaks the entire app's i18n loader | mitigate | JSON.parse smoke check in verify; pnpm lint:i18n catches structural issues; tsc compilation fails on next-intl `getTranslations` for invalid keys (Plan 04-03+). |

ASVS Level 1: V5 (input validation extends to externalized strings); no HIGH-severity threats unaddressed.
</threat_model>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"` exits 0
- `pnpm test:unit -- --run tests/unit/i18n-phase-04-strings.test.ts` exits 0
- `pnpm lint:i18n` exits 0
- VALIDATION.md row "bg.json keys for Phase 4 are present and resolved" satisfied
</verification>

<success_criteria>
- All 4 must-have truths hold
- bg.json well-formed; lint:i18n green; i18n test green
- Wave 2 plans can write `getTranslations('submission.proposals')`, `getTranslations('admin.queue')`, etc. without falling back to "MISSING_MESSAGE"
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-submissions-editorial-moderation/04-02-SUMMARY.md` recording:
- Number of new top-level keys added to bg.json
- Final byte-size delta of bg.json
- Any UI-SPEC §Copywriting Contract row that was reworded during implementation (if zero, state "no drift from UI-SPEC")
</output>
