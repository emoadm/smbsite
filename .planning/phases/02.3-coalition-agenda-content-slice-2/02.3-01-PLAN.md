---
phase: 02.3-coalition-agenda-content-slice-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(frontend)/agenda/page.tsx
  - tests/unit/agenda-toc-anchors.test.ts
autonomous: false
mode: mvp
slice: simple-prose-chapters
requirements:
  - REQ-AGENDA-S2-01
  - REQ-AGENDA-S2-02
  - REQ-AGENDA-S2-04
  - REQ-AGENDA-S2-05
  - D-CoalitionContent-Agenda
must_haves:
  truths:
    - "A visitor opening /agenda sees four NEW top-level chapters appended after Икономика, in this exact source order: Енергетика, ресурси и околна среда (#energetika); Земеделие (#zemedelie); Здравеопазване (#zdraveopazvane); Външна сигурност и отбрана (#vanshna-sigurnost)"
    - "Each new chapter is its own <section> containing exactly one <h2 id=\"…\"> anchor target plus prose body (paragraphs, lists)"
    - "TOC_ITEMS has been extended from 3 → 7 entries with the four new IDs appended in source order; the rendered Table of Contents (sticky desktop sidebar + mobile <details>) shows all 7 chapter labels"
    - "Clicking any of the 7 TOC entries jumps the viewport to the matching <h2>; scroll-padding-top: 5rem keeps the heading visible under the sticky header"
    - "The <Alert> draftAlert banner is STILL PRESENT (its removal is reserved for plan 03); messages/bg.json agenda namespace is UNCHANGED in this plan"
    - "pnpm lint:i18n still exits 0 (the file-level EXEMPT_FILES entry from Phase 02.2 already covers all new content; no edit to lint-i18n.mjs is needed in this plan)"
    - "tests/unit/agenda-toc-anchors.test.ts has been bumped to assert ≥7 entries and to lock the four new slice-2 anchors via expect.arrayContaining; pnpm vitest run tests/unit/agenda-toc-anchors.test.ts exits 0"
    - "pnpm typecheck exits 0; no orphan import or i18n key reference is broken by this plan"
  artifacts:
    - path: "src/app/(frontend)/agenda/page.tsx"
      provides: "TOC_ITEMS extended to 7 entries; four new <section><h2 id=…> blocks for Енергетика, Земеделие, Здравеопазване, Външна сигурност и отбрана rendered as Bulgarian JSX inside <article className='prose prose-slate prose-lg max-w-none'>"
      contains: "id=\"energetika\""
    - path: "tests/unit/agenda-toc-anchors.test.ts"
      provides: "Anchor-drift guard extended for slice-2 simple-prose chapters: ≥7 entries assertion + new locked-anchors block for the four new IDs"
      contains: "energetika"
  key_links:
    - from: "TOC_ITEMS in src/app/(frontend)/agenda/page.tsx"
      to: "<h2 id=…> anchors in the same file"
      via: "shared id strings (#energetika, #zemedelie, #zdraveopazvane, #vanshna-sigurnost)"
      pattern: "id=\"energetika\"|id=\"zemedelie\"|id=\"zdraveopazvane\"|id=\"vanshna-sigurnost\""
    - from: "tests/unit/agenda-toc-anchors.test.ts"
      to: "src/app/(frontend)/agenda/page.tsx"
      via: "readFileSync regex extraction of TOC_ITEMS ids and <h2 id=…> ids"
      pattern: "expect\\.arrayContaining"
---

<objective>
Ship slice 1 of Phase 02.3 — the four "simple-prose" chapters of the coalition agenda. These are the lowest-cognitive-load chapters in the slice-2 content range: mostly `<p>` paragraphs with occasional `<ul>` flat-bullet lists, no `<h3>` sub-sections requiring planner judgment, no `<strong>`-prefixed `<ol>` blocks. Doing them first establishes executor rhythm with the inherited content-shape rules (UI-SPEC §3.3.E lead paragraphs, §3.3.B flat lists, §3.3.F source fidelity) before plan 02 introduces the structurally complex numbered-policy chapters.

Per RESEARCH §"Pattern 1: SPIDR slicing by structural shape", this plan ships:
- **Chapter 4** — Енергетика, ресурси и околна среда (`#energetika`, agenda-raw.txt:319-460)
- **Chapter 5** — Земеделие (`#zemedelie`, agenda-raw.txt:464-552, with one ~17-bullet flat `<ul>`)
- **Chapter 8** — Здравеопазване (`#zdraveopazvane`, agenda-raw.txt:747-895)
- **Chapter 11** — Външна сигурност и отбрана (`#vanshna-sigurnost`, agenda-raw.txt:1034-1107)

Skipped to plan 02 (numbered-policy): Образование (#6), Социална политика (#7), Култура (#10).
Skipped to plan 03 (`<h3>`-subsection + cleanup): Правосъдие (#9), Защита на ценностите (#12).

Purpose: Inherit and apply the architectural contract Phase 02.2 locked. After this plan, 7 of 12 chapters are live; the page.tsx is ~3000-3500 lines (slice-1's 627 + ~2500 added); the TOC has 7 entries; the page is shippable mid-phase if planning is interrupted.

Output: Updated `agenda/page.tsx` (TOC extended + 4 new `<section>` blocks appended after `#ikonomika`); updated anchor-drift unit test (incremental extension per VALIDATION.md Wave 0 strategy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-VALIDATION.md
@.planning/phases/02.2-coalition-agenda-content/02.2-01-SUMMARY.md
@.planning/phases/02.2-coalition-agenda-content/SKELETON.md
@.planning/coalition/agenda-raw.txt
@src/app/(frontend)/agenda/page.tsx
@src/components/landing/TableOfContents.tsx
@scripts/lint-i18n.mjs
@tests/unit/agenda-toc-anchors.test.ts
@messages/bg.json

<interfaces>
<!-- Existing scaffold contracts the executor must preserve verbatim. -->

From src/app/(frontend)/agenda/page.tsx (CURRENT — to extend):

```tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';
import { MainContainer } from '@/components/layout/MainContainer';
import { Alert, AlertDescription } from '@/components/ui/alert';   // KEEP unchanged in plan 01
import { SectionEyebrow } from '@/components/landing/SectionEyebrow';
import { TableOfContents } from '@/components/landing/TableOfContents';

export const revalidate = 3600;

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations('agenda');
  // ...
  // description: t('draftAlert'),                  // KEEP unchanged in plan 01 (cleanup is plan 03)
}

const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },              // slice 1 — IMMUTABLE (UI-SPEC L-11)
  { id: 'desen-konsensus', label: 'Десен консенсус' },// slice 1 — IMMUTABLE
  { id: 'ikonomika', label: 'Икономика' },            // slice 1 — IMMUTABLE
  // ← APPEND 4 NEW ENTRIES HERE in this plan
];

// Article body shape (line 83 onward):
//   <article className="prose prose-slate prose-lg max-w-none">
//     <section><h2 id="manifest">…</h2>…</section>
//     <section><h2 id="desen-konsensus">…</h2>…</section>
//     <section><h2 id="ikonomika">…</h2>…</section>
//     ← APPEND 4 NEW <section> BLOCKS HERE
//   </article>
```

From tests/unit/agenda-toc-anchors.test.ts (CURRENT — to extend):

```ts
it('has at least 3 TOC entries (slice 1)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(3);
});

it('contains the locked slice-1 anchors', () => {
  expect(tocIds).toEqual(
    expect.arrayContaining(['manifest', 'desen-konsensus', 'ikonomika']),
  );
});
```

From src/components/landing/TableOfContents.tsx (UNCHANGED):

```tsx
export function TableOfContents(props: {
  items: { id: string; label: string }[];
  label: string;
  variant: 'mobile' | 'desktop' | 'both';
}): JSX.Element;
// IntersectionObserver works on any number of <h2 id=…> targets without component changes.
```

UI-SPEC §7.2 LOCKED 12-entry TOC array (this plan adds entries 4, 5, 8, 11 from this lock):

```ts
{ id: 'energetika',         label: 'Енергетика' },              // entry 4 — plan 01
{ id: 'zemedelie',          label: 'Земеделие' },               // entry 5 — plan 01
{ id: 'obrazovanie',        label: 'Образование' },             // entry 6 — plan 02
{ id: 'sotsialna-politika', label: 'Социална политика' },       // entry 7 — plan 02
{ id: 'zdraveopazvane',     label: 'Здравеопазване' },          // entry 8 — plan 01
{ id: 'pravosadie',         label: 'Правосъдие' },              // entry 9 — plan 03
{ id: 'vanshna-sigurnost',  label: 'Външна сигурност и отбрана' },// entry 10 — plan 01
{ id: 'kultura',            label: 'Култура' },                 // entry 11 — plan 02
{ id: 'zashtita-tsennosti', label: 'Защита на ценностите' },    // entry 12 — plan 03
```

Plan 01 inserts entries 4, 5, 8, 10 in source order (NOT alphabetical, NOT grouped — they sit between the slice-1 anchors and the plan-02/plan-03 anchors at indices that match their source position). Final source order in the TOC is: manifest, desen-konsensus, ikonomika, **energetika, zemedelie**, obrazovanie (plan 02), sotsialna-politika (plan 02), **zdraveopazvane**, pravosadie (plan 03), **vanshna-sigurnost**, kultura (plan 02), zashtita-tsennosti (plan 03).

**CRITICAL:** because plan 01 cannot insert "future" entries 6, 7 (Образование, Социална политика — plan 02), 9 (Правосъдие — plan 03), the TOC must end up with the four plan-01 chapters as entries 4, 5, 6, 7 (consecutive) at the end of plan 01. Plan 02 will INSERT its three chapters BETWEEN entries 5 (zemedelie) and 6 (zdraveopazvane), making the running TOC at end of plan 02 be entries 1-10. Plan 03 inserts its remaining two chapters in the right slots. The CSS Grid sidebar reflows automatically.

This means plan 01's TOC at the END of the plan is: manifest, desen-konsensus, ikonomika, energetika, zemedelie, zdraveopazvane, vanshna-sigurnost (7 entries). Entries are still in source-order RELATIVE to what's been authored — plan 02 will reorder by inserting in the right positions per UI-SPEC §7.2.

Confirm by reading UI-SPEC §7.2 directly: the LOCKED final-state array places `obrazovanie` between `zemedelie` and `sotsialna-politika`, and `pravosadie` between `zdraveopazvane` and `vanshna-sigurnost`, and `kultura` between `vanshna-sigurnost` and `zashtita-tsennosti`. Plan 01's intermediate state is acceptable because it's the same set minus the three plan-02 chapters and the two plan-03 chapters.
</interfaces>
</context>

<source_content_map>
For each chapter the executor authors, the source line range and structural shape rules from UI-SPEC §3.3 + RESEARCH §"Source Content Map":

| Chapter | Source lines | h2 (verbatim) | Anchor ID | Internal shape |
|---------|--------------|---------------|-----------|----------------|
| Енергетика | agenda-raw.txt:319-460 | "Енергетика, ресурси и околна среда" | `energetika` | Lead `<p>` × 2 (lines 321-329); 2 sub-sections each introduced by "1." prefix in source — render each as `<h3>` per UI-SPEC §3.3.A (sub-section #1 "Енергетика и ресурси" lines 331-410; sub-section #2 "Опазване на околната среда и защита на природата" lines 412-459). Each sub-section is ~10 `<p>` paragraphs, no lists, no `<strong>` prefixes. Strip the source's `1.` numerical prefix from the rendered `<h3>` text — render as `<h3>Енергетика и ресурси</h3>` not `<h3>1. Енергетика и ресурси</h3>`. |
| Земеделие | agenda-raw.txt:464-552 | "Земеделие" | `zemedelie` | Lead `<p>` × 1 ("Синя България ще работи за:" line 466); long flat `<ul>` with ~17 bullets per UI-SPEC §3.3.B Pattern #3 (lines 468-518 — count exactly during authoring; the source uses inconsistent indentation but each bullet is a discrete policy); closing `<p>` paragraphs about "Земеделие и развитие на селата…" / "Земеделие и европейската политика…" (lines 520-551). NO numbered list, NO `<h3>`. |
| Здравеопазване | agenda-raw.txt:747-895 | "Здравеопазване" | `zdraveopazvane` | Lead `<p>` × ~5 (lines 749-768); flat `<ul>` of ~3 governing principles (lines 770-784); then 5-6 numbered "1." sub-section markers — render each as `<h3>` per UI-SPEC §3.3.A (the source's pdftotext output renders all as "1." due to per-page numbering reset, but the chapter visually has ~6 named sub-sections like "Осигуряване на всеобхватна грижа за детското здраве", "Своевременна и качествена болнична помощ", "Ефективно управление на финансирането", etc.). Each `<h3>` sub-section is followed by `<p>` body and possibly one nested flat `<ul>` (e.g., the 4-item infrastructure-cost list under "Своевременна и качествена…"). Strip the "1." prefix from rendered `<h3>` text. |
| Външна сигурност и отбрана | agenda-raw.txt:1034-1107 | "Външна сигурност и отбрана" | `vanshna-sigurnost` | Lead `<p>` × 7-9 of manifesto-style rhetoric; one flat `<ul>` of ~7 "Конкретните стъпки" items, with one `<li>` containing a nested `<ul>` of ~3 sub-items per UI-SPEC §3.3.B (Икономика item-4 nested-ul precedent); closing `<p>` × 1. NO `<ol>`, NO `<h3>`. |

**Authoring discipline (UI-SPEC §3.3.F + L-18, applies to ALL chapters):**
- Em-dashes (`–`), curly quotes (`„…"`), and non-breaking spaces preserved verbatim
- pdftotext line de-wrap rule: blank line in source = paragraph break (new `<p>`); consecutive non-blank lines = same paragraph (joined into one `<p>`)
- Coalition prose rendered verbatim — NO rewording, NO summarizing, NO restructuring beyond the shape rules above
- Em-dash bullet markers in source (none expected in plan-01 chapters; reserved for plan 02 Култура items 6-10) are stripped from rendered text per UI-SPEC §3.3.B exception
- For sub-section `<h3>`: NO `id=` attribute (UI-SPEC §3.3.A "Anchor ids on `<h3>`: none"); only top-level `<h2>` chapters get anchor IDs

**Allowed JSX traceability comment (one per `<section>` only, per PATTERNS.md):**
```tsx
<section>
  {/* Source: agenda-raw.txt lines 319-460 */}
  <h2 id="energetika">Енергетика, ресурси и околна среда</h2>
  …
</section>
```
NEVER per-paragraph annotations.

**Forbidden styling overrides (UI-SPEC §3.2 + L-10, PATTERNS.md):**
- NO `mt-*`, `mb-*`, `py-*`, `px-*` on `<p>`, `<h2>`, `<h3>`, `<ul>`, `<ol>`, `<li>`, `<strong>`
- NO `text-*`, `font-*`, `leading-*`, `tracking-*` on any prose element
- NO `list-disc`, `list-inside`, `list-decimal` — `prose` supplies markers
- NO `className="lead"` or `<blockquote>` for source's left-indented lead paragraphs (UI-SPEC §3.3.E lock — render as plain `<p>`)
- The `<article className="prose prose-slate prose-lg max-w-none">` wrapper class is LOCKED (line 83) — do NOT modify
</source_content_map>

<tasks>

<task type="auto">
  <name>Task 1: Author Енергетика + Земеделие chapters; extend TOC by 2 entries; bump anchor-drift test to ≥5</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (full file — 627 lines; understand current shape end-to-end before editing)
- .planning/coalition/agenda-raw.txt lines 319-552 (Енергетика 319-460; Земеделие 464-552)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3 (content-shape rules), §3.3.A (`<h3>` sub-sections), §3.3.B (`<ul>` flat lists incl. Pattern #3 long-flat-list), §3.3.E (lead paragraphs as plain `<p>`), §3.3.F (source punctuation fidelity), §7.1, §7.2 (locked TOC entries 4 + 5)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md §"Pattern Assignments" 1 (page.tsx — paragraph + flat `<ul>` + `<h3>` patterns) and §"Pattern Assignments" 3 (anchor-drift test extension)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md §"Source Content Map" rows for Енергетика and Земеделие (assumptions A-block context)
- tests/unit/agenda-toc-anchors.test.ts (full file — 43 lines; understand current locks before extending)
  </read_first>
  <action>
**Step 1.1 — Append two new TOC_ITEMS entries.** In `src/app/(frontend)/agenda/page.tsx`, locate the `TOC_ITEMS` const (currently lines 46-50, ends `]`). Append two entries AFTER `{ id: 'ikonomika', label: 'Икономика' }`:

```tsx
const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },
  { id: 'desen-konsensus', label: 'Десен консенсус' },
  { id: 'ikonomika', label: 'Икономика' },
  { id: 'energetika', label: 'Енергетика' },
  { id: 'zemedelie', label: 'Земеделие' },
];
```

These two IDs are LOCKED by UI-SPEC §7.2 — do NOT alter casing, spacing, or punctuation.

**Step 1.2 — Author the Енергетика chapter.** Read `agenda-raw.txt:319-460` directly via the Read tool (use offset+limit to load just that range). Then append a new `<section>` block AFTER the closing `</section>` tag of `#ikonomika` (currently the last `</section>` before the `</article>` closer in `page.tsx`):

```tsx
<section>
  {/* Source: agenda-raw.txt lines 319-460 */}
  <h2 id="energetika">Енергетика, ресурси и околна среда</h2>
  <p>{/* Lead paragraph 1 from lines 321-324 — de-wrap pdftotext line breaks into one <p> */}</p>
  <p>{/* Lead paragraph 2 from lines 326-329 */}</p>

  <h3>Енергетика и ресурси</h3>
  {/* Note the source has " 1.   Енергетика и ресурси" at line 331 — strip the "1." prefix; the <h3> text is "Енергетика и ресурси" only. UI-SPEC §3.3.A. */}
  <p>{/* Sub-section #1 paragraph from lines 333-336 */}</p>
  <p>{/* Sub-section #1 paragraph from lines 338-341 */}</p>
  {/* …continue authoring all paragraphs in lines 333-410 as <p> elements, one per source paragraph, de-wrapping consecutive non-blank lines */}

  <h3>Опазване на околната среда и защита на природата</h3>
  {/* Source line 412 says " 1.   Опазване на околната среда и защита на природата" — strip the "1." prefix */}
  <p>{/* Sub-section #2 paragraph from lines 414-415 */}</p>
  <p>{/* Sub-section #2 paragraph from lines 417-422 */}</p>
  {/* …continue authoring all paragraphs in lines 414-459 as <p> elements */}
</section>
```

**Authoring rules (apply verbatim):**
- Each blank line in the source = paragraph break = new `<p>` element
- Consecutive non-blank source lines join into a single `<p>` (de-wrap pdftotext line wrapping)
- Em-dashes `–`, curly quotes `„…"`, NBSPs preserved verbatim
- The `<h3>` elements get NO `id=` attribute (UI-SPEC §3.3.A — anchors are top-level only)
- The "1." prefix in front of the source's sub-section labels is a pdftotext numbering artifact (the PDF visually used "1." then "1." — a per-page reset). Strip it from the `<h3>` text. The label is purely descriptive — "Енергетика и ресурси" / "Опазване на околната среда и защита на природата".
- This chapter has NO `<ul>`, NO `<ol>`, NO `<strong>` inline emphasis — it is plain prose only.

**Step 1.3 — Author the Земеделие chapter.** Read `agenda-raw.txt:464-552` directly. Append a second new `<section>` block AFTER the Енергетика `</section>`:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 464-552 */}
  <h2 id="zemedelie">Земеделие</h2>
  <p>Синя България ще работи за:</p>

  <ul>
    {/* Source lines 468-518 contain ~17 discrete policy bullets visually separated by blank lines and inconsistent indentation. Per UI-SPEC §3.3.B Pattern #3 (long-flat-list), render as a SINGLE flat <ul> with one <li> per bullet. The first bullet at line 468-469 reads:
        "Осигуряване на продоволствената сигурност на българския народ, като part от европейската сигурност"
        (use the verbatim source — NOT this approximation).
        Continue with the bullet at lines 471-473 ("Конкурентно и развито селско стопанство…"),
        line 475-476 ("Намаляване на бюрократичната тежест…"),
        line 478 ("Финансови стимули за начинаещи и млади фермери…"),
        and so on through line 518. Count the bullets during authoring — the executor counts directly from source, NOT from the planner's estimate.
        Each bullet, no matter how its source indentation looks, gets ONE <li> in the flat <ul>. NEVER split into multiple <ul> blocks unless the source clearly intervenes with a paragraph or sub-heading. */}
    <li>{/* bullet text from source */}</li>
    {/* …repeat for every bullet in lines 468-518 */}
  </ul>

  {/* After the bulleted list, the source resumes with closing paragraphs. Source lines 520-551 contain paragraph blocks like "Земеделие и развитие на селата" (which is NOT an <h3> — it's a paragraph topic phrase used inline; check the source to disambiguate). For Земеделие, treat the closing paragraphs as plain <p> unless a clearly-named subsection appears (read the source carefully to decide). */}
  <p>{/* Closing paragraph 1 */}</p>
  {/* …continue */}
</section>
```

**Pitfall (UI-SPEC §3.3.B Pattern #3):** The source's irregular indentation around bullets in lines 468-518 may suggest grouping, but the structural answer is a SINGLE flat `<ul>`. Do NOT introduce `<h3>` sub-headings or split the list at indentation changes unless the source clearly has a paragraph or sub-heading between groups.

**Pitfall (RESEARCH §"Pitfall 1"):** Do NOT introduce `list-disc list-inside`, `<em>`, custom markers, or em-dash prefixes — `prose` supplies the disc.

**Step 1.4 — Extend the anchor-drift unit test.** Edit `tests/unit/agenda-toc-anchors.test.ts`:

(a) Update the count assertion (currently lines 28-30):
```ts
it('has at least 5 TOC entries (slice 2 — plan 01 partial)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(5);
});
```

(b) KEEP the existing "every TOC id has a matching <h2 id=…>" block (lines 32-36) UNCHANGED — it scales automatically.

(c) KEEP the existing "contains the locked slice-1 anchors" block (lines 38-42) UNCHANGED.

(d) APPEND a new `it()` block AFTER the slice-1 anchors block:
```ts
it('contains the slice-2 plan-01 anchors (Енергетика + Земеделие)', () => {
  expect(tocIds).toEqual(
    expect.arrayContaining(['energetika', 'zemedelie']),
  );
});
```

**Step 1.5 — Validate.** Run the per-task gate stack:
```bash
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
pnpm typecheck
pnpm lint:i18n
```
All three MUST exit 0. If `typecheck` flags an unused import, do NOT remove `Alert` / `AlertDescription` — they are still used by the `<Alert>` block at lines 60-64 (cleanup is plan 03). If `lint:i18n` flags Cyrillic literals, the `EXEMPT_FILES` allowlist on `scripts/lint-i18n.mjs:25` is missing the agenda page.tsx entry — but that should be already-present from Phase 02.2; do NOT add it again.

**DO NOT in this task:**
- Do NOT touch `messages/bg.json` (cleanup is plan 03)
- Do NOT touch `scripts/lint-i18n.mjs` (entry already present from Phase 02.2)
- Do NOT remove or modify the `<Alert>` block (cleanup is plan 03)
- Do NOT modify `generateMetadata` or any other code outside `TOC_ITEMS` and the `<article>` body
- Do NOT pre-allocate plan 02 / plan 03 TOC slots (do not include `obrazovanie`, `sotsialna-politika`, `zdraveopazvane`, `pravosadie`, `vanshna-sigurnost`, `kultura`, `zashtita-tsennosti` in this task — those land in their respective plans)
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'id="energetika"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1 (the `<h2 id="energetika">` exists)
- `grep -c 'id="zemedelie"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c "id: 'energetika'" src/app/\(frontend\)/agenda/page.tsx` returns 1 (TOC entry present)
- `grep -c "id: 'zemedelie'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥5 (3 from slice 1 + 2 new)
- `grep -c '<h3>' src/app/\(frontend\)/agenda/page.tsx` returns ≥2 (the two `<h3>` sub-sections in Енергетика — first time `<h3>` appears in this file)
- `grep -c 'id=' src/app/\(frontend\)/agenda/page.tsx` count of `id=` attributes on `<h3>` MUST be 0 (verify via `grep -E '<h3[^>]*id=' src/app/\(frontend\)/agenda/page.tsx` returns nothing)
- `grep -c '<Alert' src/app/\(frontend\)/agenda/page.tsx` returns ≥1 (Alert block STILL present — cleanup is plan 03)
- `grep -c "agenda.body" messages/bg.json` returns 1 (`agenda.body` STILL present — cleanup is plan 03)
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; output shows ≥4 specs passing (count + every-id-matches + slice-1-anchors-present + slice-2-plan-01-anchors-present)
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
- Source-fidelity spot-check: `grep -c '„' src/app/\(frontend\)/agenda/page.tsx` ≥ source curly-quote count (em-dashes, curly quotes preserved verbatim)
- No `list-disc`, `list-inside`, `list-decimal` className override on any list element: `grep -cE 'class(Name)?="[^"]*list-(disc|inside|decimal)' src/app/\(frontend\)/agenda/page.tsx` returns 0
- No `lead` className override on any `<p>`: `grep -cE 'class(Name)?="[^"]*lead' src/app/\(frontend\)/agenda/page.tsx` returns 0
  </acceptance_criteria>
  <done>
- `src/app/(frontend)/agenda/page.tsx` contains 5 `<h2 id="…">` anchors (3 slice-1 + `energetika` + `zemedelie`); 2 `<h3>` sub-sections inside Енергетика with no `id=` attribute; one long flat `<ul>` for Земеделие's policy bullets
- `TOC_ITEMS` has 5 entries in source order
- `tests/unit/agenda-toc-anchors.test.ts` asserts `≥5` count + the new `expect.arrayContaining(['energetika', 'zemedelie'])` block
- All three automated gates exit 0
- `<Alert>` block, `Alert` / `AlertDescription` imports, `agenda.body` and `agenda.draftAlert` keys in `bg.json` are UNTOUCHED (their lifecycle is plan 03)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Здравеопазване + Външна сигурност и отбрана chapters; extend TOC to 7 entries; bump anchor-drift test to ≥7</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (re-read to see Task 1 changes — DO NOT assume; the executor needs the Task 1 ship in context to know exactly where the next `<section>` blocks land)
- .planning/coalition/agenda-raw.txt lines 747-895 (Здравеопазване) and lines 1034-1107 (Външна сигурност и отбрана) — note these are NOT contiguous; in this plan the executor is intentionally skipping over Образование (lines 552-665), Социална политика (lines 669-744), and the gap between Здравеопазване and Външна сигурност which contains Правосъдие (lines 899-1029) and Култура → those are owned by plans 02/03
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3.A (`<h3>` sub-sections — applies to Здравеопазване), §3.3.B (`<ul>` flat lists incl. nested-in-`<li>` for Външна сигурност closing list)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md §"Pattern Assignments" 1 — the nested-`<ul>`-inside-`<li>` analog (Икономика item 4 precedent at live page.tsx lines 434-487)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md §"Source Content Map" rows for Здравеопазване (assumption A4) and Външна сигурност и отбрана; §"Open Questions" #1 (the planner's `<h3>` discretion call for Здравеопазване is documented and answered: "render as `<h3>` because the named-ness suggests `<h3>`")
  </read_first>
  <action>
**Step 2.1 — Important: this plan ships chapters in source order, but plan 01 only ships entries 4, 5, 8, 10 — entries 6, 7 (Образование, Социална политика) and 9 (Правосъдие) are owned by plans 02 and 03.** This means in `page.tsx`, after Task 1 ships, the order of `<section>` blocks is:

```
<section id="manifest">…</section>           <-- slice 1
<section id="desen-konsensus">…</section>    <-- slice 1
<section id="ikonomika">…</section>          <-- slice 1
<section id="energetika">…</section>         <-- Task 1 (this plan)
<section id="zemedelie">…</section>          <-- Task 1 (this plan)
<section id="zdraveopazvane">…</section>     <-- Task 2 (this plan)         <-- WILL be moved by plan 02 / plan 03 if needed; but plans 02/03 INSERT, not REORDER
<section id="vanshna-sigurnost">…</section>  <-- Task 2 (this plan)         <-- same
```

Plans 02 and 03 will INSERT their `<section>` blocks BETWEEN existing blocks at the right source-order positions (Образование and Социална политика go between Земеделие and Здравеопазване; Правосъдие goes between Здравеопазване and Външна сигурност; Култура goes between Външна сигурност and Защита на ценностите). No `<section>` reordering is needed across plans — each plan inserts new blocks in their final source-order positions and the file naturally accumulates.

For Task 2 **specifically:** append both Здравеопазване and Външна сигурност и отбрана `<section>` blocks at the END of the article (after the Земеделие `</section>`), in this order:
1. `<section id="zdraveopazvane">…</section>`
2. `<section id="vanshna-sigurnost">…</section>`

The intermediate state (Здравеопазване directly after Земеделие without Образование and Социална политика between them; Външна сигурност directly after Здравеопазване without Правосъдие between them) is acceptable for the wave-1 ship. Plan 02 in wave 2 will INSERT Образование + Социална политика between Земеделие and Здравеопазване; plan 03 in wave 3 will INSERT Правосъдие between Здравеопазване and Външна сигурност. The CSS Grid sidebar reflows automatically; no JavaScript changes are needed.

**Update TOC_ITEMS accordingly.** After Task 2 the array is:
```tsx
const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },
  { id: 'desen-konsensus', label: 'Десен консенсус' },
  { id: 'ikonomika', label: 'Икономика' },
  { id: 'energetika', label: 'Енергетика' },
  { id: 'zemedelie', label: 'Земеделие' },
  { id: 'zdraveopazvane', label: 'Здравеопазване' },
  { id: 'vanshna-sigurnost', label: 'Външна сигурност и отбрана' },
];
```
7 entries. Plans 02 and 03 will INSERT their entries at the right slots per UI-SPEC §7.2 final lock.

**Step 2.2 — Author the Здравеопазване chapter** (`agenda-raw.txt:747-895`). Read those lines via Read tool (offset 747, limit ~150). Structure per UI-SPEC §3.3.A + RESEARCH Source Content Map TOC#8:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 747-895 */}
  <h2 id="zdraveopazvane">Здравеопазване</h2>
  <p>{/* Lead paragraph 1 — likely the rhetorical opener about health policy */}</p>
  <p>{/* Lead paragraph 2 */}</p>
  <p>{/* Lead paragraph 3 */}</p>
  <p>{/* Lead paragraph 4 */}</p>
  <p>{/* Lead paragraph 5 */}</p>

  <ul>
    {/* ~3 governing-principles flat <ul> per RESEARCH Source Content Map. Read source to identify the exact bullet boundaries. */}
    <li>{/* bullet 1 */}</li>
    <li>{/* bullet 2 */}</li>
    <li>{/* bullet 3 */}</li>
  </ul>

  {/* The source then has 5-6 numbered "1." sub-section markers (per RESEARCH assumption A4 — pdftotext per-page reset means all start with "1." in the extracted text). Each is an <h3> sub-section per UI-SPEC §3.3.A. The named sub-sections per RESEARCH are approximately:
      - "Осигуряване на всеобхватна грижа за детското здраве"
      - "Своевременна и качествена болнична помощ" (with a nested 4-item infrastructure-cost <ul> inside the body)
      - "Ефективно управление на финансирането"
      - "Достъпна, ефективна… [continue from source]"
      - "Осигуряване на нужните хора…"
      - "Прозрачност, обоснованост…"
    Strip the source's "1." prefix from each <h3>. Read the source for exact phrasing. */}

  <h3>{/* Sub-section 1 name (verbatim from source, "1." prefix stripped) */}</h3>
  <p>{/* Body paragraph(s) for sub-section 1 */}</p>

  <h3>{/* Sub-section 2 name */}</h3>
  <p>{/* Body paragraph(s) */}</p>
  <ul>
    {/* If the source has a nested 4-item infrastructure-cost list under "Своевременна и качествена…", render as flat <ul>. UI-SPEC §3.3.B; this is NOT a nested-in-<li> case — the <ul> is at <section> level, after the <h3>. */}
  </ul>

  {/* …continue for all sub-sections through line 895 */}
</section>
```

**Pitfall — RESEARCH Pitfall 3 (sub-section vs. policy-list ambiguity):** The source's "1." prefixes for these 5-6 sub-sections look like a numbered list at first glance but are actually a per-page-reset pdftotext artifact. Each "1." introduces a NAMED multi-paragraph mini-chapter — these are `<h3>` sub-sections per UI-SPEC §3.3.A's "explicitly named sub-divisions" rule, NOT items in a single top-level `<ol>`. RESEARCH assumption A4 documents this discretion call: render as `<h3>`.

**Pitfall — RESEARCH Pitfall 1 + UI-SPEC §3.3.A:** `<h3>` elements get NO `id=` attribute. Anchors are top-level only.

**Step 2.3 — Author the Външна сигурност и отбрана chapter** (`agenda-raw.txt:1034-1107`). Read those lines. Structure per UI-SPEC §3.3.B + RESEARCH Source Content Map TOC#10:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 1034-1107 */}
  <h2 id="vanshna-sigurnost">Външна сигурност и отбрана</h2>
  <p>{/* Lead paragraph 1 — manifesto-style rhetoric */}</p>
  {/* …7-9 lead paragraphs total per RESEARCH Source Content Map */}

  <ul>
    {/* "Конкретните стъпки" flat <ul> with ~7 items per RESEARCH. ONE of the <li> items contains a nested <ul> per UI-SPEC §3.3.B (the Икономика item-4 nested-ul precedent). The nested <ul> goes INSIDE the parent <li>, not at <section> level. */}
    <li>{/* item 1 */}</li>
    <li>{/* item 2 */}</li>
    <li>
      {/* item that has a nested sub-list — read source to identify which one */}
      {/* topic sentence */}
      <ul>
        <li>{/* sub-item 1 */}</li>
        <li>{/* sub-item 2 */}</li>
        <li>{/* sub-item 3 */}</li>
      </ul>
    </li>
    {/* …continue items through line 1107 */}
  </ul>

  <p>{/* Closing paragraph */}</p>
</section>
```

**Pitfall — UI-SPEC §3.3.B Pattern: nested-`<ul>`-inside-`<li>`:** The nested `<ul>` is wrapped INSIDE the parent `<li>`, AFTER any inline topic sentence. Match the live page.tsx Икономика precedent at lines 434-487 verbatim for shape.

**Step 2.4 — Update the TOC_ITEMS array** in `page.tsx`. Append `zdraveopazvane` and `vanshna-sigurnost` after `zemedelie`:

```tsx
const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },
  { id: 'desen-konsensus', label: 'Десен консенсус' },
  { id: 'ikonomika', label: 'Икономика' },
  { id: 'energetika', label: 'Енергетика' },
  { id: 'zemedelie', label: 'Земеделие' },
  { id: 'zdraveopazvane', label: 'Здравеопазване' },
  { id: 'vanshna-sigurnost', label: 'Външна сигурност и отбрана' },
];
```

**Step 2.5 — Extend the anchor-drift unit test.** Edit `tests/unit/agenda-toc-anchors.test.ts`:

(a) Update the count assertion (currently `≥5` from Task 1, bump to `≥7`):
```ts
it('has at least 7 TOC entries (slice 2 — plan 01 complete)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(7);
});
```

(b) The "slice-2 plan-01 anchors" block created in Task 1 currently asserts `['energetika', 'zemedelie']`. EXTEND it to all four plan-01 chapters:
```ts
it('contains the slice-2 plan-01 anchors (Енергетика, Земеделие, Здравеопазване, Външна сигурност)', () => {
  expect(tocIds).toEqual(
    expect.arrayContaining([
      'energetika',
      'zemedelie',
      'zdraveopazvane',
      'vanshna-sigurnost',
    ]),
  );
});
```

KEEP the existing slice-1 anchors block (lines 38-42) UNCHANGED.

**Step 2.6 — Validate.** Run the per-task gate stack:
```bash
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
pnpm typecheck
pnpm lint:i18n
```
All three MUST exit 0.

**DO NOT in this task:**
- DO NOT touch the `<Alert>` block, `Alert` / `AlertDescription` imports, `messages/bg.json`, or `generateMetadata` (cleanup is plan 03)
- DO NOT pre-author plan 02's chapters (Образование, Социална политика, Култура) or plan 03's chapters (Правосъдие, Защита на ценностите)
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'id="zdraveopazvane"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c 'id="vanshna-sigurnost"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c "id: 'zdraveopazvane'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c "id: 'vanshna-sigurnost'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥7 (3 slice-1 + 4 slice-2 plan-01)
- `grep -c '<h3>' src/app/\(frontend\)/agenda/page.tsx` returns ≥7 (2 from Енергетика + ~5-6 from Здравеопазване)
- `grep -E '<h3[^>]*id=' src/app/\(frontend\)/agenda/page.tsx` returns nothing (no `id=` attribute on any `<h3>` per UI-SPEC §3.3.A)
- `grep -c '<Alert' src/app/\(frontend\)/agenda/page.tsx` returns ≥1 (still present)
- `grep -c "agenda.body" messages/bg.json` returns 1 (still present)
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; output shows ≥4 specs passing including the bumped `≥7` and the four-anchor `arrayContaining`
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
- Order check: in page.tsx, the literal text "id=\"zdraveopazvane\"" appears AFTER "id=\"zemedelie\"" and BEFORE "id=\"vanshna-sigurnost\"" (no source-order regression)
- Source-fidelity spot-check: 3 random Bulgarian sentences from each new chapter in agenda-raw.txt are reproducible via `grep` against page.tsx (use representative ~10-word substrings; em-dashes / NBSPs preserved)
  </acceptance_criteria>
  <done>
- `src/app/(frontend)/agenda/page.tsx` contains 7 `<h2 id="…">` anchors total; the new Здравеопазване and Външна сигурност `<section>` blocks are appended in source order after Земеделие
- `TOC_ITEMS` has 7 entries
- The Здравеопазване chapter renders ~5-6 `<h3>` sub-sections (no `id=` attributes); the Външна сигурност chapter renders one nested `<ul>` inside an `<li>` per UI-SPEC §3.3.B
- `tests/unit/agenda-toc-anchors.test.ts` asserts `≥7` count + `expect.arrayContaining(['energetika', 'zemedelie', 'zdraveopazvane', 'vanshna-sigurnost'])`
- All three automated gates exit 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual verification of plan-01 chapters (4 of 9 new chapters live)</name>
  <what-built>
The /agenda page now ships 7 of 12 final chapters. Slice-1's three (Манифест, Десен консенсус, Икономика) are unchanged. Slice-2 plan-01 has added four new `<section>` blocks: Енергетика (with two `<h3>` sub-sections), Земеделие (with one ~17-bullet flat `<ul>`), Здравеопазване (with multiple `<h3>` sub-sections and a flat infrastructure-cost `<ul>`), and Външна сигурност и отбрана (with a flat `<ul>` containing one nested-`<ul>`-inside-`<li>` per the Икономика precedent).

The TOC has grown from 3 → 7 entries (sticky desktop sidebar + mobile `<details>`). The anchor-drift unit test asserts 7 entries and locks the four new IDs. The `<Alert>` draft banner is INTENTIONALLY still present (cleanup is plan 03). The `agenda.body` and `agenda.draftAlert` keys in `messages/bg.json` are INTENTIONALLY untouched (cleanup is plan 03).

This is content-shipping work; the right verification gate is a human eyeball walkthrough on desktop AND mobile viewports. The Playwright spec for `/agenda` lands in plan 03.
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev` and navigate to `http://localhost:3000/agenda`.
2. **Eyebrow + heading + draft banner:** confirm "Какво защитаваме" eyebrow → "Програма" h1 → gray Alert "Програмата е в процес на финализиране." (Alert MUST still be visible — its removal is plan 03).
3. **Table of Contents — desktop (≥1024px viewport):** confirm the sticky sidebar shows exactly 7 entries in this order: Манифест → Десен консенсус → Икономика → Енергетика → Земеделие → Здравеопазване → Външна сигурност и отбрана. Each is a clickable link.
4. **Table of Contents — mobile (≤640px, e.g., iPhone SE 375×667 in DevTools):** the `<details>` block above the article lists the same 7 entries. Click to expand.
5. **Anchor jumps (desktop + mobile):** click each of the 7 TOC links. Each MUST scroll smoothly to the matching `<h2>`; the heading MUST NOT be hidden under the sticky header (`scroll-padding-top: 5rem` is in `globals.css`). On mobile the `<details>` should remain functional.
6. **Active-section highlighting (desktop):** scroll the page slowly. As each `<h2>` enters the upper third of the viewport, the TOC entry for that section should become bold/primary-colored. Minor flicker at section boundaries is acceptable.
7. **Енергетика chapter:** the section with `<h2>Енергетика, ресурси и околна среда</h2>` is followed by 2 lead paragraphs, then `<h3>Енергетика и ресурси</h3>` + ~10 paragraphs, then `<h3>Опазване на околната среда и защита на природата</h3>` + ~10 paragraphs. The `<h3>` headings render in the prose default size (NOT Gilroy ExtraBold — that's only for the page h1). Spot-check 2 paragraphs against `.planning/coalition/agenda-raw.txt:319-460` — wording must match verbatim incl. em-dashes and curly quotes.
8. **Земеделие chapter:** the section with `<h2>Земеделие</h2>` is followed by the lead "Синя България ще работи за:" paragraph, then a single flat bulleted list with ~17 bullets. The bullets render with `prose` default discs (NOT em-dashes, NOT Bulgarian numerals). Spot-check 2 bullets against `agenda-raw.txt:464-518`.
9. **Здравеопазване chapter:** the section with `<h2>Здравеопазване</h2>` is followed by ~5 lead paragraphs, a 3-item flat bulleted list of governing principles, and then 5-6 `<h3>` sub-sections each with body paragraphs. Confirm at least one sub-section has a nested flat `<ul>` (the infrastructure-cost list per UI-SPEC §3.3.A and RESEARCH Source Content Map TOC#8). The `<h3>` sub-sections do NOT have anchor IDs (right-click → Inspect → confirm `<h3>` has no `id=` attribute).
10. **Външна сигурност и отбрана chapter:** the section with `<h2>Външна сигурност и отбрана</h2>` is followed by ~7-9 lead paragraphs, then a single flat `<ul>` of "Конкретните стъпки" with ~7 items. Confirm one of those `<li>` items contains a nested `<ul>` of ~3 sub-items (matching the Икономика item-4 precedent — same nesting shape).
11. **Typography rhythm:** vertical spacing between paragraphs and between `<h2>` / `<h3>` and body content is governed entirely by `prose-lg` defaults. Confirm there are no jarring spacing jumps between chapters (i.e., no rogue `mt-*`/`mb-*` classes) and no font-family discontinuities.
12. **No content from plans 02 / 03:** confirm the page does NOT yet show Образование, Социална политика, Култура (plan 02) or Правосъдие, Защита на ценностите (plan 03). The `<section>` after Земеделие should be Здравеопазване (plan 02 will INSERT Образование + Социална политика between them in the next plan); the `<section>` after Здравеопазване should be Външна сигурност (plan 03 will INSERT Правосъдие between them).
13. **Linter sanity:** from repo root, run `pnpm lint:i18n`. Must exit 0 with `PUB-05 OK`.
14. **Test sanity (Phase 02.2 SUMMARY D-4 — use `pnpm vitest run`, NEVER `pnpm test --run`):** run `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts`. All specs green; the count assertion shows ≥7; the new four-anchor `arrayContaining` passes.
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 checks pass. If anything is off (paragraph mismatch, missing list item, broken anchor, sub-section rendered with `<h2>` instead of `<h3>`, nested-`<ul>` rendered as flat, etc.), describe the issue and the executor will revise that chapter before re-requesting verification.</resume-signal>
  <acceptance_criteria>
- Operator response is "approved" (or substantively equivalent confirmation)
- If revision is requested, the resume signal includes the specific issue and the executor lands a follow-up commit before this task is marked done
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Phase 02.3 is content-only; no new auth, input, API, or trust boundary is introduced. The `/agenda` route remains a public read-only Server Component rendering static JSX. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-01-01 | Tampering / Information disclosure | `src/app/(frontend)/agenda/page.tsx` rendered Bulgarian content | accept | All chapter content is JSX literals authored at edit time. React's default escaping covers any pasted Bulgarian string. NO `dangerouslySetInnerHTML` is used. NO user input is rendered. ASVS V5 Input Validation does not apply (no input). |
| T-02.3-01-02 | Information disclosure | Anchor URL fragments (`/agenda#energetika`) | accept | Native browser `:target` behavior only; fragments are never echoed server-side, never used for navigation logic, never written to logs. |
| T-02.3-01-03 | Tampering | ISR cache for `/agenda` | accept | Cache key is the URL only; no cookie/header variation; revalidation gated by `revalidate = 3600`. Inherited posture from Phase 02.2; unchanged in this plan. |

**Security delta for plan 01:** Zero. ASVS V2-V14 not applicable per RESEARCH §"Security Domain". This plan introduces no new attack surface.
</threat_model>

<verification>
After all three tasks land:

- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0 with ≥4 specs passing
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥7
- `grep -c "id: '" src/app/\(frontend\)/agenda/page.tsx` (TOC entries) returns 7 within the `TOC_ITEMS` const block
- The `<Alert>` block, `Alert` / `AlertDescription` imports, and `agenda.body` / `agenda.draftAlert` keys in `messages/bg.json` are UNCHANGED (their lifecycle is plan 03)
- Operator visual walkthrough signed off (Task 3 resume-signal = "approved")
</verification>

<success_criteria>
Plan 01 of Phase 02.3 is complete when:

1. A visitor opening `/agenda` can read 7 of 12 chapters: the 3 slice-1 chapters PLUS the four "simple-prose" chapters (Енергетика, Земеделие, Здравеопазване, Външна сигурност и отбрана) — enough content to make further evaluative reading worthwhile
2. The TOC has 7 entries in source order; clicking any entry jumps to its `<h2>` anchor; active-section highlighting works on desktop
3. The anchor-drift unit test catches any future drift between TOC ids and rendered `<h2 id>` values
4. The architectural contract from Phase 02.2 has been re-applied to four new chapters with three new content shapes (`<h3>` sub-sections, long flat `<ul>`, nested-`<ul>`-inside-`<li>`) without introducing any styling override or component change
5. The `<Alert>` draft banner is intentionally still present; final-slice cleanup is reserved for plan 03
</success_criteria>

<output>
After all three tasks pass, create `.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-01-SUMMARY.md` documenting:

- Counts: chapters added (4), `<h2 id>` anchors (4 new), `<h3>` sub-sections (the actual count from authoring), `<p>` paragraphs added (count from final ship), `<ul>` lists added, `<ol>` lists added (expected: 0 — `<ol>` is plan-02 territory)
- Source-content ambiguities encountered (places where pdftotext line wrapping or sub-section labeling required judgment — particularly any deviation from the "render as `<h3>`" call for Здравеопазване's "1." sub-sections)
- Final `page.tsx` line count and TOC_ITEMS length
- Confirmation that `<Alert>`, `agenda.body`, `agenda.draftAlert`, and `generateMetadata` are intentionally untouched
- Handoff note for plan 02 (numbered-policy chapters insertion points: between Земеделие and Здравеопазване for Образование + Социална политика; between Външна сигурност и отбрана and end-of-article for Култура — wait, Култура goes between Външна сигурност and Защита на ценностите per UI-SPEC §7.2; clarify in summary based on final-state ship)
</output>
</content>
</invoke>