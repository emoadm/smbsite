---
phase: 02.3-coalition-agenda-content-slice-2
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/app/(frontend)/agenda/page.tsx
  - messages/bg.json
  - tests/unit/agenda-toc-anchors.test.ts
  - tests/unit/agenda-i18n-cleanup.test.ts
  - tests/e2e/agenda.spec.ts
autonomous: false
mode: mvp
slice: h3-subsection-and-final-cleanup
requirements:
  - REQ-AGENDA-S2-01
  - REQ-AGENDA-S2-02
  - REQ-AGENDA-S2-03
  - REQ-AGENDA-S2-04
  - REQ-AGENDA-S2-05
  - D-CoalitionContent-Agenda
must_haves:
  truths:
    - "A visitor opening /agenda sees ALL 12 final chapters in source order: Манифест → Десен консенсус → Икономика → Енергетика → Земеделие → Образование → Социална политика → Здравеопазване → Правосъдие → Външна сигурност и отбрана → Култура → Защита на ценностите"
    - "Правосъдие renders 4 named <h3> sub-sections per UI-SPEC §3.3.A (Продължителността на съдебното производство; Съдебни разходи и правна помощ; Срокове; Правни средства за защита, достъпни на европейско и международно равнище), each with body paragraphs; flat <ul> blocks where the source uses dash-prefixed bullets"
    - "Защита на българските ценности, традиции и интереси renders 5 named <h3> sub-sections (България е свободна и демократична държава; Ценности и традиции; ЛГБТ и нахлуването на полова идентичност в обществото; България е християнска държава; България е европейска държава) with body paragraphs only, no lists"
    - "The <Alert> draftAlert banner is REMOVED (the page no longer shows «Програмата е в процес на финализиране.»); the Alert / AlertDescription imports are removed; the lint passes"
    - "messages/bg.json agenda namespace has been cleaned: agenda.body key DROPPED, agenda.draftAlert key DROPPED, agenda.metadataDescription key ADDED with verbatim copy «Какво защитаваме и каква промяна искаме за МСП сектора в България. Пълна политическа програма на коалиция Синя България.»"
    - "src/app/(frontend)/agenda/page.tsx generateMetadata().description now reads t('metadataDescription') instead of t('draftAlert'); pnpm check:placeholders exits 0 (the [ТЕКСТ ОТ КОАЛИЦИЯ] placeholder is gone from agenda namespace)"
    - "TOC_ITEMS has exactly 12 entries in source order matching UI-SPEC §7.2 verbatim; the anchor-drift unit test asserts ≥12 + the full slice-2 nine-anchor lock"
    - "A new tests/unit/agenda-i18n-cleanup.test.ts (~30 lines) asserts: agenda.body NOT in bg.json; agenda.draftAlert NOT in bg.json; agenda.metadataDescription IS in bg.json; no [ТЕКСТ ОТ КОАЛИЦИЯ] in agenda namespace"
    - "A new tests/e2e/agenda.spec.ts asserts: /agenda renders <h1> + 12 <h2 id> anchors; clicking each TOC link scrolls into viewport; no role='alert' element on the page; meta name='description' does NOT contain 'финализиране'"
    - "Full validation gate stack passes: pnpm vitest run + pnpm typecheck + pnpm lint:i18n + pnpm check:placeholders + pnpm test:e2e tests/e2e/agenda.spec.ts + pnpm test:e2e tests/e2e/responsive.spec.ts (the latter already covers /agenda)"
  artifacts:
    - path: "src/app/(frontend)/agenda/page.tsx"
      provides: "Final 12-entry TOC_ITEMS; 12 <section><h2 id=…> blocks total; <Alert> + Alert/AlertDescription imports REMOVED; generateMetadata().description swapped to t('metadataDescription')"
      contains: "id=\"zashtita-tsennosti\""
    - path: "messages/bg.json"
      provides: "agenda namespace cleaned: title + leadEyebrow retained; body + draftAlert dropped; metadataDescription added"
      contains: "metadataDescription"
    - path: "tests/unit/agenda-toc-anchors.test.ts"
      provides: "Final lock: ≥12 entries assertion + slice-2 nine-anchor expect.arrayContaining"
      contains: "zashtita-tsennosti"
    - path: "tests/unit/agenda-i18n-cleanup.test.ts"
      provides: "i18n cleanup regression guard (~30 lines): asserts bg.json agenda namespace shape post-slice-2"
      contains: "metadataDescription"
    - path: "tests/e2e/agenda.spec.ts"
      provides: "Playwright spec covering h1 + 12 anchors + anchor-jump + Alert-absence + OG-description correctness"
      contains: "12"
  key_links:
    - from: "src/app/(frontend)/agenda/page.tsx generateMetadata().description"
      to: "messages/bg.json agenda.metadataDescription"
      via: "next-intl getTranslations('agenda').then(t => t('metadataDescription'))"
      pattern: "t\\('metadataDescription'\\)"
    - from: "scripts/check-coalition-placeholders.mjs"
      to: "messages/bg.json (no [ТЕКСТ ОТ КОАЛИЦИЯ] anywhere in agenda namespace)"
      via: "file-grep gate"
      pattern: "ТЕКСТ ОТ КОАЛИЦИЯ"
    - from: "tests/unit/agenda-i18n-cleanup.test.ts"
      to: "messages/bg.json"
      via: "readFileSync + regex scoped to agenda namespace"
      pattern: "\"agenda\":\\s*\\{[\\s\\S]*?\\n\\s*\\}"
---

<objective>
Ship the final slice — Phase 02.3's "final" condition. Two things happen in this plan:

**1. Add the last two chapters** (the `<h3>`-subsection group from RESEARCH §"Pattern 1" Slice C):
- **Chapter 9** — Правосъдие (`#pravosadie`, agenda-raw.txt:899-1029) — INSERTED between Здравеопазване and Външна сигурност и отбрана
- **Chapter 12** — Защита на българските ценности, традиции и интереси (`#zashtita-tsennosti`, agenda-raw.txt:1247-1332) — APPENDED at end-of-article (after Култура)

**2. Final-slice cleanup deltas** (UI-SPEC §4 + ROADMAP success criterion #3):
- Remove the `<Alert className="mb-8">` block in `page.tsx` (lines ~60-64 currently)
- Remove the `Alert, AlertDescription` import (line 4)
- Drop `agenda.draftAlert` from `messages/bg.json`
- Drop `agenda.body` from `messages/bg.json`
- Add `agenda.metadataDescription` to `messages/bg.json` with the UI-SPEC §4.3 Option A copy
- Swap `description: t('draftAlert')` → `description: t('metadataDescription')` in `generateMetadata`
- Add `tests/unit/agenda-i18n-cleanup.test.ts` (regression guard, ~30 lines, per PATTERNS.md analog)
- Add `tests/e2e/agenda.spec.ts` (Playwright spec, ~50 lines, per PATTERNS.md analog) — UI-SPEC §4.4 marks this as planner discretion; planner included it per RESEARCH §"Open Questions" #3 recommendation

After this plan ships, Phase 02.3 is complete: 12/12 chapters live; draft banner gone; OG description correct; i18n catalogue cleaned; full automated + manual verification stack green.

Output: Updated `agenda/page.tsx` (TOC at 12 entries, two new `<section>` blocks, Alert removal + import cleanup + generateMetadata swap); cleaned `messages/bg.json` agenda namespace; updated anchor-drift unit test (final ≥12 lock); new i18n-cleanup unit test; new Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-VALIDATION.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-01-SUMMARY.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-02-SUMMARY.md
@.planning/phases/02.2-coalition-agenda-content/02.2-01-SUMMARY.md
@.planning/coalition/agenda-raw.txt
@src/app/(frontend)/agenda/page.tsx
@src/app/(frontend)/faq/page.tsx
@messages/bg.json
@scripts/check-coalition-placeholders.mjs
@tests/unit/agenda-toc-anchors.test.ts
@tests/unit/newsletter-coalition-placeholder.test.ts
@tests/e2e/landing.spec.ts
@tests/e2e/responsive.spec.ts

<interfaces>
<!-- Cleanup-related contracts the executor must honor verbatim. -->

From src/app/(frontend)/agenda/page.tsx (CURRENT shape after plans 01+02 ship — to clean up):

```tsx
import type { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';
import { MainContainer } from '@/components/layout/MainContainer';
import { Alert, AlertDescription } from '@/components/ui/alert';   // ← REMOVE in Task 3
import { SectionEyebrow } from '@/components/landing/SectionEyebrow';
import { TableOfContents } from '@/components/landing/TableOfContents';

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations('agenda');
  // …
  return {
    title: `${t('title')} — ${tSite('brandName')}`,
    description: t('draftAlert'),    // ← SWAP to t('metadataDescription') in Task 3
    // …
  };
}

// In the JSX body:
//   <Alert className="mb-8">                                       // ← REMOVE in Task 3
//     <AlertDescription className="text-sm text-muted-foreground">
//       {t('draftAlert')}
//     </AlertDescription>
//   </Alert>
```

From src/app/(frontend)/faq/page.tsx (PRECEDENT for the metadataDescription pattern — DO NOT modify):

```tsx
// faq/page.tsx line 19 (paraphrased):
description: t('metadataDescription'),
```

This same pattern is what the cleanup applies to `agenda/page.tsx`. The shape was already validated by Phase 2 PUB-04.

From messages/bg.json (CURRENT — lines 262-267 — to clean up):

```json
"agenda": {
  "title": "Програма",
  "leadEyebrow": "Какво защитаваме",
  "draftAlert": "Програмата е в процес на финализиране.",   // ← DROP in Task 3
  "body": "[ТЕКСТ ОТ КОАЛИЦИЯ]"                            // ← DROP in Task 3
},
"faq": {                                                     // ← `faq.metadataDescription` precedent at line 270
  "title": "Често задавани въпроси",
  "metadataDescription": "Как работи общността…",
  // …
}
```

Target state:

```json
"agenda": {
  "title": "Програма",
  "leadEyebrow": "Какво защитаваме",
  "metadataDescription": "Какво защитаваме и каква промяна искаме за МСП сектора в България. Пълна политическа програма на коалиция Синя България."
},
```

CRITICAL JSON pitfall (PATTERNS.md): the LAST key inside `"agenda": { … }` must NOT have a trailing comma. Final-key shape: `"metadataDescription": "…"` followed by `}` on the next line.

From scripts/check-coalition-placeholders.mjs:
The script greps `messages/bg.json` for `[ТЕКСТ ОТ КОАЛИЦИЯ]`. Currently FAILS because `agenda.body` contains the placeholder. After this plan's Task 3 cleanup, the script PASSES (the placeholder is gone from the agenda namespace; verify no other namespace has reintroduced it).

From tests/e2e/landing.spec.ts (analog for new agenda.spec.ts):
```ts
import { test, expect } from '@playwright/test';

test.describe('Public surface — /', () => {
  test('homepage renders hero + CTA', async ({ page }) => {
    await page.goto('/');
    await expect(page.locator('h1').first()).toBeVisible();
    // …
  });
});
```

From tests/unit/newsletter-coalition-placeholder.test.ts (analog for new agenda-i18n-cleanup.test.ts):
```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';

describe('newsletter coalition placeholder', () => {
  const bg = readFileSync('messages/bg.json', 'utf8');
  // …file-grep + assertion shape per PATTERNS.md
});
```

UI-SPEC §3.3 / §4 rules for this plan:
- §3.3.A (`<h3>` for explicitly named sub-divisions in Правосъдие 4 sub-sections + Защита на ценностите 5 sub-sections)
- §3.3.B (flat `<ul>` for Правосъдие's dash-prefixed legislative-measures lists)
- §3.3.E (lead paragraphs as plain `<p>`)
- §3.3.F (source punctuation fidelity)
- §4.1 (`<Alert>` removal — entire JSX block deleted; import line cleaned)
- §4.2 (`agenda.body` key dropped)
- §4.3 Option A (drop `agenda.draftAlert`, add `agenda.metadataDescription`, swap generateMetadata().description)
- §4.4 (E2E spec is planner discretion — included here per RESEARCH recommendation)
</interfaces>
</context>

<source_content_map>

| Chapter | Source lines | h2 (verbatim) | Anchor ID | Internal shape |
|---------|--------------|---------------|-----------|----------------|
| Правосъдие | agenda-raw.txt:899-1029 | "Правосъдие" | `pravosadie` | Lead `<p>` × ~7; flat `<ul>` of ~6 numbered legislative measures (rendered as `<ul>`, NOT `<ol>` — the source uses dash-prefixed bullets per RESEARCH Source Content Map TOC#9); a paragraph "Актуализиране на съдебните карти, което да стане чрез…" + flat `<ul>` of ~8 factors; then `<h3>Продължителността на съдебното производство` + `<p>`s; then `<h3>Съдебни разходи и правна помощ` + `<ul>` of ~3 items; then `<h3>Срокове` + `<p>`s; then `<h3>Правни средства за защита, достъпни на европейско и международно равнище` + `<p>`s. NO top-level `<ol>`, NO `<strong>` policy-name prefix. |
| Защита на ценностите | agenda-raw.txt:1247-1332 (end of slice-2 range) | "Защита на българските ценности, традиции и интереси" | `zashtita-tsennosti` | Lead `<p>` × 1; then 5 named `<h3>` sub-sections per UI-SPEC §3.3.A: (1) "България е свободна и демократична държава" + 3 `<p>`; (2) "Ценности и традиции" + 3 `<p>`; (3) "ЛГБТ и нахлуването на полова идентичност в обществото" + 2 `<p>`; (4) "България е християнска държава" + 2 `<p>`; (5) "България е европейска държава" + 3 `<p>`. NO lists in this chapter. |

**Authoring discipline (UI-SPEC §3.3.F + L-18, applies to ALL chapters):**
- Em-dashes (`–`), curly quotes (`„…"`), NBSPs preserved verbatim
- pdftotext line de-wrap rule: blank line in source = paragraph break (new `<p>`); consecutive non-blank lines = same paragraph (joined into one `<p>`)
- Coalition prose rendered verbatim — NO rewording, NO summarizing, NO restructuring
- For `<h3>` sub-section text in Правосъдие: the source has these as inline-headers in the prose flow (NOT prefixed with numerals); render verbatim text minus any leading whitespace/dashes
- For Защита на ценностите `<h3>` sub-section text: NO numeric prefix to strip — the source uses descriptive sub-section labels directly
- All `<h3>` elements: NO `id=` attribute (UI-SPEC §3.3.A — anchors are top-level only)
- Правосъдие's first `<ul>` of 6 legislative measures uses dash-prefixed bullets in source (`–`); strip the `–` from rendered `<li>` text per UI-SPEC §3.3.B exception (Правосъдие is a similar pattern to Култура items 6-10 — the dash IS the bullet marker, not prose)

**Allowed JSX traceability comment (one per `<section>` only):**
```tsx
<section>
  {/* Source: agenda-raw.txt lines 899-1029 */}
  <h2 id="pravosadie">Правосъдие</h2>
  …
</section>
```

**Forbidden styling overrides (UI-SPEC §3.2 + L-10):** same as plans 01 and 02 — `prose` defaults only; no className overrides on body elements.
</source_content_map>

<tasks>

<task type="auto">
  <name>Task 1: Author Правосъдие chapter; INSERT between Здравеопазване and Външна сигурност и отбрана; bump anchor-drift test to ≥11</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (re-read AFTER plans 01+02 ship — find the Здравеопазване `</section>` closing tag and the Външна сигурност `<section>` opening tag for the insertion point)
- .planning/coalition/agenda-raw.txt lines 899-1029 (Правосъдие source content)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3.A (4 `<h3>` sub-sections in Правосъдие; the listed sub-divisions are "Съдебни разходи и правна помощ" at line ~1007 and "Срокове" at line ~1015, plus per RESEARCH "Продължителността на съдебното производство" earlier and "Правни средства за защита…" at ~line 1025)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3.B (em-dash strip rule applies to Правосъдие's dash-prefixed legislative-measures `<ul>`)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md §"Source Content Map" TOC#9 (Правосъдие full structural breakdown)
  </read_first>
  <action>
**Step 1.1 — Insertion point.** Locate the Здравеопазване `</section>` closing tag (created in plan 01) and the Външна сигурност `<section>` opener. Правосъдие goes BETWEEN them.

**Step 1.2 — Insert TOC entry.** In `TOC_ITEMS`, insert between `{ id: 'zdraveopazvane', … }` and `{ id: 'vanshna-sigurnost', … }`:

```tsx
{ id: 'zdraveopazvane', label: 'Здравеопазване' },
{ id: 'pravosadie', label: 'Правосъдие' },              // ← INSERT
{ id: 'vanshna-sigurnost', label: 'Външна сигурност и отбрана' },
```

**Step 1.3 — Author the Правосъдие chapter.** Read `agenda-raw.txt:899-1029` directly (offset 899, limit ~135). Compose:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 899-1029 */}
  <h2 id="pravosadie">Правосъдие</h2>

  {/* ~7 lead paragraphs — read source for exact count */}
  <p>{/* Lead paragraph 1 */}</p>
  <p>{/* Lead paragraph 2 */}</p>
  {/* …continue */}

  {/* First <ul>: 6 legislative measures with dash-prefixed bullets in source.
      UI-SPEC §3.3.B exception — strip the "–" from rendered <li> text. */}
  <ul>
    <li>{/* legislative measure 1 (dash stripped) */}</li>
    <li>{/* legislative measure 2 */}</li>
    {/* …continue all 6 */}
  </ul>

  {/* Linking paragraph */}
  <p>{/* "Актуализиране на съдебните карти, което да стане чрез…" or similar verbatim text */}</p>

  {/* Second <ul>: ~8 factors */}
  <ul>
    <li>{/* factor 1 */}</li>
    {/* …continue all factors */}
  </ul>

  {/* Sub-section 1 — likely "Продължителността на съдебното производство" per RESEARCH; verify against source */}
  <h3>{/* verbatim sub-section name */}</h3>
  <p>{/* body */}</p>
  <p>{/* body cont. */}</p>

  {/* Sub-section 2 — "Съдебни разходи и правна помощ" per UI-SPEC §3.3.A */}
  <h3>Съдебни разходи и правна помощ</h3>
  <p>{/* body */}</p>
  <ul>
    {/* ~3-item flat <ul> per RESEARCH */}
    <li>{/* item 1 */}</li>
    <li>{/* item 2 */}</li>
    <li>{/* item 3 */}</li>
  </ul>

  {/* Sub-section 3 — "Срокове" per UI-SPEC §3.3.A */}
  <h3>Срокове</h3>
  <p>{/* body */}</p>

  {/* Sub-section 4 — "Правни средства за защита, достъпни на европейско и международно равнище" */}
  <h3>{/* verbatim source heading */}</h3>
  <p>{/* body */}</p>
</section>
```

**Critical authoring rules:**

1. **Правосъдие has NO top-level `<ol>` and NO `<strong>` prefix.** It's a different shape from Образование / Култура. The 6 legislative measures are a flat `<ul>` (UI-SPEC §3.3.B + RESEARCH Source Content Map TOC#9 explicitly: "rendered as <ul> because the source uses dash-prefixed bullets, NOT a numbered <ol>").

2. **Strip the source's `–` (en-dash) bullet markers** from the rendered `<li>` text in the legislative-measures `<ul>` and the 8-factor `<ul>` per UI-SPEC §3.3.B exception. The rendered text starts AFTER the dash. NEVER include `–` as the first character of an `<li>` text node.

3. **4 named `<h3>` sub-sections** per UI-SPEC §3.3.A. The 4 names are (verify each against source):
   - "Продължителността на съдебното производство" (or whatever the source actually says — read it)
   - "Съдебни разходи и правна помощ"
   - "Срокове"
   - "Правни средства за защита, достъпни на европейско и международно равнище" (or similar)

4. **No `id=` on `<h3>`** per UI-SPEC §3.3.A.

5. **Лead paragraphs render as plain `<p>`** per UI-SPEC §3.3.E — no `<blockquote>`, no `lead` className.

**Step 1.4 — Extend the anchor-drift unit test.** Edit `tests/unit/agenda-toc-anchors.test.ts`:

(a) Update count assertion (currently `≥10` from plan 02, bump to `≥11`):
```ts
it('has at least 11 TOC entries (slice 2 — plan 03 partial)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(11);
});
```

(b) APPEND a new `it()` block AFTER the slice-2 plan-02 anchors block:
```ts
it('contains the slice-2 plan-03 anchor for Правосъдие', () => {
  expect(tocIds).toContain('pravosadie');
});
```

KEEP all earlier blocks unchanged (slice-1 anchors immutable, slice-2 plan-01 four-anchor block, slice-2 plan-02 three-anchor block).

**Step 1.5 — Validate.**
```bash
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
pnpm typecheck
pnpm lint:i18n
```

**DO NOT in this task:**
- Do NOT touch `<Alert>`, `messages/bg.json`, `generateMetadata` (Task 3 cleanup)
- Do NOT pre-author Защита на ценностите (Task 2)
- Do NOT remove the `Alert` / `AlertDescription` import (Task 3)
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'id="pravosadie"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c "id: 'pravosadie'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥11
- Order check: in `page.tsx`, line numbers for `<h2 id>` anchors yield `id="zdraveopazvane"` < `id="pravosadie"` < `id="vanshna-sigurnost"`
- `<h3>` count grew by ≥4: total `<h3>` count after this task ≥ ~16 (cumulative across all chapters; the 4 new ones are inside Правосъдие)
- No `–` em-dash at start of `<li>` text in Правосъдие: `grep -nE '<li>\s*–' src/app/\(frontend\)/agenda/page.tsx` returns nothing
- No top-level `<ol>` in the Правосъдие `<section>` (the chapter uses `<ul>` only) — verify by reading the section
- `<Alert>` block STILL present (Task 3 removes it): `grep -c '<Alert' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; output shows ≥11 specs passing including the new `pravosadie` `toContain`
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
  </acceptance_criteria>
  <done>
- `src/app/(frontend)/agenda/page.tsx` contains the Правосъдие `<section>` INSERTED between Здравеопазване and Външна сигурност и отбрана, with 4 `<h3>` sub-sections (no `id=`), 2-3 flat `<ul>` blocks (em-dash bullets stripped), and no `<ol>`
- `TOC_ITEMS` has 11 entries; `pravosadie` is the 9th entry (index 8, 0-indexed)
- Anchor-drift unit test asserts `≥11` and the new `expect(tocIds).toContain('pravosadie')`
- All three automated gates exit 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Защита на ценностите chapter; APPEND at end-of-article; lock anchor-drift test at ≥12 (final)</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (re-read AFTER Task 1 ship — locate the Култура `</section>` closing tag; Защита на ценностите is APPENDED after it, just before the closing `</article>` tag)
- .planning/coalition/agenda-raw.txt lines 1247-1332 (end of source content; verify line range against actual source)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3.A (5 named `<h3>` sub-sections), §7.1 (locked anchor ID `zashtita-tsennosti`), §7.2 (full 12-entry TOC array)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md §"Source Content Map" TOC#12 + §"Code Examples" Example 3 (the synthesized JSX snippet for this chapter)
  </read_first>
  <action>
**Step 2.1 — Insertion point.** Защита на ценностите is APPENDED at end-of-article — directly after the Култура `</section>` closing tag (created in plan 02) and BEFORE the closing `</article>` tag.

**Step 2.2 — Insert TOC entry.** In `TOC_ITEMS`, append AFTER `{ id: 'kultura', … }`:

```tsx
{ id: 'kultura', label: 'Култура' },
{ id: 'zashtita-tsennosti', label: 'Защита на ценностите' },     // ← APPEND (final entry)
```

After this task, the final 12-entry TOC matches UI-SPEC §7.2 verbatim:

```tsx
const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },
  { id: 'desen-konsensus', label: 'Десен консенсус' },
  { id: 'ikonomika', label: 'Икономика' },
  { id: 'energetika', label: 'Енергетика' },
  { id: 'zemedelie', label: 'Земеделие' },
  { id: 'obrazovanie', label: 'Образование' },
  { id: 'sotsialna-politika', label: 'Социална политика' },
  { id: 'zdraveopazvane', label: 'Здравеопазване' },
  { id: 'pravosadie', label: 'Правосъдие' },
  { id: 'vanshna-sigurnost', label: 'Външна сигурност и отбрана' },
  { id: 'kultura', label: 'Култура' },
  { id: 'zashtita-tsennosti', label: 'Защита на ценностите' },
];
```

**Step 2.3 — Author the Защита на ценностите chapter.** Read `agenda-raw.txt:1247-1332` directly. Compose per UI-SPEC §3.3.A + RESEARCH §"Code Examples" Example 3:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 1247-1332 */}
  <h2 id="zashtita-tsennosti">Защита на българските ценности, традиции и интереси</h2>

  {/* 1 lead paragraph */}
  <p>{/* Lead paragraph — verbatim from source line ~1249 */}</p>

  <h3>България е свободна и демократична държава</h3>
  <p>{/* Body paragraph 1 — Свободата е толкова ценна, но и толкова уязвима… */}</p>
  <p>{/* Body paragraph 2 */}</p>
  <p>{/* Body paragraph 3 */}</p>

  <h3>Ценности и традиции</h3>
  <p>{/* Body paragraph 1 — Докато някои хора дори в нашата държава спорят… */}</p>
  <p>{/* Body paragraph 2 */}</p>
  <p>{/* Body paragraph 3 */}</p>

  <h3>ЛГБТ и нахлуването на полова идентичност в обществото</h3>
  <p>{/* Body paragraph 1 */}</p>
  <p>{/* Body paragraph 2 */}</p>

  <h3>България е християнска държава</h3>
  <p>{/* Body paragraph 1 */}</p>
  <p>{/* Body paragraph 2 */}</p>

  <h3>България е европейска държава</h3>
  <p>{/* Body paragraph 1 */}</p>
  <p>{/* Body paragraph 2 */}</p>
  <p>{/* Body paragraph 3 */}</p>
</section>
```

**Critical authoring rules:**

1. **5 named `<h3>` sub-sections in source order** — verify each name against the actual source. The expected names per UI-SPEC §3.3.A and RESEARCH Source Content Map TOC#12 are listed above; if the source uses slightly different wording, render verbatim source.

2. **NO numeric prefix to strip** — unlike Образование (which had "1." prefixes), Защита на ценностите uses descriptive sub-section labels directly.

3. **NO lists** — this chapter is plain prose only per RESEARCH Source Content Map TOC#12. No `<ul>`, no `<ol>`.

4. **NO `id=` on `<h3>`** per UI-SPEC §3.3.A.

5. **The chapter h2 text is the FULL source heading** — "Защита на българските ценности, традиции и интереси" (verbatim). The TOC label is the SHORT form "Защита на ценностите" per UI-SPEC §7.1 transliteration scheme. Both are correct in their respective contexts.

**Step 2.4 — Lock the anchor-drift unit test at the final 12-entry state.** Edit `tests/unit/agenda-toc-anchors.test.ts`:

(a) Update count assertion (bump from `≥11` to `≥12` and update label to "final"):
```ts
it('has at least 12 TOC entries (slice 2 final)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(12);
});
```

(b) Replace the slice-2 plan-03 anchor block from Task 1 (which currently asserts only `pravosadie`) with the full two-anchor lock:
```ts
it('contains the slice-2 plan-03 anchors (Правосъдие, Защита на ценностите)', () => {
  expect(tocIds).toEqual(
    expect.arrayContaining(['pravosadie', 'zashtita-tsennosti']),
  );
});
```

(c) FINAL STATE of the test file should have these spec blocks (in this order):
1. `'has at least 12 TOC entries (slice 2 final)'` — count assertion
2. `'every TOC id has a matching <h2 id="…">'` — UNCHANGED from plan 01 (immutable)
3. `'contains the locked slice-1 anchors'` — UNCHANGED from slice 1 (immutable; manifest/desen-konsensus/ikonomika)
4. `'contains the slice-2 plan-01 anchors (Енергетика, Земеделие, Здравеопазване, Външна сигурност)'` — UNCHANGED from plan 01 Task 2
5. `'contains the slice-2 plan-02 anchors (Образование, Социална политика, Култура)'` — UNCHANGED from plan 02 Task 3
6. `'contains the slice-2 plan-03 anchors (Правосъдие, Защита на ценностите)'` — NEW from this task

Total: 6 specs. All MUST pass. Verify at end of task.

**Step 2.5 — Validate.**
```bash
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
pnpm typecheck
pnpm lint:i18n
```
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'id="zashtita-tsennosti"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c "id: 'zashtita-tsennosti'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥12
- Order check: in `page.tsx`, line numbers for the last two `<h2 id>` anchors yield `id="kultura"` < `id="zashtita-tsennosti"` (Защита на ценностите is at end-of-article)
- The Защита на ценностите `<section>` `<h2>` text is the LONG form: `grep 'Защита на българските ценности' src/app/\(frontend\)/agenda/page.tsx` returns ≥1 hit
- The TOC entry uses the SHORT form: `grep "label: 'Защита на ценностите'" src/app/\(frontend\)/agenda/page.tsx` returns 1 hit
- The Защита на ценностите `<section>` has 5 `<h3>` sub-sections (verify by reading the section); no `id=` attributes on them
- The Защита на ценностите `<section>` has NO `<ul>` and NO `<ol>` — pure prose
- `<Alert>` block STILL present (Task 3 removes it)
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; output shows EXACTLY 6 specs passing (count + every-id-matches + slice-1 + slice-2-plan-01 + slice-2-plan-02 + slice-2-plan-03)
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
  </acceptance_criteria>
  <done>
- `src/app/(frontend)/agenda/page.tsx` contains the Защита на ценностите `<section>` APPENDED at end-of-article (just before `</article>`); 5 `<h3>` sub-sections in source order; no lists; no `id=` on `<h3>`s
- `TOC_ITEMS` has the FINAL 12 entries matching UI-SPEC §7.2 verbatim
- The anchor-drift unit test asserts `≥12` and the three slice-2 plan-arrays plus the slice-1 immutable lock
- All three automated gates exit 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Final-slice cleanup — remove <Alert>, swap generateMetadata, clean bg.json, add i18n-cleanup unit test</name>
  <files>src/app/(frontend)/agenda/page.tsx, messages/bg.json, tests/unit/agenda-i18n-cleanup.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (full file — to find exact lines for `<Alert>` block, import statement, and `generateMetadata` call)
- src/app/(frontend)/faq/page.tsx (verbatim precedent for `description: t('metadataDescription')` pattern — line 19 area)
- messages/bg.json lines 262-271 (current agenda + faq blocks; faq.metadataDescription at line 270 is the precedent shape)
- scripts/check-coalition-placeholders.mjs (full file — understand the grep gate so the executor can verify it passes after edit)
- tests/unit/newsletter-coalition-placeholder.test.ts (analog — same file-grep-against-bg.json shape)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §4.1 (Alert removal), §4.2 (body key drop), §4.3 Option A (recommended copy + draftAlert drop), §11 (OG description swap)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md §"Pattern Assignments" 2 (bg.json cleanup) and §"Pattern Assignments" 5 (i18n-cleanup unit test, verbatim shape)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md §"Cleanup Safety Audit" (verification commands)
  </read_first>
  <action>
**Step 3.1 — Remove the `<Alert>` JSX block from `page.tsx`.**

Locate the `<Alert className="mb-8">` opening (currently around lines 60-64; line numbers may have shifted after plans 01+02 added ~5500 lines of content — use `grep -n '<Alert' src/app/\(frontend\)/agenda/page.tsx` to find the exact line). Delete the entire block:

```tsx
<Alert className="mb-8">
  <AlertDescription className="text-sm text-muted-foreground">
    {t('draftAlert')}
  </AlertDescription>
</Alert>
```

After deletion, the JSX flow goes directly from the `<h1>` to the mobile `<TableOfContents>`:

```tsx
<h1 className="mt-2 mb-6 font-display text-3xl">{t('title')}</h1>

{TOC_ITEMS.length > 0 && (
  <TableOfContents
    items={TOC_ITEMS}
    label={tA11y('primaryNavLabel')}
    variant="mobile"
  />
)}
```

**Per UI-SPEC §4.1:** the `<h1>`'s `mb-6` provides sufficient gap. Do NOT replace `mb-6` with `mb-8` or any other value unless the operator visual walkthrough in Task 4 explicitly identifies a spacing problem.

**Step 3.2 — Remove the unused import.**

Edit the import line at the top of `page.tsx` (currently line 4):

```tsx
// BEFORE:
import { Alert, AlertDescription } from '@/components/ui/alert';

// AFTER:
// (line removed entirely)
```

The `Alert` and `AlertDescription` symbols are no longer used; if `pnpm typecheck` flags an unused-import warning, the import line was missed. Re-grep with `grep -n 'Alert' src/app/\(frontend\)/agenda/page.tsx` — the output should be EMPTY (no remaining references to `Alert` or `AlertDescription`).

**Step 3.3 — Swap `generateMetadata().description`.**

Locate `generateMetadata` (currently around line 23). Change line 28 (the `description:` field):

```tsx
// BEFORE:
description: t('draftAlert'),

// AFTER:
description: t('metadataDescription'),
```

This matches the `faq/page.tsx` precedent at its line 19. The new `metadataDescription` key will be added to `bg.json` in Step 3.4 — order matters: typecheck will fail if you ship the `t('metadataDescription')` call before the key exists in `bg.json`. So do Step 3.4 FIRST, then Step 3.3, OR do them together in the same commit.

**Step 3.4 — Clean up `messages/bg.json` agenda namespace.**

Locate the agenda namespace block (currently lines 262-267). Replace the block:

```json
"agenda": {
  "title": "Програма",
  "leadEyebrow": "Какво защитаваме",
  "draftAlert": "Програмата е в процес на финализиране.",
  "body": "[ТЕКСТ ОТ КОАЛИЦИЯ]"
},
```

With this final shape:

```json
"agenda": {
  "title": "Програма",
  "leadEyebrow": "Какво защитаваме",
  "metadataDescription": "Какво защитаваме и каква промяна искаме за МСП сектора в България. Пълна политическа програма на коалиция Синя България."
},
```

**JSON pitfalls:**
- The LAST key inside `"agenda": { … }` (now `"metadataDescription"`) MUST NOT have a trailing comma. The `}` follows on the next line.
- The `},` after `"agenda": { … }` (closing the namespace block, comma to separate from `"faq": { … }`) IS required — DO NOT remove that comma.
- Verify with `pnpm typecheck` (next-intl typed routes will catch any orphan key reference) and `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json', 'utf8'))"` (raw JSON parse).

**Step 3.5 — Create `tests/unit/agenda-i18n-cleanup.test.ts`** (per PATTERNS.md §"Pattern Assignments" 5 — verbatim from the analog at `tests/unit/newsletter-coalition-placeholder.test.ts`):

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';

/**
 * Regression guard for Phase 02.3 i18n cleanup. Asserts the agenda
 * namespace in messages/bg.json has the post-slice-2 shape — no
 * draftAlert, no body placeholder, but a populated metadataDescription.
 */
describe('Phase 02.3 — agenda i18n cleanup', () => {
  const bg = readFileSync('messages/bg.json', 'utf8');
  const agendaBlock = bg.match(/"agenda":\s*\{[\s\S]*?\n\s*\}/)?.[0] ?? '';

  it('agenda namespace has been parsed', () => {
    expect(agendaBlock).toContain('"agenda"');
    expect(agendaBlock).toContain('"title"');
  });

  it('agenda.body key has been dropped', () => {
    expect(agendaBlock).not.toContain('"body"');
  });

  it('agenda.draftAlert key has been dropped', () => {
    expect(agendaBlock).not.toContain('"draftAlert"');
  });

  it('agenda.metadataDescription key has been added', () => {
    expect(agendaBlock).toContain('"metadataDescription"');
  });

  it('no [ТЕКСТ ОТ КОАЛИЦИЯ] placeholder remains in agenda', () => {
    expect(agendaBlock).not.toContain('[ТЕКСТ ОТ КОАЛИЦИЯ]');
  });
});
```

The regex `\/"agenda":\s*\{[\s\S]*?\n\s*\}\/` scopes the assertions to ONLY the agenda namespace block — preventing false positives from the string `"agenda"` appearing elsewhere in `bg.json` (e.g., the navigation key `nav.agenda` or the FAQ nav-link).

**Step 3.6 — Validate the cleanup.** Run:

```bash
# 1. JSON well-formed
node -e "JSON.parse(require('fs').readFileSync('messages/bg.json', 'utf8'))"

# 2. No orphan i18n key references
grep -n "t('draftAlert')" "src/app/(frontend)/agenda/"          # MUST return nothing
grep -n "t('body')" "src/app/(frontend)/agenda/"                # MUST return nothing
grep -n "t('metadataDescription')" "src/app/(frontend)/agenda/" # MUST return exactly 1 match

# 3. No <Alert> on the agenda page
grep -n '<Alert' "src/app/(frontend)/agenda/page.tsx"           # MUST return nothing
grep -n "from '@/components/ui/alert'" "src/app/(frontend)/agenda/page.tsx"  # MUST return nothing

# 4. Coalition-placeholder grep gate
pnpm check:placeholders

# 5. Type + lint + i18n unit test
pnpm typecheck
pnpm lint:i18n
pnpm vitest run tests/unit/agenda-i18n-cleanup.test.ts
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
```

All MUST pass. If `pnpm check:placeholders` fails, the placeholder is still somewhere in `bg.json` — find it with `grep -n 'ТЕКСТ ОТ КОАЛИЦИЯ' messages/bg.json`. (Note: the script may also fail for other namespaces if they have placeholders; this plan only resolves the `agenda.body` placeholder. If other namespaces fail, that's NOT this plan's concern — defer to the relevant phase.)

**DO NOT in this task:**
- Do NOT modify `src/components/ui/alert.tsx` (the source component is unchanged; only the agenda page's usage is removed)
- Do NOT modify any other namespace in `bg.json` (only `agenda.*` is in scope)
- Do NOT modify `scripts/lint-i18n.mjs` (the existing `EXEMPT_FILES` entry is correct)
- Do NOT modify `scripts/check-coalition-placeholders.mjs`
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-i18n-cleanup.test.ts && pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n && pnpm check:placeholders</automated>
  </verify>
  <acceptance_criteria>
- `grep -c '<Alert' src/app/\(frontend\)/agenda/page.tsx` returns 0 (Alert block removed)
- `grep -c 'AlertDescription' src/app/\(frontend\)/agenda/page.tsx` returns 0 (component reference removed)
- `grep -cE "from ['\"]@/components/ui/alert['\"]" src/app/\(frontend\)/agenda/page.tsx` returns 0 (import line removed)
- `grep -c "t('metadataDescription')" src/app/\(frontend\)/agenda/page.tsx` returns 1 (generateMetadata().description swapped)
- `grep -c "t('draftAlert')" src/app/\(frontend\)/agenda/page.tsx` returns 0 (no remaining reference)
- `grep -c "t('body')" src/app/\(frontend\)/agenda/page.tsx` returns 0
- `grep -c '"draftAlert"' messages/bg.json` returns 0 (key removed from bg.json — verified across whole file because `agenda.draftAlert` was the only definition)
- `grep -cE '"agenda":\s*\{[\s\S]*?"body"' messages/bg.json` returns 0 (or use a multi-line capable tool: the agenda namespace must NOT contain the `body` key)
- `grep -c '"metadataDescription"' messages/bg.json` returns ≥2 (one in `faq` from before, one new in `agenda`)
- `grep -c 'ТЕКСТ ОТ КОАЛИЦИЯ' messages/bg.json` returns 0 (placeholder gone from agenda; if it remains anywhere else, that's a separate phase's responsibility but is NOT introduced by this plan)
- `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json', 'utf8'))"` exits 0 (JSON well-formed)
- `pnpm vitest run tests/unit/agenda-i18n-cleanup.test.ts` exits 0; output shows EXACTLY 5 specs passing (parse + body-dropped + draftAlert-dropped + metadataDescription-added + no-placeholder)
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; output shows 6 specs passing (Tasks 1+2 unchanged)
- `pnpm typecheck` exits 0 (no orphan i18n key reference, no unused import)
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
- `pnpm check:placeholders` exits 0 (the agenda placeholder is gone)
  </acceptance_criteria>
  <done>
- `<Alert>` JSX block, `Alert`/`AlertDescription` imports, `t('draftAlert')` and `t('body')` references all removed from `src/app/(frontend)/agenda/page.tsx`
- `generateMetadata().description` reads `t('metadataDescription')` (matching the faq precedent)
- `messages/bg.json` agenda namespace has the final 3-key shape: title + leadEyebrow + metadataDescription (no body, no draftAlert)
- The `metadataDescription` value is the verbatim copy from UI-SPEC §4.3: "Какво защитаваме и каква промяна искаме за МСП сектора в България. Пълна политическа програма на коалиция Синя България."
- `tests/unit/agenda-i18n-cleanup.test.ts` exists and passes 5/5 specs
- `pnpm check:placeholders` now exits 0 (the placeholder gate that was previously red is now green)
- All five automated gates exit 0
  </done>
</task>

<task type="auto">
  <name>Task 4: Add Playwright E2E spec for /agenda (anchor presence + Alert absence + OG description correctness)</name>
  <files>tests/e2e/agenda.spec.ts</files>
  <read_first>
- tests/e2e/landing.spec.ts (analog — same `/agenda`-touching surface, Playwright shape, `test.describe`/`test`/`page.goto`/`expect` patterns)
- tests/e2e/responsive.spec.ts (verifies that `/agenda` is already in the responsive sweep at line 5; this new spec is COMPLEMENTARY — covers anchor + alert + meta concerns the responsive sweep doesn't)
- src/app/(frontend)/agenda/page.tsx (state AFTER Task 3 — to know what selectors will work)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md §"Pattern Assignments" 4 (verbatim shape for the new spec)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §4.4 (planner discretion to add this spec; included per RESEARCH §"Open Questions" #3 recommendation)
- playwright.config.ts (4 viewport projects: desktop-chrome 1440x900, tablet-chrome 768x1024, mobile-iphone-se 375x667, mobile-pixel-5 360x740 — the new spec runs on all 4)
  </read_first>
  <action>
Create `tests/e2e/agenda.spec.ts` with this verbatim shape (copied from PATTERNS.md §"Pattern Assignments" 4):

```ts
import { test, expect } from '@playwright/test';

/**
 * /agenda E2E coverage — Phase 02.3 final slice.
 * Asserts:
 *   1. Page loads; <h1> "Програма" is visible
 *   2. All 12 <h2 id> chapter headings render
 *   3. Clicking each TOC link scrolls to the matching <h2>
 *   4. The draft <Alert> banner is no longer present
 *   5. The OG meta description does NOT contain "финализиране"
 *
 * Complements tests/e2e/responsive.spec.ts (which already covers /agenda for
 * horizontal-scroll across 4 viewports — that's PUB-06 territory, not this).
 */
test.describe('Public surface — /agenda (Phase 02.3 final)', () => {
  test('renders h1 + 12 chapter headings', async ({ page }) => {
    await page.goto('/agenda');
    await expect(page.locator('h1').first()).toBeVisible();

    // 12 <h2 id> chapter headings (UI-SPEC §7.2 final lock)
    const h2s = page.locator('article h2[id]');
    await expect(h2s).toHaveCount(12);
  });

  test('clicking each TOC link scrolls to the matching <h2>', async ({ page }) => {
    await page.goto('/agenda');

    const ANCHORS = [
      'manifest', 'desen-konsensus', 'ikonomika',
      'energetika', 'zemedelie', 'obrazovanie',
      'sotsialna-politika', 'zdraveopazvane', 'pravosadie',
      'vanshna-sigurnost', 'kultura', 'zashtita-tsennosti',
    ];

    for (const id of ANCHORS) {
      // Use the desktop TOC link (mobile <details> is collapsed by default).
      // .first() because the TOC variant prop renders both desktop and mobile
      // navs in the DOM with CSS-controlled visibility — pick whichever shows.
      await page.locator(`nav a[href="#${id}"]`).first().click();
      await expect(page.locator(`#${id}`)).toBeInViewport();
    }
  });

  test('draft <Alert> banner is no longer present', async ({ page }) => {
    await page.goto('/agenda');
    // shadcn Alert renders role="alert" — the slice-1 banner had this role.
    await expect(page.locator('[role="alert"]')).toHaveCount(0);
  });

  test('OG meta description does NOT contain "финализиране"', async ({ page }) => {
    await page.goto('/agenda');
    const description = await page
      .locator('meta[name="description"]')
      .getAttribute('content');
    expect(description).toBeTruthy();
    expect(description).not.toContain('финализиране');
  });
});
```

**Step 4.1 — Run the spec.** Per VALIDATION.md and Phase 02.2 SUMMARY D-4 invocation guidance:

```bash
pnpm test:e2e tests/e2e/agenda.spec.ts
```

This runs the spec across all 4 Playwright viewport projects (per `playwright.config.ts`). All 4 specs × 4 viewports = 16 test runs MUST pass.

If any anchor-jump check fails, the most likely cause is that the desktop TOC `<nav>` is not yet hydrated when `page.locator(...)` runs. Possible mitigation: add `await page.waitForLoadState('networkidle')` after `page.goto('/agenda')`. The Playwright config's default `webServer.url` and dev-server are inherited — no fixture changes needed.

**Step 4.2 — Run the full validation gate stack** (the entire phase's gate, not just this task's):

```bash
pnpm vitest run                                                              # all unit tests
pnpm test:e2e tests/e2e/agenda.spec.ts                                        # this new spec
pnpm test:e2e tests/e2e/responsive.spec.ts                                    # /agenda still in PUB-06 sweep
pnpm typecheck
pnpm lint:i18n
pnpm lint:tokens
pnpm check:placeholders
```

All MUST exit 0. This is the per-VALIDATION.md "Phase gate (before /gsd-verify-work)" stack — passing it confirms Phase 02.3 is shippable.

**DO NOT in this task:**
- Do NOT add a Playwright fixture for `/agenda` (the spec is fixture-free; it uses the dev server only)
- Do NOT change `playwright.config.ts` (4 viewport projects already correct)
- Do NOT modify any of the prior tasks' files
  </action>
  <verify>
    <automated>pnpm test:e2e tests/e2e/agenda.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `tests/e2e/agenda.spec.ts` exists at the expected path
- The file contains exactly 4 `test(...)` blocks: renders-h1-and-12-h2; clicking-each-toc-link; draft-Alert-not-present; OG-description-correct
- The file contains the `ANCHORS` array with all 12 IDs in source order
- `pnpm test:e2e tests/e2e/agenda.spec.ts` exits 0; output shows 4 tests × 4 viewport projects = 16 passing
- The other gates from Step 4.2 also pass:
  - `pnpm vitest run` exits 0 (all unit tests across the repo)
  - `pnpm test:e2e tests/e2e/responsive.spec.ts` exits 0 (`/agenda` horizontal-scroll sweep still green)
  - `pnpm typecheck` exits 0
  - `pnpm lint:i18n` exits 0
  - `pnpm lint:tokens` exits 0
  - `pnpm check:placeholders` exits 0
  </acceptance_criteria>
  <done>
- `tests/e2e/agenda.spec.ts` exists with 4 test blocks
- Full validation gate stack from VALIDATION.md "Phase gate" runs green
- The Playwright spec covers anchor presence, anchor-jump behavior, Alert absence, and OG-description correctness — addressing REQ-AGENDA-S2-02 + REQ-AGENDA-S2-03 mechanical assertions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator visual verification of final slice (12 of 12 chapters live, draft banner removed, full validation green)</name>
  <what-built>
The /agenda page now ships 12 of 12 final chapters. Plan 03 has:

1. **Inserted Правосъдие** (between Здравеопазване and Външна сигурност) — 4 named `<h3>` sub-sections (Продължителността на съдебното производство; Съдебни разходи и правна помощ; Срокове; Правни средства за защита, достъпни на европейско и международно равнище), 2-3 flat `<ul>` blocks with em-dash bullets stripped, no top-level `<ol>`, no `<strong>` prefix
2. **Appended Защита на българските ценности, традиции и интереси** (at end-of-article) — 5 named `<h3>` sub-sections (България е свободна и демократична държава; Ценности и традиции; ЛГБТ и нахлуването на полова идентичност в обществото; България е християнска държава; България е европейска държава), no lists
3. **Removed the `<Alert>` draft banner** — the page no longer shows "Програмата е в процес на финализиране."; the `Alert`/`AlertDescription` imports are removed
4. **Cleaned `messages/bg.json`** — `agenda.body` and `agenda.draftAlert` keys are dropped; `agenda.metadataDescription` is added with the UI-SPEC §4.3 copy
5. **Swapped `generateMetadata().description`** — the OG/Twitter card description now reads the new metadataDescription copy instead of the misleading "Програмата е в процес на финализиране."
6. **Added `tests/unit/agenda-i18n-cleanup.test.ts`** — regression guard against future reintroduction of the dropped keys
7. **Added `tests/e2e/agenda.spec.ts`** — 4-test Playwright spec covering anchor presence, anchor-jump, Alert absence, OG description correctness; runs on 4 viewports

The TOC has reached its final 12-entry state matching UI-SPEC §7.2 verbatim. The anchor-drift unit test asserts ≥12 with the full slice-2 nine-anchor lock. `pnpm check:placeholders` now exits 0 (it was failing before this plan due to the `agenda.body` placeholder).

This is the FINAL operator verification gate for Phase 02.3.
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev` and navigate to `http://localhost:3000/agenda`.
2. **No more draft banner:** confirm the page goes directly from the eyebrow + h1 ("Какво защитаваме" + "Програма") to the mobile `<TableOfContents>` and the 2-column grid. There is NO "Програмата е в процес на финализиране." Alert anywhere on the page.
3. **TOC count + order — desktop (≥1024px):** sticky sidebar shows EXACTLY 12 entries in this exact order: Манифест → Десен консенсус → Икономика → Енергетика → Земеделие → Образование → Социална политика → Здравеопазване → **Правосъдие** → Външна сигурност и отбрана → Култура → **Защита на ценностите**. The two new entries from this plan are bold above.
4. **TOC count + order — mobile (iPhone SE 375×667 in DevTools):** the `<details>` block lists the same 12 entries in the same order; expand and confirm.
5. **Anchor jumps:** click each of the 12 TOC links. Each MUST scroll smoothly to the matching `<h2>`; the heading MUST NOT be hidden under the sticky header. Confirm on both desktop and mobile.
6. **Active-section highlighting (desktop):** scroll the page slowly through all 12 chapters. As each `<h2>` enters the upper third of the viewport, the corresponding TOC entry should highlight (font-weight + primary color).
7. **Правосъдие chapter content:** scroll to `#pravosadie`. Confirm ~7 lead paragraphs, then a flat `<ul>` of ~6 legislative measures (rendered with prose default discs — NOT em-dashes), then a linking paragraph, then a flat `<ul>` of ~8 factors. Then 4 `<h3>` sub-sections (Продължителността… / Съдебни разходи… / Срокове / Правни средства за защита…) each with body paragraphs. Sub-section "Съдебни разходи и правна помощ" has a 3-item flat `<ul>`. NO numbered list anywhere in this chapter.
8. **No em-dash artifacts in Правосъдие:** confirm none of the `<li>` items in either flat `<ul>` start with `–` (the source's em-dash bullet markers were stripped). Right-click → Inspect → confirm `<li>` text content begins with the policy text, not with a dash.
9. **Защита на ценностите chapter content:** scroll to `#zashtita-tsennosti`. Confirm the `<h2>` text is "Защита на българските ценности, традиции и интереси" (the FULL source heading, NOT the short TOC label). Then 1 lead paragraph, then 5 `<h3>` sub-sections in order: България е свободна и демократична държава → Ценности и традиции → ЛГБТ и нахлуването на полова идентичност в обществото → България е християнска държава → България е европейска държава. Each with body paragraphs only. NO lists in this chapter.
10. **TOC label vs h2 text mismatch is INTENTIONAL** for chapter 12: the TOC says "Защита на ценностите" (short, ~2 words) while the rendered `<h2>` says "Защита на българските ценности, традиции и интереси" (full source title, ~6 words). This matches UI-SPEC §7.2 — the TOC needs short labels to fit the 220px sidebar; the h2 is verbatim source.
11. **Sub-section `<h3>` styling:** all sub-sections in Правосъдие and Защита на ценностите render in `prose-lg`-default `<h3>` size (smaller than h2, larger than body); NO `id=` attribute (verify via Inspect).
12. **OG/Twitter card description (view-source):** in browser DevTools → Elements → search for `<meta name="description"`. The `content` attribute MUST be the new copy "Какво защитаваме и каква промяна искаме за МСП сектора в България. Пълна политическа програма на коалиция Синя България." (or whatever the operator finalized). It MUST NOT contain "финализиране".
13. **No raw [ТЕКСТ ОТ КОАЛИЦИЯ] anywhere:** open `messages/bg.json` → search for `[ТЕКСТ`. No matches in the agenda namespace (other namespaces with placeholders are NOT this plan's concern; document any if seen).
14. **Mobile responsive sweep:** in DevTools, swap to iPhone SE (375×667) viewport. Scroll through the entire page. Confirm:
    - No horizontal scrollbar
    - The mobile `<details>` TOC expands to show all 12 entries
    - All 12 chapters render readable Bulgarian prose
    - All `<h2>` and `<h3>` elements wrap correctly without overflow
    - All `<ul>` and `<ol>` items render with bullets/numerals visible
15. **Linter sanity:** from repo root, run:
    ```bash
    pnpm lint:i18n          # must exit 0 with PUB-05 OK
    pnpm lint:tokens        # must exit 0
    pnpm check:placeholders # must exit 0 (this was failing before this plan)
    pnpm typecheck          # must exit 0
    ```
16. **Test sanity (Phase 02.2 SUMMARY D-4 — `pnpm vitest run`, NEVER `pnpm test --run`):**
    ```bash
    pnpm vitest run tests/unit/agenda-toc-anchors.test.ts          # 6 specs
    pnpm vitest run tests/unit/agenda-i18n-cleanup.test.ts         # 5 specs
    pnpm test:e2e tests/e2e/agenda.spec.ts                         # 4 tests × 4 viewports
    pnpm test:e2e tests/e2e/responsive.spec.ts                     # /agenda still in PUB-06 sweep
    ```
    All four MUST exit 0.
17. **Final regression check on view-source:** confirm the sequence of `<h2 id>` anchors top-to-bottom on the rendered page is exactly: manifest → desen-konsensus → ikonomika → energetika → zemedelie → obrazovanie → sotsialna-politika → zdraveopazvane → pravosadie → vanshna-sigurnost → kultura → zashtita-tsennosti. (Source order final lock per UI-SPEC §7.2.)
  </how-to-verify>
  <resume-signal>Type "approved" if all 17 checks pass — Phase 02.3 ships. If anything is off (especially: draft Alert still visible; rendered `<li>` text starts with `–`; sub-section ordering wrong; OG description still says "финализиране"; `pnpm check:placeholders` still failing; TOC count not exactly 12; any chapter using a numbered list where the source uses dashes or vice versa), describe the issue and the executor will revise that area before re-requesting verification.</resume-signal>
  <acceptance_criteria>
- Operator response is "approved" (or substantively equivalent)
- If revision is requested, the resume signal includes the specific issue and the executor lands a follow-up commit before this task is marked done
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Phase 02.3 final slice is content-only + i18n key cleanup + test addition. The `<Alert>` removal eliminates one passive UI element; no new auth, input, API, or trust boundary is introduced. The `tests/e2e/agenda.spec.ts` runs against the local dev server during CI/local test invocation only — it is not a runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-03-01 | Tampering / Information disclosure | Bulgarian content rendered in Правосъдие and Защита на ценностите `<section>` blocks | accept | All content is JSX literals authored at edit time. React's default escaping covers any pasted Bulgarian string. NO `dangerouslySetInnerHTML`. NO user input. ASVS V5 not applicable. |
| T-02.3-03-02 | Tampering | OG meta description (`generateMetadata().description`) | accept | Description is sourced from a static i18n catalogue key (`agenda.metadataDescription`); no user input flows to the description; the key value is build-time-resolved by next-intl. ASVS V5 not applicable. |
| T-02.3-03-03 | Information disclosure | i18n catalogue keys in `messages/bg.json` | accept | The agenda namespace cleanup REMOVES information surface (drops 2 keys); ADDS no new sensitive surface. The new `metadataDescription` key value is public-facing copy already approved by UI-SPEC. |
| T-02.3-03-04 | Tampering | ISR cache for `/agenda` | accept | After this plan ships, the `/agenda` ISR cache will revalidate within 3600s and serve the new content; cache key remains URL-only. No new cache poisoning vector. |
| T-02.3-03-05 | Information disclosure | New Playwright spec `tests/e2e/agenda.spec.ts` | accept | Test spec runs against local/CI dev server only; no production traffic; no secrets in the spec. |

**Security delta for plan 03:** Net negative. The `<Alert>` removal eliminates one passive UI element; the i18n key cleanup REMOVES information from the catalogue. ASVS V2-V14 categories remain not-applicable per RESEARCH §"Security Domain". No new attack surface; one existing surface (the placeholder-shaped `agenda.body` key) is eliminated.
</threat_model>

<verification>
After all five tasks land:

- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0 with 6 specs passing (`≥12` count + every-id-matches + slice-1 + slice-2-plan-01 + slice-2-plan-02 + slice-2-plan-03)
- `pnpm vitest run tests/unit/agenda-i18n-cleanup.test.ts` exits 0 with 5 specs passing (parse + body-dropped + draftAlert-dropped + metadataDescription-added + no-placeholder)
- `pnpm test:e2e tests/e2e/agenda.spec.ts` exits 0 with 4 tests × 4 viewports passing
- `pnpm test:e2e tests/e2e/responsive.spec.ts` exits 0 (existing `/agenda` PUB-06 sweep still green)
- `pnpm typecheck` exits 0 (no orphan i18n key reference, no unused import, no missing key)
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
- `pnpm lint:tokens` exits 0
- `pnpm check:placeholders` exits 0 (the `[ТЕКСТ ОТ КОАЛИЦИЯ]` placeholder is gone from the agenda namespace)
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥12
- `grep -c '<Alert' src/app/\(frontend\)/agenda/page.tsx` returns 0
- `grep -c '"draftAlert"' messages/bg.json` returns 0
- `grep -c '"metadataDescription"' messages/bg.json` returns ≥2 (faq + agenda)
- Operator visual walkthrough signed off (Task 5 resume-signal = "approved")
</verification>

<success_criteria>
Phase 02.3 is COMPLETE when:

1. All 12 chapters of the coalition political program render on `/agenda` in source order with the correct content shapes per UI-SPEC §3.3
2. The `<Alert>` draft banner has been removed; the page no longer claims to be "in process of finalization"
3. The OG/Twitter card description has been swapped to verbatim final copy that does NOT contain "финализиране"
4. The `agenda.body` and `agenda.draftAlert` i18n keys have been dropped; `agenda.metadataDescription` has been added
5. `pnpm check:placeholders` (which was failing before this plan) now exits 0 — the platform-wide grep gate against `[ТЕКСТ ОТ КОАЛИЦИЯ]` passes for the agenda namespace
6. The full automated validation gate stack (unit tests + E2E + typecheck + i18n lint + tokens lint + placeholders) is green
7. The operator visual walkthrough on desktop AND mobile confirms typography, list rendering, anchor jumps, active-section highlighting, and OG metadata work end-to-end (REQ-AGENDA-S2-05)
8. `D-CoalitionContent-Agenda` is fully resolved (slice 2 final ships; the Phase 02.2 walking-skeleton is now complete)
</success_criteria>

<output>
After all five tasks pass, create `.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-03-SUMMARY.md` documenting:

- Counts: chapters added in this plan (2 — Правосъдие + Защита на ценностите), final `<h2 id>` count (12), final `<h3>` count across the entire page (cumulative — record exact value), final `<ul>` count, final `<ol>` count (still ≥3 from plan 02 — no new `<ol>` added here), `<strong>` count (unchanged from plan 02; this plan adds NO new `<strong>` prefixes)
- Final `page.tsx` line count and TOC_ITEMS length (12)
- Final `messages/bg.json` agenda namespace shape (3 keys: title + leadEyebrow + metadataDescription)
- Source-content ambiguities encountered (e.g., were Правосъдие's 4 `<h3>` sub-section names exactly as predicted? Did Защита на ценностите have additional sub-sections beyond the 5 expected?)
- Confirmation that `pnpm check:placeholders` now exits 0 (the gate that was failing before this plan)
- Confirmation that the new tests/e2e/agenda.spec.ts runs green on all 4 Playwright viewports
- Phase gate sign-off summary: 6/6 unit specs + 4/4 e2e specs (×4 viewports) + 4 lint scripts + typecheck = full validation green
- Handoff note for Phase 03: D-CoalitionContent-Agenda is now FULLY resolved; Phase 03 (Idea Catalog + Voting) remains hard-blocked on the GDPR Art.9 lawyer opinion (external dependency, not this phase's concern)
- Note for STATE.md update: deferred items table for `D-CoalitionContent-Agenda` should flip from "partial Phase 02.2" to "resolved Phase 02.3"
</output>
</content>
</invoke>