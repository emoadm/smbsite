---
phase: 02.3-coalition-agenda-content-slice-2
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/app/(frontend)/agenda/page.tsx
  - tests/unit/agenda-toc-anchors.test.ts
autonomous: false
mode: mvp
slice: numbered-policy-chapters
requirements:
  - REQ-AGENDA-S2-01
  - REQ-AGENDA-S2-02
  - REQ-AGENDA-S2-04
  - REQ-AGENDA-S2-05
  - D-CoalitionContent-Agenda
must_haves:
  truths:
    - "A visitor opening /agenda sees three NEW top-level chapters inserted in source order: Образование (#obrazovanie) and Социална политика (#sotsialna-politika) BETWEEN Земеделие and Здравеопазване; Култура (#kultura) BETWEEN Външна сигурност и отбрана and (eventual) Защита на ценностите"
    - "Образование renders multiple <ol> blocks with <strong>{policy-name}.</strong> prefixes per UI-SPEC §3.3.D, organized under <h3> sub-sections per UI-SPEC §3.3.A (one for школско образование, one for висше образование)"
    - "Социална политика renders 5 named <h3> sub-sections (Връщане на личната отговорност / Подкрепа за децата и семействата / Политика на пазара на труда / Развитие на системата за дългосрочна грижа / Пенсионна система), each followed by body paragraphs and zero or more flat <ul> blocks"
    - "Култура renders a single <ol> with 10 items (the explicitly-named «пакет от 10 основни мерки»), each item starting with <strong>{measure-name}.</strong>; items 6-10 each contain a nested <ul> of em-dash sub-bullets per UI-SPEC §3.3.B exception (em-dash markers stripped from rendered text)"
    - "TOC_ITEMS extends from 7 → 10 entries with the three new IDs inserted at the correct UI-SPEC §7.2 source-order positions: obrazovanie + sotsialna-politika between zemedelie and zdraveopazvane; kultura between vanshna-sigurnost and (slot for plan-03 zashtita-tsennosti)"
    - "The <Alert> draftAlert banner is STILL PRESENT (its removal is reserved for plan 03)"
    - "messages/bg.json agenda namespace is UNCHANGED in this plan (cleanup is plan 03)"
    - "tests/unit/agenda-toc-anchors.test.ts has been bumped to assert ≥10 entries and to lock the three new slice-2 plan-02 anchors via expect.arrayContaining; pnpm vitest run exits 0"
    - "pnpm typecheck exits 0; pnpm lint:i18n still passes"
  artifacts:
    - path: "src/app/(frontend)/agenda/page.tsx"
      provides: "TOC_ITEMS at 10 entries; three new <section><h2 id=…> blocks for Образование, Социална политика, Култура with <ol>+<strong>+<h3>+nested <ul> structures per UI-SPEC §3.3.A/B/C/D"
      contains: "id=\"obrazovanie\""
    - path: "tests/unit/agenda-toc-anchors.test.ts"
      provides: "Anchor-drift guard extended for slice-2 numbered-policy chapters: ≥10 entries assertion + new locked-anchors block for the three new IDs"
      contains: "obrazovanie"
  key_links:
    - from: "TOC_ITEMS in src/app/(frontend)/agenda/page.tsx"
      to: "<h2 id=…> anchors in the same file"
      via: "shared id strings (#obrazovanie, #sotsialna-politika, #kultura)"
      pattern: "id=\"obrazovanie\"|id=\"sotsialna-politika\"|id=\"kultura\""
    - from: "Култура items 6-10 nested <ul>"
      to: "UI-SPEC §3.3.B em-dash exception"
      via: "source em-dash bullets stripped; structural <ul><li> retained"
      pattern: "<ul>"
---

<objective>
Ship slice 1 of the numbered-policy chapters — the three structurally complex chapters that exercise the `<ol>` + `<strong>` + nested-`<ul>` + `<h3>` rendering rules from UI-SPEC §3.3. These chapters use every advanced shape rule in the spec and inherit the load-bearing precedent from Икономика slice-1 (live page.tsx lines 434-487).

Per RESEARCH §"Pattern 1: SPIDR slicing by structural shape", this plan ships:
- **Chapter 6** — Образование (`#obrazovanie`, agenda-raw.txt:552-665) — `<h3>` sub-sections + multiple `<ol>` with `<strong>` prefixes
- **Chapter 7** — Социална политика (`#sotsialna-politika`, agenda-raw.txt:669-744) — 5 named `<h3>` sub-sections
- **Chapter 11** — Култура (`#kultura`, agenda-raw.txt:1111-1242) — single 10-item `<ol>` with `<strong>` + nested `<ul>` em-dash sub-bullets

These are inserted into `page.tsx` at the right source-order positions (Образование + Социална политика go BETWEEN plan-01's Земеделие and Здравеопазване; Култура goes BETWEEN plan-01's Външна сигурност и отбрана and the future plan-03 Защита на ценностите). After this plan, 10 of 12 chapters are live.

Skipped to plan 03: Правосъдие (#9) + Защита на ценностите (#12) + final-slice cleanup (`<Alert>` removal, `agenda.body` drop, `agenda.metadataDescription` add, optional E2E spec, optional i18n-cleanup unit test).

Purpose: Apply the five most demanding UI-SPEC §3.3 rules — `<ol>` re-numbering when source restarts at "1.", `<strong>{name}.</strong>` policy-name prefix, multi-paragraph `<li>` bodies wrapped in `<p>`, `<h3>` for explicitly named sub-divisions (no `id=` attribute), em-dash bullet stripping in nested `<ul>`. These are the rules that, if violated, produce visible defects (double-numerals, ghost em-dashes, mis-ordered list markers).

Output: Updated `agenda/page.tsx` (TOC extended to 10 entries; three new `<section>` blocks INSERTED at the right source-order positions); updated anchor-drift unit test (incremental extension to ≥10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-VALIDATION.md
@.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-01-SUMMARY.md
@.planning/phases/02.2-coalition-agenda-content/02.2-01-SUMMARY.md
@.planning/coalition/agenda-raw.txt
@src/app/(frontend)/agenda/page.tsx
@tests/unit/agenda-toc-anchors.test.ts

<interfaces>
<!-- Existing scaffold + the Икономика precedent the executor must replicate verbatim. -->

From src/app/(frontend)/agenda/page.tsx (state AFTER plan-01 ships — to extend):

```tsx
const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },                       // slice 1
  { id: 'desen-konsensus', label: 'Десен консенсус' },         // slice 1
  { id: 'ikonomika', label: 'Икономика' },                     // slice 1
  { id: 'energetika', label: 'Енергетика' },                   // plan 01
  { id: 'zemedelie', label: 'Земеделие' },                     // plan 01
  // ← INSERT: obrazovanie, sotsialna-politika (this plan)
  { id: 'zdraveopazvane', label: 'Здравеопазване' },           // plan 01
  { id: 'vanshna-sigurnost', label: 'Външна сигурност и отбрана' }, // plan 01
  // ← INSERT: kultura (this plan)
];
```

After this plan, TOC_ITEMS final state (10 entries):

```tsx
const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },
  { id: 'desen-konsensus', label: 'Десен консенсус' },
  { id: 'ikonomika', label: 'Икономика' },
  { id: 'energetika', label: 'Енергетика' },
  { id: 'zemedelie', label: 'Земеделие' },
  { id: 'obrazovanie', label: 'Образование' },                 // ← inserted by this plan
  { id: 'sotsialna-politika', label: 'Социална политика' },    // ← inserted by this plan
  { id: 'zdraveopazvane', label: 'Здравеопазване' },
  { id: 'pravosadie',  /* — owned by plan 03 — */ },           // ← NOT in this plan
  { id: 'vanshna-sigurnost', label: 'Външна сигурност и отбрана' },
  { id: 'kultura', label: 'Култура' },                         // ← inserted by this plan
];
```

(With Правосъдие inserted by plan 03 between Здравеопазване and Външна сигурност, the count after plan 02 is 10 entries; plan 03 adds Правосъдие + Защита на ценностите to reach the final 12.)

The Икономика precedent (live page.tsx lines 434-487) — verbatim shape to inherit for Образование and Култура `<ol>` blocks:

```tsx
<ol>
  <li>
    <strong>Бюджетна политика — Дългове и дефицити.</strong>{' '}
    Синя България е за нисък държавен дълг, …
    <p>
      Синя България е за балансиран държавен бюджет, …
    </p>
    <p>
      Нисък бюджетен дефицит и публичен дълг са също задължителни …
    </p>
  </li>
  <li>
    <strong>Данъчна политика.</strong> Синя България е за намаляване …
    <p>Прилагането на дясната икономическа програма …</p>
    <ul>
      <li>ДДС от 20% на 19% през 2027 г. …</li>
      <li>Намаляване на плоските корпоративен и подоходен данък …</li>
    </ul>
  </li>
  …
</ol>
```

Note the JSX whitespace pattern `</strong>{' '}` (with explicit JSX space-after-close) — preserves a space between `<strong>` and the inline body when the JSX line wraps after `</strong>`. Reuse verbatim per PATTERNS.md.

UI-SPEC §3.3 rules invoked by this plan (verbatim references):
- §3.3.A — `<h3>` sub-sections for explicitly named sub-divisions; NO `id=` attribute on `<h3>`
- §3.3.B — `<ul>` flat lists; nested `<ul>` allowed inside `<li>` per Икономика precedent; em-dash sub-bullets in Култура items 6-10 ARE stripped from rendered text (the `–` was the PDF's bullet glyph)
- §3.3.C — `<ol>` re-numbered 1-N in rendered HTML even when source restarts at "1." (pdftotext page-break artifact); multi-paragraph `<li>` bodies wrap subsequent paragraphs in `<p>`
- §3.3.D — `<strong>{policy-name}.</strong>` inline at the start of `<li>` items; `<em>` not expected
- §3.3.E — Lead paragraphs render as plain `<p>` (NEVER `<blockquote>`, NEVER `lead` className)
- §3.3.F — Source punctuation fidelity: em-dashes (`–`), curly quotes (`„…"`), NBSPs preserved verbatim
</interfaces>
</context>

<source_content_map>
For each chapter the executor authors:

| Chapter | Source lines | h2 (verbatim) | Anchor ID | Internal shape (per UI-SPEC §3.3 + RESEARCH Source Content Map) |
|---------|--------------|---------------|-----------|------------------------------------------------------------------|
| Образование | agenda-raw.txt:552-665 | "Образование" | `obrazovanie` | Lead `<p>` × ~3; then 2 sub-sections each introduced by "1." prefix in source — render each as `<h3>` per UI-SPEC §3.3.A (sub-section #1 likely "Нашата цел в областта на училищното образование…"; sub-section #2 likely "В сферата на висшето образование…"). Strip the source's "1." prefix from `<h3>` text. Each `<h3>` is followed by an `<ol>` with `<strong>{policy-name}.</strong>` prefix per §3.3.D. School-ed `<ol>` has 3 named policies ("Промяна на учебното съдържание", "Връщане на училището към основните му задачи", "Възстановяване и защита на авторитета на учителя"). Higher-ed `<ol>` has 5 named policies ("Спиране на безконтролното разрастване…", "Диференцирано финансиране…", "Диференцирано отношение…", "Увеличаване на средствата за наука…", "Промени в регулациите…"). Both `<ol>` blocks re-number 1-N in HTML; do NOT hand-code numerals. |
| Социална политика | agenda-raw.txt:669-744 | "Социална политика" | `sotsialna-politika` | NO lead paragraph (chapter starts directly with sub-section). 5 named `<h3>` sub-sections per UI-SPEC §3.3.A (RESEARCH assumption A3 documents this discretion call): (1) "Връщане на личната отговорност" — body `<p>`s + flat `<ul>` of 4 principles; (2) "Подкрепа за децата и семействата" — body `<p>`s only; (3) "Политика на пазара на труда" — body `<p>`s + flat `<ul>` of 5 measures; (4) "Развитие на системата за дългосрочна грижа" — body `<p>`s only; (5) "Пенсионна система" — body `<p>`s + flat `<ul>` of 4 goals. The source uses "1.", "2.", "3.", "4.", "5." prefixes — strip these from rendered `<h3>` text. NO top-level `<ol>` in this chapter; the numbered prefixes ARE the sub-section markers, not list items. |
| Култура | agenda-raw.txt:1111-1242 | "Култура" | `kultura` | Lead `<p>` × ~8 (thesis paragraphs about culture); then a `<p>` sentence "За целта предлагаме пакет от 10 основни мерки…" (or similar — read source for verbatim phrasing); then a SINGLE `<ol>` with EXACTLY 10 `<li>` items, each prefixed with `<strong>{measure-name}.</strong>` per UI-SPEC §3.3.D. **Items 6-10 each contain a nested `<ul>` of em-dash-prefixed sub-bullets per UI-SPEC §3.3.B exception** — the source uses `–` (en-dash) as bullet markers, which MUST be stripped from rendered text (the rendered `<li>` text starts AFTER the dash; `prose` supplies the disc). The nested `<ul>` is wrapped INSIDE the parent `<li>` of the top-level `<ol>`, AFTER any inline body sentence. NO `<h3>` in this chapter. |

**Authoring discipline (UI-SPEC §3.3.F + L-18, applies to ALL chapters):**
- Em-dashes (`–`), curly quotes (`„…"`), and non-breaking spaces preserved verbatim
- pdftotext line de-wrap rule: blank line in source = paragraph break (new `<p>`); consecutive non-blank lines = same paragraph (joined into one `<p>`)
- Coalition prose rendered verbatim — NO rewording, NO summarizing, NO restructuring
- `<ol>` numerals are CSS-rendered by `prose` — NEVER hand-code "1." / "2." / "3." prefixes as text
- For Култура items 6-10: the source's `–` em-dash bullet markers are stripped; render as `<ul><li>{text-without-dash}</li></ul>` inside the parent `<ol><li>`
- For sub-section `<h3>`: NO `id=` attribute; only top-level `<h2>` chapters get anchor IDs

**Allowed JSX traceability comment (one per `<section>` only, per PATTERNS.md):**
```tsx
<section>
  {/* Source: agenda-raw.txt lines 552-665 */}
  <h2 id="obrazovanie">Образование</h2>
  …
</section>
```

**Forbidden styling overrides (UI-SPEC §3.2 + L-10, repeated from plan 01 for the executor's convenience):**
- NO `mt-*`, `mb-*`, `py-*`, `px-*` on `<p>`, `<h2>`, `<h3>`, `<ul>`, `<ol>`, `<li>`, `<strong>`
- NO `text-*`, `font-*`, `leading-*`, `tracking-*` on any prose element
- NO `list-disc`, `list-inside`, `list-decimal` — `prose` supplies markers
- NO `className="lead"` or `<blockquote>` — UI-SPEC §3.3.E lock
</source_content_map>

<tasks>

<task type="auto">
  <name>Task 1: Author Образование chapter; INSERT it between Земеделие and Здравеопазване; bump anchor-drift test to ≥8</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (re-read AFTER plan-01 ship — necessary because the file is now ~3000+ lines and the executor needs to find the exact lines where the Земеделие `</section>` closes and Здравеопазване `<section>` opens)
- .planning/coalition/agenda-raw.txt lines 552-665 (Образование source content)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3.A (`<h3>` for "1." sub-sections, with the source prefix stripped from rendered text), §3.3.C (`<ol>` re-numbering), §3.3.D (`<strong>` prefix on `<li>` items), §3.3.E (lead `<p>`s)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md §"Pattern Assignments" 1 — the `<ol>+<strong>+nested-<ul>` Икономика precedent at lines 434-487 (read this snippet carefully; it's the load-bearing precedent for Образование and Култура)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md §"Pitfall 3" (sub-section vs. policy-list ambiguity in Образование — answered: the "1." sub-section markers ARE `<h3>` per RESEARCH assumption A2)
- src/app/(frontend)/agenda/page.tsx lines 434-487 (the live Икономика `<ol>` precedent — read directly; this is the verbatim shape to replicate)
  </read_first>
  <action>
**Step 1.1 — Locate the insertion point.** Re-read `src/app/(frontend)/agenda/page.tsx` to find:
(a) The `</section>` that closes the Земеделие chapter (this is the `<section>` whose `<h2>` is `id="zemedelie"` — locate it with `grep -n 'id="zemedelie"' src/app/\(frontend\)/agenda/page.tsx` first, then Read the file with offset around that line to find the closing `</section>`).
(b) The `<section>` opener for Здравеопазване (whose `<h2>` is `id="zdraveopazvane"`).

The Образование `<section>` block is INSERTED between (a) and (b) — directly after the Земеделие `</section>` closing tag and before the Здравеопазване `<section>` opening tag.

**Step 1.2 — Insert TOC entry.** In the `TOC_ITEMS` const, insert ONE new entry between `{ id: 'zemedelie', … }` and `{ id: 'zdraveopazvane', … }`:

```tsx
{ id: 'zemedelie', label: 'Земеделие' },
{ id: 'obrazovanie', label: 'Образование' },     // ← INSERT
{ id: 'zdraveopazvane', label: 'Здравеопазване' },
```

**Step 1.3 — Author the Образование chapter.** Read `agenda-raw.txt:552-665` directly via the Read tool. Compose a new `<section>` block:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 552-665 */}
  <h2 id="obrazovanie">Образование</h2>

  {/* Lead paragraphs — likely 2-3 from lines ~552-565 — read source */}
  <p>{/* Lead paragraph 1 */}</p>
  <p>{/* Lead paragraph 2 */}</p>
  {/* …continue lead paragraphs through the source's pre-sub-section block */}

  {/* Sub-section 1: school education
      Source has a "1." prefix (e.g., " 1.   Нашата цел в областта на училищното образование…") that gets STRIPPED from the rendered <h3> text per UI-SPEC §3.3.A.
      The exact verbatim sub-section name comes from the source — read it. The H3 below uses a placeholder; replace with verbatim source text. */}
  <h3>{/* Verbatim sub-section name with "1." stripped, e.g., "Нашата цел в областта на училищното образование" */}</h3>

  {/* Optional: any introductory paragraph(s) between the <h3> and the <ol>. Read source. */}
  <p>{/* If source has an intro paragraph for this sub-section */}</p>

  <ol>
    <li>
      <strong>Промяна на учебното съдържание.</strong>{' '}
      {/* Body of policy 1 — verbatim from source. May span multiple source paragraphs; wrap each subsequent paragraph in <p> per UI-SPEC §3.3.C "li items with multi-paragraph bodies" */}
      <p>{/* Optional second paragraph */}</p>
    </li>
    <li>
      <strong>Връщане на училището към основните му задачи.</strong>{' '}
      {/* Body of policy 2 */}
    </li>
    <li>
      <strong>Възстановяване и защита на авторитета на учителя.</strong>{' '}
      {/* Body of policy 3 */}
    </li>
  </ol>

  {/* Sub-section 2: higher education
      Source has another "1." prefix (a per-page-reset pdftotext artifact) — STRIP from rendered text. */}
  <h3>{/* Verbatim sub-section name, e.g., "В сферата на висшето образование" */}</h3>

  <p>{/* Any intro paragraph */}</p>

  <ol>
    <li>
      <strong>{/* Verbatim policy 1 name from source */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Verbatim policy 2 name */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Verbatim policy 3 name */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Verbatim policy 4 name */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Verbatim policy 5 name */}.</strong>{' '}
      {/* Body */}
    </li>
  </ol>
</section>
```

**Critical authoring rules for this chapter:**

1. **The `<h3>` text is the sub-section NAME ONLY** — strip the source's "1." prefix. Example: source line " 1.   Нашата цел в областта на училищното образование…" → `<h3>Нашата цел в областта на училищното образование</h3>` (NOT `<h3>1. Нашата цел…</h3>`).

2. **Each `<ol>` re-numbers 1-N in HTML** — even though the source's school-ed list and the higher-ed list both visually start at "1." (the PDF presents two distinct numbered lists, and pdftotext renders both starting at "1." with no extraction artifact this time, but even if it did the rule is the same). Use real `<ol><li>` semantics.

3. **NEVER hand-code "1." / "2." / "3." prefixes as text inside `<li>` elements** — the browser renders the numerals via `prose`'s `<ol>` styling. The `<strong>{name}.</strong>` is JUST the policy name + period, not the list numeral.

4. **Use the JSX whitespace pattern `</strong>{' '}`** (with explicit JSX space) — match the Икономика precedent at live page.tsx lines 436, 489, 506, 542 verbatim.

5. **Multi-paragraph `<li>` bodies** — when an `<li>` contains a topic sentence followed by additional paragraph-length elaboration, wrap each subsequent paragraph in `<p>` tags inside the `<li>` per UI-SPEC §3.3.C. Match Икономика precedent lines 437-440.

**Step 1.4 — Extend the anchor-drift unit test.** Edit `tests/unit/agenda-toc-anchors.test.ts`:

(a) Update count assertion (currently `≥7` from plan 01, bump to `≥8`):
```ts
it('has at least 8 TOC entries (slice 2 — plan 02 partial)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(8);
});
```

(b) APPEND a new `it()` block AFTER the slice-2 plan-01 anchors block:
```ts
it('contains the slice-2 plan-02 anchor for Образование', () => {
  expect(tocIds).toContain('obrazovanie');
});
```

KEEP all existing blocks unchanged (slice-1 anchors, slice-2 plan-01 anchors, "every TOC id has a matching <h2 id>").

**Step 1.5 — Validate.**
```bash
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
pnpm typecheck
pnpm lint:i18n
```
All three MUST exit 0.

**DO NOT in this task:**
- Do NOT touch `<Alert>` or `messages/bg.json` or `generateMetadata` (cleanup is plan 03)
- Do NOT pre-author Социална политика or Култура (later tasks in this plan)
- Do NOT hand-code list numerals — `<ol>` is real semantics, not text
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'id="obrazovanie"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c "id: 'obrazovanie'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥8 (3 slice-1 + 4 plan-01 + 1 plan-02 task-1)
- Order check (insertion point correct): `grep -n 'id="' src/app/\(frontend\)/agenda/page.tsx | head -20` shows `id="obrazovanie"` AFTER `id="zemedelie"` AND BEFORE `id="zdraveopazvane"` (BOTH for the `<h2 id>` anchors and for the `TOC_ITEMS` entry)
- `grep -cE '<ol>' src/app/\(frontend\)/agenda/page.tsx` returns ≥4 (existing 2 from Икономика + 2 new from Образование)
- `grep -cE '<strong>[^<]+\.</strong>' src/app/\(frontend\)/agenda/page.tsx` returns ≥15 (slice-1 Икономика's `<strong>` count + at least 8 new from Образование: 3 school-ed + 5 higher-ed)
- No hand-coded numerals in `<li>` text: `grep -nE '<li>[ \t\n]*[0-9]+\.\s' src/app/\(frontend\)/agenda/page.tsx` returns nothing (no `<li>1. …` patterns)
- `<h3>` elements have no `id=` attribute: `grep -E '<h3[^>]*id=' src/app/\(frontend\)/agenda/page.tsx` returns nothing
- `<Alert>` block STILL present: `grep -c '<Alert' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `agenda.body` STILL present: `grep -c '"body":\s*"\[ТЕКСТ' messages/bg.json` returns 1
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; output shows ≥5 specs passing (count + every-id-matches + slice-1 + slice-2-plan-01 + slice-2-plan-02-obrazovanie)
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
  </acceptance_criteria>
  <done>
- `src/app/(frontend)/agenda/page.tsx` contains the Образование `<section>` INSERTED between Земеделие and Здравеопазване, with 2 `<h3>` sub-sections (no `id=`) and 2 `<ol>` blocks (3 + 5 items, all with `<strong>{name}.</strong>` prefix)
- `TOC_ITEMS` has 8 entries; `obrazovanie` sits between `zemedelie` and `zdraveopazvane`
- The anchor-drift unit test asserts `≥8` and the new `expect(tocIds).toContain('obrazovanie')`
- All three automated gates exit 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Социална политика chapter; INSERT it between Образование and Здравеопазване; bump anchor-drift test to ≥9</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (re-read AFTER Task 1 ship — find the Образование `</section>` closing tag and the Здравеопазване `<section>` opening tag for the insertion point)
- .planning/coalition/agenda-raw.txt lines 669-744 (Социална политика source content)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3.A (`<h3>` sub-sections), §3.3.B (flat `<ul>` Pattern #1)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-RESEARCH.md §"Source Content Map" TOC#7, §"Open Questions" #1 (assumption A3 documents the discretion call: render numbered sub-sections as `<h3>`, not as a single top-level `<ol>`)
  </read_first>
  <action>
**Step 2.1 — Insertion point.** Locate the Образование `</section>` (created in Task 1) and the Здравеопазване `<section>` opener. The Социална политика `<section>` block INSERTS between them.

**Step 2.2 — Insert TOC entry.** In `TOC_ITEMS`, insert between `{ id: 'obrazovanie', … }` and `{ id: 'zdraveopazvane', … }`:

```tsx
{ id: 'obrazovanie', label: 'Образование' },
{ id: 'sotsialna-politika', label: 'Социална политика' },     // ← INSERT
{ id: 'zdraveopazvane', label: 'Здравеопазване' },
```

**Step 2.3 — Author the Социална политика chapter.** Read `agenda-raw.txt:669-744` directly. Per RESEARCH §"Source Content Map" TOC#7 + assumption A3, the chapter has 5 named `<h3>` sub-sections (NOT a top-level `<ol>` — the source's "1.", "2.", "3.", "4.", "5." prefixes are sub-section markers, not list items, because each is followed by multiple paragraphs of body content). Compose:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 669-744 */}
  <h2 id="sotsialna-politika">Социална политика</h2>

  {/* Optional opening paragraph(s) — read source to check */}

  {/* Sub-section 1 — strip "1." prefix */}
  <h3>Връщане на личната отговорност</h3>
  <p>{/* Body paragraph 1 */}</p>
  <p>{/* Body paragraph 2 */}</p>
  <ul>
    {/* 4-item flat list of principles per RESEARCH Source Content Map */}
    <li>{/* principle 1 verbatim from source */}</li>
    <li>{/* principle 2 */}</li>
    <li>{/* principle 3 */}</li>
    <li>{/* principle 4 */}</li>
  </ul>

  <h3>Подкрепа за децата и семействата</h3>
  <p>{/* Body paragraph(s) — no list expected per RESEARCH */}</p>

  <h3>Политика на пазара на труда</h3>
  <p>{/* Body paragraph(s) */}</p>
  <ul>
    {/* 5-item flat list of measures */}
    <li>{/* measure 1 */}</li>
    <li>{/* measure 2 */}</li>
    <li>{/* measure 3 */}</li>
    <li>{/* measure 4 */}</li>
    <li>{/* measure 5 */}</li>
  </ul>

  <h3>Развитие на системата за дългосрочна грижа</h3>
  <p>{/* Body paragraph(s) — no list expected */}</p>

  <h3>Пенсионна система</h3>
  <p>{/* Body paragraph(s) */}</p>
  <ul>
    {/* 4-item flat list of goals */}
    <li>{/* goal 1 */}</li>
    <li>{/* goal 2 */}</li>
    <li>{/* goal 3 */}</li>
    <li>{/* goal 4 */}</li>
  </ul>
</section>
```

**Critical authoring rules:**

1. **5 `<h3>` sub-sections, NO top-level `<ol>`** — RESEARCH assumption A3 documents this. The source uses "1." through "5." as sub-section prefixes, but each is a multi-paragraph mini-chapter, not a one-line policy item. Render as `<h3>` per UI-SPEC §3.3.A.

2. **Strip "1.", "2.", "3.", "4.", "5." prefixes** from rendered `<h3>` text. The `<h3>` text is the descriptive name only.

3. **No `id=` on `<h3>`** per UI-SPEC §3.3.A.

4. **Flat `<ul>` lists** appear in 3 of 5 sub-sections (per RESEARCH Source Content Map TOC#7) — render with `prose` defaults; no `list-disc` overrides.

5. **The exact item counts (4 / 5 / 4) are estimates from RESEARCH** — count from source during authoring; if the source has 3 items in a list, render 3, not the planner's estimate. Source-fidelity wins.

**Step 2.4 — Extend the anchor-drift unit test.** Edit `tests/unit/agenda-toc-anchors.test.ts`:

(a) Update count assertion (bump from `≥8` to `≥9`):
```ts
it('has at least 9 TOC entries (slice 2 — plan 02 partial)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(9);
});
```

(b) The "slice-2 plan-02 anchor for Образование" block from Task 1 currently asserts `tocIds.toContain('obrazovanie')`. EXTEND it to a `expect.arrayContaining` block covering BOTH:
```ts
it('contains the slice-2 plan-02 anchors for Образование and Социална политика', () => {
  expect(tocIds).toEqual(
    expect.arrayContaining(['obrazovanie', 'sotsialna-politika']),
  );
});
```

(Replace the Task-1-version-of-this-block, not append a new one — keep the test file from accumulating redundant blocks.)

**Step 2.5 — Validate.**
```bash
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
pnpm typecheck
pnpm lint:i18n
```
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'id="sotsialna-politika"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c "id: 'sotsialna-politika'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥9
- Order check: `grep -nE '<h2 id="(obrazovanie|sotsialna-politika|zdraveopazvane)"' src/app/\(frontend\)/agenda/page.tsx` shows them in this order: `obrazovanie` < `sotsialna-politika` < `zdraveopazvane` (line numbers strictly increasing)
- `grep -E '<h3[^>]*id=' src/app/\(frontend\)/agenda/page.tsx` returns nothing (no `id=` on `<h3>`)
- `<h3>` count grew by ~5: total `<h3>` count ≥ 12 (Енергетика 2 + Здравеопазване ~5-6 + Образование 2 + Социална политика 5)
- No hand-coded "1. " / "2. " / "3. " / "4. " / "5. " prefix at the start of `<h3>` text in the Социална политика section: `grep -E '<h3>[ \t]*[0-9]+\.' src/app/\(frontend\)/agenda/page.tsx` returns nothing
- `<Alert>` block STILL present
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
  </acceptance_criteria>
  <done>
- `src/app/(frontend)/agenda/page.tsx` contains the Социална политика `<section>` inserted between Образование and Здравеопазване
- Social policy renders 5 `<h3>` sub-sections (no `id=`) with body paragraphs and 3 flat `<ul>` lists per RESEARCH Source Content Map
- `TOC_ITEMS` has 9 entries; `sotsialna-politika` is the 7th entry (index 6, 0-indexed)
- Anchor-drift unit test asserts `≥9` and `expect.arrayContaining(['obrazovanie', 'sotsialna-politika'])`
- All three automated gates exit 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Author Култура chapter; INSERT it between Външна сигурност и отбрана and end-of-article; bump anchor-drift test to ≥10</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <read_first>
- src/app/(frontend)/agenda/page.tsx (re-read AFTER Task 2 ship — find the Външна сигурност `</section>` closing tag; in plan 02 there is no Защита на ценностите yet — that lands in plan 03 — so Култура goes AFTER Външна сигурност and AT THE END of the article)
- .planning/coalition/agenda-raw.txt lines 1111-1242 (Култура source content)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-UI-SPEC.md §3.3.B exception (em-dash sub-bullets in Култура items 6-10 — strip the `–` from rendered text), §3.3.C (`<ol>` re-numbering), §3.3.D (`<strong>` prefix)
- .planning/phases/02.3-coalition-agenda-content-slice-2/02.3-PATTERNS.md §"Pattern Assignments" 1 — em-dash bullet stripping rule
- src/app/(frontend)/agenda/page.tsx lines 434-487 (Икономика nested-`<ul>`-inside-`<li>` precedent — verbatim shape to replicate)
  </read_first>
  <action>
**Step 3.1 — Insertion point.** In plan 02, Култура goes AFTER Външна сигурност и отбрана and BEFORE end-of-article (i.e., immediately before the closing `</article>` tag). Plan 03 will INSERT Защита на ценностите AFTER Култура (at end-of-article post-plan-03).

**Step 3.2 — Insert TOC entry.** In `TOC_ITEMS`, append AFTER `{ id: 'vanshna-sigurnost', … }`:

```tsx
{ id: 'vanshna-sigurnost', label: 'Външна сигурност и отбрана' },
{ id: 'kultura', label: 'Култура' },     // ← INSERT (last entry until plan 03 adds zashtita-tsennosti)
```

**Step 3.3 — Author the Култура chapter.** Read `agenda-raw.txt:1111-1242` directly. Per RESEARCH §"Source Content Map" TOC#11 + UI-SPEC §3.3.B exception, the chapter has 8 lead `<p>` paragraphs, then a thesis-statement `<p>` like "За целта предлагаме пакет от 10 основни мерки…", then a SINGLE `<ol>` with EXACTLY 10 `<li>` items, where items 6-10 each contain a nested `<ul>` of em-dash sub-bullets. Compose:

```tsx
<section>
  {/* Source: agenda-raw.txt lines 1111-1242 */}
  <h2 id="kultura">Култура</h2>

  {/* ~8 lead paragraphs — read source for exact count and content */}
  <p>{/* Lead paragraph 1 */}</p>
  <p>{/* Lead paragraph 2 */}</p>
  {/* …continue */}

  <p>{/* Verbatim from source — likely "За целта предлагаме пакет от 10 основни мерки…" */}</p>

  <ol>
    <li>
      <strong>{/* Verbatim measure 1 name from source */}.</strong>{' '}
      {/* Body of measure 1 */}
    </li>
    <li>
      <strong>{/* Measure 2 name */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Measure 3 name */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Measure 4 name */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Measure 5 name */}.</strong>{' '}
      {/* Body */}
    </li>
    <li>
      <strong>{/* Measure 6 name */}.</strong>{' '}
      {/* Body topic sentence (the policy framing) */}
      <ul>
        {/* Source has em-dash–prefixed sub-bullets like:
              –   Sub-policy text
            Strip the "–" character from the rendered <li> text. The <li> body starts AFTER the dash. UI-SPEC §3.3.B exception. */}
        <li>{/* Sub-bullet 1 (em-dash stripped) */}</li>
        <li>{/* Sub-bullet 2 */}</li>
        {/* …continue all sub-bullets in measure 6 from source */}
      </ul>
    </li>
    <li>
      <strong>{/* Measure 7 name */}.</strong>{' '}
      {/* Body */}
      <ul>
        <li>{/* Sub-bullet 1 (em-dash stripped) */}</li>
        {/* …continue */}
      </ul>
    </li>
    <li>
      <strong>{/* Measure 8 name */}.</strong>{' '}
      {/* Body */}
      <ul>
        <li>{/* Sub-bullets */}</li>
        {/* …continue */}
      </ul>
    </li>
    <li>
      <strong>{/* Measure 9 name */}.</strong>{' '}
      {/* Body */}
      <ul>
        <li>{/* Sub-bullets */}</li>
        {/* …continue */}
      </ul>
    </li>
    <li>
      <strong>{/* Measure 10 name */}.</strong>{' '}
      {/* Body */}
      <ul>
        <li>{/* Sub-bullets */}</li>
        {/* …continue */}
      </ul>
    </li>
  </ol>
</section>
```

**Critical authoring rules:**

1. **Exactly 10 `<li>` items in the top-level `<ol>`** — the source explicitly names "пакет от 10 основни мерки". Count carefully during authoring.

2. **Items 6-10 each contain a nested `<ul>`** per UI-SPEC §3.3.B exception. The source uses `–` (en-dash) as bullet markers; STRIP these from rendered text. The rendered text starts AFTER the dash. NEVER include `–` as the first character of an `<li>` text node.

3. **Items 1-5 do NOT have a nested `<ul>`** (the source presents them as plain prose-only policies). Confirm against source — if items 1-5 also have nested sub-bullets, render them too. The "items 6-10 only" rule comes from RESEARCH; verify against actual source.

4. **`<strong>{name}.</strong>{' '}` pattern** — use the JSX whitespace literal explicitly, matching the Икономика precedent.

5. **NO hand-coded "1." / "2." / "3." prefixes** — `<ol>` numerals are CSS-rendered.

6. **`<em>` not expected** — if the source has italics, render as `<em>` per UI-SPEC §3.3.D, but it's rare in pdftotext output.

**Step 3.4 — Extend the anchor-drift unit test.** Edit `tests/unit/agenda-toc-anchors.test.ts`:

(a) Update count assertion (bump from `≥9` to `≥10`):
```ts
it('has at least 10 TOC entries (slice 2 — plan 02 complete)', () => {
  expect(tocIds.length).toBeGreaterThanOrEqual(10);
});
```

(b) Replace the slice-2 plan-02 anchors block with the full three-anchor list:
```ts
it('contains the slice-2 plan-02 anchors (Образование, Социална политика, Култура)', () => {
  expect(tocIds).toEqual(
    expect.arrayContaining(['obrazovanie', 'sotsialna-politika', 'kultura']),
  );
});
```

**Step 3.5 — Validate.**
```bash
pnpm vitest run tests/unit/agenda-toc-anchors.test.ts
pnpm typecheck
pnpm lint:i18n
```
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/agenda-toc-anchors.test.ts && pnpm typecheck && pnpm lint:i18n</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'id="kultura"' src/app/\(frontend\)/agenda/page.tsx` returns ≥1
- `grep -c "id: 'kultura'" src/app/\(frontend\)/agenda/page.tsx` returns 1
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥10
- Order check: in `page.tsx`, `id="kultura"` appears AFTER `id="vanshna-sigurnost"` (Култура at end-of-article in plan-02 final state)
- Култура `<ol>` has exactly 10 items: parsing the Култура `<section>` block, `grep -c '<li>' <(awk '/id="kultura"/,/<\/section>/' src/app/\(frontend\)/agenda/page.tsx)` returns ≥10 top-level `<li>`s plus additional nested ones (use `awk` or read the file and count manually if grep is awkward)
- No em-dash bullet markers in rendered `<li>` text: `grep -nE '<li>\s*–' src/app/\(frontend\)/agenda/page.tsx` returns nothing (the `–` from source has been stripped from `<li>` text content)
- Items 6-10 of Култура each have a nested `<ul>` — within the Култура section, count nested `<ul>` elements: should be ≥5 nested `<ul>` blocks inside the chapter's top-level `<ol>` (verify by reading the section)
- `<strong>` count grew by ≥10 (10 measure-name prefixes): total `grep -cE '<strong>[^<]+\.</strong>' src/app/\(frontend\)/agenda/page.tsx` returns ≥25 (slice-1 + plan-02-task-1 + this task)
- `<Alert>` block STILL present
- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; output shows `≥10` count + the three-anchor `arrayContaining`
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
  </acceptance_criteria>
  <done>
- `src/app/(frontend)/agenda/page.tsx` contains the Култура `<section>` inserted after Външна сигурност и отбрана and at end-of-article (plan-03 will append Защита на ценностите after it)
- Култура renders ~8 lead `<p>` + 1 thesis `<p>` + a single `<ol>` with EXACTLY 10 `<li>` items, each prefixed with `<strong>{measure-name}.</strong>{' '}`
- Items 6-10 of the `<ol>` each contain a nested `<ul>` with em-dash bullets STRIPPED from rendered text (rendered as `prose`-default disc bullets)
- `TOC_ITEMS` has 10 entries; `kultura` is the 10th entry (last until plan 03 adds `zashtita-tsennosti`)
- Anchor-drift unit test asserts `≥10` and `expect.arrayContaining(['obrazovanie', 'sotsialna-politika', 'kultura'])`
- All three automated gates exit 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator visual verification of plan-02 chapters (10 of 12 chapters live)</name>
  <what-built>
The /agenda page now ships 10 of 12 final chapters. Plan 01 added Енергетика, Земеделие, Здравеопазване, Външна сигурност и отбрана. Plan 02 has now INSERTED three structurally complex chapters at the right source-order positions:

- **Образование** (between Земеделие and Здравеопазване): 2 `<h3>` sub-sections, 2 `<ol>` blocks (3 + 5 named policies) with `<strong>` prefixes
- **Социална политика** (between Образование and Здравеопазване): 5 `<h3>` sub-sections, 3 flat `<ul>` lists, no top-level `<ol>`
- **Култура** (after Външна сигурност и отбрана): 8 lead paragraphs + thesis statement + a single `<ol>` with 10 measures (each prefixed with `<strong>{name}.</strong>`); items 6-10 each contain a nested `<ul>` of em-dash sub-bullets (em-dashes stripped from rendered text)

The TOC has grown from 7 → 10 entries (sticky desktop sidebar + mobile `<details>`); entries are in source order. The anchor-drift unit test now asserts ≥10. The `<Alert>` draft banner is INTENTIONALLY still present (cleanup is plan 03). The `agenda.body` and `agenda.draftAlert` keys in `messages/bg.json` are INTENTIONALLY untouched (cleanup is plan 03).

Two chapters remain for plan 03: Правосъдие (will be inserted between Здравеопазване and Външна сигурност) and Защита на ценностите (will be appended after Култура).
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev` and navigate to `http://localhost:3000/agenda`.
2. **TOC count + order — desktop:** confirm the sticky sidebar shows exactly 10 entries in this order: Манифест → Десен консенсус → Икономика → Енергетика → Земеделие → **Образование** → **Социална политика** → Здравеопазване → Външна сигурност и отбрана → **Култура**. Three new entries in bold are this plan's deliverables.
3. **TOC count + order — mobile (iPhone SE 375×667):** the `<details>` block lists the same 10 entries in the same order.
4. **Anchor jumps:** click each new TOC link (Образование, Социална политика, Култура). Each MUST scroll smoothly to the matching `<h2>`; the heading MUST NOT be hidden under the sticky header.
5. **Образование chapter content:** scroll to `#obrazovanie`. Confirm 2-3 lead paragraphs, then `<h3>` "Нашата цел в областта на училищното образование" (or the verbatim source label without the "1." prefix), then a numbered list of 3 policies (rendered with browser-numbered "1.", "2.", "3." — NOT hand-coded), each item prefixed with bold policy name + period (e.g., "**Промяна на учебното съдържание.** Body…"). Then the second sub-section `<h3>` "В сферата на висшето образование" (verbatim source) followed by a numbered list of 5 policies, same `<strong>{name}.</strong>` shape.
6. **Образование `<ol>` re-numbering:** the second `<ol>` (higher-ed, 5 items) MUST start at "1." in the rendered HTML, NOT continue from "4." or "9." (despite both `<ol>` blocks being in the same `<section>`). Real `<ol>` semantics restart per-list.
7. **Социална политика chapter content:** scroll to `#sotsialna-politika`. Confirm 5 `<h3>` sub-sections in this order: Връщане на личната отговорност → Подкрепа за децата и семействата → Политика на пазара на труда → Развитие на системата за дългосрочна грижа → Пенсионна система. Each `<h3>` is followed by body paragraphs; sub-sections 1, 3, 5 each have a flat bulleted list (4 / 5 / 4 items per RESEARCH — count from source if doubtful).
8. **Социална политика — no `<ol>`:** confirm the chapter has NO numbered list. The source's "1.", "2.", "3.", "4.", "5." are sub-section markers (rendered as `<h3>`), not list items.
9. **Култура chapter content:** scroll to `#kultura`. Confirm ~8 lead paragraphs about culture, then a thesis sentence ending with "пакет от 10 основни мерки" (or similar verbatim phrasing from source), then a single ordered list with EXACTLY 10 items.
10. **Култура items 6-10 nested sub-bullets:** for items 6, 7, 8, 9, 10 of the `<ol>`, confirm each contains a nested bulleted list (`<ul>`). The bullets render with `prose` default discs. CRITICAL: the bullet text must NOT start with `–` (em-dash). The source's em-dash bullet markers have been stripped; only the policy text remains.
11. **Култура items 1-5:** confirm items 1-5 do NOT have nested bullet lists (they're plain prose-only policy items, per the source). If any of items 1-5 has a nested `<ul>` matching the source structure, that's also acceptable (source-fidelity wins).
12. **`<strong>` policy names render as inline-bold:** for both Образование `<ol>` blocks and the Култура `<ol>`, the policy name at the start of each `<li>` renders in bold weight, followed by a period and a space, then the body in normal weight. There is NO line break between the bold name and the body (it's an inline `<strong>`, not a heading).
13. **Sub-section `<h3>` styling:** all `<h3>` elements (in Енергетика, Здравеопазване, Образование, Социална политика) render in the prose default size (smaller than `<h2>`, larger than body). They use the prose-default font stack — NOT Gilroy ExtraBold (Gilroy is only for the page `<h1>`). Right-click → Inspect → confirm `<h3>` has NO `id=` attribute.
14. **`<Alert>` banner — STILL VISIBLE:** confirm the draft banner is still present above the TOC. Its removal is plan 03.
15. **Linter sanity:** `pnpm lint:i18n` exits 0 with `PUB-05 OK`.
16. **Test sanity (per Phase 02.2 D-4 — `pnpm vitest run`, NEVER `pnpm test --run`):** `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0; the count assertion shows `≥10`; the slice-2-plan-02-anchors `arrayContaining` passes.
17. **Order regression check:** view-source on `/agenda` and confirm the sequence of `<h2 id>` anchors top-to-bottom is exactly: manifest → desen-konsensus → ikonomika → energetika → zemedelie → obrazovanie → sotsialna-politika → zdraveopazvane → vanshna-sigurnost → kultura. (Plan 03 will INSERT pravosadie between zdraveopazvane and vanshna-sigurnost, then APPEND zashtita-tsennosti after kultura.)
  </how-to-verify>
  <resume-signal>Type "approved" if all 17 checks pass. If anything is off (especially: rendered `<li>` text starts with `–`; `<ol>` items show double-numerals like "1. 1. Promyana…"; `<h3>` rendered as `<h2>` or vice versa; inserted sub-section ordering wrong; em-dash bullets visible in Култура items 6-10), describe the issue and the executor will revise that chapter before re-requesting verification.</resume-signal>
  <acceptance_criteria>
- Operator response is "approved" (or substantively equivalent)
- If revision is requested, the resume signal includes the specific issue and the executor lands a follow-up commit before this task is marked done
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Phase 02.3 is content-only; no new auth, input, API, or trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-02-01 | Tampering / Information disclosure | Bulgarian content rendered in `<ol>`, `<ul>`, `<strong>` JSX literals | accept | All content is JSX literals authored at edit time. React's default escaping covers the `<strong>` policy-name strings and the em-dash sub-bullet text. NO `dangerouslySetInnerHTML`. NO user input. ASVS V5 not applicable. |
| T-02.3-02-02 | Information disclosure | Anchor URL fragments (`/agenda#kultura`, etc.) | accept | Native browser `:target` only; never echoed server-side; never used for navigation logic. Inherited from plan 01. |
| T-02.3-02-03 | Tampering | ISR cache for `/agenda` | accept | Cache key is the URL only; `revalidate = 3600`. Inherited posture. |

**Security delta for plan 02:** Zero. ASVS V2-V14 not applicable per RESEARCH §"Security Domain". This plan introduces no new attack surface; the `<ol>` numbering and `<strong>` rendering are pure presentational concerns with no security implication.
</threat_model>

<verification>
After all four tasks land:

- `pnpm vitest run tests/unit/agenda-toc-anchors.test.ts` exits 0
- `pnpm typecheck` exits 0
- `pnpm lint:i18n` exits 0 with `PUB-05 OK`
- `grep -c '<h2 id=' src/app/\(frontend\)/agenda/page.tsx` returns ≥10
- `TOC_ITEMS` contains 10 entries in source order: manifest, desen-konsensus, ikonomika, energetika, zemedelie, obrazovanie, sotsialna-politika, zdraveopazvane, vanshna-sigurnost, kultura
- Култура `<ol>` has exactly 10 `<li>` items at the top level; items 6-10 each contain a nested `<ul>`; no rendered `<li>` text starts with `–`
- `<Alert>` block, `Alert` / `AlertDescription` imports, `agenda.body` / `agenda.draftAlert` keys in `bg.json`: all UNCHANGED
- Operator visual walkthrough signed off (Task 4 resume-signal = "approved")
</verification>

<success_criteria>
Plan 02 of Phase 02.3 is complete when:

1. The structurally complex chapters (Образование, Социална политика, Култура) are rendered with the correct content shapes per UI-SPEC §3.3 — no double-numerals, no ghost em-dashes, no mis-ordered list markers, no `<h3>` masquerading as `<h2>` or vice versa
2. Each `<ol>` block re-numbers 1-N via real semantics; `<strong>{name}.</strong>` policy-name prefix uses the JSX whitespace literal `{' '}` matching the Икономика precedent
3. Култура items 6-10 nested `<ul>` blocks correctly strip the source's em-dash bullet glyph
4. The TOC reflows from 7 → 10 entries in source order; the anchor-drift unit test asserts the new state
5. The `<Alert>` draft banner is still present; final-slice cleanup is reserved for plan 03
</success_criteria>

<output>
After all four tasks pass, create `.planning/phases/02.3-coalition-agenda-content-slice-2/02.3-02-SUMMARY.md` documenting:

- Counts: chapters added (3), `<h2 id>` anchors (3 new, 10 total), `<h3>` sub-sections added (the actual count from authoring; expected ≥7 = 2 in Образование + 5 in Социална политика), `<ol>` blocks added (expected: 3 = 2 in Образование + 1 in Култура), top-level `<li>` items in Култура `<ol>` (expected: exactly 10), nested `<ul>` blocks in Култура items 6-10 (expected: 5 — one per item), `<strong>` policy-name prefix occurrences added (expected: ≥18 = 3 + 5 + 10)
- Source-content ambiguities encountered (e.g., did Социална политика's 5th sub-section have a flat `<ul>` of 4 or 5 items in source? Did Култура items 1-5 have any nested sub-bullets? Document the exact source-derived counts for plan-03 alignment)
- Final `page.tsx` line count and TOC_ITEMS length
- Confirmation that `<Alert>`, `agenda.body`, `agenda.draftAlert`, and `generateMetadata` are intentionally untouched
- Handoff note for plan 03: Правосъдие insertion point (between Здравеопазване and Външна сигурност), Защита на ценностите append point (after Култура at end-of-article); cleanup tasks (Alert removal, bg.json edits, generateMetadata swap, optional E2E spec, optional i18n-cleanup unit test)
</output>
</content>
</invoke>