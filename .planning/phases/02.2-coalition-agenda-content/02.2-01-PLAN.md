---
phase: 02.2-coalition-agenda-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lint-i18n.mjs
  - src/app/(frontend)/agenda/page.tsx
  - tests/unit/agenda-toc-anchors.test.ts
autonomous: false
requirements:
  - D-CoalitionContent-Agenda
mode: mvp
slice: walking-skeleton
must_haves:
  truths:
    - "A visitor opening /agenda sees the coalition manifesto (НИЕ СМЕ СИНЯ БЪЛГАРИЯ! header + the 13 'Обръщаме се…' paragraphs + 'Синя България предлага:' lead-in)"
    - "Below the manifesto, the visitor sees the first top chapter 'ДЕСЕН КОНСЕНСУС ЗА УПРАВЛЕНИЕ НА ДЪРЖАВАТА' with its 5 framing paragraphs and the 6-item bulleted list of state responsibilities"
    - "Below that, the visitor sees the 'Икономика' detailed sub-chapter with paragraphs, the 6-item state-responsibility list, the 9 numbered economic-policy points, and the 6 numbered fiscal-policy points (plus their nested concession bullets)"
    - "A Table of Contents (sticky on md+, collapsible <details> on mobile) lists three sections — 'Манифест', 'Десен консенсус', 'Икономика' — and clicking any item jumps to its section via in-page anchor"
    - "The draft-mode <Alert> banner ('Програмата е в процес на финализиране.') remains visible above the TOC — this slice does NOT remove it"
    - "`pnpm lint:i18n` passes — no PUB-05 violation is introduced by the hardcoded Bulgarian JSX"
    - "A unit test asserts that every TOC_ITEMS id has a matching <h2 id=\"…\"> in the agenda page source, preventing TOC-anchor drift"
  artifacts:
    - path: "src/app/(frontend)/agenda/page.tsx"
      provides: "Hardcoded JSX for manifesto + 'Десен консенсус' + 'Икономика' chapters; populated TOC_ITEMS array with three entries"
      contains: "#manifest"
    - path: "scripts/lint-i18n.mjs"
      provides: "EXEMPT_FILES entry for src/app/(frontend)/agenda/page.tsx — file-level i18n linter exemption"
      contains: "agenda/page.tsx"
    - path: "tests/unit/agenda-toc-anchors.test.ts"
      provides: "Anchor-drift guard: parses page.tsx source and asserts TOC ids ⊆ rendered <h2 id> values"
      contains: "TOC_ITEMS"
  key_links:
    - from: "TOC_ITEMS in src/app/(frontend)/agenda/page.tsx"
      to: "<h2 id=…> elements in the same file"
      via: "shared id strings (#manifest, #desen-konsensus, #ikonomika)"
      pattern: "id=\"manifest\"|id=\"desen-konsensus\"|id=\"ikonomika\""
    - from: "src/app/(frontend)/agenda/page.tsx"
      to: "scripts/lint-i18n.mjs EXEMPT_FILES"
      via: "absolute-path entry must match path.resolve from lint-i18n's perspective"
      pattern: "src/app/\\(frontend\\)/agenda/page.tsx"
---

<objective>
Ship slice 1 of the coalition agenda content (walking-skeleton MVP slice) into `/agenda`. Replace the `<p>{t('body')}</p>` placeholder with hardcoded Bulgarian JSX containing the manifesto/intro, the first top chapter ('ДЕСЕН КОНСЕНСУС ЗА УПРАВЛЕНИЕ НА ДЪРЖАВАТА'), and the first detailed sub-chapter ('Икономика'). Populate `TOC_ITEMS` with three anchor entries. Exempt the agenda page from the PUB-05 i18n linter (file-level allowlist — chosen over per-line pragmas because slice 2 will add ~10× more Bulgarian JSX and per-line `// i18n-allow:` comments would dwarf the content). Keep the `draftAlert` banner — its removal belongs to Phase 02.3 (final slice).

Purpose: Prove the JSX-with-prose-and-anchors render approach works end-to-end on real coalition content (~210 lines of source), establishing the architectural contract that Phase 02.3 will inherit when it ships the remaining 9-10 chapters. After this slice, a visitor lands on /agenda and can read the coalition's mission framing and first concrete policy chapter — enough to decide whether to register.

Output: Updated `agenda/page.tsx` (hardcoded JSX + populated TOC_ITEMS), one-line addition to `lint-i18n.mjs` allowlist, anchor-drift unit test, manual operator visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/coalition/agenda-raw.txt
@.planning/phases/02-public-surface-pre-warmup/02-UI-SPEC.md
@src/app/(frontend)/agenda/page.tsx
@src/components/landing/TableOfContents.tsx
@src/components/landing/SectionEyebrow.tsx
@scripts/lint-i18n.mjs
@.planning/phases/02.2-coalition-agenda-content/SKELETON.md

<interfaces>
<!-- Existing scaffold contracts the executor must preserve. -->

From src/app/(frontend)/agenda/page.tsx (current shape — to be modified):
```tsx
export const revalidate = 3600;
export async function generateMetadata(): Promise<Metadata> { /* unchanged */ }
const TOC_ITEMS: { id: string; label: string }[] = []; // currently empty
export default async function AgendaPage() {
  // <MainContainer width="prose">
  //   <SectionEyebrow>{t('leadEyebrow')}</SectionEyebrow>
  //   <h1>{t('title')}</h1>
  //   <Alert>{t('draftAlert')}</Alert>            // KEEP for slice 1
  //   {TOC_ITEMS.length > 0 && <TableOfContents items={TOC_ITEMS} label={…} />}
  //   <article className="prose prose-slate prose-lg max-w-none">
  //     <p>{t('body')}</p>                         // REPLACE with hardcoded JSX
  //   </article>
  // </MainContainer>
}
```

From src/components/landing/TableOfContents.tsx:
```tsx
export function TableOfContents({
  items,
  label,
}: { items: { id: string; label: string }[]; label: string }): JSX.Element;
// Renders sticky sidebar on md+, collapsible <details> on mobile.
// Already wraps in IntersectionObserver for active-section highlighting.
// Component is unchanged in this slice.
```

From src/components/landing/SectionEyebrow.tsx:
```tsx
export function SectionEyebrow({ children }: { children: ReactNode }): JSX.Element;
// Tiny caps eyebrow above section h2s — already used at the top of /agenda.
// Component is unchanged in this slice.
```

From scripts/lint-i18n.mjs (current — to be extended):
```js
const EXEMPT_FILES = [
  path.resolve('src/lib/oblast-names.ts'),
];
// ADD: path.resolve('src/app/(frontend)/agenda/page.tsx')
```

UI-SPEC §5.3 anchor-id rules: each h2 has a stable id; TOC links use `href="#<id>"` with smooth scrolling. Header height ~80px → `scroll-padding-top: 5rem` already configured globally (see UI-SPEC review_flag).
</interfaces>

# Slice scope (read this carefully)

**IN scope** — agenda-raw.txt lines:
- 1–53: Manifesto/intro
- 55–103: ДЕСЕН КОНСЕНСУС ЗА УПРАВЛЕНИЕ НА ДЪРЖАВАТА
- 108–318: Икономика (ends at line 318; line 319 is the first line of the next chapter)

**OUT of scope** — DO NOT include:
- Anything from line 319 onward ("Енергетика, ресурси и околна среда" and beyond)
- Removal of the `<Alert>` draftAlert (deferred to Phase 02.3 final slice)
- Removal of the `agenda.body` key in `messages/bg.json` (deferred to Phase 02.3 — harmless because nothing reads it after this slice)
- Any new components, dependencies, or schema changes

**TOC contract (locked):** three entries, in this order, with these exact ids and labels:
1. `id: 'manifest'`, `label: 'Манифест'`
2. `id: 'desen-konsensus'`, `label: 'Десен консенсус'`
3. `id: 'ikonomika'`, `label: 'Икономика'`

Phase 02.3 will append additional entries; do not pre-allocate slots for them in this slice.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Exempt /agenda from PUB-05 linter, then convert manifesto + first top chapter</name>
  <files>scripts/lint-i18n.mjs, src/app/(frontend)/agenda/page.tsx</files>
  <action>
**Step 1.1 — Linter allowlist** (do this FIRST so lint:i18n stays green during the conversion edits).

Edit `scripts/lint-i18n.mjs`. In the `EXEMPT_FILES` array (currently line 22-24), add a second entry:

```js
const EXEMPT_FILES = [
  path.resolve('src/lib/oblast-names.ts'),
  path.resolve('src/app/(frontend)/agenda/page.tsx'),
];
```

Update the inline help text at the bottom of the file (around line 71) to mention the new exemption:
```js
console.error('  - src/lib/oblast-names.ts          (proper-noun lookup)');
console.error('  - src/app/(frontend)/agenda/page.tsx (coalition agenda content)');
```

Rationale to embed in a leading comment near `EXEMPT_FILES` (one line is fine): `// /agenda content is hardcoded JSX from the coalition PDF; per-line pragmas would dwarf the content. File-level exemption.`

Run `pnpm lint:i18n` after the edit — it must still print `PUB-05 OK`.

**Step 1.2 — Source the manifesto JSX** from `agenda-raw.txt` lines 6-53. Read `.planning/coalition/agenda-raw.txt` lines 6-53 directly; the source has been pdftotext-extracted with over-eager line wrapping where a single paragraph spans multiple lines. **De-wrap paragraphs** when converting to JSX: a logical paragraph in the JSX = one `<p>` element containing the joined text of consecutive non-blank source lines (blank lines = paragraph breaks). Preserve Bulgarian punctuation, em-dashes (`–`), curly quotes (`„…"`) exactly as in source.

**Step 1.3 — Source the first top chapter** from lines 55-103. The chapter has:
- Line 55: H2 heading "ДЕСЕН КОНСЕНСУС ЗА УПРАВЛЕНИЕ НА ДЪРЖАВАТА"
- Lines 56-73: 4 framing paragraphs (the "Синя България е за…" block ending with the colon-introducer at line 73)
- Lines 75-80: a 6-item bulleted list (държавна отбрана / благоустройство / образование+здравеопазване / нуждаещи се / правораздаване / околна среда). Render as `<ul>` with `<li>` children.
- Lines 83-99: 3 paragraphs (the "Ние не предлагаме намаляване…", "Чрез драстичното намаляване…", and "Около тези политики…" sequence — note that the third one starts at line 101, the second one ends at line 99; lines 100 and 104-107 are blank).
- Lines 101-103: closing paragraph about forming a right-wing parliamentary majority.

**Step 1.4 — Replace the placeholder body in the JSX**. In `src/app/(frontend)/agenda/page.tsx`:

(a) Replace the empty `TOC_ITEMS` const with the locked three-entry array:
```tsx
const TOC_ITEMS: { id: string; label: string }[] = [
  { id: 'manifest', label: 'Манифест' },
  { id: 'desen-konsensus', label: 'Десен консенсус' },
  { id: 'ikonomika', label: 'Икономика' },
];
```

(b) Inside `<article className="prose prose-slate prose-lg max-w-none">`, replace `<p>{t('body')}</p>` with two `<section>` blocks:
- First section: `<section><h2 id="manifest">НИЕ СМЕ СИНЯ БЪЛГАРИЯ!</h2>` + the 13 "Обръщаме се..." paragraphs + the "Към тези, които искат..." couplet (lines 49-51) + final lead-in `<p>Синя България предлага:</p>`. Each paragraph is its own `<p>` element. Use the natural Tailwind `prose` typography for spacing — do NOT add manual margin classes per paragraph.
- Second section: `<section><h2 id="desen-konsensus">ДЕСЕН КОНСЕНСУС ЗА УПРАВЛЕНИЕ НА ДЪРЖАВАТА</h2>` + the framing paragraphs, the 6-item `<ul>`, and the closing paragraphs (per Step 1.3).

(c) Leave the `Икономика` chapter for Task 2 (do not begin it here). Add a JSX comment marker `{/* slice 1 of 2 — 'Икономика' chapter follows in next task; chapters from Енергетика onward are Phase 02.3 */}` after the second section so the structure is obvious to the next task and to Phase 02.3.

(d) Do NOT touch the `<Alert>` block — the draftAlert MUST remain. Do NOT remove or rename `t('body')` from `messages/bg.json` — that key stays untouched (harmless; nothing reads it after this slice).

**Pitfalls (don't):**
- Don't introduce `dangerouslySetInnerHTML`. All content is plain JSX text nodes.
- Don't add `t('agenda.manifest.heading')`-style i18n keys "for future slice 2". The decision is hardcoded JSX (locked above).
- Don't change `MainContainer width="prose"` — that 768px width is the UI-SPEC §5.3 lock.
- Don't remove `tA11y('primaryNavLabel')` — the TOC `label` prop is required.
- Don't pre-render the third h2 (`#ikonomika`) without its body — TOC entries must point to sections that actually contain content. Either both the entry AND its section are in this task, or neither. Locked: TOC_ITEMS gets all 3 entries here, but the third <section> is added in Task 2 — that's the only acceptable mid-task asymmetry. The anchor-drift unit test in Task 2 will catch the gap until then; that's fine because Task 1 ends before lint:e2e runs.
  - Actually, to keep CI green at the Task 1 boundary: include the third `<section id="ikonomika">` as a stub `<section><h2 id="ikonomika">Икономика</h2><p>{/* populated in next task */}</p></section>` in Task 1, and replace its body in Task 2. This way the anchor-drift test passes throughout. Use this stub approach.
  </action>
  <verify>
    <automated>pnpm lint:i18n && pnpm tsc --noEmit</automated>
  </verify>
  <done>
- `scripts/lint-i18n.mjs` has a second `EXEMPT_FILES` entry for `src/app/(frontend)/agenda/page.tsx`
- `pnpm lint:i18n` exits 0 with "PUB-05 OK"
- `src/app/(frontend)/agenda/page.tsx` contains: three `TOC_ITEMS` entries; an `<h2 id="manifest">` section with the manifesto content; an `<h2 id="desen-konsensus">` section with the first top chapter; a stub `<h2 id="ikonomika">` section to be populated in Task 2
- The `<Alert>` draftAlert is unchanged and still renders above the TOC
- `pnpm tsc --noEmit` passes (no type errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert 'Икономика' chapter + add TOC anchor-drift unit test</name>
  <files>src/app/(frontend)/agenda/page.tsx, tests/unit/agenda-toc-anchors.test.ts</files>
  <action>
**Step 2.1 — Source the Икономика chapter** from `agenda-raw.txt` lines 108-318. Re-read those lines directly. The chapter structure (de-wrap paragraphs as in Task 1):

- Lines 110-116: Opening paragraph (одна логическая абзацная группа — the source visually splits it at line breaks but the grammar runs through). One `<p>` element.
- Lines 118-120: "Синя България е за засилване..." paragraph. One `<p>`.
- Lines 122-127: 6-item bulleted list (same shape as the equivalent list in chapter 1, lines 75-80) — render as `<ul><li>` × 6.
- Lines 129-137: "Ние не предлагаме намаляване..." extended paragraph. One `<p>`. Note that the source includes an additional sentence vs. the equivalent paragraph in chapter 1 ("Опитите за ценови контрол…") — preserve it.
- Lines 139-147: "Чрез драстичното намаляване..." paragraph. One `<p>`.
- Lines 149-189: a numbered ordered list. The source uses `<ol>` semantics implicit in the "1. … 2. … 3. …" prefixing. Render as `<ol>` with 9 `<li>` children:
  1. (line 149) "Извеждане на държавата от ролята на търговец" — single-sentence item
  2. (lines 150-160) "Привличане на стратегически инвеститори..." — multi-sentence item
  3. (lines 161-169) "Предлагане на значителни части от капитала от държавни фирми на борсата..." — multi-sentence
  4. (lines 170-177) "Затваряне на паразитни държавни търговски структури..." — multi-sentence
  5. (lines 178-185) "Ограничаването на директния контрол на държавата..." — multi-sentence
  6. (lines 186-187) "Изваждане на държавата от ролята на крупен инвеститор..." — short
  7. (lines 188-189) "Прекратяване на настоящия модел на финансиране..." — short
  8. (line 190) "Замяната на тази порочна политика..." — short
  9. (line 192) "Ликвидиране на основния източник за голямата политическа корупция." — short
  Each `<li>` contains one or more inline `<p>` elements (or just plain text + `<br/>` if Tailwind prose handles it; prefer the simpler text-only approach where possible, since `prose` styles `<li>` content well without needing nested paragraphs unless the item has multiple paragraphs).
- Lines 193-209: "Изграждането на модерна инфраструктура от частни инвеститори..." paragraph (the source places this OUTSIDE the ordered list as a standalone paragraph). One `<p>` after the closing `</ol>`.
- Lines 211-212: "Спирането на милиардите нагласени обществени поръчки..." paragraph. One `<p>`.
- Lines 214-318: a SECOND numbered ordered list with 6 sub-policy items (Бюджетна политика / Дългове и дефицити / Данъчна политика / Икономически растеж и растеж на доходите / Развитие на капиталовия пазар / Осигуряване на необходимите човешки ресурси). The source numbers them inconsistently (it restarts at "1." several times because it's a pdftotext artifact of multi-page lists) — **re-number them 1 through 6 in the rendered HTML**. Each item has body text from its associated lines. Where the source has a heading-like prefix (e.g., "Бюджетна политика", "Дългове и дефицити", "Данъчна политика", "Икономически растеж и растеж на доходите", "Развитие на капиталовия пазар", "Осигуряване на необходимите човешки ресурси"), render it as bold inline text at the start of the `<li>` (or as a separate sub-heading if you prefer — your call, but stay consistent across all 6 items). Inside item 4 ("Привличане на чуждестранни инвестиции"), the source has a nested 4-item bulleted sub-list (lines 261-273) about concession procedures, asset sales, fiscal predictability, and EU integration — render as a nested `<ul>` inside that `<li>`. Inside the same item there are also 5 numbered sub-points (lines 248-318) but on inspection these are not nested inside item 4 — they're parallel sub-policies of the chapter. Group them all under the same `<ol>` so item 4 IS "Икономически растеж и растеж на доходите" and the 5 nested sub-bullets become a `<ul>` inside it. (If structure is genuinely ambiguous mid-conversion, default to: top-level `<ol>` numbered 1-6, nested bullets as `<ul>` only where the source uses them — preserve the source's hierarchy as best as possible without inventing structure.)

**Step 2.2 — Replace the stub** in `src/app/(frontend)/agenda/page.tsx`. The `<section id="ikonomika">` stub created in Task 1 receives its full body now. Keep `<h2 id="ikonomika">Икономика</h2>` at the top.

**Step 2.3 — Anchor-drift unit test**. Create `tests/unit/agenda-toc-anchors.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';

/**
 * Guards against TOC drift on /agenda: every TOC_ITEMS id must have a
 * matching <h2 id="..."> in page.tsx. Slice 1 ships 3 anchors; Phase 02.3
 * will extend this set. This test parses the source string with regex
 * (no JSX runtime needed) — it's a static-source assertion.
 */
describe('/agenda TOC anchors', () => {
  const source = readFileSync(
    resolve(__dirname, '../../src/app/(frontend)/agenda/page.tsx'),
    'utf8',
  );

  // Extract TOC ids from the TOC_ITEMS array literal
  const tocBlock = source.match(/const TOC_ITEMS[\s\S]*?\];/)?.[0] ?? '';
  const tocIds = [...tocBlock.matchAll(/id:\s*['"]([^'"]+)['"]/g)].map(
    (m) => m[1],
  );

  // Extract h2 ids from JSX (id="..." on h2 elements)
  const h2Ids = [...source.matchAll(/<h2\s+id=["']([^"']+)["']/g)].map(
    (m) => m[1],
  );

  it('has at least 3 TOC entries (slice 1)', () => {
    expect(tocIds.length).toBeGreaterThanOrEqual(3);
  });

  it('every TOC id has a matching <h2 id="…">', () => {
    for (const id of tocIds) {
      expect(h2Ids).toContain(id);
    }
  });

  it('contains the locked slice-1 anchors', () => {
    expect(tocIds).toEqual(
      expect.arrayContaining(['manifest', 'desen-konsensus', 'ikonomika']),
    );
  });
});
```

The test framework is Vitest (already in the repo per Phase 1.02). The test is intentionally regex-based against source text — no JSX rendering needed and no DOM. Fast, deterministic, and resilient to JSX whitespace.

**Pitfalls (don't):**
- Don't add a 4th TOC entry "for slice 2 readiness". Slice 2 is a different phase.
- Don't normalize the source's punctuation. Em-dashes, curly quotes, non-breaking spaces stay verbatim.
- Don't render the `<ol>` items with hand-coded `1.` `2.` prefixes — let the browser numbering happen via real `<ol>` semantics. Tailwind `prose` styles `<ol>` correctly.
- Don't introduce a route file or MDX. Pure JSX in `page.tsx`.
- Don't shorten paragraphs or summarize. The whole point is faithful conversion.
- Don't include source line numbers as JSX comments — they'll go stale and add noise. (One JSX comment block at the top of the `<section id="ikonomika">` saying `{/* Source: agenda-raw.txt lines 108-318 */}` is acceptable for traceability, but no per-paragraph annotations.)
  </action>
  <verify>
    <automated>pnpm lint:i18n && pnpm tsc --noEmit && pnpm test --run tests/unit/agenda-toc-anchors.test.ts</automated>
  </verify>
  <done>
- `src/app/(frontend)/agenda/page.tsx` `<section id="ikonomika">` contains the full Икономика chapter: opening paragraphs, 6-item state-responsibility `<ul>`, two extended paragraphs, the 9-item `<ol>` of state-economic-role policies, the infrastructure paragraph, the `<ol>` of 6 fiscal/economic-growth sub-policies (with the nested `<ul>` inside item 4)
- `tests/unit/agenda-toc-anchors.test.ts` exists and passes (3 specs, all green)
- `pnpm lint:i18n` still passes (PUB-05 OK)
- `pnpm tsc --noEmit` passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual verification of /agenda slice 1</name>
  <what-built>
The /agenda page now renders the coalition manifesto + 'Десен консенсус' top chapter + 'Икономика' detailed chapter, with a populated 3-entry Table of Contents (sticky on desktop, collapsible on mobile) and the draftAlert banner still in place above the TOC. The PUB-05 i18n linter is exempted for this single file. An anchor-drift unit test prevents TOC ids from diverging from the rendered h2 ids.

This is content-shipping work; the right verification gate is a human eyeball walkthrough of the page, not a Playwright spec. (Playwright coverage of /agenda's visual completeness belongs to Phase 02.3 once the full agenda is shipped — slice 1 doesn't justify an e2e because the page is intentionally incomplete.)
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev` and navigate to `http://localhost:3000/agenda`.
2. **Eyebrow + heading:** confirm the eyebrow caps text "Какво защитаваме" appears above the page h1 "Програма".
3. **Draft banner:** confirm the gray Alert box "Програмата е в процес на финализиране." is still visible directly under the h1 (it MUST persist — its removal is Phase 02.3's job).
4. **Table of Contents — desktop (≥768px):** scroll up to the top. The TOC is rendered as a sticky sidebar on md+ (it sits in the left/right margin per UI-SPEC §5.3). Confirm three entries — "Манифест", "Десен консенсус", "Икономика" — and that each is a clickable link.
5. **Table of Contents — mobile (≤640px):** open Chrome DevTools → device toolbar → iPhone SE preset. Confirm the TOC has collapsed to a `<details>` summary block above the article body. Click to expand; confirm the same three links appear.
6. **Anchor jumps:** click each TOC link in turn. The page must scroll smoothly to the corresponding h2; the h2 must NOT be hidden under the sticky header (scroll-padding-top: 5rem should keep it visible). Verify on both desktop and mobile.
7. **Active-section highlighting (desktop only):** scroll the page slowly. As each h2 enters the upper third of the viewport, the TOC entry for that section should become bold/primary-colored (per the IntersectionObserver in TableOfContents.tsx). This is a soft check — minor flicker at section boundaries is acceptable.
8. **Manifesto content:** the first section (under "НИЕ СМЕ СИНЯ БЪЛГАРИЯ!") contains the 13 "Обръщаме се..." paragraphs + the "Към тези, които искат..." couplet + the lead-in "Синя България предлага:" line. Spot-check three paragraphs against `.planning/coalition/agenda-raw.txt` lines 6-53 — the wording must match verbatim.
9. **'Десен консенсус' chapter:** the second h2 leads into 4 framing paragraphs, then a 6-item bulleted list (отбрана / благоустройство / образование+здравеопазване / нуждаещи се / правораздаване / околна среда), then 3 closing paragraphs ending with the right-wing-majority sentence.
10. **'Икономика' chapter:** the third h2 leads into the opening framing paragraphs, the 6-item state-responsibility bullet list, the two extended paragraphs ("Ние не предлагаме намаляване..." and "Чрез драстичното намаляване..."), the 9-item numbered ordered list of economic-role policies (item 2 about strategic investors should be a long multi-sentence item; items 6-9 should be short), the infrastructure paragraph, and the second numbered ordered list of 6 fiscal/economic-growth sub-policies (with 4-item nested bullets inside item 4 about чуждестранни инвестиции).
11. **Typography:** the article uses Tailwind `prose prose-slate prose-lg`. Confirm the body font is readable, paragraphs have natural spacing, ordered/unordered list markers render correctly, and h2s have appropriate weight + color (Sinya primary).
12. **No content from line 319+:** scroll to the bottom of the page. The last visible section must be the "Икономика" chapter (ending around the human-resources subsection about Северна Македония / Сърбия / Молдова / Украйна). There MUST NOT be any "Енергетика, ресурси и околна среда" content — that's Phase 02.3.
13. **Linter sanity:** run `pnpm lint:i18n` from the repo root. Must print "PUB-05 OK".
14. **Test sanity:** run `pnpm test --run tests/unit/agenda-toc-anchors.test.ts`. All 3 specs green.
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 checks pass. If anything is off, describe the issue (paragraph mismatch, missing list, broken anchor, etc.) and the executor will revise that section before re-requesting verification.</resume-signal>
</task>

</tasks>

<verification>
After all three tasks land:

- `pnpm lint:i18n` exits 0
- `pnpm tsc --noEmit` exits 0
- `pnpm test --run tests/unit/agenda-toc-anchors.test.ts` passes (3/3 specs)
- Operator has signed off on the manual walkthrough (Task 3 resume-signal = "approved")
- `/agenda` renders the manifesto + first top chapter + Икономика chapter with a 3-entry TOC and the persistent draftAlert
- No content from agenda-raw.txt line 319 onward appears on the page
</verification>

<success_criteria>
The phase 02.2 walking-skeleton slice is complete when:

1. A visitor opening `/agenda` can read the coalition's manifesto, the framing of its right-wing-consensus governing thesis, and one detailed policy chapter (Икономика) — enough content to make an informed decision about whether to register.
2. The TOC works on desktop and mobile and all three anchors jump to the correct section.
3. The architectural contract for Phase 02.3 is established: hardcoded JSX in `page.tsx`, file-level i18n linter exemption, anchor-drift test guarding TOC integrity. Phase 02.3 will extend this same shape for the remaining 9-10 chapters.
4. The draftAlert banner is intentionally still present (slice 2 / final slice removes it).
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-coalition-agenda-content/02.2-01-SUMMARY.md` documenting:

- Number of paragraphs / list items / ordered-list items rendered (for slice 2 sizing reference)
- Any source-content ambiguities encountered (places where pdftotext line wrapping was hard to disambiguate) and how they were resolved
- Any deviations from the planned 3-entry TOC (e.g., if you found you needed sub-section anchors inside Икономика — none expected, but document if you did)
- Confirmation that the draftAlert and `agenda.body` i18n key are intentionally untouched (handoff to Phase 02.3)
</output>
