---
phase: 1
plan: 04
title: "i18n + Tailwind v4 theme + shadcn/ui registry + messages/bg.json + Payload Users collection"
wave: 0
depends_on: [01, 03]
files_modified:
  - package.json
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - next.config.ts
  - src/styles/globals.css
  - src/app/(frontend)/layout.tsx
  - components.json
  - src/components/ui/.gitkeep
  - src/lib/utils.ts
  - messages/bg.json
  - src/collections/Users.ts
  - src/payload.config.ts
  - public/logo-placeholder.svg
autonomous: true
requirements: [BRAND-01, BRAND-06, PUB-05]
estimated_duration: "L"
---

## Objective

Lock the visual + i18n conventions every other plan inherits: next-intl v4 single-locale (`bg`) wiring, Tailwind v4 CSS-first `@theme` block with the Sinya Bulgaria color palette + 4-size/2-weight typography (BRAND-01, BRAND-06), shadcn/ui registry initialization with the locked component set from D-26 + UI-SPEC, the complete `messages/bg.json` enumerating every key from UI-SPEC § Copywriting Contract, and the Payload `Users` collection (CMS editorial users — distinct from auth users) with its first-user gate (D-25). Payload runs its own migrations at the end of this plan (`pnpm payload migrate`) — the second `[BLOCKING]` schema push.

## Truths

- D-26: locked shadcn component set = button, input, label, alert, card, input-otp, checkbox, select, form (9 components, no more).
- D-27: ALL strings via next-intl `t()`. No hardcoded Cyrillic anywhere in `src/`.
- BRAND-01 (UI-SPEC § Color): palette = `#FFFFFF` background, `#F1F5F9` surface, `#004A79` accent, `#E72E4D` destructive, `#009F54` success, `#00B7ED` sky, `#0F172A` foreground, `#475569` muted-foreground, `#E2E8F0` border.
- BRAND-06 (UI-SPEC § Typography): Roboto + Roboto Slab with `cyrillic`, `cyrillic-ext`, `latin` subsets. 4 sizes (14/16/20/28), 2 weights (400/600).
- RESEARCH § next-intl v4 (lines 841-880): `defineRouting({ locales: ['bg'], defaultLocale: 'bg', localePrefix: 'never' })`. `NextIntlClientProvider` REQUIRED in root layout (Pitfall D).
- RESEARCH § Tailwind v4 CSS-First (lines 758-786): `@import "tailwindcss"` + `@theme` block; no `tailwind.config.js`.
- D-25: Payload first-user-creates-admin localhost-only. Users collection access control: `admin` role only, no public signup.
- Q3: logo SVG not yet delivered by coalition. Use a placeholder SVG with the navy `#004A79` color.

## Tasks

<task type="auto" tdd="false">
  <name>Task 1.04.1: Install + configure next-intl v4 with single-locale Bulgarian</name>
  <files>package.json, src/i18n/routing.ts, src/i18n/request.ts, next.config.ts</files>
  <read_first>
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md § next-intl v4 Setup (lines 841-880)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md Pitfall D (lines 1279-1283)
  </read_first>
  <action>
    **Step 1.** Install (exact versions): `pnpm add next-intl@4.11.0 zod@3.24.2 zod-i18n-map@2.27.0`.

    **Step 2.** `src/i18n/routing.ts`:
    ```typescript
    import { defineRouting } from 'next-intl/routing';

    export const routing = defineRouting({
      locales: ['bg'],
      defaultLocale: 'bg',
      localePrefix: 'never',  // single locale → URLs are /register, not /bg/register
    });

    export type AppLocale = (typeof routing.locales)[number];
    ```

    **Step 3.** `src/i18n/request.ts`:
    ```typescript
    import { getRequestConfig } from 'next-intl/server';
    import { routing } from './routing';

    export default getRequestConfig(async ({ requestLocale }) => {
      const requested = await requestLocale;
      const locale = (requested && routing.locales.includes(requested as never))
        ? requested
        : routing.defaultLocale;

      return {
        locale,
        messages: (await import(`../../messages/${locale}.json`)).default,
        timeZone: 'Europe/Sofia',
      };
    });
    ```

    **Step 4.** Update `next.config.ts` to wrap with `withNextIntl` (Sentry wrapper added in plan 11):
    ```typescript
    import { withPayload } from '@payloadcms/next/withPayload';
    import createNextIntlPlugin from 'next-intl/plugin';
    import type { NextConfig } from 'next';

    const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

    const nextConfig: NextConfig = {
      output: 'standalone',
      experimental: { reactCompiler: false },
    };

    export default withNextIntl(withPayload(nextConfig));
    ```

    **Step 5.** Run `pnpm typecheck`.
  </action>
  <verify>
    <automated>cd /Users/emoadm/projects/SMBsite && grep -q "next-intl" package.json && grep -q "defineRouting" src/i18n/routing.ts && grep -q "locales: \['bg'\]" src/i18n/routing.ts && grep -q "localePrefix: 'never'" src/i18n/routing.ts && grep -q "getRequestConfig" src/i18n/request.ts && grep -q "createNextIntlPlugin" next.config.ts && grep -q "withNextIntl(withPayload" next.config.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/i18n/routing.ts` exists with `locales: ['bg']` and `localePrefix: 'never'`
    - `src/i18n/request.ts` exists and dynamic-imports `messages/${locale}.json`
    - `next.config.ts` wraps the export with `withNextIntl(withPayload(nextConfig))`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>next-intl v4 wired for single-locale Bulgarian; root layout will be hooked up in Task 1.04.4.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.04.2: Tailwind v4 + shadcn/ui registry + locked component set</name>
  <files>package.json, src/styles/globals.css, components.json, src/lib/utils.ts, src/components/ui/.gitkeep</files>
  <read_first>
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md § Tailwind v4 CSS-First Configuration (lines 758-786)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md § shadcn/ui Initialization (lines 788-804)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-UI-SPEC.md § Color (lines 86-128)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-UI-SPEC.md § Typography (lines 67-83)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-UI-SPEC.md § Design System (Components in scope, lines 29-43)
  </read_first>
  <action>
    **Step 1.** Install (exact versions): `pnpm add tailwindcss@4.2.4 @tailwindcss/postcss@4.2.4 lucide-react@0.469.0 react-hook-form@7.54.2 @hookform/resolvers@5.2.2 class-variance-authority@0.7.1 clsx@2.1.1 tailwind-merge@2.6.0`.

    **Step 2.** Create `postcss.config.mjs`:
    ```javascript
    export default { plugins: { '@tailwindcss/postcss': {} } };
    ```

    **Step 3.** Create `src/styles/globals.css` with the EXACT @theme block from UI-SPEC + RESEARCH:
    ```css
    @import "tailwindcss";

    @theme {
      /* Brand palette — Sinya Bulgaria (BRAND-01, UI-SPEC § Color) */
      --color-background: #FFFFFF;
      --color-surface: #F1F5F9;
      --color-foreground: #0F172A;
      --color-muted-foreground: #475569;
      --color-border: #E2E8F0;
      --color-accent: #004A79;
      --color-destructive: #E72E4D;
      --color-success: #009F54;
      --color-sky: #00B7ED;

      /* Typography scale — locked to 4 sizes (UI-SPEC § Typography) */
      --text-sm: 14px;     /* label */
      --text-base: 16px;   /* body */
      --text-xl: 20px;     /* heading */
      --text-3xl: 28px;    /* display */

      /* Layout containers (UI-SPEC § Layout Shell) */
      --container-form: 480px;
      --container-legal: 720px;
      --container-page: 1140px;

      /* Font families (BRAND-06) — actual face loaded via next/font in layout */
      --font-sans: var(--font-roboto), system-ui, -apple-system, "Segoe UI", Tahoma, sans-serif;
      --font-display: var(--font-roboto-slab), Georgia, serif;
    }

    /* OTP digit slots: tabular-nums for unambiguous reading (UI-SPEC § Typography rule 2) */
    [data-slot="input-otp-slot"] {
      font-variant-numeric: tabular-nums;
      font-weight: 600;
    }
    ```

    **Step 4.** Create `components.json` (shadcn registry config) — `new-york` + `slate` + CSS variables, prefix empty (UI-SPEC § Design System):
    ```json
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "new-york",
      "rsc": true,
      "tsx": true,
      "tailwind": {
        "config": "",
        "css": "src/styles/globals.css",
        "baseColor": "slate",
        "cssVariables": true,
        "prefix": ""
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui",
        "lib": "@/lib",
        "hooks": "@/hooks"
      },
      "iconLibrary": "lucide"
    }
    ```

    **Step 5.** Create `src/lib/utils.ts` (shadcn requirement):
    ```typescript
    import { type ClassValue, clsx } from 'clsx';
    import { twMerge } from 'tailwind-merge';

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs));
    }
    ```

    **Step 6.** Run shadcn add commands (write components into `src/components/ui/`):
    ```bash
    pnpm dlx shadcn@latest add button input label alert card checkbox select form input-otp --yes
    ```
    The `--yes` flag accepts defaults and overwrites if needed. After completion, the `src/components/ui/` directory must contain (at minimum) files: `button.tsx`, `input.tsx`, `label.tsx`, `alert.tsx`, `card.tsx`, `checkbox.tsx`, `select.tsx`, `form.tsx`, `input-otp.tsx`. NO other component files (D-26 lock — no nav, no dialog, no tabs, etc.).

    **Step 7.** Run `pnpm lint:i18n` to confirm shadcn-injected components have no Cyrillic. Run `pnpm typecheck`.
  </action>
  <verify>
    <automated>cd /Users/emoadm/projects/SMBsite && test -f src/styles/globals.css && grep -q "\-\-color-accent: #004A79" src/styles/globals.css && grep -q "\-\-color-destructive: #E72E4D" src/styles/globals.css && grep -q "\-\-text-3xl: 28px" src/styles/globals.css && test -f components.json && grep -q "\"style\": \"new-york\"" components.json && grep -q "\"baseColor\": \"slate\"" components.json && test -f src/lib/utils.ts && grep -q "twMerge" src/lib/utils.ts && for c in button input label alert card checkbox select form input-otp; do test -f "src/components/ui/$c.tsx" || { echo "MISSING $c"; exit 1; }; done && pnpm lint:i18n && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/globals.css` contains all 9 CSS color variables (one per BRAND palette role) AND all 4 typography sizes
    - `components.json` declares `style: "new-york"`, `baseColor: "slate"`, `cssVariables: true`
    - `src/lib/utils.ts` exports `cn()` using `twMerge(clsx())`
    - All 9 shadcn component files exist under `src/components/ui/`: button, input, label, alert, card, checkbox, select, form, input-otp
    - NO additional shadcn component files exist (D-26 lock — count files in `src/components/ui/` = exactly 9)
    - `pnpm lint:i18n` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Tailwind v4 theme tokens locked; shadcn registry initialized; 9 components owned in src/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.04.3: Pre-populate messages/bg.json with every UI-SPEC string + theme test</name>
  <files>messages/bg.json, tests/unit/theme.test.ts</files>
  <read_first>
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-UI-SPEC.md § Copywriting Contract (lines 132-230) — every line is a message key
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md § next-intl v4 Setup — Message file structure (line 880)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-PATTERNS.md § File Naming (Message keys row, line 199)
  </read_first>
  <action>
    **Step 1.** Create `messages/bg.json` with EVERY string from UI-SPEC § Copywriting Contract, preserving the canonical Bulgarian text exactly. Structure (dot-notation grouped under top-level namespaces):

    ```json
    {
      "auth": {
        "register": {
          "title": "Регистрация",
          "cta": "Регистрирай се",
          "rateLimited": "Твърде много опити за регистрация. Опитай отново след един час.",
          "invalidEmail": "Този имейл не може да се използва. Опитай с друг адрес.",
          "captchaFailed": "Проверката за бот не премина. Презареди страницата и опитай отново.",
          "fields": {
            "fullName": "Име и фамилия",
            "email": "Имейл",
            "emailHelp": "Ще ти изпратим код за потвърждение.",
            "sector": "Сектор",
            "sectorPlaceholder": "Избери сектор",
            "role": "Роля",
            "rolePlaceholder": "Избери роля",
            "submit": "Регистрирай се"
          },
          "consents": {
            "privacyTerms": "Съгласявам се с [Политиката за поверителност] и [Условията за ползване].",
            "cookies": "Съгласявам се с използването на бисквитки и анализи за работата на сайта.",
            "newsletter": "Желая да получавам политически бюлетин от Синя България по имейл.",
            "politicalOpinion": "Съгласявам се записването на гласовете и авторските ми предложения да бъде обработвано като данни за политически възгледи по чл. 9 от GDPR."
          },
          "sectors": {
            "it": "ИТ",
            "trade": "Търговия",
            "production": "Производство",
            "services": "Услуги",
            "other": "Друго"
          },
          "roles": {
            "owner": "Собственик",
            "manager": "Управител",
            "employee": "Служител",
            "other": "Друго"
          }
        },
        "login": {
          "title": "Влез в профила си",
          "cta": "Изпрати код за вход",
          "rateLimited": "Твърде много опити за вход. Опитай отново след един час.",
          "fields": {
            "email": "Имейл",
            "emailHelp": "Ще ти изпратим 6-цифрен код за вход."
          }
        },
        "otp": {
          "title": "Въведи кода",
          "body": "Изпратихме ти 6-цифрен код на {email}. Кодът е валиден {validityMinutes} минути.",
          "cta": "Потвърди",
          "resend": "Изпрати нов код",
          "resendPrompt": "Не получи код? Изпрати нов.",
          "wrongEmail": "Грешен имейл? Започни отново.",
          "inputAriaLabel": "6-цифрен код за потвърждение",
          "invalid": "Кодът не е валиден. Провери имейла или поискай нов код.",
          "expired": "Кодът изтече. Поискай нов код.",
          "locked": "Изчерпани са опитите. Поискай нов код.",
          "sent": "Изпратихме ти 6-цифрен код на {email}. Кодът е валиден 10 минути."
        },
        "confirmed": {
          "cta": "Към профила"
        },
        "loggedOut": "Излязохте от профила си."
      },
      "nav": {
        "login": "Вход",
        "logout": "Изход"
      },
      "member": {
        "placeholder": {
          "heading": "Добре дошли в платформата",
          "body": "Скоро тук ще можете да гласувате по политически идеи и да предлагате решения. Засега следете имейла си за актуална информация."
        }
      },
      "legal": {
        "draft": {
          "marker": "проект, последна редакция {date}"
        },
        "privacy": {
          "title": "Политика за поверителност"
        },
        "terms": {
          "title": "Условия за ползване"
        }
      },
      "footer": {
        "legalGroupAria": "Правна информация",
        "privacy": "Политика за поверителност",
        "terms": "Условия за ползване",
        "contact": "Контакт",
        "copyright": "© {year} Коалиция Синя България. Всички права запазени."
      },
      "cookieBanner": {
        "heading": "Бисквитки и поверителност",
        "body": "Използваме бисквитки за работата на сайта и за анонимни анализи. Можеш да избереш кои да приемеш.",
        "acceptAll": "Приеми всички",
        "rejectAll": "Само необходимите",
        "customize": "Настрой",
        "categories": {
          "necessary": "Необходими",
          "analytics": "Анализи",
          "marketing": "Маркетинг"
        }
      },
      "error": {
        "generic": {
          "heading": "Нещо се обърка.",
          "body": "Опитай да презаредиш страницата. Ако проблемът се повтаря, пиши ни на {supportEmail}."
        },
        "notFound": {
          "heading": "Страницата не съществува.",
          "body": "Може би линкът е изтекъл или сме променили адреса. Върни се към началото."
        },
        "maintenance": {
          "heading": "Системата е в поддръжка.",
          "body": "Връщаме се след малко. Благодарим за търпението."
        }
      },
      "destructive": {
        "deleteAccountConfirm": "Сигурни ли сте, че искате да изтриете профила си? Това действие е необратимо. Всички ваши гласове, предложения и сигнали ще бъдат изтрити в рамките на 30 дни."
      }
    }
    ```

    **Step 2.** Replace the failing-by-design `tests/unit/theme.test.ts` stub from plan 02 with a real assertion that BRAND-01 tokens exist in globals.css:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { readFileSync } from 'node:fs';
    import path from 'node:path';

    describe('BRAND-01 — Tailwind v4 theme tokens', () => {
      const css = readFileSync(path.resolve('src/styles/globals.css'), 'utf8');
      const tokens = [
        ['--color-background', '#FFFFFF'],
        ['--color-surface', '#F1F5F9'],
        ['--color-foreground', '#0F172A'],
        ['--color-muted-foreground', '#475569'],
        ['--color-border', '#E2E8F0'],
        ['--color-accent', '#004A79'],
        ['--color-destructive', '#E72E4D'],
        ['--color-success', '#009F54'],
        ['--color-sky', '#00B7ED'],
        ['--text-sm', '14px'],
        ['--text-base', '16px'],
        ['--text-xl', '20px'],
        ['--text-3xl', '28px'],
      ] as const;

      for (const [name, value] of tokens) {
        it(`declares ${name} = ${value}`, () => {
          expect(css).toMatch(new RegExp(`${name.replace(/[-]/g, '\\-')}\\s*:\\s*${value.replace(/[#]/g, '\\#')}`));
        });
      }
    });
    ```

    **Step 3.** Run `pnpm test:unit -- theme` — must pass.
  </action>
  <verify>
    <automated>cd /Users/emoadm/projects/SMBsite && test -f messages/bg.json && python3 -c "import json; m=json.load(open('messages/bg.json')); assert m['auth']['register']['cta']=='Регистрирай се'; assert m['auth']['otp']['title']=='Въведи кода'; assert m['nav']['login']=='Вход'; assert m['footer']['privacy']=='Политика за поверителност'; assert m['cookieBanner']['acceptAll']=='Приеми всички'; assert m['error']['generic']['heading']=='Нещо се обърка.'; print('messages/bg.json OK')" && pnpm test:unit -- theme</automated>
  </verify>
  <acceptance_criteria>
    - `messages/bg.json` is valid JSON
    - Contains EXACT canonical strings: `auth.register.cta == "Регистрирай се"`, `auth.otp.title == "Въведи кода"`, `auth.register.invalidEmail == "Този имейл не може да се използва. Опитай с друг адрес."`, `nav.login == "Вход"`, `footer.privacy == "Политика за поверителност"`
    - Contains all 4 sector keys (`it`, `trade`, `production`, `services`, `other`) and all 4 role keys
    - Contains all 4 consent message keys
    - `tests/unit/theme.test.ts` no longer contains `SCAFFOLD MISSING` and asserts all 13 token values
    - `pnpm test:unit -- theme` exits 0
  </acceptance_criteria>
  <done>messages/bg.json contains every Phase 1 string; theme test enforces palette + typography tokens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.04.4: Root layout (font, NextIntlClientProvider, lang="bg") + Payload Users collection + placeholder logo</name>
  <files>src/app/(frontend)/layout.tsx, src/collections/Users.ts, src/payload.config.ts, public/logo-placeholder.svg</files>
  <read_first>
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md § Root Layout (lines 882-899)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md § Typography (lines 808-828)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md Pitfall D (lines 1279-1283)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-CONTEXT.md D-25 (Payload first-user-creates-admin)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md Q3 (line 1348 — logo placeholder acceptable)
  </read_first>
  <action>
    **Step 1.** Replace the create-payload-app default `src/app/(frontend)/layout.tsx` with the canonical root layout (Header + Footer come in plan 1.08; for now use a minimal `<main>` so the page boots):
    ```typescript
    import { Roboto, Roboto_Slab } from 'next/font/google';
    import { NextIntlClientProvider } from 'next-intl';
    import { getLocale, getMessages } from 'next-intl/server';
    import '@/styles/globals.css';

    const roboto = Roboto({
      weight: ['400', '600'],
      subsets: ['cyrillic', 'cyrillic-ext', 'latin'],
      display: 'swap',
      variable: '--font-roboto',
    });

    const robotoSlab = Roboto_Slab({
      weight: ['600'],
      subsets: ['cyrillic', 'cyrillic-ext', 'latin'],
      display: 'swap',
      variable: '--font-roboto-slab',
    });

    export const metadata = {
      title: 'Синя България — Платформа на МСП',
      description: 'Платформа за политическо застъпничество на МСП сектора в България.',
    };

    export default async function RootLayout({ children }: { children: React.ReactNode }) {
      const locale = await getLocale();
      const messages = await getMessages();

      return (
        <html lang={locale} className={`${roboto.variable} ${robotoSlab.variable}`}>
          <body className="bg-background text-foreground font-sans antialiased">
            <NextIntlClientProvider locale={locale} messages={messages} timeZone="Europe/Sofia">
              <main>{children}</main>
            </NextIntlClientProvider>
          </body>
        </html>
      );
    }
    ```

    Note: this layout deliberately omits Header/Footer — plan 1.08 adds them. The current file boots cleanly so dev mode works.

    **Step 2.** Create `src/collections/Users.ts` (Payload — CMS-only, NOT auth users — D-25):
    ```typescript
    import type { CollectionConfig } from 'payload';

    export const Users: CollectionConfig = {
      slug: 'users',
      auth: true,
      admin: {
        useAsTitle: 'email',
        // First-user signup is allowed only on localhost during initial bootstrap (D-25).
        // Subsequent admin invitations go through Payload's built-in invite UI.
        // No public admin signup ever — D-25 lock.
      },
      access: {
        // Only authenticated CMS admins can read or write CMS users.
        // No anonymous access; no self-signup outside Payload's localhost-only bootstrap.
        read:   ({ req }) => Boolean(req.user),
        create: ({ req }) => Boolean(req.user),
        update: ({ req }) => Boolean(req.user),
        delete: ({ req }) => Boolean(req.user),
      },
      fields: [
        { name: 'name', type: 'text' },
        { name: 'role', type: 'select', defaultValue: 'editor', options: [
          { label: 'Admin',  value: 'admin' },
          { label: 'Editor', value: 'editor' },
        ]},
      ],
    };
    ```

    **Step 3.** Update `src/payload.config.ts` (the file create-payload-app generated) to register the Users collection. Confirm it imports `@payloadcms/db-postgres` and points to `process.env.PAYLOAD_DATABASE_URL` (which equals DIRECT_URL per plan 01 .env.example). The file already exists from plan 01; locate the `collections: [...]` array and ensure `Users` from `./collections/Users` is included. Also confirm `secret: process.env.PAYLOAD_SECRET!` is set.

    Skeleton (confirm or update):
    ```typescript
    import { buildConfig } from 'payload';
    import { postgresAdapter } from '@payloadcms/db-postgres';
    import { lexicalEditor } from '@payloadcms/richtext-lexical';
    import path from 'node:path';
    import { fileURLToPath } from 'node:url';
    import { Users } from './collections/Users';

    const filename = fileURLToPath(import.meta.url);
    const dirname  = path.dirname(filename);

    export default buildConfig({
      admin: { user: Users.slug, importMap: { baseDir: path.resolve(dirname) } },
      collections: [Users],
      editor: lexicalEditor(),
      secret: process.env.PAYLOAD_SECRET!,
      typescript: { outputFile: path.resolve(dirname, 'payload-types.ts') },
      db: postgresAdapter({
        pool: { connectionString: process.env.PAYLOAD_DATABASE_URL! },
      }),
    });
    ```

    **Step 4.** Create `public/logo-placeholder.svg` — a navy `#004A79` rectangle with the word "СБ" centered (per Q3 — placeholder acceptable until coalition delivers the real SVG):
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 40" width="120" height="40">
      <rect width="120" height="40" rx="6" fill="#004A79"/>
      <text x="60" y="26" text-anchor="middle" font-family="Roboto Slab, Georgia, serif"
            font-size="20" font-weight="600" fill="#FFFFFF">СБ</text>
    </svg>
    ```

    **Step 5.** Run `pnpm typecheck`. Verify Payload generates types (run `pnpm payload generate:types` — produces `src/payload-types.ts`).
  </action>
  <verify>
    <automated>cd /Users/emoadm/projects/SMBsite && grep -q "next/font/google" src/app/\(frontend\)/layout.tsx && grep -q "NextIntlClientProvider" src/app/\(frontend\)/layout.tsx && grep -q 'lang={locale}' src/app/\(frontend\)/layout.tsx && grep -q "subsets: \['cyrillic', 'cyrillic-ext', 'latin'\]" src/app/\(frontend\)/layout.tsx && test -f src/collections/Users.ts && grep -q "slug: 'users'" src/collections/Users.ts && grep -q "auth: true" src/collections/Users.ts && grep -q "Users" src/payload.config.ts && test -f public/logo-placeholder.svg && grep -q "#004A79" public/logo-placeholder.svg && pnpm lint:i18n && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(frontend)/layout.tsx` imports `Roboto` and `Roboto_Slab` from `next/font/google` with `subsets: ['cyrillic', 'cyrillic-ext', 'latin']`
    - Layout wraps children in `<NextIntlClientProvider>` with `locale` and `messages` props
    - `<html lang={locale}>` (resolves to `bg`)
    - `src/collections/Users.ts` declares `slug: 'users'`, `auth: true`, no public-create access
    - `src/payload.config.ts` includes `Users` in collections and references `process.env.PAYLOAD_DATABASE_URL`
    - `public/logo-placeholder.svg` exists and contains `#004A79`
    - `pnpm lint:i18n` exits 0 (the layout uses no Cyrillic literals — `metadata.title` literal is allowed because it's not user-facing UI; if lint flags, move to a separate `metadata.ts` const)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Root layout renders Bulgarian by default with Cyrillic-ready Roboto fonts and i18n provider; Payload Users collection ready for first-user bootstrap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.04.5: [BLOCKING] Push Payload schema to Neon (`pnpm payload migrate`)</name>
  <files>(no files modified — runs Payload migrations against the live Neon DB)</files>
  <read_first>
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-RESEARCH.md § Drizzle vs Payload DB Layer Separation (lines 320-328)
    - /Users/emoadm/projects/SMBsite/.planning/phases/01-foundation/01-CONTEXT.md D-25
  </read_first>
  <what-built>
    Plan 1.04 added a Payload `Users` collection. Payload manages its own internal tables (e.g. `payload_preferences`, `payload_migrations`, `users_*` Payload-internal columns) using its own migration system, distinct from Drizzle. The live Neon DB currently has the 5 Drizzle tables from plan 1.03 but no Payload tables. This blocking task pushes Payload's schema.
  </what-built>
  <action>
    Pre-requisite: `.env.local` has `PAYLOAD_DATABASE_URL` set to the DIRECT_URL value (not pooled). Confirm with `grep PAYLOAD_DATABASE_URL .env.local`.

    Run:
    ```bash
    CI=true PAYLOAD_MIGRATING=true pnpm payload migrate
    ```

    The `CI=true` and `PAYLOAD_MIGRATING=true` env flags are documented in the schema-push frontmatter requirement of this phase to bypass interactive prompts in non-TTY mode.

    Payload will:
    1. Create the `payload_migrations` bookkeeping table.
    2. Apply schema migrations for the `users` Payload-auth collection (CMS-only — distinct from the `users` table Drizzle owns; Payload prefixes its columns or uses a separate table — verify after).

    After running, verify:
    ```bash
    psql "$DIRECT_URL" -c "\dt"
    ```
    The list should now include `payload_migrations` and any Payload-internal `*_payload_*` tables. The Drizzle `users` table from plan 1.03 must STILL be present and unchanged.

    **If a name collision arises** between Drizzle `users` and Payload `users` (the collection slug is `users` per Step 2 of Task 1.04.4), Payload may complain. The mitigation is to update `src/collections/Users.ts` `slug: 'users'` → `slug: 'cms_users'` and re-run migrate. Document the chosen path in resume-signal.
  </action>
  <how-to-verify>
    1. `psql "$DIRECT_URL" -c "\dt"` — Drizzle's 5 tables still present + Payload tables added.
    2. `psql "$DIRECT_URL" -c "SELECT * FROM payload_migrations LIMIT 5"` — at least 1 row showing the migration ran.
    3. Boot dev server: `pnpm dev`, visit `http://localhost:3000/admin` — Payload login screen renders. (No first user yet — that's plan 12's procedure.)
  </how-to-verify>
  <resume-signal>Type "payload migrated" with confirmation of the table list, OR describe collision and resolution path taken.</resume-signal>
  <acceptance_criteria>
    - `psql "$DIRECT_URL" -c "\dt"` lists `payload_migrations`
    - The 5 Drizzle tables (users, verification_tokens, sessions, accounts, consents) are still present
    - `http://localhost:3000/admin` renders Payload's login UI (verifies the Users collection registered)
    - User signals "payload migrated"
  </acceptance_criteria>
  <done>Payload schema pushed; both ORMs share the same Neon DB without conflict.</done>
</task>

## Threat Model

<threat_model>
| threat_id | description | severity | mitigation |
|-----------|-------------|----------|------------|
| T-04-cyrillic-leak-in-layout | A future PR adds a hardcoded Bulgarian string to layout.tsx | medium | `pnpm lint:i18n` runs in CI (plan 12); root layout uses metadata.title which is permitted but flagged in code review. |
| T-04-payload-public-admin-signup | An attacker registers as a Payload admin via `/admin` | critical | D-25: Users collection denies all create unless `req.user` is already authenticated. First-user bootstrap is documented as localhost-only in plan 12. No public admin signup route. |
| T-04-cms-vs-app-users-conflict | Payload's `users` slug collides with Drizzle's `users` table | medium | Verified in Task 1.04.5 acceptance — if collision, slug is renamed to `cms_users` and documented. |
| T-04-stale-message-key | A component references `t('auth.foo')` but the key isn't in `bg.json` — runtime missing-key warning | low | next-intl logs missing keys to console. Plan 1.09 (UI plan) typescripts message keys. |
</threat_model>

## Verification

<verification>
must_haves:
  truths:
    - "next-intl v4 wired with single locale `bg` and no URL prefix"
    - "Tailwind v4 @theme block declares all 9 color tokens + 4 typography sizes from UI-SPEC"
    - "All 9 shadcn components from D-26 are owned in src/components/ui (no more, no less)"
    - "messages/bg.json contains every UI-SPEC § Copywriting Contract string"
    - "Root layout loads Roboto + Roboto Slab with Cyrillic subsets and wraps children in NextIntlClientProvider"
    - "Payload Users collection registered with no public signup; Payload schema pushed to Neon"
  artifacts:
    - { path: "src/i18n/routing.ts", provides: "next-intl routing", contains: "locales: ['bg']" }
    - { path: "src/styles/globals.css", provides: "BRAND-01 palette tokens", contains: "--color-accent: #004A79" }
    - { path: "components.json", provides: "shadcn registry config", contains: "\"baseColor\": \"slate\"" }
    - { path: "messages/bg.json", provides: "all Phase 1 Bulgarian strings", contains: "Регистрирай се" }
    - { path: "src/components/ui/button.tsx", provides: "shadcn Button" }
    - { path: "src/components/ui/input-otp.tsx", provides: "OTP input component" }
    - { path: "src/collections/Users.ts", provides: "Payload CMS users collection", contains: "auth: true" }
    - { path: "src/app/(frontend)/layout.tsx", provides: "i18n + font + lang root", contains: "NextIntlClientProvider" }
  key_links:
    - { from: "src/app/(frontend)/layout.tsx", to: "messages/bg.json", via: "getMessages() → NextIntlClientProvider", pattern: "NextIntlClientProvider" }
    - { from: "src/styles/globals.css", to: "src/components/ui/*", via: "@theme tokens consumed by shadcn", pattern: "@theme" }
</verification>

## Notes

- The placeholder `logo-placeholder.svg` is replaced when the coalition delivers the real SVG (Q3) — plan 1.08 (branding shell) handles the swap. Until then, Header points at `/logo-placeholder.svg`.
- The `Users` collection slug "users" may collide with the Drizzle `users` table — Task 1.04.5 verifies and resolves at push time.
- Header and Footer components consume `messages/bg.json` directly; plan 1.08 builds them.
