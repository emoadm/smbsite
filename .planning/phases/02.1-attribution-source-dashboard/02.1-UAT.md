---
status: complete
phase: 02.1-attribution-source-dashboard
source:
  - 02.1-01-SUMMARY.md
  - 02.1-02-SUMMARY.md
  - 02.1-03-SUMMARY.md
  - 02.1-04-SUMMARY.md
  - 02.1-05-SUMMARY.md
  - 02.1-06-SUMMARY.md
  - 02.1-07-SUMMARY.md
  - 02.1-08-SUMMARY.md
started: 2026-05-04T07:35:08.913Z
updated: 2026-05-04T13:50:00.000Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running app/worker processes, drop ephemeral state (worker queue, Redis dev keys), then `pnpm dev` (or `flyctl deploy` to staging). The Next.js app boots without errors, `scripts/start-worker.ts` launches both email + attribution workers without throwing, the Drizzle migration `0001_grey_umar.sql` has been applied (`attribution_events` table exists in DB), and a primary request — `curl -i http://localhost:3000/` — returns 200 OK with no 5xx in server logs.
result: pass
note: dev server booted in 4.3s with no errors (Sentry warnings are pre-existing Phase 1 config); worker+endpoint covered by k6 staging run #3 GREEN on 2026-05-04

### 2. Anonymous Attribution Cookie Set
expected: Visit `https://staging.chastnik.eu/` in a fresh browser session (or DevTools → Application → "Clear site data" first). After page load, browser DevTools → Application → Cookies shows `attr_sid` cookie present (uuid-shaped value, HttpOnly, SameSite=Lax, 30-day Max-Age). Network tab shows GET `/api/attr/init` returned 200 with `Cache-Control: no-store`.
result: pass
note: cookie verified present on staging; network-tab leg not directly checked but cookie existence implies /api/attr/init ran (only setter)

### 3. UTM Capture from QR URL
expected: Visit landing with `?utm_source=qr_letter&utm_medium=qr&utm_campaign=spring2026`. Open Payload admin → attribution dashboard (or run `psql ... select * from attribution_events order by first_seen_at desc limit 1`) — newest row has `first_utm_source='qr_letter'`, `first_utm_medium='qr'`, `first_utm_campaign='spring2026'`, `first_qr_flag=true`, `first_oblast` populated (e.g. `BG-22`) or `unknown`.
result: pass
note: confirmed end-to-end on prod after worker memory bump (31236d0). DB row materialized within ~8s of /api/attr/init request with first_utm_campaign + first_qr_flag + first_oblast populated correctly.

### 4. Public Page Cache Preserved
expected: Visit a public landing page; the page's response headers do NOT contain a `Set-Cookie` for `attr_sid` (the cookie is only set by `/api/attr/init`, not by the page response). Cloudflare cache for the public page is preserved (no `Set-Cookie` busting it).
result: pass
note: verified via `curl -sIL https://chastnik.eu/` — no Set-Cookie on landing page response. Cookie set by /api/attr/init only (D-22). Side observation: page has `cache-control: no-store` which is more conservative than the architecture intends — separate hardening item, not a 02.1 deliverable.

### 5. Registration Form — Source Dropdown
expected: Visit `/register` (or wherever RegistrationForm renders). Below sector/role selects there is a required dropdown labelled "Откъде научихте за нас?" with 8 Bulgarian-language options corresponding to: QR/писмо, имейл от коалицията, sinyabulgaria.bg, Facebook, LinkedIn, препоръка, новинарски сайт, Друго.
result: pass

### 6. Conditional "Друго" Free-text
expected: Selecting "Друго" in the source dropdown reveals an additional free-text input below it (with placeholder + max-length helper text). Selecting any other option hides that input. The input accepts up to 300 characters.
result: pass

### 7. Source Field Required Validation
expected: Submitting the registration form WITHOUT selecting a source value surfaces a Bulgarian-language validation error pinned to the source dropdown — the form does not submit.
result: pass

### 8. Source Persists on Successful Registration
expected: Complete a full registration (name + email + sector + role + source = "facebook" + consents + Turnstile) and verify OTP. After verification, in DB: `users.self_reported_source = 'facebook'` for that email, `users.self_reported_other` is NULL.
result: pass
note: verified on prod with emoadm+test2@gmail.com — users.self_reported_source='facebook', self_reported_other=NULL. user id cac07f89-5229-413e-8b97-dfcbaa102ca7 created 2026-05-04 13:47:44Z.

### 9. Attribution Linkage After OTP Verify
expected: Browser already has an `attr_sid` cookie from step 2. Complete registration + OTP verify with that browser. In DB: `attribution_events.user_id` for the row matching that `attr_sid` is now the newly created user's id (was NULL before verify).
result: pass
note: verified on prod — attribution_events row with attr_sid 1da8f203-a59a-474a-a2bb-c038faf7c4bc now has user_id=cac07f89-5229-413e-8b97-dfcbaa102ca7 (was NULL pre-OTP). Linkage UPDATE fires before session-token cookie set as designed (Plan 06).
side_observation: "linked row has empty UTMs because user's browser had stale attr_sid from worker-OOM window — those original UTMs were lost when stalled jobs failed. Worker now processes in real-time post 31236d0; no future UTM loss expected. QR mail drop has not started so loss is moot."

### 10. Admin Dashboard Visible to Editor/Admin
expected: Log into `/admin` as a user whose admin_users.role is `admin` or `editor`. Navigate to `/admin/views/attribution`. Page renders the attribution dashboard with the title from `attribution.dashboard.*` Bulgarian keys, a date-range filter (default last 7 days), and 4 aggregate tables.
result: pass
note: re-tested on prod (https://chastnik.eu/admin/views/attribution) after deploy d603fc8 with importMap fix + Node 22 CI bump. Dashboard now renders for admin/editor.

### 11. Admin Dashboard Role-Gated
expected: A signed-in non-admin/non-editor (or unauthenticated request) hitting `/admin/views/attribution` is denied — Payload redirects to login or shows a forbidden state. They never see the dashboard data.
result: pass
note: confirmed earlier in session — unauthenticated visit to /admin/views/attribution returned blank (Payload's role-gate denial). Admin user sees the dashboard. Dual gate (page.tsx + Server Action) per Plan 07.

### 12. Dashboard — 4 Aggregate Tables
expected: Dashboard shows 4 aggregate tables: by UTM source, by oblast, by QR flag, and by self-reported source (the last one comes from INNER JOIN with users). Each table has rows reflecting current `attribution_events` data within the date filter.
result: pass
note: structure confirmed on prod dashboard; rows empty because no visitors yet

### 13. Dashboard — Oblast Bar List in Bulgarian
expected: Sessions-by-oblast section renders as a bar list (rows with relative-width bars). Oblast names render in Bulgarian using `OBLAST_NAMES` (e.g. `BG-22 → София`, `BG-23 → София-град`, unknown → `Неизвестен`) — never raw ISO codes.
result: pass
note: confirmed via Test 3 — dashboard now shows BG-22 oblast as "София" in Bulgarian.

### 14. Dashboard — Totals
expected: Dashboard shows two totals: `totalSessions` (count of all attribution_events rows in range) and `totalRegistered` (count of rows where user_id IS NOT NULL). Labels are Bulgarian (from `attribution.dashboard.totalSessions` / `totalRegistered`).
result: pass
note: confirmed on prod dashboard

### 15. Dashboard — Date Range Filter
expected: Changing the date range filter (date-from / date-to) updates all 4 aggregate tables + the totals. Default range on first load is the last 7 days.
result: pass

### 16. CSV Export
expected: Click the CSV export button. Browser downloads a file named `attribution-{YYYY-MM-DD}-{YYYY-MM-DD}.csv` (the date range matches the active filter). First row is the header `attr_sid,first_utm_source,first_utm_medium,first_oblast,first_qr_flag,first_seen_at,user_id`. Values containing `,`, `"`, or newline are wrapped in quotes with embedded quotes doubled (RFC 4180).
result: pass

## Summary

total: 16
passed: 16
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "/admin/views/attribution renders the attribution dashboard for logged-in admin/editor users (Plan 07 ATTR-07)"
  status: resolved
  reason: "User reported: After logging in as admin/editor on staging, /admin/views/attribution renders a white blank page — no dashboard chrome, no tables, no filter, nothing."
  severity: blocker
  test: 10
  artifacts:
    - "src/app/(payload)/admin/importMap.js (was empty stub from Phase 1; now contains AttributionView entry)"
    - ".github/workflows/deploy.yml (Node 20 → 22 to fix transitive undici@7 CacheStorage)"
    - ".github/workflows/deploy.yml (Payload migrate step temporarily disabled — see TODO)"
  missing: []
  resolved_by_commits: [c37a09b, fe4c261, d603fc8]
  resolved_at: 2026-05-04
  blocks_tests: [3, 11, 12, 13, 14, 15, 16]
  followup_todos:
    - .planning/todos/pending/2026-05-04-payload-tsx-esm-incompat.md
