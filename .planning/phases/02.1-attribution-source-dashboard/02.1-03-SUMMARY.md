---
phase: 02.1-attribution-source-dashboard
plan: 03
subsystem: i18n
tags: [i18n, bg-copy, attribution, next-intl, vitest]

# Dependency graph
requires:
  - phase: 02.1-attribution-source-dashboard
    provides: D-09/D-10 locked dropdown enum + D-12/D-21 dashboard scope/tone (CONTEXT.md)
  - phase: 02-public-surface-pre-warmup
    provides: coalition-placeholder grep gate (Plan 02-08) — convention this plan inherits
  - phase: 01-foundation
    provides: D-27 zero-hardcoded-Cyrillic-in-JSX rule + sectors/roles dropdown pattern in messages/bg.json
provides:
  - "messages/bg.json#auth.register.source — 12 keys (8 locked enum values + label/placeholder/otherPlaceholder/otherMaxLengthHelp)"
  - "messages/bg.json#attribution.dashboard — 12 sub-keys (title/subtitle/filters/tables/columns/export/unknown/noData/denied/loginRequired/totalSessions/totalRegistered)"
  - "tests/unit/attribution-i18n.test.ts — 8 vitest assertions locking enum contract + tone gate"
affects: [02.1-04 schema, 02.1-06 RegistrationForm extension, 02.1-07 Payload attribution dashboard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tone-lock vitest pattern (mirrors tests/unit/queue.test.ts D-27 check) extended to attribution + register.source namespaces"
    - "Enum-shape lock test — Zod enum in Plan 06 must match SOURCE_ENUM array in attribution-i18n.test.ts; drift surfaces red"

key-files:
  created:
    - tests/unit/attribution-i18n.test.ts
  modified:
    - messages/bg.json

key-decisions:
  - "Honored plan-prescribed task ordering (JSON first, test second) over strict TDD RED-first because Task 2 explicitly depends on Task 1's keys per its <read_first> block"
  - "Did not auto-fix pre-existing pnpm check:placeholders failures (D-CoalitionContent-Hero / D-CoalitionContent-Agenda) — out-of-scope per executor scope-boundary rule, tracked in STATE.md as coalition deferred items"

patterns-established:
  - "Pattern: greppable enum lock-in test — for each new Zod enum that appears in a Server Action, add a vitest source-grep test that reads bg.json and asserts each enum value resolves to a non-empty Bulgarian string"
  - "Pattern: tone gate per namespace — JSON.stringify(namespace) + .not.toMatch(/Уважаеми/) + .not.toMatch(/Уважаема/) is the cheap structural check (mirrors queue.test.ts:35-37)"

requirements-completed: [ATTR-06, ATTR-07]

# Metrics
duration: 4min
completed: 2026-05-03
---

# Phase 02.1 Plan 03: Attribution Bulgarian Copy + Tone Lock Summary

**Bulgarian-language strings for the registration "Откъде научихте за нас?" dropdown (8 D-10 enum values) + Payload attribution dashboard (filters/tables/columns/totals) added to messages/bg.json with a vitest tone-and-enum-lock-in test.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-05-03T10:57:46Z
- **Completed:** 2026-05-03T11:01:18Z
- **Tasks:** 2/2
- **Files modified:** 1 (messages/bg.json)
- **Files created:** 1 (tests/unit/attribution-i18n.test.ts)
- **Total commits:** 2 (per-task)

## Accomplishments

- Added `auth.register.source` namespace with 12 keys: label, placeholder, the 8 D-10 locked enum values, otherPlaceholder, otherMaxLengthHelp. All 8 enum keys match the Zod enum Plan 06 will declare (`qr_letter, email_coalition, sinya_site, facebook, linkedin, referral, news_media, other`).
- Added top-level `attribution.dashboard` namespace with 12 sub-keys including the two `totalSessions` / `totalRegistered` keys that Plan 07 will bind via `t.totalSessions` / `t.totalRegistered` to keep AttributionDashboard.tsx free of hardcoded Cyrillic (Phase 1 D-27).
- Locked the contract with 8 vitest assertions in a new `tests/unit/attribution-i18n.test.ts` file — mirrors `tests/unit/queue.test.ts:29-38` tone-check pattern.
- `bg.attribution.dashboard.unknown === "Неизвестен"` matches the `OBLAST_NAMES.unknown` value Plan 02 will populate.
- All new strings pass the formal-respectful tone gate (no `Уважаеми` / `Уважаема` vocative forms, D-21).
- All 62 vitest tests across 10 test files pass green.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add auth.register.source + attribution.dashboard namespaces to messages/bg.json** — `13bb0d0` (feat)
2. **Task 2: Vitest tone + key-presence test for new namespaces** — `9dde06b` (test)

_Note: Both tasks have `tdd="true"` in the plan but the plan author ordered Task 1 (JSON) before Task 2 (test) because Task 2 explicitly depends on Task 1. See "Decisions Made" below._

## Files Created/Modified

- `messages/bg.json` — added `auth.register.source.{label, placeholder, qr_letter, email_coalition, sinya_site, facebook, linkedin, referral, news_media, other, otherPlaceholder, otherMaxLengthHelp}` (12 keys) and top-level `attribution.dashboard.{title, subtitle, filters, tables, columns, export, unknown, noData, denied, loginRequired, totalSessions, totalRegistered}` (12 keys, with `filters`/`tables`/`columns` being nested objects).
- `tests/unit/attribution-i18n.test.ts` (NEW) — 2 describe blocks, 8 it() cases asserting label/placeholder shape, all 8 D-10 enum values present + non-empty Bulgarian strings, totals labels present with exact text, unknown label cross-module consistency with `OBLAST_NAMES.unknown`, filter sub-keys present, and tone gate (no vocative `Уважаеми`/`Уважаема`).

## Verbatim list of 8 D-10 enum option labels

For cross-reference with Plan 06 Zod enum (`SelfReportedSourceEnum`):

| Enum key | Bulgarian label |
|---|---|
| `qr_letter` | QR код в писмо |
| `email_coalition` | Имейл от коалицията |
| `sinya_site` | Сайт sinyabulgaria.bg |
| `facebook` | Facebook |
| `linkedin` | LinkedIn |
| `referral` | Препоръка от приятел/колега |
| `news_media` | Новина / медия |
| `other` | Друго |

## Verifications run

| Check | Result |
|---|---|
| `node -e "JSON.parse(...)"` | PASS — bg.json parses cleanly |
| Inline `node -e` keys+counts check (plan §verify) | PASS — source: 12 keys, dashboard: 12 keys |
| `pnpm exec vitest run tests/unit/attribution-i18n.test.ts` | PASS — 8/8 tests green |
| `pnpm exec vitest run` (full suite) | PASS — 62/62 tests across 10 files green |
| `pnpm lint:i18n` | PASS — `PUB-05 OK: no hardcoded Cyrillic in src/` |
| `pnpm exec prettier --write messages/bg.json tests/unit/attribution-i18n.test.ts` | UNCHANGED — already formatted correctly |

## Decisions Made

- **Task ordering:** Followed the plan-prescribed Task 1 → Task 2 order (JSON first, test second) rather than strict-TDD RED-first because Task 2's `<read_first>` explicitly references "messages/bg.json (after Task 1) — assertion target". Per the TDD gate fail-fast rule documented in the executor flow, "if a test passes unexpectedly during the RED phase (before any implementation), STOP" — but here the plan deliberately encodes the test as a regression-lock-in for already-shipped JSON. The plan's own commit type signals this: `feat(...)` for Task 1, `test(...)` for Task 2 (rather than `test → feat → refactor` which would imply true RED-first TDD).
- **Pre-existing failures left unfixed:** `pnpm check:placeholders` reports 3 unresolved coalition placeholders (`landing.hero.headline`, `landing.hero.subheadline`, `agenda.body`) — these are tracked in STATE.md deferred items as `D-CoalitionContent-Hero` and `D-CoalitionContent-Agenda` awaiting coalition copy delivery. Per the executor scope-boundary rule, only directly-caused issues are auto-fixed; these are out of scope for Plan 03.

## Deviations from Plan

None — plan executed exactly as written. All Bulgarian copy matches D-10 verbatim and the Pattern Map's prescribed JSON shape, including the additional `subtitle`, `filters.dateRangeDefault`, `filters.applyFilters`, `filters.clearFilters`, `tables.byUtmSource/byOblast/byQrFlag/bySelfReportedSource`, `columns.qrFlag`, `columns.selfReportedSource`, `noData`, `denied`, `loginRequired` keys that the plan added on top of the leaner `02.1-PATTERNS.md` example.

## Issues Encountered

- **`node_modules` missing in worktree.** The agent worktree was created without dependencies installed. Resolved by running `pnpm install --prefer-offline --ignore-scripts` (4.3 s; pnpm reused the global store cache). Not a deviation — purely environmental setup needed to run prettier/vitest. No impact on planned task content.

## TDD Gate Compliance

The plan declared `type: execute` (not `type: tdd`) at the plan level, so plan-level RED→GREEN→REFACTOR commit-sequence enforcement does not apply. Each task individually carries `tdd="true"` and was verified for its prescribed `<behavior>` and `<verify>` checks before commit. The lock-in vitest test (`9dde06b`) functions as a permanent RED-on-regression gate for both ATTR-06 (enum drift) and ATTR-07 (tone) per the plan's threat register T-02.1-09 and T-02.1-10.

## User Setup Required

None — no external service configuration required for this plan.

## Threat Flags

None — the new keys are pure-display Bulgarian copy + a structural vitest test. No new network endpoints, auth paths, file access patterns, or schema changes. The plan's `<threat_model>` (T-02.1-09 enum drift, T-02.1-10 tone regression) is fully mitigated by Task 2's grep test as designed.

## Next Phase Readiness

- **Plan 06 (RegistrationForm extension)** can now wire `useTranslations('auth.register.source')` and consume all 12 keys without any hardcoded Cyrillic. Its Zod `SelfReportedSourceEnum` MUST list the 8 keys in this exact order — drift will turn the lock-in test red.
- **Plan 07 (Payload attribution dashboard)** can now consume `attribution.dashboard.*` via `loadT('attribution.dashboard')` (worker-style direct JSON load — Payload RSC sits outside next-intl provider per `02.1-PATTERNS.md` § Payload section). The `totalSessions` / `totalRegistered` keys explicitly enable Plan 07's totals line to remain Cyrillic-free in JSX.
- **Plan 02 (oblast names lib)** must keep `OBLAST_NAMES.unknown === "Неизвестен"` — the lock-in test asserts both sides agree.
- No blockers introduced. No changes to STATE.md or ROADMAP.md from this agent (per parallel-executor protocol — orchestrator will record after the wave completes).

## Self-Check: PASSED

- File `messages/bg.json`: FOUND (modified, 51 insertions)
- File `tests/unit/attribution-i18n.test.ts`: FOUND (new, 75 lines)
- Commit `13bb0d0` (Task 1 feat): FOUND in `git log`
- Commit `9dde06b` (Task 2 test): FOUND in `git log`
- All 8 vitest assertions pass green at HEAD
- JSON parses cleanly at HEAD

---
*Phase: 02.1-attribution-source-dashboard*
*Completed: 2026-05-03*
