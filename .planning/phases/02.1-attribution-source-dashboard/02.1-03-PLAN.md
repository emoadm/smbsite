---
phase: 02.1-attribution-source-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/bg.json
  - tests/unit/attribution-i18n.test.ts
autonomous: true
requirements: [ATTR-06, ATTR-07]
tags: [i18n, bg-copy, attribution]

must_haves:
  truths:
    - "messages/bg.json has auth.register.source namespace with 8 dropdown labels + label/placeholder/otherPlaceholder + 'other' triggering free-text (D-09, D-10, D-21)"
    - "messages/bg.json has top-level attribution.dashboard namespace with title/filters/table/export/unknown copy (D-12, D-21)"
    - "All new Bulgarian strings use formal-respectful tone — no Уважаеми, no vocative forms (D-21)"
    - "lint:i18n script (existing Phase 1 / Phase 2 gate) passes — no orphan keys, no missing references"
  artifacts:
    - path: "messages/bg.json"
      provides: "auth.register.source namespace + attribution namespace with all required strings"
      contains: "register.source.qr_letter"
    - path: "tests/unit/attribution-i18n.test.ts"
      provides: "Vitest assertions that the 8 enum-shaped registration source labels exist + dashboard keys + tone (no Уважаеми)"
      exports: []
  key_links:
    - from: "src/components/forms/RegistrationForm.tsx (Plan 06)"
      to: "messages/bg.json#auth.register.source"
      via: "useTranslations('auth.register.source')"
      pattern: "useTranslations\\('auth\\.register\\.source'\\)"
    - from: "src/app/(payload)/admin/views/attribution/page.tsx (Plan 07)"
      to: "messages/bg.json#attribution.dashboard"
      via: "loadT('attribution.dashboard') (worker-style direct JSON load — Payload RSC outside next-intl provider)"
      pattern: "attribution\\.dashboard"
---

<objective>
Add the Bulgarian-language strings for (a) the registration form's "Откъде научихте за нас?" dropdown + 8 fixed options + free-text label, and (b) the editor-facing attribution dashboard's filter labels, table headers, button text, totals labels, and "unknown" fallback. Verify the strings are present and toned correctly.

Purpose: Plan 06 (registration form extension) and Plan 07 (Payload dashboard) both consume strings that must NOT be hardcoded Cyrillic in JSX (project rule, Phase 1 D-27, enforced by `coalition-placeholder` grep gate from Phase 2 plan 02-08). Centralizing them here keeps the rule airtight and makes Wave 2 implementation pure code (no copy decisions).

Output: Two new top-level namespaces in `messages/bg.json` (`auth.register.source.*` already exists conceptually as a sibling of `sectors`/`roles`; `attribution.*` is brand-new), plus a vitest grep test that locks in the 8 enum values + tone discipline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.1-attribution-source-dashboard/02.1-CONTEXT.md
@.planning/phases/02.1-attribution-source-dashboard/02.1-PATTERNS.md
@.planning/phases/02.1-attribution-source-dashboard/02.1-RESEARCH.md
@messages/bg.json
@tests/unit/queue.test.ts

<interfaces>
<!-- Existing JSON shape this plan extends. Sibling under `auth.register` for source.*; new top-level `attribution` for dashboard.* -->

From messages/bg.json (current shape — extending under auth.register.* and adding new top-level attribution.*):
```json
{
  "auth": {
    "register": {
      "title": "Регистрация",
      "fields": { "fullName": "Име и фамилия", "email": "Имейл", "sector": "Сектор", ... },
      "sectors": { "it": "ИТ", "trade": "Търговия", ... },
      "roles":   { "owner": "Собственик", "manager": "Управител", ... }
      // ADD: "source": { ... 8 labels + UI strings ... }
    }
  },
  // ADD top-level: "attribution": { "dashboard": { ... filters, table, export, unknown ... } }
}
```

From tests/unit/queue.test.ts lines 28-43 — pattern for tone-check tests (greppable string assertions on bg.json):
```typescript
const bg = JSON.parse(readFileSync('messages/bg.json', 'utf8')) as Record<string, any>;
const blob = JSON.stringify(bg.email);
expect(blob).not.toMatch(/Уважаеми/);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add auth.register.source namespace + attribution.dashboard namespace to messages/bg.json</name>
  <files>messages/bg.json</files>
  <read_first>
    - messages/bg.json full file — locate `auth.register` block (lines 7-43) and the top-level structure
    - .planning/phases/02.1-attribution-source-dashboard/02.1-CONTEXT.md D-09, D-10, D-21 — 8 locked Bulgarian options, tone rules
    - .planning/phases/02.1-attribution-source-dashboard/02.1-PATTERNS.md § "messages/bg.json" — full new key blocks (verbatim)
    - .planning/phases/02.1-attribution-source-dashboard/02.1-RESEARCH.md § "Pattern" code excerpts for the form
  </read_first>
  <behavior>
    - `messages/bg.json` parses as valid JSON after edits.
    - `bg.auth.register.source.label === "Откъде научихте за нас?"` and the 8 enum values are present as keys with the exact Bulgarian labels per D-10.
    - `bg.attribution.dashboard.title === "Разпределение по канали"` and the filter/table/export sub-objects are populated.
    - No `Уважаеми` / `Уважаема` / `Уважаеми г-не` in any new key (D-21 vocative ban).
    - `bg.attribution.dashboard.unknown === "Неизвестен"` matches the OBLAST_NAMES.unknown value from Plan 02 (consistency).
    - All 8 enum source values match Zod enum that Plan 06 will declare: `qr_letter, email_coalition, sinya_site, facebook, linkedin, referral, news_media, other` (D-11).
    - `bg.attribution.dashboard.totalSessions === "Общо посетители"` and `bg.attribution.dashboard.totalRegistered === "Регистрирани"` exist so Plan 07's AttributionDashboard.tsx totals line is bound via t.totalSessions / t.totalRegistered (Phase 1 D-27 — zero hardcoded Cyrillic in JSX).
  </behavior>
  <action>
Open `messages/bg.json`. **Step 1**: Inside `auth.register` (currently ends at line 42 with the `roles` object), add a new sibling object `source` immediately after `roles` (mirrors the sectors/roles shape). The exact insert (paste verbatim — Bulgarian copy is locked by D-10):

```json
      "source": {
        "label": "Откъде научихте за нас?",
        "placeholder": "Изберете вариант",
        "qr_letter": "QR код в писмо",
        "email_coalition": "Имейл от коалицията",
        "sinya_site": "Сайт sinyabulgaria.bg",
        "facebook": "Facebook",
        "linkedin": "LinkedIn",
        "referral": "Препоръка от приятел/колега",
        "news_media": "Новина / медия",
        "other": "Друго",
        "otherPlaceholder": "Моля, уточнете",
        "otherMaxLengthHelp": "До 300 символа."
      }
```

The resulting `auth.register` object has keys in this order: `title, cta, rateLimited, invalidEmail, captchaFailed, fields, consents, sectors, roles, source`. Make sure the trailing comma after `roles` closing `}` is added (currently absent because roles was the last key).

**Step 2**: Add a NEW top-level `attribution` object as a sibling of `email` (after the `email` block, which currently ends near the end of the file). The exact insert — note the two new totals labels (`totalSessions`, `totalRegistered`) added per Warning W4 / Phase 1 D-27 to keep AttributionDashboard.tsx free of hardcoded Cyrillic:

```json
  "attribution": {
    "dashboard": {
      "title": "Разпределение по канали",
      "subtitle": "Откъде идват посетителите ни — UTM, QR и оператор „откъде научихте“.",
      "filters": {
        "dateRange": "Период",
        "dateRangeDefault": "Последните 7 дни",
        "utmSource": "Канал (UTM source)",
        "oblast": "Област",
        "qrFlag": "Само QR посещения",
        "selfReportedSource": "Откъде са научили",
        "applyFilters": "Приложи филтрите",
        "clearFilters": "Изчисти"
      },
      "tables": {
        "byUtmSource": "По UTM канал",
        "byOblast": "По област",
        "byQrFlag": "По QR флаг",
        "bySelfReportedSource": "По „откъде научихте“"
      },
      "columns": {
        "utmSource": "UTM канал",
        "oblast": "Област",
        "qrFlag": "QR флаг",
        "selfReportedSource": "Откъде са научили",
        "count": "Брой посетители",
        "registered": "От тях регистрирани"
      },
      "export": "Изтегли CSV",
      "unknown": "Неизвестен",
      "noData": "Няма данни за избрания период.",
      "denied": "Нямате права за достъп до тази страница.",
      "loginRequired": "Необходим е вход в системата.",
      "totalSessions": "Общо посетители",
      "totalRegistered": "Регистрирани"
    }
  }
```

Make sure to add a trailing comma after the existing `email` object so the new `attribution` key follows valid JSON syntax.

**Step 3**: Run `pnpm format` to normalize JSON formatting (preserves the changes; only adjusts whitespace). Then verify the file still parses by running `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json', 'utf8'))"` — it must exit with code 0.

  </action>
  <verify>
    <automated>node -e "const bg = JSON.parse(require('fs').readFileSync('messages/bg.json', 'utf8')); if (!bg.auth.register.source.qr_letter) throw 'missing source.qr_letter'; if (!bg.attribution.dashboard.title) throw 'missing attribution.dashboard.title'; if (!bg.attribution.dashboard.totalSessions) throw 'missing attribution.dashboard.totalSessions'; if (!bg.attribution.dashboard.totalRegistered) throw 'missing attribution.dashboard.totalRegistered'; if (Object.keys(bg.auth.register.source).length < 12) throw 'source keys count low: ' + Object.keys(bg.auth.register.source).length; if (Object.keys(bg.attribution.dashboard).length < 12) throw 'dashboard keys count low: ' + Object.keys(bg.attribution.dashboard).length;"</automated>
  </verify>
  <done>
    - messages/bg.json parses as valid JSON.
    - `bg.auth.register.source` has 12 keys (label, placeholder, 8 enum values, otherPlaceholder, otherMaxLengthHelp).
    - `bg.attribution.dashboard` has 12 sub-keys: title, subtitle, filters, tables, columns, export, unknown, noData, denied, loginRequired, totalSessions, totalRegistered.
    - `bg.attribution.dashboard.totalSessions === "Общо посетители"` and `bg.attribution.dashboard.totalRegistered === "Регистрирани"` are present so Plan 07 AttributionDashboard.tsx can bind them via t.totalSessions / t.totalRegistered (zero hardcoded Cyrillic in JSX).
    - All Bulgarian strings exact match D-10 / D-21 / patterns.md.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest tone + key-presence test for new namespaces</name>
  <files>tests/unit/attribution-i18n.test.ts (NEW)</files>
  <read_first>
    - tests/unit/queue.test.ts lines 28-43 — canonical pattern for bg.json tone-check + key-presence
    - .planning/phases/02.1-attribution-source-dashboard/02.1-CONTEXT.md D-21 (tone) + D-10 (locked enum values)
    - messages/bg.json (after Task 1) — assertion target
  </read_first>
  <behavior>
    - All 8 enum keys present under bg.auth.register.source with non-empty Bulgarian strings.
    - Top-level bg.attribution.dashboard exists with required sub-keys including totalSessions + totalRegistered.
    - The serialized JSON of both new namespaces contains zero matches for the vocative forms `Уважаеми`, `Уважаема`, `Уважаеми г-не`.
    - The `unknown` label in dashboard matches `OBLAST_NAMES.unknown` from Plan 02 (`Неизвестен`) — consistency check.
  </behavior>
  <action>
Create `tests/unit/attribution-i18n.test.ts` with the exact content below. This is the lock-in test that future copy edits cannot accidentally break the enum contract or reintroduce vocative tone.

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';

const bg = JSON.parse(readFileSync('messages/bg.json', 'utf8')) as Record<string, any>;

describe('ATTR-06 / D-09 / D-10 — auth.register.source namespace', () => {
  const SOURCE_ENUM = [
    'qr_letter',
    'email_coalition',
    'sinya_site',
    'facebook',
    'linkedin',
    'referral',
    'news_media',
    'other',
  ] as const;

  it('contains label + placeholder + otherPlaceholder', () => {
    const s = bg.auth.register.source;
    expect(s.label).toBe('Откъде научихте за нас?');
    expect(s.placeholder).toBeTruthy();
    expect(s.otherPlaceholder).toBeTruthy();
  });

  it('contains all 8 locked enum values (D-10) with non-empty Bulgarian labels', () => {
    const s = bg.auth.register.source;
    for (const key of SOURCE_ENUM) {
      expect(s[key], `auth.register.source.${key}`).toBeTruthy();
      expect(typeof s[key], `auth.register.source.${key} is string`).toBe('string');
    }
  });

  it('uses formal-respectful tone — no vocative Уважаеми forms (D-21)', () => {
    const blob = JSON.stringify(bg.auth.register.source);
    expect(blob).not.toMatch(/Уважаеми/);
    expect(blob).not.toMatch(/Уважаема/);
  });
});

describe('ATTR-07 / D-12 / D-21 — attribution.dashboard namespace', () => {
  it('has title, filters, columns, tables, export, unknown, denied, loginRequired keys', () => {
    const d = bg.attribution.dashboard;
    expect(d.title).toBeTruthy();
    expect(d.filters).toBeTypeOf('object');
    expect(d.columns).toBeTypeOf('object');
    expect(d.tables).toBeTypeOf('object');
    expect(d.export).toBeTruthy();
    expect(d.unknown).toBeTruthy();
    expect(d.denied).toBeTruthy();
    expect(d.loginRequired).toBeTruthy();
  });

  it('has totalSessions + totalRegistered keys (Phase 1 D-27 — Plan 07 dashboard binds via t.totalSessions / t.totalRegistered, zero hardcoded Cyrillic in JSX)', () => {
    const d = bg.attribution.dashboard;
    expect(d.totalSessions).toBe('Общо посетители');
    expect(d.totalRegistered).toBe('Регистрирани');
  });

  it('unknown label matches OBLAST_NAMES.unknown for cross-module consistency', () => {
    expect(bg.attribution.dashboard.unknown).toBe('Неизвестен');
  });

  it('uses formal-respectful tone — no vocative Уважаеми forms (D-21)', () => {
    const blob = JSON.stringify(bg.attribution.dashboard);
    expect(blob).not.toMatch(/Уважаеми/);
    expect(blob).not.toMatch(/Уважаема/);
  });

  it('declares filter sub-keys: dateRange, utmSource, oblast, qrFlag, selfReportedSource', () => {
    const f = bg.attribution.dashboard.filters;
    for (const key of ['dateRange', 'utmSource', 'oblast', 'qrFlag', 'selfReportedSource']) {
      expect(f[key], `filters.${key}`).toBeTruthy();
    }
  });
});
```

  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/attribution-i18n.test.ts</automated>
  </verify>
  <done>
    - All 8 test cases PASS green (added one new test for totalSessions/totalRegistered keys).
    - Removing any of the 8 enum source keys, or adding `Уважаеми` to either namespace, makes the corresponding test fail RED.
    - Existing Phase 1 / Phase 2 i18n lint scripts (`pnpm lint:i18n`, `pnpm check:placeholders`) still pass — verify with `pnpm lint:i18n` (no new warnings).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| messages/bg.json → JSX render | All public copy lives here; renderers (RegistrationForm, dashboard) call `t(key)`. Hardcoded JSX strings would bypass i18n + grep gates. |
| Bulgarian translator → coalition tone | Tone discipline (D-21 — never vocative) is enforced by Task 2's grep test as a cheap structural check, not a real linguistic review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-09 | Tampering | messages/bg.json enum drift | mitigate | Task 2 grep test asserts the 8 SOURCE_ENUM keys are present. If a coalition edit removes/renames a key, the test goes red and Plan 06's Zod enum is forced to update synchronously. |
| T-02.1-10 | Tampering | tone regression (vocative reintroduced) | mitigate | Task 2 greps for `Уважаеми` / `Уважаема` and fails red on match. Cheap structural check — full linguistic review remains operator-side at coalition sign-off. |
</threat_model>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json'))"` exits 0
- `pnpm vitest run tests/unit/attribution-i18n.test.ts` passes all 8 assertions green
- `pnpm lint:i18n` passes (no new orphan/missing keys)
- `pnpm check:placeholders` passes (no coalition-placeholder violations introduced)
</verification>

<success_criteria>
- bg.auth.register.source has 12 keys including 8 D-10 enum values
- bg.attribution.dashboard has 12 sub-keys including totalSessions + totalRegistered
- All new strings pass the no-vocative tone check
- Plan 06 RegistrationForm and Plan 07 dashboard can consume strings via useTranslations() / loadT() without any hardcoded Cyrillic
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-attribution-source-dashboard/02.1-03-SUMMARY.md` documenting:
- Number of new keys added under each namespace
- Verbatim list of 8 enum option labels (for cross-reference with Plan 06 Zod enum)
- Whether `pnpm lint:i18n` reports any orphans (expected: none — Plan 06 + Plan 07 will reference all keys)
</output>
</content>
</invoke>