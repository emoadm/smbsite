---
phase: 02.1-attribution-source-dashboard
plan: 06
subsystem: registration-attribution
tags: [server-actions, registration, otp, ui]
requires:
  - 02.1-01-SUMMARY (users.self_reported_* + attribution_events.user_id columns)
  - 02.1-03-SUMMARY (auth.register.source.* + auth.register.source.other* keys in bg.json)
provides:
  - Required self_reported_source dropdown (8 options) on RegistrationForm
  - Conditional Друго free-text input (max 300 chars)
  - register Server Action validates + persists self_reported_{source,other}
  - verifyOtp Server Action links anonymous attr_sid → user_id on success (D-07/ATTR-05)
affects:
  - tests/e2e/registration.spec.ts (sibling spec; not modified — new attribution.spec.ts is sibling)
tech-stack-added: []
tech-stack-patterns:
  - "Zod enum sibling of SectorEnum/RoleEnum for self_reported_source"
  - "useState + onValueChange shadcn Select for conditional render"
  - "cookies().get(name)?.value ?? null then conditional db.update for silent-on-miss linkage"
key-files:
  created:
    - tests/unit/register-attribution.test.ts
    - tests/unit/attribution-linkage.test.ts
    - tests/e2e/attribution.spec.ts
  modified:
    - src/app/actions/register.ts (SelfReportedSourceEnum + 2 schema fields + 2 INSERT cols)
    - src/app/actions/verify-otp.ts (attribution_events import + cookieJar.get + conditional UPDATE)
    - src/components/forms/RegistrationForm.tsx (tSource + sourceValue + Select + conditional Input)
key-decisions:
  - "SelfReportedSourceEnum declared adjacent to SectorEnum/RoleEnum — three siblings, easy to grep, mirrors PATTERNS.md"
  - "self_reported_other.optional() (no default) so absence = null in DB rather than empty string — distinct semantics for analytics"
  - "Linkage UPDATE silent on attrSid absence — direct registration without prior landing-page visit is a valid path (D-07)"
  - "Linkage UPDATE placed BEFORE session-token cookie set — atomicity check (test asserts ordering)"
  - "Conditional Друго input rendered via {sourceValue === 'other' && (...)} (no separate component) — keeps form file readable; mirrors existing turnstileStatus useState pattern"
requirements-completed: [ATTR-05, ATTR-06]
duration: 18 min
completed: 2026-05-03
---

# Phase 02.1 Plan 06: Registration Source Field + Attribution Linkage Summary

Wired the "Откъде научихте за нас?" dropdown into the registration funnel: extended the Zod schema and INSERT path in `register` to capture and persist self-reported source + optional Друго free-text; extended `verifyOtp` to link the anonymous attribution row to the now-confirmed user via the `attr_sid` cookie; surfaced the dropdown + conditional free-text in `RegistrationForm` using existing shadcn Select primitives.

## Execution Mode

Inline sequential — same fallback as Plan 04 after Wave 2's parallel subagents stalled on the 600s stream watchdog.

## Tasks

| # | Name | Commit | Tests |
|---|------|--------|-------|
| 1 | register Zod enum + INSERT cols | `76978e7` | 4/4 PASS |
| 2 | verify-otp linkage UPDATE | `48dc123` | 6/6 PASS |
| 3 | RegistrationForm dropdown + conditional Input + e2e spec | `fb47fb9` | typecheck PASS; e2e deferred to local Playwright run |

Total new vitest assertions: 10. Full project unit suite: 97/97 PASS.

## Diff Summaries

**register.ts** — added `SelfReportedSourceEnum` (8 values) above `RegistrationSchema`; schema gains `self_reported_source` (required) + `self_reported_other` (optional max 300); `tx.insert(users).values({})` carries both. All other lines preserved (consents INSERT, OTP gen, addEmailJob).

**verify-otp.ts** — schema barrel import extended with `attribution_events`; after `db.insert(sessions)` and `await cookies()`, conditional `db.update(attribution_events).set({user_id}).where(eq(attribution_events.attr_sid, attrSid))` if `attrSid` present; session-token cookie set after the UPDATE so test ordering assertion holds.

**RegistrationForm.tsx** — added `tSource = useTranslations('auth.register.source')`; new `[sourceValue, setSourceValue]` useState; new `<div>` block with Select (8 SelectItems) + conditional Input rendered when `sourceValue === 'other'`. All other JSX (sector/role Selects, consent checkboxes, Turnstile, submit) unchanged.

## Verification

- `pnpm tsc --noEmit` — 0 errors
- `pnpm vitest run tests/unit/register-attribution.test.ts tests/unit/attribution-linkage.test.ts` — 10/10 PASS
- Full unit suite — 97/97 PASS
- `pnpm exec playwright test tests/e2e/attribution.spec.ts` — deferred to local environment with WebKit installed (current local env is missing the WebKit binary; pre-existing — not a Wave 2 regression)

## Pre-existing Issues Noted

- `pnpm check:placeholders` fails on 3 coalition placeholders (`landing.hero.headline`, `landing.hero.subheadline`, `agenda.body`) — already tracked as `D-CoalitionContent-Hero` and `D-CoalitionContent-Agenda`. Not introduced by this plan.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

Ready for Plan 02.1-05 (`/api/attr/init` route + `<AttrInit/>` client mount) and Plan 02.1-07 (Payload admin dashboard).
