# Phase 04.1 Plan 04 — SUMMARY

**Plan:** 04.1-04 — Operator Neon SQL apply (manual gate)
**Status:** COMPLETE
**Branch:** main (acceptance_criteria deviation — see §Deviations)
**Tasks:** 2 / 2

---

## Tasks

| # | Type | Outcome | Commit |
|---|------|---------|--------|
| 1 | auto (write runbook) | OPS-RUNBOOK.md, 290 lines, all 8 acceptance-criteria proofs pass | `8778d7e` |
| 2 | checkpoint:human-action (apply backfill SQL to prod) | APPLY-LOG.md, all 7 STEP-0 and 6 STEP-7 verification gates pass | this commit |

---

## Wave 4 outcome (operator apply)

- **Apply window:** 2026-05-12 05:30–05:37 UTC (~7 minutes wall-clock, single Neon SQL Editor session)
- **Backfill SQL applied:** `.planning/ops/2026-05-12-payload-schema-phase-04-backfill.sql` (commit `3ef28cd`)
- **Operator method:** paste-and-run per STEP via Neon SQL Editor browser UI
- **STEP 0 (pre-apply read-only):** 6/6 expectations matched. No prod-state surprises.
- **STEP 1–6 (write DDL):** all returned `statement executed successfully` with zero ERROR / WARNING. `payload_kv` block was a documented no-op (pre-existing).
- **STEP 7 (post-apply read-only):** 6/6 verification gates pass:
  - 7a `pages`, `payload_ideas`, `payload_kv` all present
  - 7b `pages_id (integer, YES)` + `payload_ideas_id (integer, YES)` columns on `payload_locked_documents_rels`
  - 7c **0 rows** on `payload_preferences_rels` — proves the asymmetric column add (DELTA.md §5/§11 D-1) held in prod
  - 7d enum now [admin, editor, super_editor]
  - 7e 5 FK constraints (incl. new `…_pages_fk` and `…_ideas_fk`)
  - 7f 9 Drizzle tables untouched
- **Sign-off:** committed in APPLY-LOG.md `## Sign-off` section at 2026-05-12 05:37 UTC.

---

## Deviations

1. **Branch placement (carried forward from Plans 04.1-02 / 04.1-03 / Task 1 of this plan):** plan acceptance_criteria called for commit on `gsd/phase-04-user-submissions-editorial-moderation`; actual commits on `main` because `.planning/phases/04.1-*/` directory only exists on main. Same convention as Plans 04.1-02 / 04.1-03. The Phase 4 branch (`gsd/phase-04-*`) holds source-code changes only; `.planning/` artifacts live on main.

2. **APPLY-LOG.md authored by orchestrator from operator's pasted Neon output (not by operator directly).** PLAN.md Task 2's template was for the operator to fill in by hand. In practice, the operator pasted Neon's JSON-array output blocks back into the chat; the orchestrator parsed them, formatted them into the markdown template, and wrote the file. Substance identical; medium changed. Recorded here for auditability.

3. **`payload_kv` already in prod pre-apply (anticipated; STEP 3c no-op).** Per DELTA.md §11 D-4 the SQL defensively included `CREATE TABLE IF NOT EXISTS payload_kv` for prod-version uncertainty. Prod's v52 image had already booted Payload v3.84 at least once and created the table, so STEP 3c's CREATE TABLE was skipped (no error) and STEP 5d's `CREATE UNIQUE INDEX IF NOT EXISTS payload_kv_key_idx` likewise. The idempotent design held as intended.

4. **`__rels` (double underscore) NOT observed in prod.** The Fly crash log's failing query string `column payload_locked_documents__rels.pages_id does not exist` was a render artifact — STEP 0d confirms the actual table name is single-underscore `payload_locked_documents_rels`. Canonical-schema.sql assumption holds; no SQL revision needed.

---

## Commits

| File | SHA | Branch | Subject |
|------|-----|--------|---------|
| 04.1-OPS-RUNBOOK.md | `8778d7e` | main | `docs(04.1-04): operator runbook — Neon SQL Editor apply + rollback playbook [skip ci]` |
| 04.1-APPLY-LOG.md + 04.1-04-SUMMARY.md | (this commit) | main | `ops(04.1-04): record prod Neon schema-apply outcome (Wave 4 manual gate signed off) [skip ci]` |

---

## Wave 5 readiness

✓ Prod Neon schema now matches `canonical-schema.sql` for the Pages + Ideas-as-payload_ideas additions.
✓ The originally-failing query (`SELECT … payload_locked_documents_rels.pages_id …`) will now resolve correctly.
✓ No destructive operations applied; the additive-only schema is forward- and backward-compatible (v52 image does not consume the new tables; future re-deploy will see them and use them).

Wave 5 (Plan 04.1-05) can proceed: revert-the-revert of `244eb85` on main → deploy.yml fires → operator approves the `migrate` job (no-op, gated per D-23) → flyctl rolling deploy → post-deploy smoke gate.

---

## Files touched by this plan

| Path | Mode | Owner |
|------|------|-------|
| `.planning/phases/04.1-payload-schema-reconciliation/04.1-OPS-RUNBOOK.md` | create | Task 1 (auto) |
| `.planning/phases/04.1-payload-schema-reconciliation/04.1-APPLY-LOG.md` | create | Task 2 (operator + orchestrator transcription) |
| `.planning/phases/04.1-payload-schema-reconciliation/04.1-04-SUMMARY.md` | create | this file |
| prod Neon schema | mutate (additive) | Task 2 (operator-driven via Neon SQL Editor) |
