# Phase 04.1 Plan 05 — SUMMARY

**Plan:** 04.1-05 — Revert-of-revert + push + deploy (manual gate)
**Status:** COMPLETE
**Branch:** main
**Tasks:** 2 / 2

---

## Tasks

| # | Type | Outcome | Commit |
|---|------|---------|--------|
| 1 | checkpoint:human-action (orchestrator-driven git ops, operator-approved) | Revert-of-revert + cherry-pick landed on main; 478/478 unit tests + typecheck + lint clean; pushed to origin/main | `5d6f289` + `c1e3d7f` |
| 2 | checkpoint:human-action (operator gate approval + verification) | deploy.yml all-green, smoke gate passed, `/admin/login` renders in fresh incognito | n/a (verification outcome) |

---

## Wave 5 outcome

### Git operations (Task 1)

- **Pre-flight:** Wave 4 sign-off line (`Phase 04.1 Wave 4 schema apply COMPLETE`) confirmed in `04.1-APPLY-LOG.md`. Local `main` workspace returned to clean state after discarding two SDK-side bookkeeping diffs (`gsd-sdk query state.begin-phase` had mangled STATE.md frontmatter — see §Deviations).
- **Revert-the-revert:** `git revert 244eb85 --no-edit` → new commit `5d6f289` ("Revert "revert: rollback merge of PR #2 (deaadc0) …""). 94 files changed, +8696 / -164 — restores the full Phase 4 + Phase 02.3 surface (Pages.ts, Ideas.ts, submissions.ts schema, oblast SVG, all moderation-queue + DSA UI, all integration tests, agenda Phase 02.3 content).
- **Cherry-pick Plan 04.1-01:** `git cherry-pick 294deb91` (the dbName commit from `gsd/phase-04-user-submissions-editorial-moderation`) → new commit `c1e3d7f` ("fix(04.1-01): Ideas collection uses dbName: 'payload_ideas' to avoid Drizzle table collision"). 2 files, +19. Verifies: `dbName: 'payload_ideas'` present 1× in `src/collections/Ideas.ts`; `payload_ideas` present 2× in `tests/unit/ideas-collection.test.ts`.
- **Local sanity (Step 1.4):**
  - `pnpm test:unit -- ideas-collection`: 478 / 478 passed across 64 files (vitest's `--` filter ran the full suite rather than just the targeted file — stricter than the plan's "8 passing tests" gate).
  - `pnpm typecheck`: clean (zero `tsc` output).
  - `pnpm lint`: clean (only pre-existing warnings — unused eslint-disable directives in 5 files + the documented `beforeInteractive` cookie banner warning + the Sentry `onRouterTransitionStart` advisory; all pre-date this plan).
- **Push:** `git push origin main` → fast-forward `a42b779..c1e3d7f` (10 commits — 2 code + 8 `[skip ci]` planning). deploy.yml fired on commit `c1e3d7f`.

### Deploy + verification (Task 2)

- **Workflow run:** <https://github.com/emoadm/smbsite/actions/runs/25716160705>
- **deploy.yml jobs:** all 3 green (verify-eu-dsn, migrate, deploy). Operator approved the `migrate` job's production-environment required-reviewer gate per D-23. `migrate` was a no-op (Drizzle ledger 0003 backfilled 2026-05-11 via `.planning/ops/2026-05-11-drizzle-ledger-backfill-0003.sql`; Payload migrate skipped per tsx ESM incompat).
- **New Fly image:** `registry.fly.io/smbsite-prod:deployment-01KRDC046F0X6X8W3MPAXYYJMC` (380 MB). Release number: v53 (inferable as next-after-v52; operator can confirm via `flyctl releases --app smbsite-prod` if needed for release-tracking artifacts).
- **Post-deploy smoke gate output (verbatim):**

  ```
  OK / (attempt 1): 200 92609B
  OK /register (attempt 1): 200 77971B
  OK /login (attempt 1): 200 66323B
  OK: post-deploy smoke gate passed (3 paths × ≤3 attempts).
  ```

- **`/admin/login` first-success:** ~2026-05-12 09:30 EEST (~06:30 UTC) in a fresh incognito browser session. The originally-failing surface — the query `column payload_locked_documents__rels.pages_id does not exist` — now resolves correctly against the Wave-4-backfilled prod Neon schema. The 2026-05-11 schema-drift incident is closed.

---

## Side outcome — ci.yml workflow

A separate `ci.yml` (Playwright/test suite) workflow ran on the same push and failed. Operator reported this without detail. Per STATE.md deferred items, the expected baseline failures are:

- `D-Phase5-E2E-SeedingHarness` — 4 admin/newsletter specs hard-fail without seeded editor/member env vars
- `D-PlaywrightBatchC-Други-Label` — registration form's free-text "Други" input lacks `<Label htmlFor>` (a11y regression)
- `D-PlaywrightBatchD-LandingCacheControl` — landing page emits `Cache-Control: private, no-cache` from origin (because `<Header/>` calls `auth()`); spec expects `s-maxage=3600`

If the ci.yml failure matches this set, no action needed for Plan 04.1-05. If a NEW spec failed (regression introduced by the Phase 4 / Phase 02.3 code that was previously hidden by the v52 rollback), it surfaces as a Plan 04.1-06 input — to be investigated when Wave 6's E2E smoke runs.

**Open follow-up:** operator to paste the ci.yml run URL + failed-spec list during Plan 04.1-06 execution so the orchestrator can diff against the known baseline. Not blocking — the deploy gate's success criteria are met independently of ci.yml.

---

## Deviations

1. **SDK-side bookkeeping discarded before revert.** The workflow's `gsd-sdk query state.begin-phase` call (run at orchestrator start) mechanically rewrote STATE.md frontmatter: incremented `completed_phases` to 7 prematurely (04.1 isn't done yet), reset `Plan: 1 of 6` (we were starting Wave 5, not Wave 1), and overwrote the rich "Current focus" paragraph with a generic one-liner — losing the v52 rollback context and Phase 6 next-up note. A parallel `_auto_chain_active=false` write touched `config.json` with whitespace-only churn. Both diffs were discarded with `git checkout --` to keep the revert commit clean. STATE.md will be updated by hand at Phase 04.1 close-out with accurate outcomes. Recorded here for SDK-behavior auditability.

2. **Orchestrator drove the git operations (operator-approved deviation from PLAN.md's `checkpoint:human-action` framing).** The plan was written as operator-driven (commands handed off). The operator chose "I drive the git steps (Recommended)" at the wave-entry checkpoint; orchestrator executed `git revert`, `git cherry-pick`, sanity checks, and `git push`. The operator handled the irreversible-to-prod parts: GitHub Actions migrate-gate approval + browser session for `/admin/login`. Substance identical; medium changed. Speed gain ~10 minutes vs. command-paste mode.

3. **`pnpm test:unit -- ideas-collection` ran the full suite, not just the targeted file.** Vitest's `--` filter syntax didn't isolate the file (probable cause: the scripts/test:unit setup uses a multi-arg `vitest` wrapper that consumes `--`). All 478 tests across 64 files passed — strictly stronger evidence than the plan's "8 passing tests in ideas-collection" gate. No action needed.

4. **Release number not captured as `vNN`.** Operator provided the immutable image SHA (`registry.fly.io/smbsite-prod:deployment-01KRDC046F0X6X8W3MPAXYYJMC`) but not the human-friendly v-number from `flyctl releases`. v53 is the next-after-v52 inference, but the image SHA is the pinnable artifact for any rollback. No functional gap; operator can backfill the v-number in OPS-RUNBOOK if needed for release-history.

---

## Verification (against `<verification>` block in PLAN.md)

- ✓ New commit on origin/main reverting `244eb85` (subject starts with `Revert "revert:`) — `5d6f289`
- ✓ Second commit applies Plan 04.1-01 dbName change — `c1e3d7f`, `Ideas.ts` gets `dbName: 'payload_ideas'`
- ✓ GitHub Actions workflow run completed all three jobs (verify-eu-dsn, migrate, deploy) with exit 0 — run 25716160705
- ✓ Post-deploy smoke gate output shows OK for `/`, `/register`, `/login` — verbatim above
- ✓ `https://chastnik.eu/admin/login` returns 200 in a fresh browser session — confirmed ~09:30 EEST 2026-05-12
- ◯ `git diff origin/main..gsd/phase-04-user-submissions-editorial-moderation -- src/collections/Ideas.ts` returns empty — not run as gate (the cherry-pick made the diff empty by construction; trivially satisfied)

---

## Commits

| File | SHA | Branch | Subject |
|------|-----|--------|---------|
| (94 files — Phase 4 + Phase 02.3 source restoration) | `5d6f289` | main → origin/main | `Revert "revert: rollback merge of PR #2 (deaadc0) …"` |
| `src/collections/Ideas.ts` + `tests/unit/ideas-collection.test.ts` | `c1e3d7f` | main → origin/main | `fix(04.1-01): Ideas collection uses dbName: 'payload_ideas' …` |
| `04.1-05-SUMMARY.md` | (this commit) | main | `docs(04.1-05): SUMMARY — Wave 5 revert+deploy outcome [skip ci]` |

---

## Wave 6 readiness

✓ Phase 4 + Phase 02.3 application code is live on prod against the Wave-4-backfilled schema.
✓ The originally-failing `/admin/*` surface renders correctly.
✓ Smoke gate confirmed the three foundation paths (/`, `/register`, `/login`) serve 200s with >100-byte bodies.
✓ The bug-free re-deploy resolves the 2026-05-11 incident at the application-code layer.

Wave 6 (Plan 04.1-06) can proceed: operator runs through the six functional smokes from CONTEXT.md §7 + ROADMAP Phase 04.1 success criteria #5, records outcomes in `04.1-VERIFICATION.md`, and the phase closes out.

**Carry-forward for Wave 6:** if the ci.yml failure on this push surfaces a NEW spec (not in the documented `D-Phase5-E2E-SeedingHarness` / `D-PlaywrightBatchC` / `D-PlaywrightBatchD` baseline), that becomes a Wave 6 input. Otherwise Wave 6 only tests the manually-verified functional surfaces.

---

## Files touched by this plan

| Path | Mode | Owner |
|------|------|-------|
| (94 files across `src/`, `public/`, `tests/`) | restore (via revert) | Task 1 (orchestrator-driven, operator-approved) |
| `src/collections/Ideas.ts` | mutate | Task 1 (cherry-pick) |
| `tests/unit/ideas-collection.test.ts` | mutate | Task 1 (cherry-pick) |
| origin/main | append 2 commits | Task 1 (push) |
| Fly.io smbsite-prod app (image) | new release (v53 / `deployment-01KRDC046F0X6X8W3MPAXYYJMC`) | Task 2 (deploy.yml) |
| Neon prod | no schema change (Wave 4 already applied) | n/a |
| `.planning/phases/04.1-payload-schema-reconciliation/04.1-05-SUMMARY.md` | create | this file |
