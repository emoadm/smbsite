---
phase: 04.1-payload-schema-reconciliation
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - .planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md
autonomous: false
manual: true
requirements: []
unblocks:
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EDIT-05
  - EDIT-06
  - EDIT-07
tags: [phase-04.1, wave-6, BLOCKING, manual, smoke-test, ops-recovery]
must_haves:
  truths:
    - "The originally-failing surface `/admin/login` returns 200 on prod and login succeeds with operator super_editor credentials"
    - "`/admin/views/moderation-queue` renders the queue UI for a super_editor session (no Next.js server-side exception)"
    - "An end-to-end approve flow works: editor approves a Phase 4 test submission (proposal kind) → `submissions.status` flips from 'pending' to 'approved' in prod Neon → BullMQ status email arrives at submitter's inbox → one new row appears in `moderation_log` with `action='submission_approve'`, non-NULL `actor_user_id` matching the editor's user id, `target_kind='submission'`, `target_id` matching the submission UUID"
    - "`/predlozheniya` returns 200 (Phase 4 D-B1 read-only public proposals catalog — surface restored)"
    - "`/problemi` returns 200 (Phase 4 D-D1 problem-report heat-map — surface restored)"
    - "`/agenda` returns 200 and renders all 12 chapters of the Phase 02.3 content (Phase 02.3 surface restored)"
    - "An operator-signed verification document at `.planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md` records each must-have outcome with timestamps + the prod row ids/SQL outputs / email-arrival evidence"
  artifacts:
    - path: .planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md
      provides: "Operator-signed end-to-end smoke record. Closes Phase 04.1 acceptance per CONTEXT.md §7."
  key_links:
    - from: "(operator browser session on prod)"
      to: "Phase 4 + Phase 02.3 user-facing surfaces"
      via: "manual click-through smoke per the runbook §5"
      pattern: "200 OK"
    - from: "Phase 4 approveSubmission Server Action"
      to: "prod Neon submissions + moderation_log tables, BullMQ status-email queue, Brevo transactional email"
      via: "End-to-end approve flow exercising the Phase 4 critical path"
      pattern: "INSERT INTO moderation_log"
---

<objective>
Operator end-to-end smoke gate. Run through the six functional smokes from CONTEXT.md §7 + ROADMAP Phase 04.1 success criteria #5, recording outcomes in `.planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md`. This closes Phase 04.1 — without this gate, the phase is "code deployed" but not "functionality confirmed."

Purpose: The 2026-05-11 incident proved that a green CI + smoke gate (`/`, `/register`, `/login`) is necessary-but-not-sufficient for Phase 4 success. The originally-failing surface was `/admin/*` — which the deploy.yml smoke gate does NOT probe. This plan runs the operator through the actual Phase 4 functional paths (admin login, moderation queue, approve flow, public surfaces) so we have observed evidence the system works, not just observed evidence the homepage loads.

Output: A signed-off verification document. STATE.md update marking Phase 04.1 closed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.1-payload-schema-reconciliation/04.1-CONTEXT.md
@.planning/phases/04.1-payload-schema-reconciliation/04.1-OPS-RUNBOOK.md
@.planning/phases/04.1-payload-schema-reconciliation/04.1-APPLY-LOG.md
@.planning/phases/04-user-submissions-editorial-moderation/04-OPS-RUNBOOK.md

<interfaces>
<!-- Operator click-through paths — verbatim per CONTEXT.md §7 + ROADMAP Phase 04.1 success criteria. -->

Smoke 1 — Admin login renders:
1. Open a fresh incognito browser session.
2. Navigate to https://chastnik.eu/admin/login
3. Expected: page loads with email + password fields, no 500 error, no "column ... does not exist" message in browser console or network tab.
4. Log in with super_editor credentials (operator's account — bootstrapped per 04-OPS-RUNBOOK.md §1).
5. Expected: redirect to /admin (Payload admin dashboard) with the side nav showing collections (Users, Newsletters, Pages, Ideas) and custom views (Attribution, Moderation Queue).

Smoke 2 — Moderation queue UI:
1. From the admin dashboard, click "Moderation Queue" in the side nav (path: /admin/views/moderation-queue).
2. Expected: the queue table renders with Tabs (Pending / Approved / Rejected / Hidden), Table columns (Submitter, Kind, Title/Body excerpt, Topic, Created, Action), and a Review button per row.
3. If the queue is empty: confirm by inspection. To create a test row, complete Smoke 3 first.

Smoke 3 — End-to-end approve flow:
1. As a regular member (NOT the editor — use a separate test account, or log out + register a new one):
   a) Navigate to https://chastnik.eu/predlozheniya/нов (or whatever the proposal-submission slug is — verify from `src/app/(frontend)/predlozheniya/` route structure).
   b) Submit a proposal: title="04.1 test предложение", body="Test от Phase 04.1 verification", topic="economy".
   c) Confirm Turnstile passes, server responds with 200, and you see the "submitted" confirmation page.
2. Log out. Log in as the super_editor.
3. Navigate to /admin/views/moderation-queue.
4. Find the new test proposal (most recent in the Pending tab).
5. Click "Прегледай заявката" → ReviewDialog opens.
6. Click "Одобри" → ConfirmActionDialog opens → confirm with optional moderator note ("Phase 04.1 verification smoke").
7. Expected: dialog closes, queue refreshes, proposal moves out of Pending tab.
8. Verify in prod Neon SQL Editor (read-only):
   ```sql
   SELECT id, status, title, reviewer_id, reviewed_at, approved_at
   FROM submissions
   WHERE title = '04.1 test предложение'
   ORDER BY created_at DESC LIMIT 1;
   ```
   Expected: 1 row, status='approved', reviewer_id=<editor user id>, reviewed_at non-NULL, approved_at non-NULL.

   ```sql
   SELECT action, actor_user_id, target_kind, target_id, note, created_at
   FROM moderation_log
   WHERE created_at > NOW() - INTERVAL '10 minutes'
   ORDER BY created_at DESC;
   ```
   Expected: 1 row with action='submission_approve', actor_user_id=<editor user id> (NOT NULL), target_kind='submission', target_id=<the submission UUID from previous query>.
9. Check the submitter's email inbox (use a test email you control):
   Expected: email arrives within ~30s with subject "Вашето предложение беше одобрено" (per 04-OPS-RUNBOOK.md §8).
   If after 2 min no email: check Sentry for worker errors, check Brevo dashboard transactional log.

Smoke 4 — `/predlozheniya` returns 200:
1. Navigate to https://chastnik.eu/predlozheniya (anonymous browser session — log out first or use a different browser).
2. Expected: page loads, shows the approved test proposal from Smoke 3 (after page revalidation — may need to hard-refresh if ISR caching delays).
3. Confirm the byline is anonymous ("Член на коалицията" or "Анонимен сигнал" per D-C1 — exact label TBD by Phase 4 implementation).

Smoke 5 — `/problemi` returns 200:
1. Navigate to https://chastnik.eu/problemi (anonymous).
2. Expected: heat-map page renders. Bulgaria SVG + per-oblast counts visible (counts may be 0 if no problem reports approved yet — that's OK; we're testing the page renders without crashing, not that there's data).

Smoke 6 — `/agenda` returns 200 (Phase 02.3 restoration):
1. Navigate to https://chastnik.eu/agenda (anonymous).
2. Expected: full coalition program renders with all 12 chapters from the TOC (manifesto, Десен консенсус, Икономика, Енергетика, Земеделие, Здравеопазване, Външна сигурност и отбрана, Образование, Социална политика, Култура, Правосъдие, Защита на ценностите). Click each TOC entry; confirm anchor jumps work.
3. Confirm the draftAlert `<Alert>` banner is absent (Phase 02.3 final-slice removed it).
4. Confirm typography renders (Bulgarian H2s, ordered lists for numbered policy items).
</interfaces>

<critical_constraints>
- This plan runs against PROD. Use a CLEARLY-LABELED test submission (e.g., title="04.1 test предложение") so editors can identify and delete it after smoke completion if desired.
- The test submitter account should be one the operator controls and can delete cleanly. Do NOT use the operator's primary super_editor account as the submitter (the moderation actions must be performed by a DIFFERENT user — `submitter_id != reviewer_id` is a Phase 4 invariant).
- If Smoke 1 fails (admin login 500s), STOP. Follow OPS-RUNBOOK.md §6 (rollback to v52). The Wave 5 deploy must have introduced a NEW failure — surface back to planner.
- If Smoke 3 step 8 (moderation_log row check) returns `actor_user_id IS NULL`, the editor's `admin_users` row email does not match a `users` row email. Follow 04-OPS-RUNBOOK.md §1 to bootstrap the operator's super_editor row correctly. Phase 4 PATTERNS.md §Two-Identity-Model is the underlying constraint.
- Do NOT click "Suspend account" or any irreversible destructive action during smoke. Approve flow is the only mutation exercised.
- Test data cleanup is optional. The test submission can stay in `submissions` (status='approved') indefinitely — no GDPR concern (test submitter consented).
</critical_constraints>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: OPERATOR — run Smokes 1-6, record outcomes in 04.1-VERIFICATION.md</name>
  <what-built>
    Wave 5 deployed Phase 4 + Phase 02.3 code to prod successfully (`/admin/login` returns 200, smoke gate green). This task confirms the system actually WORKS end-to-end by exercising the originally-failing surfaces + the Phase 4 critical path (approve flow).
  </what-built>
  <how-to-verify>
    Open `.planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md` and create it with this skeleton, filling in outcomes as you go:

    ```markdown
    # Phase 04.1 — End-to-End Verification

    **Operator:** <handle>
    **Verification window:** <YYYY-MM-DD HH:MM>–<HH:MM> UTC
    **Prod release:** Fly.io smbsite-prod release v<NN> (from Plan 04.1-05 SUMMARY)
    **Build SHA:** <commit SHA of the revert-of-revert from Plan 04.1-05>

    ## Smoke 1 — /admin/login renders + super_editor login succeeds
    - Timestamp: <UTC>
    - URL tested: https://chastnik.eu/admin/login
    - HTTP status (first GET, anonymous): <200/500/...>
    - Browser console: <"no errors" OR list any errors>
    - Login result with super_editor creds: <"succeeded, redirected to /admin" OR "failed: <message>">
    - Side nav contents confirmed: <Users, Newsletters, Pages, Ideas, Attribution, Moderation Queue — all visible>
    - **Outcome:** PASS / FAIL

    ## Smoke 2 — /admin/views/moderation-queue renders
    - Timestamp: <UTC>
    - Tabs visible: Pending, Approved, Rejected, Hidden (yes/no)
    - Table columns: Submitter, Kind, Title, Topic, Created, Action (yes/no)
    - Browser console: <"no errors" OR list>
    - **Outcome:** PASS / FAIL

    ## Smoke 3 — End-to-end approve flow
    - Timestamp (submission): <UTC>
    - Test submitter account: <email>
    - Submission title: "04.1 test предложение"
    - Submission UUID (from server response or queue): <UUID>
    - Editor account: <email>
    - Approve timestamp: <UTC>
    - Email arrival timestamp: <UTC>
    - submissions row check (paste verbatim SQL output):
    ```
    <paste verbatim>
    ```
    - moderation_log row check (paste verbatim SQL output):
    ```
    <paste verbatim>
    ```
    - actor_user_id NOT NULL: <yes/no>
    - **Outcome:** PASS / FAIL

    ## Smoke 4 — /predlozheniya returns 200
    - Timestamp: <UTC>
    - URL: https://chastnik.eu/predlozheniya
    - HTTP status: <200/...>
    - Test proposal visible: <yes/no>
    - Byline check (anonymous per D-C1): <"Член на коалицията" visible — no real name leak>
    - **Outcome:** PASS / FAIL

    ## Smoke 5 — /problemi returns 200
    - Timestamp: <UTC>
    - URL: https://chastnik.eu/problemi
    - HTTP status: <200/...>
    - Bulgaria heat-map SVG renders: <yes/no>
    - Browser console: <"no errors" OR list>
    - **Outcome:** PASS / FAIL

    ## Smoke 6 — /agenda renders all 12 chapters (Phase 02.3 restoration)
    - Timestamp: <UTC>
    - URL: https://chastnik.eu/agenda
    - HTTP status: <200/...>
    - TOC entries counted: <NN — expected 12>
    - draftAlert banner present: <no — confirmed removed per Phase 02.3>
    - Sample anchor jumps tested: <"Икономика" → scrolled to #ikonomika, "Защита на ценностите" → scrolled correctly>
    - **Outcome:** PASS / FAIL

    ## Sign-off
    <Timestamp UTC>, <operator handle>:
    All six smokes PASS. Phase 04.1 closed. Phase 4 + Phase 02.3 functionality confirmed on prod.

    ## Deviations / observations
    <"none" OR enumerate any non-blocking observations (e.g., "test email arrived after 90s instead of expected 30s — within tolerance")>
    ```

    Commit the verification doc:
    ```bash
    git add .planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md
    git commit -m "docs(04.1): end-to-end smoke complete — all 6 surfaces PASS, Phase 04.1 closed"
    git push origin main
    ```

    **Resume signal:** Type "verification complete — all 6 smokes PASS" once VERIFICATION.md is committed. If ANY smoke failed, type "smoke FAILED at #<N> — <description>" and surface back to planner.
  </how-to-verify>
  <files>.planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md</files>
  <resume-signal>Type "verification complete — all 6 smokes PASS, VERIFICATION.md committed to main" once every smoke returned the expected outcome AND the doc has been committed + pushed. If any smoke fails, type the smoke number + symptom and follow OPS-RUNBOOK §6 (rollback to v52) only if Smoke 1 fails — non-Smoke-1 failures can be triaged without rollback since admin is at least operational.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md + ROADMAP.md to mark Phase 04.1 closed</name>
  <read_first>
    - `.planning/STATE.md` (current `stopped_at` references Phase 04.1; needs to advance)
    - `.planning/ROADMAP.md` (Phase 04.1 entry currently `[ ]`; needs to flip to `[x]`)
    - `.planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md` (Task 1 output — references the sign-off)
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Step 1 — Update ROADMAP.md:

    Find the line:
    ```
    - [ ] **Phase 04.1: Payload Schema Reconciliation** *(INSERTED, ops-recovery)* - ...
    ```
    Change `[ ]` to `[x]` and append ` · *Completed: 2026-05-12*` (or actual completion date).

    Find the Phase 04.1 section header (`### Phase 04.1: Payload Schema Reconciliation (INSERTED, ops-recovery)`) and update the `**Plans:**` line:
    Replace `Plans: TBD (run /gsd-plan-phase 04.1 ...)` with:
    ```
    **Plans:** 6 plans

    Plans:
    - [x] 04.1-01-PLAN.md — Ideas collection dbName: 'payload_ideas' override (resolves Drizzle-vs-Payload collision per CONTEXT §3 LOCKED option-a)
    - [x] 04.1-02-PLAN.md — Generate canonical Payload schema dump (pg_dump from fresh-Postgres + Payload push:true boot) + reproducible-procedure note
    - [x] 04.1-03-PLAN.md — Compute schema delta + write idempotent operator-facing backfill SQL (mirrors 0003 ledger-backfill pattern; 2x apply tested)
    - [x] 04.1-04-PLAN.md — [BLOCKING manual] Apply backfill SQL to prod Neon via SQL Editor; record outcome in APPLY-LOG.md
    - [x] 04.1-05-PLAN.md — [BLOCKING manual] git revert 244eb85 on main; cherry-pick Plan 04.1-01 dbName change; approve migrate gate; confirm post-deploy smoke
    - [x] 04.1-06-PLAN.md — [BLOCKING manual] End-to-end smoke on prod: /admin/login, /admin/views/moderation-queue, approve flow with moderation_log verification, /predlozheniya, /problemi, /agenda (12 chapters)
    ```

    Update the table in the Progress section:
    ```
    | 04.1. Payload Schema Reconciliation | 6/6 | Complete — Phase 4 + Phase 02.3 re-deployed cleanly; admin shell operational; approve flow verified end-to-end | 2026-05-12 |
    ```

    Update the Phase 4 entry note:
    Find: `*code-shipping complete on `gsd/phase-04-user-submissions-editorial-moderation` (PR #2); deployed + ROLLED BACK 2026-05-11/12 ...; re-deploy blocked on Phase 04.1*`
    Replace with: `*code-shipping complete and DEPLOYED 2026-05-12 after Phase 04.1 schema reconciliation; all 14 Phase 4 reqs (PROP-01..04, PROB-01..05, EDIT-03..07) operational on prod*`

    Also update the Phase 4 table row:
    ```
    | 4. User Submissions + Editorial | 8/8 | Code shipped + DEPLOYED 2026-05-12 after Phase 04.1 schema reconciliation | 2026-05-12 |
    ```

    Step 2 — Update STATE.md:

    Update the YAML frontmatter:
    ```yaml
    stopped_at: "Phase 04.1 CLOSED 2026-05-12 — Payload schema reconciled, Phase 4 + Phase 02.3 re-deployed cleanly; next active phase is Phase 6 (GDPR self-service) per ROADMAP / D-LawyerTrack continues to pause Phase 3"
    last_updated: "2026-05-12T<HH:MM>:00.000Z"
    last_activity: "2026-05-12 — Phase 04.1 6/6 plans complete; backfill SQL applied to prod Neon; revert-of-revert deployed; end-to-end smoke PASS (all 6 surfaces); Phase 4 EDIT-* surfaces now operational on prod"
    progress:
      completed_phases: 7    # 6 → 7
      completed_plans: 62    # 56 → 62 (6 new Phase 04.1 plans)
      percent: 70            # 60 → 70
    ```

    Update the "Current focus" line: replace the Phase 04.1 in-progress description with: "Phase 04.1 CLOSED 2026-05-12. Phase 6 (GDPR self-service) is the next product-track phase. Phase 3 remains paused under D-LawyerTrack."

    Update "Current Position": "Phase: Phase 04.1 — Payload Schema Reconciliation (CLOSED 2026-05-12) → next active phase pending. Status: 6/6 plans complete. End-to-end smoke PASS — see `.planning/phases/04.1-payload-schema-reconciliation/04.1-VERIFICATION.md`."

    Add a new entry to "Roadmap Evolution" (top of the list, most recent first):
    ```
    - 2026-05-12 (Phase 04.1 CLOSE): All 6 Phase 04.1 plans complete. Backfill SQL applied to prod Neon via SQL Editor (`04.1-APPLY-LOG.md` sign-off); revert-of-revert pushed to main; deploy.yml green; post-deploy smoke gate PASS; admin shell renders without the `column payload_locked_documents__rels.pages_id does not exist` regression. End-to-end smoke confirmed: /admin/login, /admin/views/moderation-queue, approve flow with moderation_log INSERT, /predlozheniya, /problemi, /agenda (all 12 Phase 02.3 chapters) — see `04.1-VERIFICATION.md`. Phase 4 + Phase 02.3 functionality fully restored on prod. The Phase 04.1 procedure (canonical-schema dump → diff → idempotent backfill → operator SQL Editor apply) becomes the canonical playbook for the next Payload schema change (Phase 3 when D-LawyerTrack unpauses) until `.planning/todos/payload-tsx-esm-incompat.md` resolves.
    ```

    Update the "Deferred Items" table — change the `D-PayloadSchemaDriftPhase04` row's `Status` column from `resolves_phase: 04.1` to `RESOLVED 2026-05-12 (Phase 04.1 close)`.

    Step 3 — Commit:
    ```bash
    git add .planning/STATE.md .planning/ROADMAP.md
    git commit -m "docs(state,roadmap): close Phase 04.1 — schema reconciled, Phase 4 + Phase 02.3 operational on prod"
    git push origin main
    ```
  </action>
  <verify>
    <automated>grep -c "Phase 04.1 CLOSED\|Phase 04.1 CLOSE" .planning/STATE.md && grep -c "Phase 04.1.*Completed: 2026-05-12\|04.1.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 04.1 CLOSED" .planning/STATE.md` returns at least 1
    - `grep -c "RESOLVED 2026-05-12" .planning/STATE.md` returns at least 1 (for D-PayloadSchemaDriftPhase04)
    - `grep -c "\\[x\\] \\*\\*Phase 04.1" .planning/ROADMAP.md` returns 1 (Phase 04.1 entry flipped to checked)
    - `grep -cE "04.1-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns at least 6 (all 6 plans listed)
    - STATE.md frontmatter `completed_phases` is 7 (was 6)
    - STATE.md frontmatter `completed_plans` is 62 (was 56)
    - Both commits pushed to origin/main
  </acceptance_criteria>
  <done>
    STATE.md and ROADMAP.md reflect Phase 04.1 closure. The deferred D-PayloadSchemaDriftPhase04 item is marked RESOLVED. The next active phase (Phase 6 GDPR self-service per ROADMAP) is clear from STATE.md.
  </done>
</task>

</tasks>

<verification>
- `04.1-VERIFICATION.md` committed on origin/main, contains a Sign-off section + PASS outcomes for all 6 smokes
- STATE.md updated: stopped_at advances past Phase 04.1, completed_phases is 7, D-PayloadSchemaDriftPhase04 marked RESOLVED
- ROADMAP.md updated: Phase 04.1 flipped to `[x]`, 6 plans listed with completion marks
- prod evidence collected: `/admin/login` 200, approve flow produces correct submissions + moderation_log rows, public surfaces operational
</verification>

<success_criteria>
Phase 04.1 closed. The 2026-05-11 deploy incident is fully resolved: prod Neon schema matches Payload's expectations, application code is live, end-to-end functionality is verified. The canonical playbook (canonical-schema dump → delta → idempotent backfill SQL → operator SQL Editor apply) is documented in OPS-RUNBOOK.md for Phase 3's eventual reactivation.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-payload-schema-reconciliation/04.1-06-SUMMARY.md` recording:
- All commit SHAs from this plan (VERIFICATION.md commit + STATE/ROADMAP update commit)
- Cross-reference to the six smoke outcomes (PASS / PASS / PASS / PASS / PASS / PASS)
- Cross-reference to the new release identifier from Plan 04.1-05
- One paragraph "lessons learned" for the GSD project's RETROSPECTIVE.md (when next written): why the schema-drift-vs-app-deploy split matters, why operator-applied manual DDL is the bridge until `payload migrate` returns to CI
</output>
