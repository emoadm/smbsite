# Phase 04.1 — Apply Log

**Operator:** emoadm
**Apply window:** 2026-05-12 05:30–05:37 UTC (approx; STEP 0 → STEP 7 wall clock)
**Prod Neon project:** SMBsite (production branch)
**Backfill SQL applied:** `.planning/ops/2026-05-12-payload-schema-phase-04-backfill.sql` (commit `3ef28cd`)
**Runbook:** `.planning/phases/04.1-payload-schema-reconciliation/04.1-OPS-RUNBOOK.md` (commit `8778d7e`)
**Apply method:** Neon SQL Editor browser UI (https://console.neon.tech/app/projects → SMBsite → production → SQL Editor); operator paste-and-run per STEP per OPS-RUNBOOK §3.

---

## STEP 0 — 2026-05-12 05:30 UTC (pre-apply verification, read-only)

### 0a) Public table list (pre-apply)
```
accounts, admin_users, admin_users_sessions, attribution_events,
community_channels, consents, ideas, moderation_log, newsletters,
payload_kv, payload_locked_documents, payload_locked_documents_rels,
payload_migrations, payload_preferences, payload_preferences_rels,
sessions, submissions, users, verification_tokens

(19 tables total)
```
Expected vs actual: **matches expected** — no `pages`, no `payload_ideas`. `payload_kv` already present from a prior Payload v3.84 boot (STEP 3c's `CREATE TABLE IF NOT EXISTS` handles this as a no-op).

### 0b) payload_locked_documents_rels columns (pre-apply)
```
id (integer, NO), order (integer, YES), parent_id (integer, NO),
path (character varying, NO), admin_users_id (integer, YES),
newsletters_id (integer, YES)

(6 columns)
```
Expected vs actual: **matches expected** — no `pages_id` or `payload_ideas_id` yet.

### 0c) payload_preferences_rels columns (pre-apply)
```
id (integer, NO), order (integer, YES), parent_id (integer, NO),
path (character varying, NO), admin_users_id (integer, YES)

(5 columns)
```
Expected vs actual: **matches expected** — 5 columns, no Phase-4 additions (per DELTA.md §5/§11 D-1, this table is NOT mutated by the backfill).

### 0d) Rels-table naming check
```
payload_locked_documents_rels
payload_preferences_rels
```
Naming convention: **single-underscore confirmed** on both. The failing-query reference to `payload_locked_documents__rels` (double underscore) in the Fly logs was a Drizzle/Payload log-render quirk, not the actual table name. Canonical-schema.sql assumption holds.

### 0e) admin_users role enum (pre-apply)
```
admin, editor

(2 rows)
```
Expected vs actual: **matches expected** — `super_editor` missing, ready to be added by STEP 2.

### 0f) Drizzle tables sanity (pre-apply)
```
accounts, attribution_events, consents, ideas, moderation_log,
sessions, submissions, users, verification_tokens

(9 rows)
```
Expected vs actual: **matches expected** — all 9 Drizzle-managed tables present.

**STEP 0 verdict:** Pre-state matches canonical-schema.sql assumptions exactly. Safe to proceed to STEP 1+.

---

## STEP 1 — 2026-05-12 05:33 UTC (create new ENUMs)
Output: `statement executed successfully` (no rows). DO-block returned no NOTICE / WARNING.
Status: **SUCCESS** — `enum_pages_status`, `enum_payload_ideas_status`, `enum_payload_ideas_topic` created.

## STEP 2 — 2026-05-12 05:33 UTC (ADD VALUE 'super_editor')
Output: `statement executed successfully` (no rows).
Status: **SUCCESS** — verified later in 7d (enum now has 3 values).

## STEP 3 — 2026-05-12 05:34 UTC (CREATE TABLE pages, payload_ideas, payload_kv)
Output: `statement executed successfully` (no rows). Each `CREATE SEQUENCE` / `CREATE TABLE` returned its DDL keyword. `payload_kv` block was a no-op (pre-existing per 0a) but did not error.
Status: **SUCCESS** — verified in 7a (all 3 tables exist).

## STEP 4 — 2026-05-12 05:34 UTC (ADD COLUMN pages_id, payload_ideas_id)
Output: `statement executed successfully` (no rows).
Status: **SUCCESS** — verified in 7b.

## STEP 5 — 2026-05-12 05:34 UTC (CREATE INDEX × 8)
Output: `statement executed successfully` (no rows). `payload_kv_key_idx` likely no-op (pre-existing); all others created fresh.
Status: **SUCCESS**.

## STEP 6 — 2026-05-12 05:35 UTC (DO-block FK constraints)
Output: `statement executed successfully` (no rows). DO-block returned no NOTICE.
Status: **SUCCESS** — verified in 7e (both new FKs present).

---

## STEP 7 — 2026-05-12 05:36 UTC (post-apply verification)

### 7a) New tables exist
```
pages         = true
payload_ideas = true
payload_kv    = true

(3 rows, all true)
```
Expected vs actual: **matches expected** ✓

### 7b) New FK columns on payload_locked_documents_rels
```
pages_id          (integer, YES)
payload_ideas_id  (integer, YES)

(2 rows)
```
Expected vs actual: **matches expected** ✓ — both columns nullable integers, ready for FK references.

### 7c) payload_preferences_rels NOT mutated
```
(0 rows)
```
Expected vs actual: **matches expected** ✓ — no FK columns added to this rels table, per DELTA.md §5/§11 D-1.

### 7d) admin_users role enum (post-apply)
```
admin, editor, super_editor

(3 rows)
```
Expected vs actual: **matches expected** ✓ — `super_editor` now valid.

### 7e) FK constraints on payload_locked_documents_rels
```
payload_locked_documents_rels_admin_users_fk    (pre-existing)
payload_locked_documents_rels_ideas_fk          (NEW)
payload_locked_documents_rels_newsletters_fk    (pre-existing)
payload_locked_documents_rels_pages_fk          (NEW)
payload_locked_documents_rels_parent_fk         (pre-existing)

(5 rows total)
```
Expected vs actual: **matches expected** ✓ — note the new `_ideas_fk` constraint name uses the Payload slug (`ideas`), not the dbName (`payload_ideas`), per DELTA.md §11 D-2.

### 7f) Drizzle tables remain untouched
```
accounts, attribution_events, consents, ideas, moderation_log,
sessions, submissions, users, verification_tokens

(9 rows — identical to 0f)
```
Expected vs actual: **matches expected** ✓ — Drizzle-managed tables unaffected. The Drizzle `ideas` (voting catalog, Phase 3) and Payload `payload_ideas` (editorial CMS) now coexist as designed.

---

## Sign-off

2026-05-12 05:37 UTC, emoadm:
Phase 04.1 Wave 4 schema apply COMPLETE — prod Neon ready for Wave 5 (revert-the-revert + re-deploy).

---

## Deviations / notes

- `payload_kv` was already present pre-apply (STEP 0a). This was anticipated in DELTA.md §11 D-4 ("defensive CREATE TABLE IF NOT EXISTS"). The `IF NOT EXISTS` guards made STEP 3c and STEP 5d's index a no-op without error. No action needed.
- STEP 0d confirmed single-underscore naming on rels tables (`payload_locked_documents_rels`, not `__rels`). The double-underscore string in the Fly crash log was a render artifact of the failing Drizzle/Payload SELECT, not the actual table name.
- All STEPs completed in a single Neon SQL Editor session window (~7 minutes total wall-clock). No Editor session drop. No partial-apply recovery needed.
- No deviations from canonical-schema.sql expectations. Wave 5 may proceed immediately.
