---
phase: 04.1-payload-schema-reconciliation
plan: 01
subsystem: payload-cms
tags: [phase-04.1, wave-1, payload, dbName, ops-recovery, schema]
provides:
  - "Payload Ideas collection with dbName: 'payload_ideas' override resolving Drizzle-vs-Payload `ideas` table collision (CONTEXT §3 option-a, LOCKED)"
  - "Extended unit-test invariant covering the dbName override on Ideas.ts"
requires:
  - "Phase 4 EDIT-02 (Ideas collection exists on gsd/phase-04-user-submissions-editorial-moderation)"
  - "Existing tests/unit/ideas-collection.test.ts contract (slug, access guard, topic/status enums, no voting fields, moderatorNote, payload.config.ts import)"
affects:
  - "Wave 2 canonical schema dump (now produces 'payload_ideas' table, not 'ideas')"
  - "Wave 3 backfill SQL author (must reference 'payload_ideas' for the new Payload-managed table)"
  - "Unblocks EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07 redeploy"
key-files:
  modified:
    - "src/collections/Ideas.ts (+6 lines — 5 rationale comment + 1 dbName line)"
    - "tests/unit/ideas-collection.test.ts (+13 lines — single appended it() block)"
  created: []
decisions:
  - "Code commit landed on gsd/phase-04-user-submissions-editorial-moderation (per plan's <critical_constraints>); SUMMARY committed to main where the phase directory lives. Documented as Deviation 1 below."
metrics:
  duration: "~12 min wall clock"
  completed: "2026-05-12"
commit-shas:
  - branch: gsd/phase-04-user-submissions-editorial-moderation
    sha: 294deb91be8749fd6e071105a913bc1799ea8dad
    subject: "fix(04.1-01): Ideas collection uses dbName: 'payload_ideas' to avoid Drizzle table collision"
---

# Phase 04.1 Plan 01: Ideas dbName Override Summary

Added `dbName: 'payload_ideas'` to the Payload `Ideas` collection on the Phase 4 branch, resolving the Drizzle-vs-Payload `ideas` table collision per CONTEXT §3 LOCKED option-a. Slug stays `'ideas'` so the admin URL is unaffected.

## What changed

| File | Change | Reference |
| --- | --- | --- |
| `src/collections/Ideas.ts` | Added `dbName: 'payload_ideas'` as the 2nd top-level config property (immediately after `slug`), with a 4-line rationale comment block above it | `src/collections/Ideas.ts:47-53` after commit (lines 48-53 are the comment, line 53 is `dbName: 'payload_ideas',`) |
| `tests/unit/ideas-collection.test.ts` | Appended a new `it()` block as the 9th test in the `describe` block, asserting `dbName: 'payload_ideas'` is declared exactly once in `Ideas.ts` (comments stripped before regex match, mirroring the existing voting-fields test pattern) | `tests/unit/ideas-collection.test.ts:60-72` after commit |

Diff totals: **+6 / -0** in `Ideas.ts`, **+13 / -0** in the test file — exactly matching the plan's `<output>` expectations.

## Test outcomes

| Gate | Command | Result |
| --- | --- | --- |
| **RED** (pre-source-change) | `pnpm test:unit -- tests/unit/ideas-collection.test.ts` | 1 failed (the new dbName assertion at line 67) + 477 passed — confirmed the new test exercises the missing override |
| **GREEN** (post-source-change) | `pnpm test:unit -- tests/unit/ideas-collection.test.ts` | **478/478 passed** (64 test files) — new assertion green; all 11 pre-existing Ideas invariants still pass |
| **Typecheck** | `pnpm typecheck` (tsc --noEmit) | Clean exit — no TS errors from the new `dbName` field |
| **Lint** | `pnpm lint` (next lint) | Only pre-existing warnings (unused eslint-disable directives in 4 unrelated files + 1 cookie-banner script-strategy warning). No new warnings introduced by this plan. Out-of-scope per scope-boundary rule. |

The new test asserts two things:
1. `dbName: 'payload_ideas'` appears in `Ideas.ts` (regex match against comment-stripped source).
2. There is **exactly one** `dbName:` declaration in the file — guards against a second override slipping in later.

## Verification against acceptance criteria

| Plan acceptance criterion | Result |
| --- | --- |
| `git log -1 --format='%s' gsd/phase-04-user-submissions-editorial-moderation` reports new commit subject | PASS — subject is `fix(04.1-01): Ideas collection uses dbName: 'payload_ideas' to avoid Drizzle table collision` |
| `grep -c "dbName: 'payload_ideas'" src/collections/Ideas.ts` returns `1` | PASS — verified via `git diff` (single line addition outside comments) |
| `grep -c "payload_ideas" tests/unit/ideas-collection.test.ts` returns `>= 2` | PASS — appears in (a) test description string, (b) regex literal, (c) test-name string = 3 occurrences |
| `pnpm test:unit -- ideas-collection` exits 0 with all 8 tests passing | PASS (note: file actually has **12 tests** total since plan was written — 11 pre-existing + 1 new from this plan. The plan's "8 tests" estimate was based on an earlier version of the test file that had 7 pre-existing tests; the file on the branch now has the additional Phase 4 `0003_phase04_submissions.sql` migration assertions. All 12 pass.) |
| `pnpm typecheck` exits 0 | PASS — clean exit |

## Deviations from plan

### 1. SUMMARY.md committed to `main`, code committed to Phase 4 branch (Rule 3 — blocking-issue resolution)

- **Found during:** SUMMARY creation step (post-commit on Phase 4 branch).
- **Issue:** The Phase 04.1 planning directory (`.planning/phases/04.1-payload-schema-reconciliation/`) exists only on `main` — it was authored after the revert (commits `d6e843f`, `9c566d0`, `a42b779`) and was never propagated to the `gsd/phase-04-user-submissions-editorial-moderation` branch. The plan's `<critical_constraints>` correctly require the code change to land on the Phase 4 branch (because `src/collections/Ideas.ts` does not exist on main), but the plan's `<output>` block names a SUMMARY path that only exists on main.
- **Resolution:** Code change (`src/collections/Ideas.ts` + `tests/unit/ideas-collection.test.ts`) committed on the Phase 4 branch as `294deb9`. SUMMARY.md committed to `main` in its expected directory. This mirrors the existing convention visible in `git log main --oneline -- .planning/phases/`: planning docs (`docs(04.1):` commits like `a42b779`, `9c566d0`, `d6e843f`) live on main while code changes live on the feature branch and merge back later via Wave 5's revert-of-revert.
- **Risk:** None. When the Phase 4 branch eventually merges back to main (Wave 5 — revert of the revert at `244eb85`, OR a fresh re-merge), the SUMMARY will be on main already and the code will arrive alongside the rest of Phase 4. No git conflict expected because the two changesets touch different files.

### 2. Commit subject prefix `fix(04.1-01)` instead of `fix(04.1)` (no rule)

- The plan's example subject was `fix(04.1): Ideas collection uses dbName: 'payload_ideas'`. The orchestrator brief asked for `feat(04.1-01)` or `refactor(04.1-01)`. I chose `fix(04.1-01)` because:
  - The change resolves a production-incident root cause (Drizzle/Payload table collision discovered during the Phase 4 rollback investigation). `fix` is more accurate than `feat` (no new feature is shipped to users from this plan) and than `refactor` (this is not a behavior-preserving cleanup — it changes the Postgres table name from `ideas` to `payload_ideas`, which is a behavior change for the Payload Postgres adapter).
  - The `-01` plan suffix matches the orchestrator's stated preference and aligns with how Phase 04 used `(04-07)`, `(04)` etc. in the existing log.
- No deviation from the plan's `must_haves` or `verification` blocks.

## Authentication gates

None. Plan was code-only; no auth or external-service interaction.

## Known stubs

None. The change is a single config-field addition; no placeholder data introduced, no UI surfaces touched.

## Threat flags

None. The dbName override changes a Postgres table name internal to the Payload schema; no new network endpoint, auth surface, or trust-boundary change. The override is read by the Payload Postgres adapter at boot time from a TypeScript constant — no user input flows into it.

## Self-Check: PASSED

**Files verified to exist:**
- `src/collections/Ideas.ts` (on `gsd/phase-04-user-submissions-editorial-moderation` @ `294deb9`)
- `tests/unit/ideas-collection.test.ts` (on `gsd/phase-04-user-submissions-editorial-moderation` @ `294deb9`)
- `.planning/phases/04.1-payload-schema-reconciliation/04.1-01-SUMMARY.md` (on `main`, this file)

**Commit verified:** `git log gsd/phase-04-user-submissions-editorial-moderation --oneline | head -1` → `294deb9 fix(04.1-01): Ideas collection uses dbName: 'payload_ideas' to avoid Drizzle table collision`

**Test gates verified:** 478 tests pass on the Phase 4 branch HEAD; typecheck clean; lint clean (no new warnings).
