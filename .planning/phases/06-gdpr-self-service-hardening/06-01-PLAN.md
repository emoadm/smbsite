---
phase: 06-gdpr-self-service-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/deletion-log-schema.test.ts
  - tests/unit/deletion-cascade.test.ts
  - tests/unit/export-bundle-shape.test.ts
  - tests/unit/bunny-signed-url.test.ts
  - tests/unit/cookie-consents-schema.test.ts
  - tests/unit/consents-region-populate.test.ts
  - tests/unit/video-player-vtt.test.ts
  - tests/unit/start-worker-deletion-log-assertion.test.ts
  - tests/e2e/a11y/.gitkeep
  - tests/e2e/data-rights/.gitkeep
  - tests/unit/users-no-delete.test.ts
autonomous: true
manual: false
requirements:
  - GDPR-04
  - GDPR-05
  - GDPR-06
  - GDPR-07
  - GDPR-08
  - BRAND-04
  - BRAND-05
unblocks:
  - 06-02 (DDL + schema)
  - 06-03 (Drizzle schema + migration apply)
  - 06-05 (Bunny + export worker)
  - 06-06 (Deletion cascade worker)
  - 06-12 (a11y audit)
tags: [phase-06, wave-0, validation, test-scaffolding, nyquist]
must_haves:
  truths:
    - "Every unit test file referenced by Wave 1..4 plans exists as a failing-but-runnable .skip()'d Vitest skeleton (RED state, no implementation)"
    - "Every E2E spec directory referenced by Wave 4 (a11y) and Wave 3 (data-rights flows) exists with a .gitkeep + at least one .skip()'d Playwright spec stub"
    - "Each test file has a header comment naming the Phase 6 plan that will turn the skip()'d test into a real assertion (traceability)"
    - "pnpm test:unit runs to completion (passes — skipped tests count as pass) and the new files appear in the test run output"
    - "No production code is touched in this wave — only test scaffolding files"
  artifacts:
    - path: tests/unit/deletion-log-schema.test.ts
      provides: "REVOKE-UPDATE/DELETE privilege assertion + no-PII source-grep stub (Plan 06-02 + 06-03 fill in)"
      contains: "deletion_log"
    - path: tests/unit/deletion-cascade.test.ts
      provides: "9-step cascade order + Brevo-mocked + idempotent retry stub (Plan 06-06 fills in)"
      contains: "deletion-cascade"
    - path: tests/unit/export-bundle-shape.test.ts
      provides: "JSON bundle 7-key snapshot stability stub (Plan 06-05 fills in)"
      contains: "format_version"
    - path: tests/unit/bunny-signed-url.test.ts
      provides: "Known-good token vector for SHA-256 base64url stub (Plan 06-04 fills in)"
      contains: "signBunnyUrl"
    - path: tests/unit/cookie-consents-schema.test.ts
      provides: "REVOKE + anon_id + JSONB shape stub (Plan 06-02 fills in)"
      contains: "cookie_consents"
    - path: tests/unit/consents-region-populate.test.ts
      provides: "New consents INSERTs populate region via GeoIP stub (Plan 06-03 fills in)"
      contains: "consents.region"
    - path: tests/unit/video-player-vtt.test.ts
      provides: "VideoPlayer renders <track> when vttSrc provided stub (Plan 06-13 fills in)"
      contains: "vttSrc"
    - path: tests/unit/start-worker-deletion-log-assertion.test.ts
      provides: "Boot-time has_table_privilege regression-lock stub (Plan 06-06 fills in)"
      contains: "has_table_privilege"
    - path: tests/unit/users-no-delete.test.ts
      provides: "Source-grep guard — no db.delete(users) anywhere in lib/data-rights (Pitfall C)"
      contains: ".delete(users)"
    - path: tests/e2e/a11y/.gitkeep
      provides: "Wave 4 spec directory exists for axe-core specs"
    - path: tests/e2e/data-rights/.gitkeep
      provides: "Wave 3 spec directory exists for export/deletion/cancel E2E specs"
  key_links:
    - from: "tests/unit/*.test.ts"
      to: "Vitest runner via existing vitest.config.ts"
      via: "Existing pnpm test:unit script (no config change)"
      pattern: "describe.skip|it.skip"
    - from: "tests/e2e/a11y/*.spec.ts"
      to: "Playwright runner via existing playwright.config.ts"
      via: "Spec file convention .spec.ts; Wave 4 turns .skip into .only-on-CI"
      pattern: "test.skip"
threat_model:
  trust_boundaries: []
  threats:
    - id: T-06-01-01
      category: tampering
      component: "test scaffolding files"
      disposition: accept
      rationale: "No production code; no runtime trust boundary crossed. Scaffolding cannot be tampered into harming users because it does nothing yet."
---

<objective>
Create the failing-but-runnable test scaffolding (Nyquist Wave 0) for every Phase 6 verification surface referenced by Waves 1..4. This is purely test-infrastructure work — no production code is touched. Each subsequent plan turns its target `.skip()`'d block into a real, passing assertion.

Purpose: Phase 6 ships 8 requirements across 4 sequential waves with manual-DDL constraints, an external CDN integration, an irreversible-with-grace destructive flow, and a WCAG 2.1 AA audit. Without test scaffolding landed up-front, mid-phase plans would silently miss verification surfaces (Pitfall E — moving target a11y audit; Pitfall G — i18n linter blind spot on `ИЗТРИЙТЕ`). Wave 0 nails the contract.

Output: One commit with ~10 test scaffolding files. `pnpm test:unit` exits 0. New files appear in the test run summary as skipped.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-gdpr-self-service-hardening/06-CONTEXT.md
@.planning/phases/06-gdpr-self-service-hardening/06-RESEARCH.md
@.planning/phases/06-gdpr-self-service-hardening/06-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- Existing test conventions to mirror. -->

Test framework: Vitest 2.1.8 (per `package.json`).

Existing unit-test pattern (source-grep style — no DB/Payload boot):
```typescript
// tests/unit/ideas-collection.test.ts:1-30 — canonical readFileSync regex pattern
import { describe, expect, it } from 'vitest';
import { readFileSync } from 'node:fs';

describe('Phase 4 EDIT-02 — Ideas collection invariants', () => {
  it("Ideas.ts has slug: 'ideas'", () => {
    const src = readFileSync('src/collections/Ideas.ts', 'utf8');
    expect(src).toMatch(/slug:\s*'ideas'/);
  });
});
```

Existing E2E pattern (Playwright):
```typescript
// tests/e2e/*.spec.ts
import { test, expect } from '@playwright/test';
test.describe('flow', () => {
  test('passes', async ({ page }) => { /* ... */ });
});
```

Skipping pattern for Wave 0:
```typescript
describe('Phase 6 — Plan XX placeholder', () => {
  it.skip('TODO Plan 06-XX: <what this test will assert>', () => {
    // Implementation lands in Plan 06-XX.
    expect(true).toBe(true);
  });
});
```
</interfaces>

<critical_constraints>
- ONLY test files in this plan. NO production code. NO migration files. NO schema changes.
- Each `.skip()` block MUST have a `TODO Plan 06-XX:` prefix naming the downstream plan that will implement it (Nyquist traceability).
- File headers MUST name the requirement IDs they cover (GDPR-04, GDPR-05, etc.).
- The Playwright spec directories (`tests/e2e/a11y/`, `tests/e2e/data-rights/`) MUST contain at least a `.gitkeep` so they're tracked; add at least one `.spec.ts` skeleton in each to ensure Playwright config discovery works on first run.
- DO NOT modify `vitest.config.ts` or `playwright.config.ts` — existing globs already cover `tests/unit/*.test.ts` and `tests/e2e/**/*.spec.ts`.
- DO NOT add `@axe-core/playwright` yet — that's a Wave 4 plan dependency installation.
- Verify `pnpm test:unit` runs cleanly before commit. If any new file accidentally executes (not skipped), fix it.
</critical_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 9 unit-test scaffolding files (one per Wave 1..4 verification surface)</name>
  <read_first>
    - `.planning/phases/06-gdpr-self-service-hardening/06-VALIDATION.md` (Wave 0 Gaps section in RESEARCH.md §Validation Architecture is the canonical list)
    - `.planning/phases/06-gdpr-self-service-hardening/06-RESEARCH.md` lines 1108-1156 (Phase Requirements → Test Map)
    - `tests/unit/ideas-collection.test.ts` (canonical readFileSync regex test style)
  </read_first>
  <files>
    tests/unit/deletion-log-schema.test.ts,
    tests/unit/deletion-cascade.test.ts,
    tests/unit/export-bundle-shape.test.ts,
    tests/unit/bunny-signed-url.test.ts,
    tests/unit/cookie-consents-schema.test.ts,
    tests/unit/consents-region-populate.test.ts,
    tests/unit/video-player-vtt.test.ts,
    tests/unit/start-worker-deletion-log-assertion.test.ts,
    tests/unit/users-no-delete.test.ts
  </files>
  <action>
    For each file below, create a Vitest describe block with `it.skip()` placeholders. Header comment names requirement IDs + the downstream plan.

    **File 1: `tests/unit/deletion-log-schema.test.ts`**
    Header: covers GDPR-07 (REVOKE) + GDPR-08 (no PII columns). Implementation in Plans 06-02 (DDL) + 06-03 (Drizzle schema).
    Skips:
    - `it.skip('TODO Plan 06-02: deletion_log table has REVOKE UPDATE, DELETE in 0004_phase06_gdpr.sql', ...)`
    - `it.skip('TODO Plan 06-03: deletion_log Drizzle schema declares only {target_user_id_hash, deleted_at, deletion_reason} — no PII columns', ...)`
    - `it.skip('TODO Plan 06-06: has_table_privilege(current_user, deletion_log, UPDATE) returns false in test DB', ...)`

    **File 2: `tests/unit/deletion-cascade.test.ts`**
    Header: covers GDPR-05 + GDPR-06. Implementation in Plan 06-06.
    Skips (one per cascade step):
    - `it.skip('TODO Plan 06-06: Step 1 — users row UPDATEd to tombstone (email/full_name/phone/oblast NULL, status=deleted)', ...)`
    - `it.skip('TODO Plan 06-06: Step 2 — attribution_events.user_id SET NULL', ...)`
    - `it.skip('TODO Plan 06-06: Step 3 — consents.region + consents.email_at_consent SET NULL', ...)`
    - `it.skip('TODO Plan 06-06: Step 4 — sessions DELETEd', ...)`
    - `it.skip('TODO Plan 06-06: Step 5 — newsletter_preferences DELETEd', ...)`
    - `it.skip('TODO Plan 06-06: Step 6 — BullMQ removeJobs for userId', ...)`
    - `it.skip('TODO Plan 06-06: Step 6.5 — data_export_jobs rows fetched + Bunny blobs DELETEd (idempotent on 404)', ...)`
    - `it.skip('TODO Plan 06-06: Step 7 — Brevo DELETE /contacts/{email} called OUTSIDE tx with retry', ...)`
    - `it.skip('TODO Plan 06-06: Step 8 — deletion_log INSERT with sha256(userId) hash, deletion_reason=user_request', ...)`
    - `it.skip('TODO Plan 06-06: submissions/votes/problem_reports rows NOT touched (anonymize-and-preserve)', ...)`
    - `it.skip('TODO Plan 06-06: Brevo failure during step 7 does NOT roll back the cascade tx', ...)`

    **File 3: `tests/unit/export-bundle-shape.test.ts`**
    Header: covers GDPR-04. Implementation in Plan 06-05.
    Skips:
    - `it.skip('TODO Plan 06-05: bundle has top-level keys {metadata, users, votes, submissions, consents, attribution_events, newsletter_preferences, moderation_log}', ...)`
    - `it.skip('TODO Plan 06-05: metadata.format_version === 1', ...)`
    - `it.skip('TODO Plan 06-05: metadata.user_id_hash is sha256 hex of user.id, NOT user.id verbatim', ...)`
    - `it.skip('TODO Plan 06-05: empty arrays render as [] not null (stable schema across users)', ...)`
    - `it.skip('TODO Plan 06-05: bundle JSON is valid UTF-8 with indent 2', ...)`

    **File 4: `tests/unit/bunny-signed-url.test.ts`**
    Header: covers GDPR-04. Implementation in Plan 06-04.
    Skips:
    - `it.skip('TODO Plan 06-04: signBunnyUrl returns URL with token & expires query params', ...)`
    - `it.skip('TODO Plan 06-04: base64url substitution applies — token contains no +, /, or = chars', ...)`
    - `it.skip('TODO Plan 06-04: expires = now+ttlSeconds with ±5s tolerance', ...)`
    - `it.skip('TODO Plan 06-04: throws if BUNNY_TOKEN_AUTH_KEY env not set and no securityKey override', ...)`
    - `it.skip('TODO Plan 06-04: known-good vector — given securityKey=X, path=/exports/abc.json, expires=1700000000 → token matches sha256 base64url of (key+path+expires)', ...)`

    **File 5: `tests/unit/cookie-consents-schema.test.ts`**
    Header: covers D-09 (carry-forward from Phase 1 plan-check N-1). Implementation in Plan 06-02 (DDL) + Plan 06-09 (Server Action).
    Skips:
    - `it.skip('TODO Plan 06-02: cookie_consents table created in 0004_phase06_gdpr.sql with columns (anon_id UUID, choices JSONB, consent_at, withdrawn_at NULL, ip_hash TEXT)', ...)`
    - `it.skip('TODO Plan 06-02: cookie_consents has REVOKE UPDATE, DELETE for app_db_user', ...)`
    - `it.skip('TODO Plan 06-03: Drizzle schema for cookie_consents matches DDL column types exactly', ...)`
    - `it.skip('TODO Plan 06-09: anon_id sources from attr_sid cookie when present, else random UUID', ...)`

    **File 6: `tests/unit/consents-region-populate.test.ts`**
    Header: covers D-10 (carry-forward from D-ConsentsRegionPopulation). Implementation in Plan 06-03 (backfill) + Plan 06-09 (new-INSERT populator).
    Skips:
    - `it.skip('TODO Plan 06-03: 0004 migration includes UPDATE consents SET region = ae.first_oblast FROM attribution_events ae WHERE consents.attr_sid = ae.attr_sid AND consents.region IS NULL', ...)`
    - `it.skip('TODO Plan 06-09: new consents INSERT path calls geoip.lookup() and populates region', ...)`

    **File 7: `tests/unit/video-player-vtt.test.ts`**
    Header: covers BRAND-05 (D-13). Implementation in Plan 06-13.
    Skips:
    - `it.skip('TODO Plan 06-13: VideoPlayer renders <track kind="subtitles" srcLang="bg" default> when vttSrc prop is provided', ...)`
    - `it.skip('TODO Plan 06-13: VideoPlayer does NOT render <track> when vttSrc is undefined', ...)`
    - `it.skip('TODO Plan 06-13: placeholder public/demo.vtt file exists with at least one cue', ...)`

    **File 8: `tests/unit/start-worker-deletion-log-assertion.test.ts`**
    Header: covers GDPR-07 boot-assertion. Implementation in Plan 06-06.
    Skips:
    - `it.skip('TODO Plan 06-06: scripts/start-worker.ts imports + calls assertDeletionLogInsertOnly()', ...)`
    - `it.skip('TODO Plan 06-06: assertDeletionLogInsertOnly process.exit(1)s when has_table_privilege returns can_update=true', ...)`
    - `it.skip('TODO Plan 06-06: assertion logs success line on green path', ...)`

    **File 9: `tests/unit/users-no-delete.test.ts`**
    Header: covers Pitfall C (FK onDelete: 'restrict' from consents.user_id makes db.delete(users) throw — guard at grep level).
    Skips → activate immediately as PASSING tests (this is a grep guard, not a downstream-fill stub):
    - `it('src/lib/data-rights/ contains no db.delete(users) call', () => { const files = globSync('src/lib/data-rights/**/*.ts'); files.forEach(f => { const src = readFileSync(f, 'utf8').split('\\n').filter(l => !l.trim().startsWith('//')).join('\\n'); expect(src).not.toMatch(/db\\.delete\\(\\s*users\\s*\\)/); }); })`

    NOTE: For file 9, the test should NOT skip — it should run as a passing grep guard from day one. On Wave 0 the `src/lib/data-rights/` dir doesn't exist yet → globSync returns empty array → forEach does nothing → test passes. Wave 2 + 3 create files in that dir; the grep then has substance to guard.

    Implementation style: Use the readFileSync + regex pattern from `tests/unit/ideas-collection.test.ts:1-30`. Import `describe, expect, it` from 'vitest'. Strip line-comments from source before matching where comments could pollute (use the existing `.split('\\n').filter(line => !line.trim().startsWith('//'))` idiom).
  </action>
  <verify>
    <automated>pnpm test:unit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files exist at the paths in `<files>`
    - `pnpm test:unit` exits 0
    - Test summary reports new files; skipped count increased by ≥ 30 (sum of `it.skip` blocks across all files)
    - `tests/unit/users-no-delete.test.ts` passes (not skipped — runs as a grep guard from day one)
    - No production source files modified
  </acceptance_criteria>
  <done>
    9 unit-test scaffolding files committed. Every Wave 1..4 verification surface has a named, traceable `.skip()`'d stub. The `users-no-delete` grep guard is live.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author E2E spec directory stubs (a11y + data-rights)</name>
  <read_first>
    - `playwright.config.ts` (verify the spec discovery glob covers `tests/e2e/**/*.spec.ts`)
    - `.planning/phases/06-gdpr-self-service-hardening/06-RESEARCH.md` lines 574-585 (D-12 page-level spec coverage matrix)
  </read_first>
  <files>
    tests/e2e/a11y/.gitkeep,
    tests/e2e/a11y/landing.a11y.spec.ts,
    tests/e2e/a11y/register.a11y.spec.ts,
    tests/e2e/a11y/login-otp.a11y.spec.ts,
    tests/e2e/a11y/member-dashboard.a11y.spec.ts,
    tests/e2e/a11y/proposals-problems.a11y.spec.ts,
    tests/e2e/a11y/agenda.a11y.spec.ts,
    tests/e2e/a11y/data-rights.a11y.spec.ts,
    tests/e2e/a11y/account-pending-deletion.a11y.spec.ts,
    tests/e2e/data-rights/.gitkeep,
    tests/e2e/data-rights/export-request.spec.ts,
    tests/e2e/data-rights/deletion-typed-text.spec.ts,
    tests/e2e/data-rights/grace-period-lockout.spec.ts,
    tests/e2e/data-rights/cancel-deletion.spec.ts
  </files>
  <action>
    Step 1 — Create `.gitkeep` files in both new dirs.

    Step 2 — For each `.spec.ts` listed, write a stub with `test.skip()`:

    Template for a11y spec (all 8 files follow this shape):
    ```typescript
    /**
     * Phase 6 BRAND-04 / D-11 / D-12 — axe-core a11y spec
     * Implementation: Plan 06-12 (Wave 4 a11y audit)
     * Covers: <flow name> per RESEARCH.md §Focus Area 2 page-level matrix
     */
    import { test } from '@playwright/test';

    test.describe('<flow> accessibility (BRAND-04, D-12)', () => {
      test.skip('TODO Plan 06-12: <URL> passes WCAG 2.1 AA (critical + serious = 0)', async () => {
        // Implementation lands in Plan 06-12 (Wave 4).
        // Pattern: AxeBuilder({ page }).withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze();
        // Filter violations to impact in ('critical','serious'); assert empty.
      });
    });
    ```

    For data-rights specs:
    ```typescript
    /**
     * Phase 6 GDPR-04 / GDPR-05 — E2E spec
     * Implementation: Plan 06-09 + 06-10 (Wave 3 UX surfaces)
     */
    import { test } from '@playwright/test';

    test.describe('<flow> E2E', () => {
      test.skip('TODO Plan 06-09: data-export request enqueues job + flips button to cooldown state', async () => { });
      // Additional skips per spec file
    });
    ```

    Concrete skips per data-rights spec:
    - `export-request.spec.ts`:
      - `TODO Plan 06-09: clicking "Заявка за извличане на данни" enqueues data-export job + shows success toast`
      - `TODO Plan 06-09: button shows 5-min cooldown state with "Заявката е изпратена" label after success`
      - `TODO Plan 06-09: rate-limit response renders destructive Alert with countdown copy`
    - `deletion-typed-text.spec.ts`:
      - `TODO Plan 06-10: typing 'ИЗТРИЙТЕ' exactly enables "Изтрий завинаги" button`
      - `TODO Plan 06-10: typing lowercase or Latin equivalent keeps button disabled`
      - `TODO Plan 06-10: submitting flips users.status to pending_deletion + redirects to /account-pending-deletion`
      - `TODO Plan 06-10: Escape key dismisses modal cleanly when no submission in flight`
    - `grace-period-lockout.spec.ts`:
      - `TODO Plan 06-10: login while status=pending_deletion redirects to /account-pending-deletion`
      - `TODO Plan 06-10: navigating to /member while pending_deletion redirects to /account-pending-deletion`
      - `TODO Plan 06-10: /account-pending-deletion renders minimal Header (no nav links)`
    - `cancel-deletion.spec.ts`:
      - `TODO Plan 06-11: clicking "Отказване на изтриването" sends OTP email + redirects to /cancel-deletion/verify`
      - `TODO Plan 06-11: OTP verification flips status back to active + redirects to /member with success toast`
      - `TODO Plan 06-11: cancel-deletion via OTP cancels pending account-deletion-cascade BullMQ job`

    Step 3 — Verify Playwright discovery (don't run the full suite — just list):
    ```bash
    pnpm exec playwright test --list 2>&1 | grep -c 'a11y\\|data-rights' || true
    ```
    Should report at least 12 spec files matched.
  </action>
  <verify>
    <automated>pnpm exec playwright test --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 12 `.spec.ts` files + 2 `.gitkeep` files created
    - `pnpm exec playwright test --list` lists all 12 specs
    - Every `test.skip()` has a `TODO Plan 06-XX:` prefix
    - `pnpm test:unit` still exits 0 (no unit-test regressions)
  </acceptance_criteria>
  <done>
    Playwright spec scaffolding in place. Waves 3 + 4 turn skips into real specs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update VALIDATION.md with concrete per-plan verification map + commit Wave 0</name>
  <read_first>
    - `.planning/phases/06-gdpr-self-service-hardening/06-VALIDATION.md` (current template)
    - `.planning/phases/06-gdpr-self-service-hardening/06-RESEARCH.md` lines 1095-1156 (Validation Architecture section)
  </read_first>
  <files>.planning/phases/06-gdpr-self-service-hardening/06-VALIDATION.md</files>
  <action>
    Replace the template-VALIDATION.md with a concrete map. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`.

    Per-task verification map MUST have one row per planned task across plans 06-02 through 06-13. Columns: Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status.

    Sampling rate:
    - Per task commit: `pnpm test:unit` (Vitest, ~30s)
    - Per wave merge: `pnpm test:unit && pnpm exec playwright test tests/e2e/data-rights tests/e2e/a11y --reporter=line` (~3-5min)
    - Pre `/gsd:verify-work`: full suite green + Lighthouse PR green + operator-signed 06-A11Y-AUDIT.md + 06-LOAD-TEST-RUN.md

    Wave 0 Requirements section — list every test file created by Tasks 1 and 2.

    Manual-only verifications:
    - Operator: Bunny Storage Zone provisioning (`D-BunnyExportZoneProvisioning`)
    - Operator: Cloudflare API token + zone ID GH Actions secrets
    - Operator: Brevo API key `contacts:delete` scope verification
    - Operator: Day-30 cascade smoke test (cannot fully automate without time machine)
    - Operator: 06-A11Y-AUDIT.md findings sign-off
    - Operator: 06-LOAD-TEST-RUN.md k6 run sign-off

    Commit all Wave 0 work in one commit:
    ```bash
    git add tests/unit/ tests/e2e/a11y/ tests/e2e/data-rights/ .planning/phases/06-gdpr-self-service-hardening/06-VALIDATION.md
    git commit -m "test(06-01): Wave 0 — Phase 6 test scaffolding + VALIDATION map [skip ci]"
    ```
  </action>
  <verify>
    <automated>grep -c 'nyquist_compliant: true' .planning/phases/06-gdpr-self-service-hardening/06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
    - Per-task verification map has ≥ 25 rows (one per task across all downstream plans)
    - Manual-only verifications section enumerates the 6 operator items
    - Commit lands on the working branch
  </acceptance_criteria>
  <done>
    VALIDATION.md is the canonical phase-wide test map. Wave 0 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — Wave 0 is entirely test scaffolding; no runtime trust boundaries are created or crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | test scaffolding files | accept | Scaffolding files do not execute production code; an attacker who tampers with a `.skip()`'d test gets a different `.skip()`'d test. No user-facing impact. |
| T-06-01-02 | Information disclosure | test files committed to repo | accept | No secrets, no PII, no environment-specific data in scaffolding. Verified by source review during commit. |
</threat_model>

<verification>
- 9 unit-test scaffolding files in `tests/unit/`
- 12 E2E spec scaffolding files in `tests/e2e/a11y/` + `tests/e2e/data-rights/`
- `tests/unit/users-no-delete.test.ts` passes (grep guard, not skipped)
- `pnpm test:unit` exits 0
- `pnpm exec playwright test --list` lists all new specs
- `06-VALIDATION.md` updated with concrete per-task map
- Single commit on working branch
</verification>

<success_criteria>
Every Phase 6 verification surface referenced by Waves 1..4 has a failing-but-runnable test scaffold. Downstream plans can refer to existing files instead of creating-and-implementing in the same task. Nyquist: sampling continuity is established before any production code lands.
</success_criteria>

<output>
After completion, create `.planning/phases/06-gdpr-self-service-hardening/06-01-SUMMARY.md` recording:
- List of 21 new test files (9 unit + 12 E2E) with byte counts
- `pnpm test:unit` skipped count delta (before / after)
- `pnpm exec playwright test --list` spec count delta
- Confirmation that `tests/unit/users-no-delete.test.ts` runs as passing test (NOT skipped)
- Pointer to updated VALIDATION.md
</output>
