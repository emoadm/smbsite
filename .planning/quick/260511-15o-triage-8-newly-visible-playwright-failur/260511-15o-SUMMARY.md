---
quick_id: 260511-15o
slug: triage-8-newly-visible-playwright-failur
status: complete
date: 2026-05-10
deliverable: 260511-15o-TRIAGE.md
---

# Quick Task 260511-15o — Summary

## What

Diagnosed the 8 Playwright spec failures on PR #2 CI run `25640408203` (commit `0668747`) that were unmasked once quick tasks `260511-04m` (standalone webServer) and `260511-0nx` (cf-ray bypass) made the CI Playwright job actually exercise the running app.

## Why

After the two webServer fixes, CI moved from 0 passed / 40 failed → 42 passed / 12 failed. Of the 12 remaining, 4 are the separately-tracked Phase 5 env-var hard-fails (admin-newsletter-composer, community-page × 2, newsletter-preferences). The other 8 are real assertion failures that needed per-spec triage before the user could dispatch fixes.

## Deliverable

`260511-15o-TRIAGE.md` — full per-finding diagnostic with:

- Headline classification: **4 code-bugs · 3 test-bugs · 1 config-bug** (0 env-bugs).
- Exact CI error message per spec.
- Test source quoted at failing line.
- Implementation source quoted with `file:line` references.
- Per-spec remediation recommendation.
- Confidence-rated classifications.
- Suggested dispatch order grouped into 4 batches (A: 4 Phase-2-contract specs; B: 1-line ci.yml env tweak that closes 2 more; C: a11y `<Label>` add; D: landing Cache-Control architecture decision).

## Files referenced in TRIAGE.md (no edits)

- `src/components/layout/Header.tsx`, `Footer.tsx`, `CookieBanner.tsx`
- `src/components/forms/RegistrationForm.tsx`
- `src/app/(frontend)/page.tsx`, `agenda/page.tsx`
- `src/app/actions/register.ts`
- 8 `tests/e2e/*.spec.ts` files at their failing lines

## Headline themes

1. **Phase 1 → Phase 2 contract drift (5 of 8):** specs written when `/` redirected and the Footer had no brand link. Phase 2/2.1/2.3 reshaped contracts, specs were not refreshed.
2. **CI env quirks (2 of 8):** `NEXT_PUBLIC_COOKIEYES_SITE_KEY=ci-cookieyes-placeholder-do-not-use` keeps CookieYes Script mounted but its source URL never resolves, blocking `networkidle` and likely injecting a stray `role="alert"`.
3. **Architecture observation (1 of 8):** `/` route uses dynamic Header (`auth()`), origin emits `Cache-Control: private, no-cache`; the `s-maxage=3600` contract lives at the Cloudflare edge per the page's own comment (page.tsx:11-17).

## Status

`complete` — diagnostic deliverable produced. No code changes. Fix dispatch is a follow-up decision for the operator.

## Next steps (user choice)

Operator may dispatch fixes in Batches A → B → C → D from TRIAGE.md's "Suggested fix order" section, or pick individual specs. None of the 8 fixes is blocking the PR #2 ship if the operator accepts a 12/56 pass rate as a known baseline for this Phase 4 ship.

## Predecessor links

- `260511-04m` — Playwright webServer standalone fix (closed)
- `260511-0nx` — Playwright cf-ray header bypass (closed)

This triage is the third in the chain that started with PR #2 CI showing 0 passing Playwright specs.
